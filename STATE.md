# Project State

> The single file every session reads first. Names what's current and what's next. Updated at every build-session close.

## Current

| Field | Value |
|---|---|
| **Project** | Paideia — knowledge mastery app on a pedagogical dependency graph |
| **GitHub** | https://github.com/StarshipSuperjam/paideia (private) |
| **Current phase** | Phase 0 — closed (S-0003); **Phase 1 — Contract Lock fully closed at S-0012**; §1.1 closed across S-0004 / S-0005 / S-0006; privacy-posture insertion landed at S-0007; §1.2 closed at S-0008; Phase 4.5 input-dataset-survey scaffolding landed at S-0009; §1.3 closed at S-0010; privacy ADRs collapsed at S-0011 (ADR 0031); project-direction supersession landed at S-0012 (ADR 0032 supersedes ADR 0002). **Phase 1.5 — Mission Realignment Overhaul** opened at S-0013 ([ADR 0033](adr/0033-mission-realignment-structured-guidance-for-self-learners.md)). Phase 1.5.2 closed at S-0014 with [ADR 0034](adr/0034-discovery-planning-engagement-triad.md) (Discovery / Planning / Engagement triad as primary product structure). Phase 1.5.3 closed at S-0015 with [ADR 0035](adr/0035-multi-platform-apple-expansion.md) (multi-platform Apple expansion superseding ADR 0032). Phase 1.5.4 closed at S-0016 with the rendering-policy voice-discipline closure ([ADR 0027](adr/0027-rendering-policy-prompt-layer-contract.md) forbidden-token enumeration extended with globe / spatial-traversal and reward / visualization categories; mirror in [`AGENT_INSTRUCTIONS.md`](AGENT_INSTRUCTIONS.md); light revisions to [ADR 0012](adr/0012-freshman-defaults-autodidact-ceiling.md) and [ADR 0014](adr/0014-sonnet-teaches-opus-reviews.md); secondary docs sweep). **Phase 1.5.5 opened at S-0017** with the inward-document voice closure: [ADR 0036](adr/0036-expression-contract-for-inward-documents.md) authors the project's second expression contract — a kindred tool to [`AGENT_INSTRUCTIONS.md`](AGENT_INSTRUCTIONS.md)'s rendering policy, separately scoped to inward-facing documentation; operational surface at [`docs/operations/document-voice.md`](docs/operations/document-voice.md). **S-0018 ran the mechanical-sweep cleanup pass** across the seven offender files identified at S-0017 boot audit ([`docs/MISSION.md`](docs/MISSION.md), [`docs/business.md`](docs/business.md), [`docs/architecture.md`](docs/architecture.md), [`docs/content-strategy.md`](docs/content-strategy.md), [`AGENT_INSTRUCTIONS.md`](AGENT_INSTRUCTIONS.md) opening blockquote + closing footer, [`docs/tensions.md`](docs/tensions.md) open entries, [`adr/README.md`](adr/README.md) orientation prose). The four-layer trace system (ADRs / CHANGELOG / MemPalace / git) carries the production trace; governed body prose no longer duplicates it. Phase 1.5 was realignment, not rebuild — the pedagogical dependency graph, AI instructor model, BYOB close reading, mastery model, privacy posture, cost ceiling all survive intact. **S-0019 ran a pre-Phase-2 machinery hardening pass** (Tier 1 of the approved adversarial-analysis plan): pre-commit hook retargeted to a stable tracked-symlink layout; Recovery sections added to session-build-lifecycle.md (eager-claim race + symlink restore) and session-shutdown-sequence.md (interrupted-shutdown taxonomy); one residual ADR 0036 violation in docs/content-strategy.md cleared; CLAUDE.md gained end-state-quality scope clarification, First-session reading order, and Posture vs machinery subsection; OQ-OUTWARD-VOICE logged to docs/tensions.md (decide-before Phase 7). **S-0020 ran Tier 2**: A2 lands [`tools/hooks/mempalace-hook-wrapper.sh`](tools/hooks/mempalace-hook-wrapper.sh) (always exits 0 to harness; routes capture failures to `.claude/logs/mempalace-hook.log` per-clone; manual failure test verified) wired into `.claude/settings.json` Stop and PreCompact hooks; boot-recovery surfacing for the log lands in `docs/operations/session-build-lifecycle.md`'s Recovery section. B4 extends [`tools/validate.py`](tools/validate.py) with a tenth check (`adr_index_consistency`) soft-warning under category `adr_index_inconsistent` when an ADR file is missing from `adr/README.md`'s per-phase tables or its Status core keyword diverges from the index entry; documented in [`docs/operations/tools-validate-interpretation.md`](docs/operations/tools-validate-interpretation.md). C4 softens STATE.md's `**Backup tag**` line from operational-fallback framing to historical-anchor framing. **S-0021 closes Phase 1.5.5** with the originally-scheduled ROADMAP.md substantive cleanup. **Phase 2 (Build Plan Scaffolding) opens at S-0022** with hardened machinery and the stable inward-voice contract both as input. |
| **Last build session** | S-0020 (2026-05-01) — **Pre-Phase-2 machinery hardening (Tier 2 of approved adversarial-analysis plan).** Three opportunistic items identified at S-0019's boot adversarial pass; bundled per the user's split into a single dedicated session bridging S-0019's Tier 1 hardening and S-0021's Phase 1.5.5 close. **A2 — MemPalace capture-hook wrapper.** [`tools/hooks/mempalace-hook-wrapper.sh`](tools/hooks/mempalace-hook-wrapper.sh) (tracked, executable) wraps `mempalace hook run`: passes args and stdin through, captures the binary's exit code and stderr, and appends a single timestamped success/failure line to `.claude/logs/mempalace-hook.log` (gitignored under the existing `.claude/*` ignore rule — per-clone state, no `.gitignore` change needed). The wrapper always exits 0 to the harness, so capture issues never block the session; the log carries the truth. Wired into [`.claude/settings.json`](.claude/settings.json) for both Stop and PreCompact hooks (relative path `tools/hooks/mempalace-hook-wrapper.sh`). [`docs/operations/mempalace-operations.md`](docs/operations/mempalace-operations.md) updated to reflect the wrapper indirection. [`docs/operations/session-build-lifecycle.md`](docs/operations/session-build-lifecycle.md) Recovery section gained a new **MemPalace capture-hook failure log** subsection naming the boot-time check (`test -s .claude/logs/mempalace-hook.log && cat …`) and the acknowledgment-by-truncation procedure; cross-referenced from `mempalace-operations.md`. Manual failure test confirmed via the `PAIDEIA_MEMPALACE_HOOK_FORCE_FAIL=1` sentinel: log line written naming the synthesized failure, harness-side exit code 0. **B4 — ADR-index/file consistency validator extension.** [`tools/validate.py`](tools/validate.py) gains a tenth check (`adr_index_consistency`) under the existing `validate_repo_structure()` function, immediately after the `adr_status` check. The check parses `adr/README.md`'s per-phase tables (rows of the form `| [NNNN](NNNN-...md) | Title | Status |`) into a `{NNNN: status_text}` map, then for each `adr/NNNN-*.md` file emits the `adr_index_inconsistent` soft-warn either when the ADR is not referenced in the index or when the ADR file's `Status:` core keyword (Accepted / Superseded / Deprecated / Proposed) differs from the index's status column for that ADR. Soft-warn rather than hard-fail because the index is human-curated and tolerates markdown-link decoration in the status column; false positives should refine the check rather than be papered over. Documented in [`docs/operations/tools-validate-interpretation.md`](docs/operations/tools-validate-interpretation.md) alongside `adr_missing_status`. Manual test: throwaway `adr/9999-test-missing-from-index.md` produced exit 1 with one `adr_index_inconsistent` warn naming the file; deletion restored exit 0. **C4 — STATE.md backup-tag framing softened** from "recoverable via `git reset --hard pre-foundation-v0.0.0`" to "historical anchor (pre-machinery state; not an operational fallback target — file-level retrieval via `git show pre-foundation-v0.0.0:path` is the legitimate use)." A reset to pre-foundation would discard all the machinery work that has happened since; the user has decided not to introduce a recurring recovery-anchor cadence; the tag's actual usefulness is file-level historical retrieval per the example in [`docs/operations/escalation-criteria.md`](docs/operations/escalation-criteria.md). `tools/validate.py` clean (0 hard-fails, 0 soft-warns) at session close. **Tier 1 + Tier 2 hardening complete**; Phase 2 (Build Plan Scaffolding) now opens at S-0022 with the originally-scheduled ROADMAP.md substantive cleanup landing at S-0021 in between. |

| **Prior build session** | S-0019 (2026-05-01) — **Pre-Phase-2 machinery hardening (Tier 1 of approved adversarial-analysis plan).** Pre-Phase-2 audit (run in plan mode before this session claimed) found seven Phase 1.5.5 offender files PASS post-S-0018 sweep with one residual defect (`docs/content-strategy.md:102` authoring-future tense) and identified machinery gaps across three categories (real defects, postures-without-enforcement, ambiguities/coverage gaps). User-approved tiered remediation: Tier 1 bundled into this session, Tier 2 dedicated to S-0020, ROADMAP.md substantive cleanup to S-0021, Phase 2 to S-0022. **A1 — pre-commit hook symlink retargeted** from a worktree-specific path to relative `../../tools/hooks/pre-commit` so the hook resolves stably across worktree churn. **A3 — Recovery sections added** to [`docs/operations/session-build-lifecycle.md`](docs/operations/session-build-lifecycle.md) (eager-claim race resolution + pre-commit symlink restore) and [`docs/operations/session-shutdown-sequence.md`](docs/operations/session-shutdown-sequence.md) (four-state interrupted-shutdown taxonomy with per-state recovery paths and split-state escalation route). **A4 — `docs/content-strategy.md:102`** rewritten to present-state declarative. **C1 — [`CLAUDE.md`](CLAUDE.md) end-state-quality rule** scope clarification appended (applies to newly-authored content; cleanup sweeps under newly-imposed contracts are exempt). **C3 — new `## First-session reading order` section in CLAUDE.md** names the four-doc cold-start reading priority. **Tier 3 — new `### Posture vs machinery` subsection in CLAUDE.md** names the three rules held by AI discipline rather than machinery enforcement (pushback rule, two-layer decision recording, startup ceremony order). **C2 log — OQ-OUTWARD-VOICE entry added to [`docs/tensions.md`](docs/tensions.md)** for the third expression-contract gap (UI labels, button text, error messages, learner-facing help, public README, App Store description, learner-visible CHANGELOG entries that neither ADR 0027 nor ADR 0036 covers); decide-before Phase 7. `tools/validate.py` clean (0 hard-fails, 0 soft-warns). The originally-scheduled S-0019 work (ROADMAP.md substantive cleanup) shifted to S-0021; Tier 2 landed at S-0020; Phase 2 opens at S-0022. |

| **Last commit on main** | (see `git log --oneline -1` on main) |
| **Backup tag** | `pre-foundation-v0.0.0` at commit `fa70b8c` — historical anchor (pre-machinery state; not an operational fallback target — file-level retrieval via `git show pre-foundation-v0.0.0:path` is the legitimate use). |

## Infrastructure

| Component | Value |
|---|---|
| **Supabase project** | `paideia-dev`, project ref `ozooosgnuzxqqypotlke`, PostgreSQL 17.6 |
| **Supabase MCP** | configured in `.mcp.json` (gitignored, contains PAT) |
| **MemPalace** | installed at `/Users/shanekidd/Library/Python/3.9/bin/`; MCP server `mempalace-mcp` configured in `.mcp.json`; wing `paideia` indexed at S-0002 (414 drawers across rooms `general` + `operations`); capture hooks wired in `.claude/settings.json` |
| **Python** | system Python 3.9.6 at `/usr/bin/python3`; user-scope packages at `/Users/shanekidd/Library/Python/3.9/bin/` |
| **Anthropic API** | env var `ANTHROPIC_API_KEY` set in local `.env` (gitignored) |

## Next session work item

**S-0021 — ROADMAP.md substantive cleanup (Phase 1.5.5 close)** *(originally scheduled for S-0019; deferred to land after the Tier 1 + Tier 2 machinery hardening sessions per the approved adversarial-analysis plan)*.

Boot procedure: type `Start Engine` (or `/start-engine`) in a fresh Claude Code session. CLAUDE.md is auto-loaded; MemPalace `mempalace_search` is queryable from boot.

### The work

[`ROADMAP.md`](ROADMAP.md) is the project's full-arc plan and the file most likely to drift because phase-narrative is part of how the file works. The contract that governs it — [ADR 0036](adr/0036-expression-contract-for-inward-documents.md), operational surface [`docs/operations/document-voice.md`](docs/operations/document-voice.md) — partially-governs ROADMAP.md per its **three-speech-acts handling** rule:

- **Phase scope, output, success criteria** — present-tense plan as it stands. Governed.
- **Cross-phase strong working commitments** — present-tense commitment list. Governed.
- **Per-phase additions and revisions** — the content survives in the phase prose at present-state voice; date-stamp markers like `**Added: YYYY-MM-DD (S-NNNN — per ADR ZZZZ ...)**` migrate to CHANGELOG.
- **Supersession narration of prior phase plans** — migrates entirely to git history; not duplicated in-document.

A pre-S-0021 grep against ROADMAP.md surfaces dense per-phase revision narration: explicit `**Added: YYYY-MM-DD (S-NNNN — ...)**` and `**Substantially rewritten: 2026-04-30 (S-NNNN — per ADR ZZZZ ...)**` markers on phase headers (Phase 1.5, 1.5.1–1.5.4, Phase 4.5, Phase 8, Phase 9 success criteria sections, etc.); supersession-narration prose inside Phase 1.5 and Phase 8 sections describing what was dropped and why; per-commitment ADR cross-reference parentheticals on most assertions; the trailing "Strong working commitments referenced throughout" enumeration with inline supersession history (commitment 2 framing, ADR 0002 → 0032 → 0035 chain).

The judgment-heavy aspect of the cleanup is the per-paragraph distinction: a sentence stating *Phase 9's platform target is iPhone + iPad first-class* is governed (present-state plan) and stays as-is or sharpens; a sentence stating *the prior three-candidate framing dropped under ADR 0032* is supersession narration and migrates to CHANGELOG / git. The plan as it stands speaks for itself; the path to that plan lives in the trace layers.

### Source documents (read at S-0021 boot beyond CLAUDE.md auto-load)

- [`STATE.md`](STATE.md) — this brief.
- [`docs/operations/document-voice.md`](docs/operations/document-voice.md) — the contract; especially the three-speech-acts handling section (lines 75–84) and the worked example (lines 32–50).
- [`adr/0036-expression-contract-for-inward-documents.md`](adr/0036-expression-contract-for-inward-documents.md) — the ADR establishing the contract.
- [`ROADMAP.md`](ROADMAP.md) — the cleanup target.
- [`CHANGELOG.md`](CHANGELOG.md) — the layer the migrated date-stamp markers and supersession narration land in (or already reside in for prior sessions).
- `/Users/shanekidd/.claude/plans/then-expression-contract-for-nifty-pie.md` — the adversarial-analysis plan; supplementary context for why this work is happening here.

### Success criteria (S-0021)

- ROADMAP.md phase scope, success criteria, and cross-phase commitment list survive at present-state voice; the four-layer trace system carries date-stamps and supersession narration.
- `**Added: YYYY-MM-DD (S-NNNN — ...)**` and `**Substantially rewritten: ... (S-NNNN — ...)**` headers stripped from phase sections; the substantive content survives.
- Per-commitment ADR cross-reference parentheticals on every-assertion form consolidated to end-of-section "See also" pointers where the section is long enough to warrant it; for shorter blocks, a single bibliographic pointer at the end suffices.
- "Strong working commitments referenced throughout" trailing section preserved as the cross-phase commitment list; inline supersession history (the *was 0002 / superseded by 0032 / superseded by 0035* chain) migrates to git and the ADR Status fields themselves; the present-state list points at current ADRs.
- Phase 1.5 narrative (the realignment-as-realignment-not-rebuild framing the project leaned on) is preserved at present-state voice; the per-section session attributions migrate.
- `tools/validate.py` clean (0 hard-fails); soft-warns acceptable.
- CHANGELOG entry under `[Unreleased]` for S-0021 categorized appropriately (Changed for the body rewrite; potentially Removed for any in-document narration that does not land elsewhere because CHANGELOG and git already carry it).
- STATE.md updated with S-0022 next-session work item (Phase 2 — Build Plan Scaffolding open: `build_plan/MANIFEST.md`, `build_plan/00_preamble.md`, `build_plan/00_session_schedule.md`, `build_plan/P_0_contract_lock.md` through `build_plan/P_13_ui_prototype.md`).
- Phase 1.5.5 closes at S-0021 close.

### Estimated context budget (S-0021)

~60% target / 70% cap (substantive-extraction tier per CLAUDE.md). One file of ~430 lines; per-paragraph judgment over the three speech acts is the work, not raw line count. Authoring at production quality the first time per the end-state-quality rule (this is a newly-authored cleanup of legacy content under a contract — the rule's scope clarification at S-0019 makes the cleanup pass itself a legitimate first-pass under ADR 0036's deferred-cleanup schedule).

### After S-0021

S-0022 opens Phase 2 (Build Plan Scaffolding) with the realigned, voice-clean contract layer as input. The original Phase 2 deliverable: `build_plan/MANIFEST.md`, `build_plan/00_preamble.md`, `build_plan/00_session_schedule.md`, `build_plan/P_0_contract_lock.md` through `build_plan/P_13_ui_prototype.md`. The build_plan tree names the per-session contracts for Phases 3–9; once it lands, every downstream session boots from a per-phase chunk plus STATE.md plus MemPalace.

## Open tensions and deferred decisions

See `docs/tensions.md`. As of S-0020 close, the OQ-prefixed active set is 9 items (OQ-PRIVACY-A and OQ-PRIVACY-B closed by ADR 0031; OQ-PHASE8-A settled by ADR 0032; OQ-BYOK-REGIME withdrawn by ADR 0032; OQ-OUTWARD-VOICE added at S-0019):

- OQ-DEC1-A through D — Phase DEC.1 architecture decisions (server-side mastery, two-hop retrieval, embedding strategy, chunk-resolver vs URL pointers); decide-before Phase 6
- **OQ-WALL-BEHAVIOR** (added S-0008, per ADR 0029; simplified S-0012) — soft-wall degradation ladder at cost cap; **single-tier ladder** under cost-priced subscription model (free-vs-paid tier-shaped framing collapsed at S-0012); four candidate steps remain (model downshift, retrieval shrink, concept-engagement length cap, soft refusal); decide-before Phase 8 cost-cap wiring
- **OQ-CONTEXT-COMPRESSION** (added S-0008, per ADR 0029; unaffected by S-0012) — token-amplification mitigation for multi-turn engagements (structured-state replacement + automatic compression + explicit summarization candidates); decide-before Phase 7
- **OQ-PEDAGOGY-INFERENCE-LOCUS** (added S-0008, tagged `watch`) — rule layer vs. distributed inference; revisit when inference registry crosses ~30 entries OR cross-domain edges per domain-pair exceed ~50 OR an operational complaint surfaces
- **OQ-OUTWARD-VOICE** (added S-0019) — third expression-contract gap covering outward product surfaces (UI labels, button text, error messages, learner-facing help, public README, App Store description, learner-visible CHANGELOG entries) that neither ADR 0027 nor ADR 0036 governs; decide-before Phase 7 (the earliest plausible moment such a surface ships)
- OQ-WATCH-FLAG-FILE (tagged `watch`) — separate watch-flag file consideration, surfaced at session-30 health check

## Paperclip integration

**Deferred.** Phase 5 trial / Phase 7 commit per ROADMAP.md. Not installed in Foundation.

## Strong working commitments

12 commitments inherited from pre-Foundation design plus 10 architectural decisions. **All 22 are recorded as ADRs in [`adr/`](adr/)** — the contract layer. The audience-facing summary lives in `docs/MISSION.md` (commitments 1–12 with ADR cross-refs; commitment 2 reframed at S-0012 as "operating discipline must not corrupt pedagogy" per ADR 0032); the canonical list with ADR pointers lives in `ROADMAP.md` ("Strong working commitments referenced throughout"). The conversational story behind each decision is recoverable from MemPalace `decision`-tagged drawers.

ADR collection landed at S-0003 close; `design-reasoning.md` retired in the same commit. Phase 1 ADRs accumulate from S-0004 onward: ADRs 0023–0024 added in S-0004 (engagement-depth aggregation, sub-signal storage shape); ADR 0025 added in S-0006 (historical maximum tracking); ADR 0026 added in S-0007 (privacy posture, structural-not-substantive); ADRs 0027–0029 added in S-0008 (rendering policy as prompt-layer contract; input-side scope structural-not-prompt; personal financial cost ceiling); ADR 0030 added in S-0010 (`confidence_level` evidentiary-mode column on Node Schema); ADR 0031 added in S-0011 (erasure mechanism — hard-delete with cascade — and individual-only data regime); ADR 0032 added in S-0012 (personal project disposition; refusal-not-deferral commercial closure; supersedes ADR 0002). **ADR count after Phase 1: 32 ADRs total — 31 Accepted plus 1 Superseded** (ADR 0002 by ADR 0032). **Phase 1.5 grows the count further**: ADR 0033 added in S-0013 (mission realignment as structured guidance for self-learners; records globe / reward visual-system obsolescence as deliberate architectural removal); ADR 0034 added in S-0014 (Discovery / Planning / Engagement triad as primary product structure; replaces the globe-as-home-screen + concept-engagement-surface architecture); ADR 0035 added in S-0015 (multi-platform Apple expansion; supersedes ADR 0032 by broadening commitment 1 from single-platform iOS to iPhone + iPad first-class via SwiftUI plus Mac via Designed-for-iPad; commitments 2–6 restate verbatim). **ADR count after S-0015: 35 ADRs total — 33 Accepted plus 2 Superseded** (ADR 0002 by ADR 0032; ADR 0032 by ADR 0035). **S-0016 (Phase 1.5.4 close) lands no new ADRs**: ADR 0027 forbidden-token enumeration extended in place per its own asymmetric-amendment discipline (additions are CHANGELOG-tracked but require no superseding ADR); light revisions to ADRs 0012 and 0014 in place; sweep of residual UI-token references across ADRs 0006 / 0007 / 0028 in place; secondary docs sweep across ~10 docs. Status remains Accepted on every revised ADR. **S-0017 reopens Phase 1.5 at Phase 1.5.5** with [ADR 0036](adr/0036-expression-contract-for-inward-documents.md) — the project's second expression contract, kindred tool to [`AGENT_INSTRUCTIONS.md`](AGENT_INSTRUCTIONS.md)'s rendering policy ([ADR 0027](adr/0027-rendering-policy-prompt-layer-contract.md)), separately scoped to inward-facing documentation. **Post-Phase-1.5.5 total: 36 ADRs (34 Accepted + 2 Superseded).** This is the contract layer Phase 2 (Build Plan Scaffolding) consumes after the cleanup passes (S-0018, S-0019) complete.
