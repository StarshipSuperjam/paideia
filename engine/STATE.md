# Project State

> The single file every session reads first. Names what's current and what's next. Updated at every build-session close.

## Current

| Field | Value |
|---|---|
| **Project** | Paideia — knowledge mastery app on a pedagogical dependency graph |
| **GitHub** | https://github.com/StarshipSuperjam/paideia (private) |
| **Current phase** | Phase 0 — closed (S-0003); **Phase 1 — Contract Lock fully closed at S-0012**. **Phase 1.5 — Mission Realignment Overhaul fully closed at S-0021.** **S-0022** committed the engine / product wall ([ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md)) and renamed `CHANGELOG.md` → `ENGINE_LOG.md`. **Phase 2 (Build Plan Scaffolding) closed at S-0023.** **S-0024–S-0025** executed the folder migration into `engine/` and `product/` subtrees and the cross-reference sweep. **S-0026** authored the third project expression contract per [ADR 0038](adr/0038-expression-contract-for-ai-authored-code.md). **S-0027 widened the expression contract** per [ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md) and **committed the build-readiness gate** per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md). **Phase 3 — SQL Schema Implementation closed at S-0028** with eight migrations deployed to `paideia-dev` (PostgreSQL 17.6); RLS enabled on every public table; CHECK constraints enforce gate T2-A enums. **S-0029 closed the infrastructure-misfire sweep**; **S-0030 landed the first project health-check audit**; **S-0031 closed Session β: harness mechanization** ([ADR 0043](adr/0043-hook-architecture.md), [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md)); **S-0032 closed the MemPalace audit cleanup + improvements** (cadence tightened 30 → 10); **S-0033 closed three coupled mechanical fixes** (pre-commit STATUS bug, side-discovery audit, scheduled-audits allowed-paths); **S-0034 closed the Phase 4 build-readiness gate exercise**; **S-0035 closed the engine-hardening pass** ([ADR 0045](adr/0045-shared-state-integrity-discipline.md)); **S-0036 closed the deferral-discipline guardrails** plus the bash 3.2 fix. **S-0039 landed the structural-reference parser tool** per new engine [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md): standing engine tool decoupled from phases; document-agnostic with per-document-type configs; output is signal-not-truth and ephemeral-not-persisted (no `engine/parser_findings/`, no cross-source corroboration, reversibility named); triage is adversarial defaulting to rejection. Implementation: `engine/tools/parse_structural_reference.py` (~600 lines mirroring `validate.py`'s contract-block + pure-detector + argparse-dispatch shape) with `HTMLInputAdapter` for SEP/IEP-shaped HTML; PDF input adapter and triage prompt template both deferred per ADR 0047 to first-real-use. `engine/tools/test_parse_structural_reference.py` adds 81 tests (one class per detector/adapter/emitter; integration against six cached SEP fixtures; CLI tests via subprocess). `beautifulsoup4>=4.12.0` added to `engine/tools/requirements.txt`. Test fixtures cached at `engine/tools/test_fixtures/sep/` (epistemology, consciousness, ethics-virtue, scientific-method, kant, qualia-knowledge — six entries spanning epistemology/ethics/phil-of-mind/phil-of-science with 6–57 cross-refs each) acquired under SEP's crawl-for-indexing permission per [`product/docs/content-strategy.md`](../product/docs/content-strategy.md) Tier 4; parser-implementation-internal, never reach the production graph or any user-facing surface; [ADR 0011](../product/adr/0011-no-hosted-copyrighted-material.md) unchanged. **S-0040 closed the MemPalace wing-filter fix (deferred from S-0038)** concurrent with S-0039: documented two upstream mempalace 3.3.3 bugs in [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) — wing-filtered search throws "Internal error: Error finding id" reproducibly across MCP and CLI; hook auto-capture wing-naming derives from the full encoded transcript-path directory rather than the documented `paideia` wing. Both upstream and not in-project remediable; unfiltered search is the workaround. **Phase 4 — Graph Validation Utility closed at S-0037** per [ADR 0016](adr/0016-graph-construction-needs-live-validation.md) and the [Phase 4 build-readiness gate](build_readiness/phase_4_graph_validation.md): `validate_graph()` connects to the live Supabase DB via psycopg with the service-role connection string; three hard-fails (duplicate node IDs, dangling edge references, prerequisite cycles via Kosaraju SCC restricted to the `pedagogical_prerequisite` subgraph); seven soft-warn categories (`undeclared_predicate`, `attribute_shape_inconsistency`, `missing_rigor_score`, `render_readiness_violation`, `synthetic_review_queue`, `orphan_leaf`, `suspicious_cross_domain_ratio`) registered in `checks_run` even when zero findings fire; `--export-snapshot PATH` mode dumps full nodes-and-edges JSON for offline review; audit-skip path when `SUPABASE_DB_URL` is unset (`graph_audit_skipped`) keeps non-seed-authoring sessions ungated on connectivity. PREDICATE_MANIFEST.md v1 registry authored (`pedagogical_prerequisite` + `historical_influence`); ROUTING.md range-collision fixed (0010 reserved for Phase 4 metadata, 0011-0019 for Phase 5 epistemology); seed-chunked-authoring.md fleshed out with the full per-session migration workflow. **Phase 4.5 — Input Dataset Survey closed at S-0038** per [ROADMAP §4.5](../ROADMAP.md) and [`build_plan/P_3_input_dataset_survey.md`](../build_plan/P_3_input_dataset_survey.md): the "Cross-Domain Reference Inventories — Survey" section in [`product/docs/content-strategy.md`](../product/docs/content-strategy.md) is populated end-to-end across the five usability axes and four tiers; Tier 4 reframed per [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) — SEP, IEP, Routledge Encyclopedia of Philosophy, Oxford Reference, Wikipedia evaluated for graph-shape value distinct from content/prose value. **New product ADR 0046** settles the structural-reference posture extension from SEP-only to a class of curated philosophy reference works. ADR count post-S-0039 + S-0040: **47 ADRs total — 45 Accepted plus 2 Superseded**, partitioned **13 engine + 34 product**. Validator clean (16 checks, 0 hard-fails, 0 soft-warns). |
| **Last build session** | S-0040 (2026-05-03) — **MemPalace wing-filter fix (deferred from S-0038)**, concurrent with S-0039. Output: [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) gains two new "Known issues" entries documenting (1) wing-filtered search throws "Internal error: Error finding id" reproducibly across MCP and CLI for every wing tested (mempalace 3.3.3 upstream bug); (2) hook auto-capture wing-naming derives from the full encoded transcript-path directory, producing one wing per worktree rather than the documented single `paideia` wing; `mempalace hook run` does not accept `--wing` so the hook wrapper cannot override. The "Project usage scope" section gains a "Wing-name reality vs. documented intent" paragraph reconciling the historical `paideia` wing (488 drawers) with the current per-worktree behavior. Boot-procedure step 3 in CLAUDE.md and `session-build-lifecycle.md` is unchanged — neither prescribed wing-filtered search; both work fine with the unfiltered fallback. No code changes; both bugs upstream and not in-project remediable. **Closes the S-0038 deferral that violated CLAUDE.md "Default to fix-in-context" posture.** Side-discovery audit at close: 0 markers. HANDOFF disposition audit: 0 new sections. outcome_summary_soft_warns: all categories at 0. Validator clean. |

| **Prior build session** | S-0039 (2026-05-03) — **Structural-reference parser tool + ADR 0047 build session.** Output: new engine [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) settling four operational decisions for consuming the structural-reference class established by [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md); `engine/tools/parse_structural_reference.py` (~600 lines mirroring `validate.py`'s contract-block + pure-detector + argparse-dispatch shape) with `InputAdapter` interface, `HTMLInputAdapter` for SEP/IEP-shaped HTML, pure-function structural detectors, `FocusingBriefEmitter`, CLI; `engine/tools/test_parse_structural_reference.py` adds 81 tests (one class per detector/adapter/emitter; integration against six cached SEP fixtures; CLI tests via subprocess); `engine/tools/test_fixtures/sep/` cached six SEP entries (epistemology, consciousness, ethics-virtue, scientific-method, kant, qualia-knowledge) under SEP's crawl-for-indexing permission; `beautifulsoup4>=4.12.0` added to `engine/tools/requirements.txt`. **New engine ADR 0047.** ADR count: **46 → 47** (12 → 13 engine + 34 product). README header count corrected from stale "11 ADRs total" (table already had 12) to "13 ADRs total — all Accepted." Validator clean (16 checks, 0 hard-fails, 0 soft-warns). 331 tests passing (was 250 at S-0037 close; +81 new). Cold-context review pass per ADR 0038: clean. Side-discovery audit: 1 marker (`deferred`) dispositioned `acceptable_no_action` via ADR 0047. HANDOFF audit: 0 new sections. **Concurrent-session note:** S-0040 was eager-claimed and committed `1a41277` to main during S-0039 substantive work; rebase clean; S-0039 archive written directly to avoid stomping S-0040's in-flight session-state files. Phase 5 epistemology gate slipped one slot to S-0041. |

| **Last commit on main** | (see `git log --oneline -1` on main) |
| **Backup tags** | `pre-foundation-v0.0.0` at commit `fa70b8c` — pre-machinery anchor. `pre-phase-3-v0.0.1` at commit `36b0907` (S-0026 close) — pre-Phase-3 anchor cut at S-0027. Both are historical anchors, not operational fallback targets — file-level retrieval via `git show <tag>:<path>` is the legitimate use. |

## Infrastructure

| Component | Value |
|---|---|
| **Supabase project** | `paideia-dev`, project ref `ozooosgnuzxqqypotlke`, PostgreSQL 17.6 |
| **Supabase MCP** | configured in `.mcp.json` (gitignored, contains PAT) |
| **MemPalace** | installed at `/Users/shanekidd/Library/Python/3.9/bin/`; MCP server `mempalace-mcp` configured in `.mcp.json`; wing `paideia` carries **485 drawers across 5 rooms** as of S-0032 close (general 330, operations 88, decisions 55, foundation-planning-s0001 10, s0003-adr-collection 2); capture hooks wired in `.claude/settings.json` (Stop + PreCompact wrapper PATH-fixed at S-0032 — auto-capture had been silently failing since S-0002 because `mempalace` CLI wasn't on the hook subshell's PATH; verify at next Stop event by checking `.claude/logs/mempalace-hook.log` for `OK exit=0` lines instead of `FAIL exit=127`); project usage scope documented at [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Project usage scope" — uses search, add_drawer, diary (agent_name `claude`), read-only inspection; explicitly excludes KG and tunnels |
| **Python** | system Python 3.9.6 at `/usr/bin/python3`; user-scope packages at `/Users/shanekidd/Library/Python/3.9/bin/` |
| **Anthropic API** | env var `ANTHROPIC_API_KEY` set in local `.env` (gitignored) |

## Next session work item

**S-0041 — Phase 5 epistemology build-readiness gate** per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md). Both S-0039 (parser ADR + scaffold) and S-0040 (MemPalace wing-filter fix) closed concurrently this session-day; the Phase 5 epistemology gate slips two slots from the original S-0039 projection because (a) S-0039 took the parser-tool work after a design conversation surfaced that uploading source PDFs (Routledge ~10K pages, Oxford Reference at scale) into AI context is operationally infeasible and stresses [ADR 0011](../product/adr/0011-no-hosted-copyrighted-material.md); the parser tool plus [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) lands the operational mechanism for [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) before any Phase 5 sub-session needs to consume it; and (b) S-0040 took the wing-filter deferral fix. The first Phase 5 sub-session (epistemology, per [`build_plan/P_4_seed_graph_build.md`](../build_plan/P_4_seed_graph_build.md) §5.1) requires a gate report at `engine/build_readiness/phase_5_epistemology.md`. The S-0041 session reads the chunk, consults the [Phase 4.5 input dataset survey](../product/docs/content-strategy.md) ("Cross-Domain Reference Inventories — Survey") for per-source disposition, considers running the structural-reference parser against an acquired SEP entry to surface candidates for the gate's Tier 1 inventory, applies adversarial reconnaissance per [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md), and authors the gate report's Tier 1 / Tier 2 / Tier 3 triage so the S-0042 build session can implement without re-deciding.

If a non-substantive operational session intercedes (health-check trigger, tools refinement, doc cleanup), that session takes S-0041 and the Phase 5 gate slips to S-0042+.

**Parser tool available for ad-hoc use.** [`engine/tools/parse_structural_reference.py`](tools/parse_structural_reference.py) per [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) is runnable whenever a structural-reference source has been acquired (currently SEP/IEP via the HTML adapter; PDF adapter deferred to first-PDF-source-acquisition). CLI: `python3 engine/tools/parse_structural_reference.py --source <path> --document-type encyclopedia --output -`. The parser produces a JSON focusing brief; the user supplies the brief to an adversarial-triage LLM prompt (template deferred to first-real-use). The brief is discarded after triage; the graph is the only durable artifact.

**MemPalace wing-filter / wing-naming bugs documented at S-0040.** Both upstream in mempalace 3.3.3; not in-project remediable. See [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Known issues" for diagnosis, reproduction, and workaround (use unfiltered `mempalace_search`).

**Phase 4.5 input dataset survey is closed at S-0038.** The "Cross-Domain Reference Inventories — Survey" section in [`product/docs/content-strategy.md`](../product/docs/content-strategy.md) is populated end-to-end across four tiers; [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) settles the structural-reference posture for the home-discipline reference class; [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) settles the operational mechanism. Phase 5 sub-sessions inherit both at boot per ROADMAP §5.2.

**Phase 4 graph validation remains implementation-closed at S-0037.** The validator code, manifests, routing, and ops-doc are all in place. Two ROADMAP §4 success criteria — `<3s on a 100-node test seed` and `pre-commit hook blocks seed-authoring commits that fail audit` — require real seed content to verify and queue for natural exercise at the first Phase 5 seed session (S-0042+ after the Phase 5 gate). The first Phase 5 session that sets `SUPABASE_DB_URL` and runs `validate.py --validate-only` after a `supabase db push` will exercise the audit end-to-end against real seed content for the first time.

**Boot-time check: cadence trigger.** Cadence is 10. At S-0041 boot, `next_id = "0041"`, so `41 % 10 = 1` — no cadence trigger this session. Next natural fire lands at S-0050. (S-0040 missed the cadence-aligned firing point at `40 % 10 = 0` because S-0040 was a deferred-fix session, not the cadence-aligned audit per [`engine/operations/health-check.md`](operations/health-check.md); the cadence trigger surfaces but is overridable when a session has explicit work directed by the user. STATE.md note for S-0050 health-check audit: include S-0036–S-0040 in the window if not consumed earlier.)

**Boot-time check: persistent-warn surface.** Per [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md). At S-0041 boot, structured `outcome_summary_soft_warns` archives in window: S-0036, S-0037, S-0038, S-0039, S-0040 (5; S-0035 rolls off). Surface clean expected — every archive in the window carries zero soft-warns.

**Boot-time check: shared-state health probe.** Per [ADR 0045](adr/0045-shared-state-integrity-discipline.md). At S-0041 boot, `session-start.sh` runs `validate.py --health-probe-only`. Healthy expected. The wing-filter / wing-naming behavior documented at S-0040 is upstream-mempalace, not a probe-categorized health issue.

**Boot-time check: side-discovery audit.** Eighth firing (S-0033 first; +5 firings through S-0038; +1 at S-0039; +1 at S-0040; S-0041 = eighth). **HANDOFF disposition audit:** fifth firing.

**Boot-time check: scheduled-audit surface.** Per `engine/scheduled_audits.json`. First entry triggers at S-0042 (pushback/lesson tag adoption) — not due at S-0041.

Boot procedure: `Start Engine` in a fresh Claude Code session. CLAUDE.md auto-loads. The S-0041 work is the Phase 5 epistemology build-readiness gate (S-0039 closed with ADR 0047 + parser scaffold; S-0040 closed with the wing-filter fix documented); the gate procedure at [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md) names the load-bearing reads.

### Source documents (S-0041 boot, Phase 5 epistemology gate)

- [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md) — gate procedure.
- [`engine/operations/seed-chunked-authoring.md`](operations/seed-chunked-authoring.md) — Phase 5 per-session migration workflow.
- [`build_plan/P_4_seed_graph_build.md`](../build_plan/P_4_seed_graph_build.md) — Phase 5 chunk file; epistemology is the first sub-session per §5.1.
- [`engine/build_readiness/phase_4_graph_validation.md`](build_readiness/phase_4_graph_validation.md) — exemplar gate report (S-0037 implementation followed it; the Phase 5 gate inherits structure).
- [`product/docs/content-strategy.md`](../product/docs/content-strategy.md) — the "Cross-Domain Reference Inventories — Survey" section authored at S-0038; Tier 4 (SEP, IEP, Routledge, Oxford, Wikipedia) carries the per-candidate graph-shape disposition for the home discipline.
- [`product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md`](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) — structural-reference posture for the philosophy reference class.
- [`engine/adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md`](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) — parser tool ADR; runnable ad-hoc to surface candidate concept inventory.
- [`engine/tools/parse_structural_reference.py`](tools/parse_structural_reference.py) — the parser tool itself.
- [`product/docs/architecture.md`](../product/docs/architecture.md), [`product/docs/learner-model.md`](../product/docs/learner-model.md) — the contracts the seed graph encodes.
- [`product/seed-graph/migrations/PREDICATE_MANIFEST.md`](../product/seed-graph/migrations/PREDICATE_MANIFEST.md), [`product/seed-graph/migrations/ROUTING.md`](../product/seed-graph/migrations/ROUTING.md) — manifest and routing the first Phase 5 migration consumes.
- [`engine/tools/validate.py`](tools/validate.py) — `validate_graph()` is live; the Phase 5 gate inherits the validator's contract rather than naming the audit categories itself.

### After S-0041

S-0042 — natural slot for the first Phase 5 seed-authoring session if the gate at S-0041 lands clean; also the slot at which the scheduled `engine/scheduled_audits.json` audit of pushback/lesson tag adoption fires at boot via the SessionStart hook (per the S-0032 audit Improvement G mechanism).

S-0050 — first cadence-10 health-check trigger that aligns precisely with `next_id mod 10 == 0` after the S-0040 deferred-fix shift (see boot-time cadence note above). Audit window will cover S-0046 through S-0050 (the most recent 5 by then).

## Open tensions and deferred decisions

See [`product/docs/tensions.md`](../product/docs/tensions.md). As of S-0026 close, the OQ-prefixed active set is 9 items (OQ-PRIVACY-A and OQ-PRIVACY-B closed by ADR 0031; OQ-PHASE8-A settled by ADR 0032; OQ-BYOK-REGIME withdrawn by ADR 0032; OQ-OUTWARD-VOICE added at S-0019):

- OQ-DEC1-A through D — Phase DEC.1 architecture decisions (server-side mastery, two-hop retrieval, embedding strategy, chunk-resolver vs URL pointers); decide-before Phase 6
- **OQ-WALL-BEHAVIOR** (added S-0008, per ADR 0029; simplified S-0012) — soft-wall degradation ladder at cost cap; **single-tier ladder** under cost-priced subscription model (free-vs-paid tier-shaped framing collapsed at S-0012); four candidate steps remain (model downshift, retrieval shrink, concept-engagement length cap, soft refusal); decide-before Phase 8 cost-cap wiring
- **OQ-CONTEXT-COMPRESSION** (added S-0008, per ADR 0029; unaffected by S-0012) — token-amplification mitigation for multi-turn engagements (structured-state replacement + automatic compression + explicit summarization candidates); decide-before Phase 7
- **OQ-PEDAGOGY-INFERENCE-LOCUS** (added S-0008, tagged `watch`) — rule layer vs. distributed inference; revisit when inference registry crosses ~30 entries OR cross-domain edges per domain-pair exceed ~50 OR an operational complaint surfaces
- **OQ-OUTWARD-VOICE** (added S-0019) — third expression-contract gap covering outward product surfaces (UI labels, button text, error messages, learner-facing help, public README, App Store description, learner-visible CHANGELOG entries) that neither ADR 0027 nor ADR 0036 governs; decide-before Phase 7 (the earliest plausible moment such a surface ships)
- OQ-WATCH-FLAG-FILE (tagged `watch`) — separate watch-flag file consideration, surfaced at session-30 health check

## Paperclip integration

**Deferred.** Phase 5 trial / Phase 7 commit per ROADMAP.md. Not installed in Foundation.

## Strong working commitments

12 commitments inherited from pre-Foundation design plus 10 architectural decisions. **All 22 are recorded as ADRs** — the contract layer, now split across [`engine/adr/`](adr/) (5 engine ADRs about the build apparatus) and [`product/adr/`](../product/adr/) (33 product ADRs about Paideia-as-product). The audience-facing summary lives in [`product/docs/MISSION.md`](../product/docs/MISSION.md) (commitments 1–12 with ADR cross-refs; commitment 2 reframed at S-0012 as "operating discipline must not corrupt pedagogy" per ADR 0032); the canonical list with ADR pointers lives in [`ROADMAP.md`](../ROADMAP.md) ("Strong working commitments referenced throughout"). The conversational story behind each decision is recoverable from MemPalace `decision`-tagged drawers.

**ADR count post-S-0039: 47 ADRs total — 45 Accepted plus 2 Superseded** (ADR 0002 by ADR 0032; ADR 0032 by ADR 0035), partitioned into **13 engine + 34 product** per [ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md). S-0039 added [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) (engine; structural-reference parser tool and adversarial-triage workflow — operational mechanism for ADR 0046's posture; standing engine tool decoupled from phases; document-agnostic with per-document-type configs; ephemeral signal-not-truth output; adversarial triage defaulting to rejection). S-0038 added [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) (product; structural-reference posture extends from SEP-only to a class of curated philosophy reference works — IEP, Routledge Encyclopedia of Philosophy, Oxford Reference, Wikipedia; graph-shape value distinct from content value; Generative Graph Independence and ADR 0011 unchanged). S-0035 added [ADR 0045](adr/0045-shared-state-integrity-discipline.md) (engine; shared-state integrity discipline — subprocess env scrubbing, atomic mempalace mine writes, boot-time health probes, shutdown soft-warn aggregation across all session invocations). S-0031 added [ADR 0043](adr/0043-hook-architecture.md) (engine; hook architecture for two-layer recording, cadence trigger, STATE.md required-fields verification) and [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md) (engine; skill conversion partition for recipe-shaped vs reference-shaped operations docs); amended [ADR 0022](adr/0022-periodic-project-health-checks.md) Decision + Consequences (cadence-trigger logic corrected from `last_claimed mod` to `next_id mod`); amended [ADR 0043](adr/0043-hook-architecture.md) Decision (post-state-edit.sh ADR-count check dropped per cold-review finding). S-0029 added [ADR 0041](adr/0041-cascade-analysis-discipline.md) (engine; cascade-analysis discipline) and [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) (engine; soft-warn lifecycle, archive-as-canon); amended [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) Consequences (gate-on-the-gate citation + cold-review pass per S-0028 case study); amended [ADR 0022](adr/0022-periodic-project-health-checks.md) Consequences (deliverable-handshake re-pointing post-S-0024 partition for `health_check.py`). The contract layer the [`build_plan/`](../build_plan/) chunks consume by reference. [`engine/tools/validate.py`](tools/validate.py)'s `adr_index_consistency` check audits the ADR-file-vs-index alignment across both subtree READMEs; the `cascade_*` checks (per ADR 0041) audit superseded-citation currency, back-reference orphans, and Consequences-deliverable promises; `validate_shared_state_health()` (per ADR 0045) audits chromadb palace and git repo sanity.
