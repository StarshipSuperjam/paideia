# Project State

> The single file every session reads first. Names what's current and what's next. Updated at every build-session close.

## Current

| Field | Value |
|---|---|
| **Project** | Paideia — knowledge mastery app on a pedagogical dependency graph |
| **GitHub** | https://github.com/StarshipSuperjam/paideia (private) |
| **Current phase** | **Phase 5 — Philosophy subdomain seeding (closed; S-0045 → S-0076).** 16/16 tasks complete; T-PHASE-5 target-met. 380 nodes + 536 edges across 15 seed migrations (epistemology / ethics / metaphysics / mind / language / science / logic / political / aesthetics + service nodes + cross-bridges). Phase 5 closeout report at [`engine/build_readiness/phase_5_closeout.md`](build_readiness/phase_5_closeout.md). **Phase 5 production audit warrant gate ran at S-0081**: PROCEED recommended pending user adjudication; gate report at [`engine/build_readiness/phase_5_production_audit_gate.md`](build_readiness/phase_5_production_audit_gate.md). Phases 0–5 all closed; Phase 6 master plan blocked behind audit and OQ-DEC1 settlement. **57 ADRs — 55 Accepted + 2 Superseded — 22 engine + 35 product** (S-0078 added [ADR 0056](adr/0056-mempalace-mechanical-adoption-checks.md) — MemPalace mechanical adoption checks). Full session-by-session history in [ENGINE_LOG.md](ENGINE_LOG.md). |
| **Last build session** | S-0082 (2026-05-06) — **Phase 5 production audit master-plan session.** Interactive `/start-engine` per S-0081 gate's PROCEED recommendation. Authored two artifacts: (1) [`engine/build_readiness/phase_5_production_audit.md`](build_readiness/phase_5_production_audit.md) — master-plan gate report containing fixed SEP-anchored prompt template (per-edge + per-node), fixed evidence file schema, sample-size policy table (35% per-subdomain density per S-0081 gate's expansion rule given 47% cross-bridge defect rate), three diagnostic syllabus targets (Gettier, modal realism, social_contract_theory substituting for absent free_speech), Tier 1/2/3 triage (T1-A through T1-F resolved at this gate; T2-A through T2-E within-session decision rules codified; T3-A through T3-F deferred to closeout), 4 pre-listed audit-system-input proposals (`edge_evidence_empty`, `discipline_label_node_at_root`, `prereq_direction_summary_inconsistency` (NLP, possibly defer), `historical_node_as_prereq_source`), and the structural-reopen pre-flag for `historical_influence` predicate activation (extrapolated 4 of 15 cross-bridges historical-not-pedagogical → ~19 of 71, exceeds ≥10 ADR-warranting threshold per the approved plan); (2) [`engine/session/auto_target.json`](session/auto_target.json) — replaced T-PHASE-5 (closed) with **T-PHASE-5-AUDIT** (12 tasks, max_sessions 14): AUDIT-CB cross-bridge census + 10 per-subdomain (AUDIT-EPI / ETH / MET / MIN / LOG / LAN / SCI / POL / AES / SVC) + AUDIT-HT hubs+traces; depends_on graph honors AUDIT-CB-runs-first → 10 parallel subdomain after CB completes → AUDIT-HT-after-all; per-task scope_lock pinned to single evidence file under `engine/build_readiness/phase_5_production_audit_evidence/<task>.md`; criteria are `file_exists` + `validate_passes` (mechanical objective verification only — content authenticity is the closeout's interactive disposition surface). check_target.py parses new target cleanly: 12 tasks pending, validate_passes already true, file_exists false-pending awaiting routine work. STATE.md / ENGINE_LOG.md updated. MemPalace diary entry filed (per ADR 0056). Considered: writing a new engine ADR formalizing the post-phase production-audit pattern; deferred to audit closeout per Tier 3-F judgment (one cross-cutting mechanism is now under exercise — the warrant-gate + master-plan + routine-block + closeout pattern; ADR-warranting structure may not crystallize until findings are in hand). Considered and not done: closeout authoring (final reports, Issue filing, ADR memos, README multi-class restructure) — that's the interactive closeout session AFTER the 12 routine evidence sessions complete; the master plan is correctly scoped to setup, not setup+execute+close. Routine sessions can now begin firing on cadence; the user can switch the routine to Hourly to exercise the audit, or leave on Manual and trigger fires deliberately. |
| **S-0081 (prior)** | S-0081 (2026-05-06) — **Phase 5 production audit warrant gate.** Interactive `/start-engine` session per the user-approved plan at `~/.claude/plans/exploration-session-phase-5-flickering-newell.md`. Single-session preflight to determine whether the proposed 14-session production audit is warranted before committing to it. Pulled live graph from paideia-dev (380 nodes / 536 edges / all `pedagogical_prerequisite`); sampled 15 of 71 cross-bridges with SEP-anchored review, 5 of 72 epistemology within-domain edges, ran topology pass (per-subdomain degree distribution, top-10 hubs, three syllabus traces — Gettier / modal realism / justice-substituting-for-absent-free-speech). Threshold-rule outcome: **PROCEED** — 8 substantive findings of 20 sampled edges (40%), 4 of 15 cross-bridges historical-not-pedagogical (extrapolating to ~19 of 71, exceeding the ≥10 ADR-warranting threshold for the `cross_domain_dependency` structural reopen), one clean reversed edge (`propositional_attitude → proposition` contradicts its own node summaries), 0/536 edges with evidence-field text (graph-wide discipline gap; `gate-feasible` audit-system-input proposal), free speech absent from political philosophy (coverage defect), and discipline-label nodes (Political Philosophy / Metaphysics / Ontology) acting as syllabus-trace roots (granularity-mismatch defect class). Within-domain epistemology: 0 findings of 5 — defects concentrate in cross-bridges. Output: gate report committed to `engine/build_readiness/phase_5_production_audit_gate.md`; minimal row added to `engine/build_readiness/README.md` for the audit-warrant-gate class (full multi-class restructure deferred to audit closeout per Issue #26). MemPalace diary entry + decision drawer filed (per ADR 0056 mechanical-adoption discipline). Considered and rejected: (a) updating `engine/session/auto_target.json` here — that's the master-plan session's scope, not the gate's; (b) filing GitHub Issues for individual findings — per plan, the closeout adjudicates dispositions; (c) full README multi-class restructure — deferred to closeout. Forward inputs to the master-plan session pre-listed in the gate report: bump per-subdomain density to 35% (cross-bridge defect rate 47% triggers the plan's expansion rule), substitute diagnostic-target for free_speech, pre-flag structural reopen for `cross_domain_dependency` / `historical_influence` activation, pre-list 4 audit-system-input proposals freeing evidence sessions for pure content review. |
| **S-0080 (prior)** | S-0080 (2026-05-06) — **Cleanup of misleading S-0079 artifacts.** Interactive `/start-engine` session. S-0079's valid deliverables (B1 audit script + audit report) survive; its misleading deliverables (B3 wrapper `recover_mempalace_from_transcript.py` + user-driven recovery guide `mempalace-recovery-guide-user-driven.md`) were both deleted because they encoded the wrong recovery shape. The user's actual ask was a brief reflection prompt to paste into ORIGINAL sessions still accessible to the user; the original session writes its own MemPalace entries in its own voice (honest first-person, attributed to its own `S-NNNN`, original session's own context — no transcript re-reading, no separate session, no "analytical voice" framing). S-0080 deleted the wrong-shaped artifacts, added a correction note to the audit report at [`engine/docs/audits/mempalace-attribution-S0001-to-S0077.md`](docs/audits/mempalace-attribution-S0001-to-S0077.md) explaining the actual recovery shape, edited the S-0079 archive's `outcome_summary` and `scope_delivery` to mark the misdirection, and corrected this STATE.md entry + `ENGINE_LOG.md` to reflect what was actually valid. The 6 dry-run mempalace items rolled back at S-0079 stay rolled back (the rollback itself was correct; the followup direction was wrong). The B1 audit script + audit report are the only S-0079 deliverables that survive S-0080's correction. T1-B (routine first-exercise of ADR 0056 mechanism) still pending; closes at next routine fire post-S-0078. |
| **S-0079 (superseded narrative)** | **2026-05-06 — produced one valid deliverable** (engine/tools/audit_mempalace_attribution.py + engine/docs/audits/mempalace-attribution-S0001-to-S0077.md) **and two misdirected deliverables** (B3 wrapper + recovery guide, both deleted at S-0080). Misdirection arose because the AI assumed Part B recovery required a separate session reading transcripts — first as subagent ventriloquism (rejected by the harness with a content-integrity denial during S-0067 dry-run), then as analytical-voice authorship from a separate session (still wrong-shaped). The user's actual ask was a brief reflection prompt for the ORIGINAL sessions, which remain accessible to the user. See S-0079's archive `outcome_summary` for the full original narrative plus S-0080's correction note. |

| **Last commit on main** | (see `git log --oneline -1` on main) |
| **Backup tags** | `pre-foundation-v0.0.0` at commit `fa70b8c` — pre-machinery anchor. `pre-phase-3-v0.0.1` at commit `36b0907` (S-0026 close) — pre-Phase-3 anchor cut at S-0027. Both are historical anchors, not operational fallback targets — file-level retrieval via `git show <tag>:<path>` is the legitimate use. |

## Infrastructure

| Component | Value |
|---|---|
| **Supabase project** | `paideia-dev`, project ref `ozooosgnuzxqqypotlke`, PostgreSQL 17.6 |
| **Supabase MCP** | configured in `.mcp.json` (gitignored, contains PAT) |
| **MemPalace** | installed at `/Users/shanekidd/Library/Python/3.9/bin/`; MCP server `mempalace-mcp` configured in `.mcp.json`; wing `paideia` carries **485 drawers across 5 rooms** as of S-0032 close (general 330, operations 88, decisions 55, foundation-planning-s0001 10, s0003-adr-collection 2); capture hooks wired in `.claude/settings.json` (Stop + PreCompact wrapper PATH-fixed at S-0032 — auto-capture had been silently failing since S-0002 because `mempalace` CLI wasn't on the hook subshell's PATH; verify at next Stop event by checking `.claude/logs/mempalace-hook.log` for `OK exit=0` lines instead of `FAIL exit=127`); project usage scope documented at [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Project usage scope" — uses search, add_drawer, diary (agent_name `claude`), read-only inspection; explicitly excludes KG and tunnels |
| **Python** | **project venv** at `<main_repo>/.venv/` (Python 3.12.13, uv-managed) per [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md) — engine tools + mempalace run from here; PATH wiring in `engine/tools/scrub_env.sh` resolves bare `python3` / `ruff` / `mypy` / `pytest` / `mempalace` to venv. Pre-S-0043 fallback: system Python 3.9.6 at `/usr/bin/python3`; user-scope packages at `/Users/shanekidd/Library/Python/3.9/bin/` (no longer used by engine) |
| **Anthropic API** | env var `ANTHROPIC_API_KEY` set in local `.env` (gitignored) |

## Next session work item

**S-0083 — first routine evidence session against T-PHASE-5-AUDIT (AUDIT-CB cross-bridge census).** The audit master plan landed at S-0082; the routine engine loop can now fire. AUDIT-CB has no `depends_on` so it's the first eligible task; subdomain tasks (AUDIT-EPI through AUDIT-SVC) become eligible after AUDIT-CB completes; AUDIT-HT runs last after all 10 subdomain tasks complete.

The user controls whether to fire routines now (switch routine to Hourly, or trigger Manual fires) or stage interactively. Either is fine. Routine boot procedure per [`engine/operations/routine-mode-operations.md`](operations/routine-mode-operations.md): boot-freshness gate → concurrency lock → mode detect → pause check → target-met check (will fail; tasks pending) → max-sessions check → eligibility selection (AUDIT-CB) → MemPalace boot query + diary read → plan authoring → scope-check → claim slot → execute work → verify completion → shutdown.

The routine session's actual work for AUDIT-CB:

1. Read [`engine/build_readiness/phase_5_production_audit.md`](build_readiness/phase_5_production_audit.md) at boot (the master plan's prompt template + evidence file schema).
2. Pull all 71 cross-bridge edges from live Supabase (paideia-dev) with full metadata.
3. Apply the SEP-anchored prompt template to each edge (verdict, reasoning, confidence).
4. Write candidate findings to `engine/build_readiness/phase_5_production_audit_evidence/crossbridges.md` per the fixed schema.
5. Commit; criteria check (`file_exists` + `validate_passes`) marks task `complete`.

AUDIT-CB's defect rate calibrates within-session sample expansion for downstream tasks per master-plan T2-A.

**After 12 routine evidence sessions complete** (target-met state), the user runs an **interactive `/start-engine` closeout session** to author the production-audit final report + audit-system-input report, file Issues, author structural-reopen ADR memos, and update the build_readiness/README.md per Issue #26. Closeout is NOT a routine task (T1-F per the master plan).

**OQ-DEC1-A/C/D settlement** (Phase 6 prereq) and **Phase 6 master plan authoring** both land after audit closeout.

**Phase 6 master plan authoring (per [ADR 0051](adr/0051-routine-mode-and-engine-loop.md), shifts to post-audit-closeout):** (a) read [ROADMAP.md](../ROADMAP.md) Phase 6 + [build_plan/P_6_self_correction.md](../build_plan/P_6_self_correction.md) for canonical phase intent; (b) adversarial reconnaissance per [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md); (c) author `engine/build_readiness/phase_6.md` (gate report) + `engine/session/auto_target.json` (new executable contract for T-PHASE-6) + any new ADRs the gate surfaces. Pre-decided Phase 6 deliverables per the closeout's "Forward pointers" section: rigor_score calibration (T3-C; 366 `missing_rigor_score` warns at Phase 5 close form the backlog counter); branch-based rollback verification (T3-A inherited from phase_3_sql.md); `parse_structural_reference.py` first-real-use plus adversarial-triage prompt template (T3-E per [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md)); philosophy of religion readmission criterion (T3-G — class-boundary clause in [ADR 0052 (product)](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md)). On audit PROCEED, audit findings flow into Phase 6 scoping cleanly.

**Routine engine loop is now live against T-PHASE-5-AUDIT.** S-0082 replaced the previous T-PHASE-5 (closed) target. Every routine fire from S-0083+ boots, reads `auto_target.json`, runs `check_target.py`, finds AUDIT-CB pending (no depends_on; eligible immediately), claims a slot, executes the cross-bridge census, commits the evidence file, marks complete, closes. After AUDIT-CB completes, the 10 subdomain tasks become parallel-eligible (subject to max one routine session at a time per the lockfile). After all 10 complete, AUDIT-HT becomes eligible. After AUDIT-HT completes, target-met → routines exit at step 3 without claiming → user runs interactive closeout. The routine can run on Hourly without harm; the user can also leave on Manual and trigger fires deliberately. 14 max_sessions (12 tasks + 2 buffer), comparable to Phase 5's 18 max_sessions (16 tasks + 2 buffer).

**Cleanup-batch surface — issue close-out campaign in progress (S-0083+).** Per the user-approved 7-session campaign plan at `~/.claude/plans/i-need-a-plan-prancy-ripple.md`. S-A (S-0083) closes Issues [#37](https://github.com/StarshipSuperjam/paideia/issues/37) (jsonl truncation), [#26](https://github.com/StarshipSuperjam/paideia/issues/26) (build_readiness/README.md closeout-class indexing), [#29](https://github.com/StarshipSuperjam/paideia/issues/29) (ideation.md retire), [#32](https://github.com/StarshipSuperjam/paideia/issues/32) + [#21](https://github.com/StarshipSuperjam/paideia/issues/21) (tiktoken/transcript_token_pct telemetry retire). Subsequent campaign sessions S-B through S-G close Issues #31, #30, #28, #36, #35, #23, #33, #34 in turn. Remaining open after campaign: #1 + #2 (upstream-blocked), #22 (Metabase deferred-with-reason), #24 (graph renderer deferred to post-audit-closeout), plus a new upstream MemPalace-repair tracker filed in S-B and any child Issues from S-D. Issues #20 and #27 closed at S-0078.

**MemPalace wing-filter / wing-naming bugs unfixed** (Issues [#1](https://github.com/StarshipSuperjam/paideia/issues/1), [#2](https://github.com/StarshipSuperjam/paideia/issues/2)) against mempalace 3.3.4. Workaround: unfiltered `mempalace_search`.

**Boot-time check: cadence trigger.** Cleared at S-0077 audit close (`last_audit_session: S-0077`). Next natural fire at S-0087 (slots_since=10 at that boot).

**Boot-time check: Issues backlog.** S-0081 added zero new Issues (gate findings recorded in the gate report; per plan, dispositions to Issues happen at audit closeout, not gate). Backlog state at S-0082 boot will match S-0081 boot from the SessionStart hook surface (0 bugs / 6 tech-debt / 2 cleanup / 5 enhancement / 4 health-check / 4 docs / 1 question; 0 urgent; 2 upstream-blocked). Note: the SessionStart hook's backlog count diverges from the older STATE.md prose (which counted only #21, #23, #22, #24, #26 = 5 issues for 6 categories). The hook's count is canonical; the prose was stale. Audit closeout will produce a fresh batch of Issues for findings.

**Boot-time check: scope-erosion signal.** Last 5 archives at S-0083 boot will be S-0078, S-0079, S-0080, S-0081, S-0082 — S-0079 had a scope-delivery correction applied at S-0080. S-0081 (gate) and S-0082 (master plan) are both clean deliveries. Signal silent.

**Boot-time check: MemPalace adoption (new at S-0078 per ADR 0056).** From S-0079 forward, the per-session `mempalace_activity` field on archives is mechanically validated. The `validate.py --final-check` audit fires at shutdown step 1 (after `scan_mempalace_activity.py` writes the rollup at step 0). Three categories:
- `mempalace_boot_query_skipped` (soft-warn) — no `mempalace_search` call recorded.
- `mempalace_diary_read_skipped` (soft-warn) — no `mempalace_diary_read` call recorded.
- `mempalace_diary_write_skipped` (hard-fail) — no `mempalace_diary_write` call recorded AND no `mempalace_unavailable_acknowledged: <reason>` token in `outcome_summary`.

The first-exercise readiness note at [`engine/build_readiness/mempalace_mechanical_adoption_first_exercise.md`](build_readiness/mempalace_mechanical_adoption_first_exercise.md) tracks T1-B (routine-context first exercise) — closes at the next routine fire post-S-0078 with non-zero `diary_write_calls` in its archived `mempalace_activity`.

**Boot-time check: shared-state health probe.** Per [ADR 0045](adr/0045-shared-state-integrity-discipline.md). Healthy expected.

**Boot-time check: side-discovery audit + HANDOFF disposition audit + archive structured-fields audit.** All three continue running per their respective contracts at every commit + every close.

**Boot-time check: scheduled-audit surface.** Active `audits` array empty. No scheduled audits due.


## Open tensions and deferred decisions

See [`product/docs/tensions.md`](../product/docs/tensions.md). As of S-0026 close, the OQ-prefixed active set is 9 items (OQ-PRIVACY-A and OQ-PRIVACY-B closed by ADR 0031; OQ-PHASE8-A settled by ADR 0032; OQ-BYOK-REGIME withdrawn by ADR 0032; OQ-OUTWARD-VOICE added at S-0019):

- OQ-DEC1-A through D — Phase DEC.1 architecture decisions (server-side mastery, two-hop retrieval, embedding strategy, chunk-resolver vs URL pointers); decide-before Phase 6
- **OQ-WALL-BEHAVIOR** (added S-0008, per ADR 0029; simplified S-0012) — soft-wall degradation ladder at cost cap; **single-tier ladder** under cost-priced subscription model (free-vs-paid tier-shaped framing collapsed at S-0012); four candidate steps remain (model downshift, retrieval shrink, concept-engagement length cap, soft refusal); decide-before Phase 8 cost-cap wiring
- **OQ-CONTEXT-COMPRESSION** (added S-0008, per ADR 0029; unaffected by S-0012) — token-amplification mitigation for multi-turn engagements (structured-state replacement + automatic compression + explicit summarization candidates); decide-before Phase 7
- **OQ-PEDAGOGY-INFERENCE-LOCUS** (added S-0008, tagged `watch`) — rule layer vs. distributed inference; revisit when inference registry crosses ~30 entries OR cross-domain edges per domain-pair exceed ~50 OR an operational complaint surfaces
- **OQ-OUTWARD-VOICE** (added S-0019) — third expression-contract gap covering outward product surfaces (UI labels, button text, error messages, learner-facing help, public README, App Store description, learner-visible CHANGELOG entries) that neither ADR 0027 nor ADR 0036 governs; decide-before Phase 7 (the earliest plausible moment such a surface ships)
- OQ-WATCH-FLAG-FILE (tagged `watch`) — separate watch-flag file consideration, surfaced at session-30 health check

## Paperclip integration

**Rejected at S-0044, superseded by [ADR 0051](adr/0051-routine-mode-and-engine-loop.md).** Investigation surfaced that Paperclip-dispatched Claude work runs under `ANTHROPIC_API_KEY` per Anthropic's April-2026 third-party-orchestration policy (subscription does not cover it; Paperclip GitHub issues #1163, #3270). For a solo project on subscription with no real parallelism need, the recurring per-token API spend doesn't justify Paperclip's value-add. The replacement architecture — Claude Code Routines + `engine/session/auto_target.json` + `engine/tools/check_routine_scope.py` — accomplishes dispatch + termination + anti-rogue at zero recurring spend, on existing apparatus. Paperclip rejection reasoning preserved in ADR 0051 "Alternatives Considered." If the project ever expands beyond Claude (multi-runtime), or hires collaborators (multi-user governance), or accepts API spend on principle (e.g., for unattended Phase 8 evaluation runs), Paperclip becomes a reasonable revisit.

## Strong working commitments

12 commitments inherited from pre-Foundation design plus 10 architectural decisions. **All 22 are recorded as ADRs** — the contract layer, now split across [`engine/adr/`](adr/) (5 engine ADRs about the build apparatus) and [`product/adr/`](../product/adr/) (33 product ADRs about Paideia-as-product). The audience-facing summary lives in [`product/docs/MISSION.md`](../product/docs/MISSION.md) (commitments 1–12 with ADR cross-refs; commitment 2 reframed at S-0012 as "operating discipline must not corrupt pedagogy" per ADR 0032); the canonical list with ADR pointers lives in [`ROADMAP.md`](../ROADMAP.md) ("Strong working commitments referenced throughout"). The conversational story behind each decision is recoverable from MemPalace `decision`-tagged drawers.

**ADR count post-S-0078: 57 ADRs total — 55 Accepted plus 2 Superseded** (ADR 0002 by ADR 0032; ADR 0032 by ADR 0035), partitioned into **22 engine + 35 product** per [ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md). S-0078 added [ADR 0056](adr/0056-mempalace-mechanical-adoption-checks.md) (engine; MemPalace mechanical adoption checks across all session types — closes Issues #27 and #20). S-0064 added [ADR 0055](adr/0055-apply-migration-wrapping-against-production-reads-gate.md) (engine; apply-migration wrapping against the production-reads classifier). S-0061 added [ADR 0054](adr/0054-lifecycle-push-wrapping-against-default-branch-push-gate.md) (engine; lifecycle-push wrapping against the default-branch push gate). S-0058 added [ADR 0053](adr/0053-mechanism-first-exercise-gate.md) (engine; mechanism first-exercise gate before unattended use). S-0046 added [ADR 0052](adr/0052-routine-boot-freshness-and-concurrency-defense.md) (engine; routine boot freshness and concurrency defense). S-0045 added [ADR 0052 (product)](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md) (Phase 5 philosophy-subdomain decomposition — 9 subjects + service nodes + cross-bridges + closeout; class-boundary admit-criterion for future readmissions; philosophy of religion and history-as-subdomain explicitly deferred). S-0044 added [ADR 0051](adr/0051-routine-mode-and-engine-loop.md) (engine; routine-mode session pattern; third session mode; two-layer architecture). S-0043 added [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md) (engine; project-local Python venv). S-0042 added [ADR 0048](adr/0048-handoff-narrowing-and-github-issues-for-cross-session-deferrals.md) and [ADR 0049](adr/0049-scope-lock-at-boot-and-descope-reorder-audit-at-shutdown.md). S-0039 added [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) (engine; structural-reference parser tool and adversarial-triage workflow). S-0038 added [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) (product; structural-reference posture for the philosophy reference class). S-0035 added [ADR 0045](adr/0045-shared-state-integrity-discipline.md) (engine; shared-state integrity discipline). S-0031 added [ADR 0043](adr/0043-hook-architecture.md) (engine; hook architecture) and [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md) (engine; skill conversion partition). S-0029 added [ADR 0041](adr/0041-cascade-analysis-discipline.md) (engine; cascade-analysis discipline) and [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) (engine; soft-warn lifecycle, archive-as-canon). The contract layer the [`build_plan/`](../build_plan/) chunks consume by reference. [`engine/tools/validate.py`](tools/validate.py)'s `adr_index_consistency` check audits the ADR-file-vs-index alignment across both subtree READMEs; the `cascade_*` checks (per ADR 0041) audit superseded-citation currency, back-reference orphans, and Consequences-deliverable promises; `validate_shared_state_health()` (per ADR 0045) audits chromadb palace and git repo sanity. (Note: `adr_index_consistency` audits machine-readable indexes; STATE.md prose claims about ADR counts are NOT mechanically audited — drift in this section requires author-discipline-at-edit-time per the Posture-vs-machinery framing in [CLAUDE.md](../CLAUDE.md). Any edit touching this section or the headline-table "Current phase" row should re-count from `engine/adr/[0-9]*.md` and `product/adr/[0-9]*.md`.)
