# Project State

> The single file every session reads first. Names what's current and what's next. Updated at every build-session close.

## Current

| Field | Value |
|---|---|
| **Project** | Paideia — knowledge mastery app on a pedagogical dependency graph |
| **GitHub** | https://github.com/StarshipSuperjam/paideia (private) |
| **Current phase** | Phase 0 — closed (S-0003); **Phase 1 — Contract Lock fully closed at S-0012**. **Phase 1.5 — Mission Realignment Overhaul fully closed at S-0021.** **S-0022** committed the engine / product wall ([ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md)) and renamed `CHANGELOG.md` → `ENGINE_LOG.md`. **Phase 2 (Build Plan Scaffolding) closed at S-0023.** **S-0024–S-0025** executed the folder migration into `engine/` and `product/` subtrees and the cross-reference sweep. **S-0026** authored the third project expression contract per [ADR 0038](adr/0038-expression-contract-for-ai-authored-code.md). **S-0027 widened the expression contract** per [ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md) and **committed the build-readiness gate** per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md). **Phase 3 — SQL Schema Implementation closed at S-0028** with eight migrations deployed to `paideia-dev` (PostgreSQL 17.6); RLS enabled on every public table; CHECK constraints enforce gate T2-A enums; three gate-report drifts corrected in-flight per architecture.md-authoritative design. **S-0029 closed the infrastructure-misfire sweep** per the approved plan: doc drift sweep across README/ROADMAP/ADR README indices; cascade-discipline ([ADR 0041](adr/0041-cascade-analysis-discipline.md), [`cascade-discipline.md`](operations/cascade-discipline.md), three new soft-warn validator checks); soft-warn lifecycle ([ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md), [`soft-warn-lifecycle.md`](operations/soft-warn-lifecycle.md), structured `outcome_summary_soft_warns` archive field); gate-on-the-gate amendment to [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) with S-0028 case-study material; [`engine/tools/health_check.py`](tools/health_check.py) per ADR 0038 three-layer contract; `docs/health-checks/` scaffolding; engine-side [`cross-references.md`](operations/cross-references.md); CLAUDE.md context-budget recalibration for 1M window. ADR count post-S-0029: **42 ADRs total — 40 Accepted plus 2 Superseded**, partitioned **9 engine + 33 product**. |
| **Last build session** | S-0029 (2026-05-02) — **Infrastructure-misfire sweep (Session α foundations) per approved plan at `/Users/shanekidd/.claude/plans/the-project-infrastructure-still-abundant-key.md`.** Ten steps closing eight surfaced infrastructure misfires plus two adjacent issues. Step 1: doc drift sweep — README.md status sentence + project-history section rewritten to point at engine/STATE.md as canonical; ROADMAP.md drift cleared; engine/adr/README.md and product/adr/README.md ADR-narration ("executed at S-0024") stripped from evergreen prose; product/adr/README.md re-points project-total ADR claim to engine/STATE.md to avoid future drift. Step 2: [ADR 0041](adr/0041-cascade-analysis-discipline.md) (engine, Accepted) commits to cascade-analysis discipline composed of three soft-warn validator checks plus manual procedures; Layer 1 source-of-truth at [`cascade-discipline.md`](operations/cascade-discipline.md) names the procedures (file rename grep, ADR supersession audit, ops-doc restructure, deliverable handshake) and the Orphan-OK annotation mechanism. Step 3: [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) (engine, Accepted) moves trend canon from gitignored `validate-history.jsonl` to committed `engine/session/archive/S-NNNN.json` field `outcome_summary_soft_warns`; Layer 1 source-of-truth at [`soft-warn-lifecycle.md`](operations/soft-warn-lifecycle.md); session-shutdown-sequence.md step 6 amended to write the structured field; tools-validate-interpretation.md gains the new categories + persistent-warn annotation section; .claude/commands/start-engine.md gains step 2b boot surface (3-of-5-archives surfaces; 10-consecutive-archives escalates to promote/accept/address). Step 4: gate-on-the-gate amendment to [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) Consequences names S-0028's four mid-build divergences (T1-A trigger under-specification, T2-C concept_id UUID→TEXT, T2-E edges schema corrections) as the failure case; build-readiness-gate.md gains step 7 (per-Tier-1/2-decision source-document citation) and step 8 (cold-review pass against citations) with the S-0028 case study as a worked example showing exactly which divergences cold-review would have caught. Step 5: `docs/health-checks/` directory + README + TEMPLATE.md scaffolding (TEMPLATE mirrors health-check.md report shape with the new ADR 0041/0042 categories). Step 6: [`engine/tools/health_check.py`](tools/health_check.py) end-to-end under ADR 0038 three-layer contract — module-level + per-function contract blocks, ruff + ruff-format + mypy --strict clean, 45 pytest tests passing; CLI `--session S-NNNN [--dry-run]`; reads committed sources only (does NOT query MemPalace per documented non-responsibility — manual augmentation per docs/health-checks/README.md step 2). Closes the [ADR 0022](adr/0022-periodic-project-health-checks.md) Consequences promise that anticipated the script "around S-0025"; ADR 0022 amended in same session per cascade-discipline.md deliverable handshake (Consequences re-points to engine/-prefixed paths post-S-0024 partition). Step 7: three new soft-warn validator checks (`validate_superseded_adr_currency`, `validate_adr_back_reference_orphan` with Orphan-OK annotation suppression, `validate_adr_consequences_deliverable_audit` with both repo-root-relative and ADR-directory-relative path resolution) integrated into `validate_repo_structure` via `r.merge(...)`; 14 new pytest tests; total pytest 115 passing. Validator post-step-7: 14 checks, 0 hard-fails, 0 soft-warns. Step 8: engine-side [`cross-references.md`](operations/cross-references.md) authored as the engine companion to `product/docs/CROSS_REFERENCES.md`; maps engine ADRs / operations docs / tools / harness to consumers; product/docs/CROSS_REFERENCES.md gains "product side" subtitle and companion link. Step 9: CLAUDE.md "Budget guidance" recalibrated for 1M window (Claude Opus 4.7) — explicit absolute-token thresholds (600K substantive cap, 800K mechanical cap), explicit "do not propose splitting based on 'this feels big' alone" rule, four worked examples at 20%/35%/65%/75% load. Cold-review pass via Explore subagent on the four touched Python files (health_check.py, test_health_check.py, validate.py extensions, test_validate.py extensions) — all four match contract; no premise drift detected. **outcome_summary_soft_warns shape (per new ADR 0042 closing discipline): all categories at 0.** ADR count: **42 total — 40 Accepted plus 2 Superseded**, partitioned **9 engine + 33 product**. Two new engine ADRs (0041, 0042) added; ADR 0040 amended; ADR 0022 amended (deliverable-handshake re-pointing). |

| **Prior build session** | S-0028 (2026-05-02) — **Phase 3 SQL schema implementation.** First substantive build phase under the universal expression contract ([ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md)) and the build-readiness gate protocol ([ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md)). Eight migrations under [`product/seed-graph/migrations/`](../product/seed-graph/migrations/) deployed to `paideia-dev` (PostgreSQL 17.6) via Supabase MCP. RLS on every public table; FKs to `public.users(id)` cascade per ADR 0031. Three gate-report drifts corrected inline per architecture.md-authoritative design (T1-A DELETE trigger; T2-C `mastery_snapshots.concept_id` UUID→TEXT; T2-E `edges.source_id`/`target_id` UUID→TEXT plus `type`→`edge_type` plus `provenance`/`evidence` JSONB→TEXT). The mid-build gate-error correction was the failure case S-0029's gate-on-the-gate amendment closes. Validator clean; SQL gates clean per file; pytest 56/56; cold-review all 8 files match. Two forward-pointers to S-0029 / Phase 4 gate (branch rollback verification, sub-signal NULL permissiveness) named in S-0028 outcome_summary; both still queued for the eventual Phase 4 gate exercise. |

| **Last commit on main** | (see `git log --oneline -1` on main) |
| **Backup tags** | `pre-foundation-v0.0.0` at commit `fa70b8c` — pre-machinery anchor. `pre-phase-3-v0.0.1` at commit `36b0907` (S-0026 close) — pre-Phase-3 anchor cut at S-0027. Both are historical anchors, not operational fallback targets — file-level retrieval via `git show <tag>:<path>` is the legitimate use. |

## Infrastructure

| Component | Value |
|---|---|
| **Supabase project** | `paideia-dev`, project ref `ozooosgnuzxqqypotlke`, PostgreSQL 17.6 |
| **Supabase MCP** | configured in `.mcp.json` (gitignored, contains PAT) |
| **MemPalace** | installed at `/Users/shanekidd/Library/Python/3.9/bin/`; MCP server `mempalace-mcp` configured in `.mcp.json`; wing `paideia` indexed at S-0002 (414 drawers across rooms `general` + `operations`); capture hooks wired in `.claude/settings.json` |
| **Python** | system Python 3.9.6 at `/usr/bin/python3`; user-scope packages at `/Users/shanekidd/Library/Python/3.9/bin/` |
| **Anthropic API** | env var `ANTHROPIC_API_KEY` set in local `.env` (gitignored) |

## Next session work item

**S-0030 — Phase 4 build-readiness gate exercise (gate session) producing `engine/build_readiness/phase_4_graph_validation.md`.** Re-queued from S-0029 after the user-directed override that took S-0029 for the infrastructure-misfire sweep. Phase 4 is the next substantive build per [ROADMAP.md](../ROADMAP.md) — fleshing out [`engine/tools/validate.py`](tools/validate.py)'s `validate_graph()` extension point against the deployed Phase 3 schema. Per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) (now amended at S-0029 with the gate-on-the-gate cold-review pass), every substantive build session is preceded by a gate session whose deliverable is a build-readiness report. S-0030 runs the gate procedure per [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md): launch up to three Explore agents in parallel against [`build_plan/P_2_graph_validation.md`](../build_plan/P_2_graph_validation.md), [ADR 0016](adr/0016-graph-construction-needs-live-validation.md), and [`product/seed-graph/migrations/PREDICATE_MANIFEST.md`](../product/seed-graph/migrations/PREDICATE_MANIFEST.md); triage findings into Tier 1 / Tier 2 / Tier 3; **cite source-document line(s) per Tier 1/2 decision** (gate-on-the-gate step 7, new at S-0029); author the report; **launch cold-review sub-agent against citations before close** (gate-on-the-gate step 8, new at S-0029); update STATE.md to point at it.

S-0030 is also the first natural moment to address **two forward-pointers from S-0028's `outcome_summary`** that the next gate session should classify and either resolve or formally defer:
1. **Branch-based rollback verification.** The Phase 3 gate report's T2-H rollback test was downgraded at S-0028 (Supabase development branches are cost-bearing; auto-mode deferred to explicit user confirmation). Decide: ship a local Supabase CLI setup as part of S-0030's deliverable so Phase 4 can verify rollback locally, OR confirm the cost-bearing branch creation as a one-time verification expense, OR accept the reduced-proof posture as standing.
2. **Sub-signal NULL permissiveness in 0005_learner_events.sql.** Gate T2-B's CHECK is permissive on NULL for composite-applicable interaction types per PostgreSQL CHECK semantics (NULL → TRUE). The strict intent ("NULL on composite-type events is a data error" per learner-model.md:60) is application-layer. Decide: tighten the schema-level CHECK in a 0009 migration, OR formalize the application-layer enforcement contract as the standing posture.

**Boot-time check: cadence trigger.** At S-0030 boot, `last_claimed = S-0029`, so `29 % 30 = 29 ≠ 0` — health-check cadence trigger does NOT fire at S-0030. The trigger fires at the boot AFTER S-0030 closes (when `last_claimed = S-0030` and `30 % 30 = 0`). The first health-check audit therefore lands at S-0031 (or later if deferred). Per [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) the trigger reads the structured `outcome_summary_soft_warns` archive field; S-0029 onward populates it.

**Boot-time check: persistent-warn surface.** Per ADR 0042 / [`soft-warn-lifecycle.md`](operations/soft-warn-lifecycle.md), boot reads the last 5 archives. The 3-of-5 threshold requires 5 archives carrying the structured field; only S-0029 (this session) has it pre-S-0030. The surface defers to the calibration window per soft-warn-lifecycle.md "Migration" section.

Boot procedure: `Start Engine` in a fresh Claude Code session. CLAUDE.md auto-loads. Boot reads `engine/STATE.md` (this file), notes that S-0030 is a **gate session, not a substantive build session** (gate sessions skip the build-readiness-report read at boot), and reads [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md) procedure, [`build_plan/P_2_graph_validation.md`](../build_plan/P_2_graph_validation.md), and [`engine/build_readiness/phase_3_sql.md`](build_readiness/phase_3_sql.md) (as reference for the report-template shape).

### The work (S-0030)

- Run the gate procedure against `build_plan/P_2_graph_validation.md`. Expected Tier 1 candidates: validate_graph's connection model (psycopg vs Supabase MCP), the soft-warn category enumeration (which categories ship in v1 vs deferred), the read-only posture under multi-session Phase 5 parallelism.
- Address the two S-0028 forward-pointers (branch rollback + sub-signal NULL strictness) explicitly in the report or via a Tier 1 conversation.
- Author `engine/build_readiness/phase_4_graph_validation.md` per the [build-readiness-gate.md](operations/build-readiness-gate.md) report template (now extended with citation discipline + cold-review pass per S-0029 amendment to ADR 0040).
- **Each Tier 1/2 decision must cite source-document line(s)** per the gate-on-the-gate amendment.
- **Cold-review sub-agent runs against citations before gate close** per the gate-on-the-gate amendment.
- Update [STATE.md](STATE.md) "Next session work item" to point S-0031 at the Phase 4 build session, gated by the new report (and note that S-0031 boot fires the cadence trigger).

### Source documents (read at S-0030 boot beyond CLAUDE.md auto-load)

- [`engine/STATE.md`](STATE.md) — current state (this file).
- [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md) — gate procedure, report template; new step 7 (citation) + step 8 (cold-review).
- [`build_plan/P_2_graph_validation.md`](../build_plan/P_2_graph_validation.md) — Phase 4 chunk contract.
- [`engine/adr/0016-graph-construction-needs-live-validation.md`](adr/0016-graph-construction-needs-live-validation.md) — load-bearing ADR for `validate_graph()`.
- [`engine/adr/0040-build-readiness-gate-before-substantive-build-sessions.md`](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) — gate ADR with S-0029 gate-on-the-gate amendment in Consequences.
- [`engine/build_readiness/phase_3_sql.md`](build_readiness/phase_3_sql.md) — reference template (the gate's first exercise).
- [`engine/tools/validate.py`](tools/validate.py) — current `validate_graph()` stub.
- [`product/seed-graph/migrations/PREDICATE_MANIFEST.md`](../product/seed-graph/migrations/PREDICATE_MANIFEST.md), [`ROUTING.md`](../product/seed-graph/migrations/ROUTING.md) — Phase 4 audit consumes the manifest.

### After S-0030

S-0031 — Phase 4 build session per the report S-0030 produces. **The first health-check cadence trigger fires at S-0031** (`last_claimed = S-0030 → 30 % 30 = 0`); the audit may take precedence over the planned Phase 4 work that session, or the user may defer the audit. The trigger is automatic at boot. Audit produces `docs/health-checks/S-0031.md` via [`engine/tools/health_check.py`](tools/health_check.py) (landed at S-0029).

## Open tensions and deferred decisions

See [`product/docs/tensions.md`](../product/docs/tensions.md). As of S-0026 close, the OQ-prefixed active set is 9 items (OQ-PRIVACY-A and OQ-PRIVACY-B closed by ADR 0031; OQ-PHASE8-A settled by ADR 0032; OQ-BYOK-REGIME withdrawn by ADR 0032; OQ-OUTWARD-VOICE added at S-0019):

- OQ-DEC1-A through D — Phase DEC.1 architecture decisions (server-side mastery, two-hop retrieval, embedding strategy, chunk-resolver vs URL pointers); decide-before Phase 6
- **OQ-WALL-BEHAVIOR** (added S-0008, per ADR 0029; simplified S-0012) — soft-wall degradation ladder at cost cap; **single-tier ladder** under cost-priced subscription model (free-vs-paid tier-shaped framing collapsed at S-0012); four candidate steps remain (model downshift, retrieval shrink, concept-engagement length cap, soft refusal); decide-before Phase 8 cost-cap wiring
- **OQ-CONTEXT-COMPRESSION** (added S-0008, per ADR 0029; unaffected by S-0012) — token-amplification mitigation for multi-turn engagements (structured-state replacement + automatic compression + explicit summarization candidates); decide-before Phase 7
- **OQ-PEDAGOGY-INFERENCE-LOCUS** (added S-0008, tagged `watch`) — rule layer vs. distributed inference; revisit when inference registry crosses ~30 entries OR cross-domain edges per domain-pair exceed ~50 OR an operational complaint surfaces
- **OQ-OUTWARD-VOICE** (added S-0019) — third expression-contract gap covering outward product surfaces (UI labels, button text, error messages, learner-facing help, public README, App Store description, learner-visible CHANGELOG entries) that neither ADR 0027 nor ADR 0036 governs; decide-before Phase 7 (the earliest plausible moment such a surface ships)
- OQ-WATCH-FLAG-FILE (tagged `watch`) — separate watch-flag file consideration, surfaced at session-30 health check

## Paperclip integration

**Deferred.** Phase 5 trial / Phase 7 commit per ROADMAP.md. Not installed in Foundation.

## Strong working commitments

12 commitments inherited from pre-Foundation design plus 10 architectural decisions. **All 22 are recorded as ADRs** — the contract layer, now split across [`engine/adr/`](adr/) (5 engine ADRs about the build apparatus) and [`product/adr/`](../product/adr/) (33 product ADRs about Paideia-as-product). The audience-facing summary lives in [`product/docs/MISSION.md`](../product/docs/MISSION.md) (commitments 1–12 with ADR cross-refs; commitment 2 reframed at S-0012 as "operating discipline must not corrupt pedagogy" per ADR 0032); the canonical list with ADR pointers lives in [`ROADMAP.md`](../ROADMAP.md) ("Strong working commitments referenced throughout"). The conversational story behind each decision is recoverable from MemPalace `decision`-tagged drawers.

**ADR count post-S-0029: 42 ADRs total — 40 Accepted plus 2 Superseded** (ADR 0002 by ADR 0032; ADR 0032 by ADR 0035), partitioned into **9 engine + 33 product** per [ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md). S-0029 added [ADR 0041](adr/0041-cascade-analysis-discipline.md) (engine; cascade-analysis discipline) and [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) (engine; soft-warn lifecycle, archive-as-canon); amended [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) Consequences (gate-on-the-gate citation + cold-review pass per S-0028 case study); amended [ADR 0022](adr/0022-periodic-project-health-checks.md) Consequences (deliverable-handshake re-pointing post-S-0024 partition for `health_check.py`). The contract layer the [`build_plan/`](../build_plan/) chunks consume by reference. [`engine/tools/validate.py`](tools/validate.py)'s `adr_index_consistency` check audits the ADR-file-vs-index alignment across both subtree READMEs; the new `cascade_*` checks (per ADR 0041) audit superseded-citation currency, back-reference orphans, and Consequences-deliverable promises.
