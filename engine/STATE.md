# Project State

> The single file every session reads first. Names what's current and what's next. Updated at every build-session close.

## Current

| Field | Value |
|---|---|
| **Project** | Paideia — knowledge mastery app on a pedagogical dependency graph |
| **GitHub** | https://github.com/StarshipSuperjam/paideia (private) |
| **Current phase** | Phase 0 — closed (S-0003); **Phase 1 — Contract Lock fully closed at S-0012**. **Phase 1.5 — Mission Realignment Overhaul fully closed at S-0021.** **S-0022** committed the engine / product wall ([ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md)) and renamed `CHANGELOG.md` → `ENGINE_LOG.md`. **Phase 2 (Build Plan Scaffolding) closed at S-0023.** **S-0024–S-0025** executed the folder migration into `engine/` and `product/` subtrees and the cross-reference sweep. **S-0026** authored the third project expression contract per [ADR 0038](adr/0038-expression-contract-for-ai-authored-code.md). **S-0027 widened the expression contract** per [ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md) and **committed the build-readiness gate** per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md). **Phase 3 — SQL Schema Implementation closed at S-0028** with eight migrations deployed to `paideia-dev` (PostgreSQL 17.6); RLS enabled on every public table; CHECK constraints enforce gate T2-A enums; three gate-report drifts corrected in-flight per architecture.md-authoritative design. **S-0029 closed the infrastructure-misfire sweep** (Session α foundations) per the approved plan. **S-0030 landed the first project health-check audit** as a manual phase-boundary fire (cadence-trigger off-by-one and two-layer-decision-recording posture gap surfaced as queued work for Session β). **S-0031 closed Session β: harness mechanization** per the approved infrastructure plan — three new non-blocking Claude Code hooks ([ADR 0043](adr/0043-hook-architecture.md)), three new user-defined Skills under `.claude/skills/` ([ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md)), cadence-trigger off-by-one fix. **S-0032 closed the MemPalace audit cleanup + improvements** per the approved plan at `/Users/shanekidd/.claude/plans/mempalace-structure-and-use-structured-sutherland.md`: PATH-fix on the Stop/PreCompact wrapper hook (the actual cause of silent auto-capture breakage since S-0002), 1/42 metric correction across ADR 0043 + ENGINE_LOG + STATE.md (cross-room compliance was ≥17%, not 2.4%), diary discipline adoption with `diary_skipped` shutdown soft-warn (Improvement F), `pushback` and `lesson` tags + room-targeting conventions (Improvement G + B), `engine/scheduled_audits.json` mechanism for one-time near-term audits (S-0042 entry filed for tag adoption check), legacy ADR-companion migration into `decisions` room (10 drawers moved general→decisions; room now 55), MemPalace audit category added to health_check.py with full test coverage, cadence tightened from 30 to 10 sessions per user direction (ADR 0022 Consequences amendment). ADR count post-S-0032: **44 ADRs total — 42 Accepted plus 2 Superseded** (no new ADRs; amendments to 0022 + 0042 + 0043 + 0044 only), partitioned **11 engine + 33 product**. |
| **Last build session** | S-0032 (2026-05-02) — **MemPalace audit cleanup + improvements** per `/Users/shanekidd/.claude/plans/mempalace-structure-and-use-structured-sutherland.md`. The audit diagnosed multiple silent failures accumulated under cadence-30; this session shipped the corrections. **Hook PATH-fix:** `engine/tools/hooks/mempalace-hook-wrapper.sh` gains the same `~/Library/Python/3.9/bin/mempalace` fallback already present in `post-adr-write.sh` per ADR 0043 — fixes the actual cause of silent auto-capture breakage since S-0002 (485 manual-MCP drawers existed; zero hook-driven captures because Claude Code subshells don't include the user-scope bin in PATH). **Project-usage doc:** `engine/operations/mempalace-operations.md` gains a "Project usage scope" section that names the surface this project uses (search, add_drawer, diary, read-only inspection) and explicitly excludes the surface it doesn't (KG, tunnels). Closes the "sessions making up usage on the fly" failure mode. **Diary discipline adopted (Improvement F):** `mempalace_diary_write` at session shutdown step 6 (new step inserted between ENGINE_LOG and outcome_summary; renumbered downstream); `mempalace_diary_read agent_name=claude last_n=3` at boot step 3b. `agent_name=claude` per project convention. `outcome_summary_soft_warns` shape gains `diary_skipped` key; ADR 0042's 3-of-5 persistent-warn surface catches drift automatically. Both ops doc and Skill mirror updated. **Tag vocabulary (Improvement G):** `pushback` and `lesson` added to `mempalace-tagging-conventions.md` (now 5 tags); room-targeting conventions added (Improvement B). `engine/scheduled_audits.json` new file with one entry: S-0042 near-term audit of pushback/lesson adoption. SessionStart hook extended to read scheduled_audits.json and surface entries whose trigger_session is at-or-before next_id. **Legacy migration:** 10 ADR-companion drawers migrated from `general` room to `decisions` room (decisions: 45 → 55). `post-adr-write.sh` extended to detect wrong-room filings (legacy `general` room) and emit a migration reminder. **Metric correction:** the S-0030 audit's "1/42 = 2.4%" headline was inflated by ~7x because it counted by room name only — cross-room `decision`-tagged drawers in legacy `general` filing pattern bring pre-mechanization compliance to ~17%. ADR 0043 Context + Consequences amended; ENGINE_LOG S-0030 entry amended; STATE.md S-0030 prior-session entry amended. **Stop reindexing operations docs (Improvement E):** install procedure no longer recommends `mempalace mine docs/`; the 88 existing operations-doc drawers can be cleaned up in a future session. **Health-check audit category (Improvement D):** `engine/tools/health_check.py` gains `audit_mempalace()` — fifth report category. Counts decisions-room drawers vs ADRs (two-layer recording), pushback/lesson tag adoption (with Tags-line filter to avoid false positives from operations docs), diary skip rate from outcome_summary_soft_warns. CLI binary resolved via the same fallback pattern as the hooks. 16 new tests added; pytest 62/62 passing; ruff + mypy --strict clean. **Cadence tightened 30 → 10 (user direction):** `register_state.json` gains `health_check_cadence: 10` + `health_check_cadence_history` array recording the change. ADR 0022 Consequences amended with rationale (the silent-failure pattern this audit surfaced makes 30-session intervals too sparse). Carriers updated: ADR 0022, ADR 0042 Context note, ADR 0043 + ADR 0044 forward-references (S-0060 → S-0040), engine/operations/{health-check.md, session-build-lifecycle.md, soft-warn-lifecycle.md}, .claude/commands/start-engine.md, .claude/skills/session-build-lifecycle/SKILL.md, ROADMAP.md, build_plan/00_session_schedule.md, docs/health-checks/README.md, scheduled_audits.json description, engine/tools/{health_check.py, hooks/session-start.sh}, engine/tools/test_health_check.py default expectation. Cold-context review pass surfaced two findings (module docstring "four categories" → five; missing tests for new MemPalace category); both addressed in-session. **Cadence default change of plans:** the originally-planned S-0032 work item (Phase 4 build-readiness gate exercise per ADR 0040) defers to S-0033. **Validator clean at close:** 14 checks, 0 hard-fails, 0 soft-warns. **outcome_summary_soft_warns at close:** all categories at 0; `diary_skipped` 0 (this session wrote its diary entry per the new step). **MemPalace state at close:** wing `paideia` carries 485 drawers across 5 rooms (general 330, operations 88, decisions 55, foundation-planning-s0001 10, s0003-adr-collection 2). 10 commits this session. |

| **Prior build session** | S-0031 (2026-05-02) — **Session β: harness mechanization** per `/Users/shanekidd/.claude/plans/the-project-infrastructure-still-abundant-key.md`. Two new engine ADRs ([ADR 0043](adr/0043-hook-architecture.md) hook architecture; [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md) skill conversion recipe-vs-reference). Three new non-blocking hook scripts under [`engine/tools/hooks/`](tools/hooks/): `post-adr-write.sh`, `session-start.sh`, `post-state-edit.sh` — all exit 0 unconditionally; logs land in `.claude/logs/<hook-name>.log`. Three new user-defined Skills with `disable-model-invocation: true` under [`.claude/skills/`](../.claude/skills/) — `session-build-lifecycle`, `session-shutdown-sequence`, `build-readiness-gate`. **Cadence-trigger off-by-one fix landed across five carriers**: `.claude/commands/start-engine.md` step 2, `engine/operations/health-check.md`, `engine/operations/session-build-lifecycle.md` step 2, `engine/tools/hooks/session-start.sh`, ADR 0022 Decision + Consequences amendment. **CLAUDE.md "Posture vs machinery" section narrowed** per ADR 0043: two-layer decision recording, STATE.md required-fields, and cadence trigger move to soft-enforced machinery; pushback rule, startup ceremony order, and Skill invocation remain posture. Cold-context review pass surfaced three real findings; all resolved in-session. **outcome_summary_soft_warns at close: all categories at 0**. Validator clean: 14 checks, 0 hard-fails, 0 soft-warns. ADR count post-close: **44 total — 42 Accepted plus 2 Superseded; 11 engine + 33 product**. |

| **Last commit on main** | (see `git log --oneline -1` on main) |
| **Backup tags** | `pre-foundation-v0.0.0` at commit `fa70b8c` — pre-machinery anchor. `pre-phase-3-v0.0.1` at commit `36b0907` (S-0026 close) — pre-Phase-3 anchor cut at S-0027. Both are historical anchors, not operational fallback targets — file-level retrieval via `git show <tag>:<path>` is the legitimate use. |

## Infrastructure

| Component | Value |
|---|---|
| **Supabase project** | `paideia-dev`, project ref `ozooosgnuzxqqypotlke`, PostgreSQL 17.6 |
| **Supabase MCP** | configured in `.mcp.json` (gitignored, contains PAT) |
| **MemPalace** | installed at `/Users/shanekidd/Library/Python/3.9/bin/`; MCP server `mempalace-mcp` configured in `.mcp.json`; wing `paideia` carries **485 drawers across 5 rooms** as of S-0032 close (general 330, operations 88, decisions 55, foundation-planning-s0001 10, s0003-adr-collection 2); capture hooks wired in `.claude/settings.json` (Stop + PreCompact wrapper PATH-fixed at S-0032 — auto-capture had been silently failing since S-0002 because `mempalace` CLI wasn't on the hook subshell's PATH; verify at next Stop event by checking `.claude/logs/mempalace-hook.log` for `OK exit=0` lines instead of `FAIL exit=127`); project usage scope documented at [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Project usage scope" — uses search, add_drawer, diary (agent_name `claude`), read-only inspection; explicitly excludes KG and tunnels |
| **Python** | system Python 3.9.6 at `/usr/bin/python3`; user-scope packages at `/Users/shanekidd/Library/Python/3.9/bin/` |
| **Anthropic API** | env var `ANTHROPIC_API_KEY` set in local `.env` (gitignored) |

## Next session work item

**S-0033 — Phase 4 build-readiness gate exercise** (deferred from the original S-0032 slot; that slot was repurposed for the MemPalace audit cleanup at user direction). Per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) (amended at S-0029 with the gate-on-the-gate citation + cold-review pass), the gate session produces `engine/build_readiness/phase_4_graph_validation.md`. Every Tier 1 and Tier 2 decision cites source-document line(s); a cold-review sub-agent runs against citations before close. Address the two S-0028 forward-pointers in the report: branch-based rollback verification (Supabase development branches; cost-gated; resolution to depend on local Supabase CLI setup or explicit user budget approval) and sub-signal NULL permissiveness (per gate T2-B in the existing phase_3_sql.md report).

**Boot-time check: cadence trigger.** Cadence tightened to 10 at S-0033 per ADR 0022 Consequences amendment (was 30 from S-0001 to S-0032). At S-0033 boot, `next_id = "0033"`, so `33 % 10 = 3` — no cadence trigger this session. The first post-tightening natural fire lands at S-0040.

**Boot-time check: persistent-warn surface.** Per [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md). At S-0033 boot, structured `outcome_summary_soft_warns` archives: S-0029, S-0030, S-0031, S-0032 (4 — calibration window still in effect, surface defers until 5 accumulate at S-0034). The SessionStart hook (`engine/tools/hooks/session-start.sh` per ADR 0043) emits the calibration-window message.

**Boot-time check: hooks fire.** S-0033 is the first session to boot with the wrapper-hook PATH-fix landed at S-0032. Expected behavior:
- `SessionStart` fires once at boot; emits cadence (no trigger; mod = 3) plus persistent-warn surface (calibration window) plus scheduled-audit surface (engine/scheduled_audits.json — first entry not due until S-0042).
- `Stop` and `PreCompact` wrapper now resolves the `mempalace` CLI via the `~/Library/Python/3.9/bin/mempalace` fallback. Verify by checking `.claude/logs/mempalace-hook.log` after the first Stop event for an `OK exit=0` line (instead of the `FAIL exit=127` that would have appeared pre-S-0032).
- `PostToolUse-on-ADR-write` may fire if S-0033 authors a new ADR; the new room-targeting check (added at S-0032) emits a reminder if a companion drawer is found in the wrong room (legacy `general` instead of `decisions`).
- Boot procedure also reads recent diary entries via `mempalace_diary_read agent_name=claude last_n=3` per the S-0032 boot procedure update; first entry is S-0032's own.

**Build-readiness scope.** Phase 4 graph validation. The `validate_graph` stub from earlier sessions becomes the production check: traverses the live `nodes`/`edges` tables in `paideia-dev`; reports orphans, cycles, missing prerequisites, malformed JSONB. Source documents to read: [`product/docs/architecture.md`](../product/docs/architecture.md) graph schema, [`product/docs/self-correction.md`](../product/docs/self-correction.md) (Phase 4 graph self-correction expectations), [`engine/build_readiness/phase_3_sql.md`](build_readiness/phase_3_sql.md) (the prior gate report's success criteria + forward-pointers).

Boot procedure: `Start Engine` in a fresh Claude Code session. CLAUDE.md auto-loads. Boot reads `engine/STATE.md` (this file). The SessionStart hook fires, surfaces calibration-window message. Read [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) + [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md) (or invoke Skill `build-readiness-gate` directly per [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md)). Then read the source documents above. Then proceed with the Tier 1/2/3 triage process.

### Source documents (read at S-0033 boot beyond CLAUDE.md auto-load)

- [`engine/STATE.md`](STATE.md) — current state (this file).
- [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md) — Layer 1 source-of-truth for the gate procedure (or invoke Skill `build-readiness-gate`).
- [`engine/build_readiness/phase_3_sql.md`](build_readiness/phase_3_sql.md) — prior gate report (Phase 3); template + worked example; success criteria + forward-pointers to inherit.
- [`product/docs/architecture.md`](../product/docs/architecture.md) — graph schema (nodes, edges, JSONB shapes); the design-source any Tier 2 schema-shape decision must cite per ADR 0040 amendment.
- [`product/docs/self-correction.md`](../product/docs/self-correction.md) — Phase 4 graph self-correction loops; what `validate_graph` is downstream of.

### After S-0033

S-0034 — Phase 4 build session per the report S-0033 produces. Implements `validate_graph` against the live database; lands tests; closes the Phase 4 commitment.

S-0040 — first cadence-10 health-check trigger fires; user accepts (the audit covers post-S-0030 telemetry plus the new MemPalace category for the first time) or defers per the standard cadence policy.

S-0042 — scheduled near-term audit of pushback/lesson tag adoption fires at boot via the SessionStart hook reading `engine/scheduled_audits.json` (per the S-0032 audit Improvement G mechanism).

## Open tensions and deferred decisions

See [`product/docs/tensions.md`](../product/docs/tensions.md). As of S-0026 close, the OQ-prefixed active set is 9 items (OQ-PRIVACY-A and OQ-PRIVACY-B closed by ADR 0031; OQ-PHASE8-A settled by ADR 0032; OQ-BYOK-REGIME withdrawn by ADR 0032; OQ-OUTWARD-VOICE added at S-0019):

- OQ-DEC1-A through D — Phase DEC.1 architecture decisions (server-side mastery, two-hop retrieval, embedding strategy, chunk-resolver vs URL pointers); decide-before Phase 6
- **OQ-WALL-BEHAVIOR** (added S-0008, per ADR 0029; simplified S-0012) — soft-wall degradation ladder at cost cap; **single-tier ladder** under cost-priced subscription model (free-vs-paid tier-shaped framing collapsed at S-0012); four candidate steps remain (model downshift, retrieval shrink, concept-engagement length cap, soft refusal); decide-before Phase 8 cost-cap wiring
- **OQ-CONTEXT-COMPRESSION** (added S-0008, per ADR 0029; unaffected by S-0012) — token-amplification mitigation for multi-turn engagements (structured-state replacement + automatic compression + explicit summarization candidates); decide-before Phase 7
- **OQ-PEDAGOGY-INFERENCE-LOCUS** (added S-0008, tagged `watch`) — rule layer vs. distributed inference; revisit when inference registry crosses ~30 entries OR cross-domain edges per domain-pair exceed ~50 OR an operational complaint surfaces
- **OQ-OUTWARD-VOICE** (added S-0019) — third expression-contract gap covering outward product surfaces (UI labels, button text, error messages, learner-facing help, public README, App Store description, learner-visible CHANGELOG entries) that neither ADR 0027 nor ADR 0036 governs; decide-before Phase 7 (the earliest plausible moment such a surface ships)
- OQ-WATCH-FLAG-FILE (tagged `watch`) — separate watch-flag file consideration, surfaced at session-30 health check

## Paperclip integration

**Deferred.** Phase 5 trial / Phase 7 commit per ROADMAP.md. Not installed in Foundation.

## Strong working commitments

12 commitments inherited from pre-Foundation design plus 10 architectural decisions. **All 22 are recorded as ADRs** — the contract layer, now split across [`engine/adr/`](adr/) (5 engine ADRs about the build apparatus) and [`product/adr/`](../product/adr/) (33 product ADRs about Paideia-as-product). The audience-facing summary lives in [`product/docs/MISSION.md`](../product/docs/MISSION.md) (commitments 1–12 with ADR cross-refs; commitment 2 reframed at S-0012 as "operating discipline must not corrupt pedagogy" per ADR 0032); the canonical list with ADR pointers lives in [`ROADMAP.md`](../ROADMAP.md) ("Strong working commitments referenced throughout"). The conversational story behind each decision is recoverable from MemPalace `decision`-tagged drawers.

**ADR count post-S-0031: 44 ADRs total — 42 Accepted plus 2 Superseded** (ADR 0002 by ADR 0032; ADR 0032 by ADR 0035), partitioned into **11 engine + 33 product** per [ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md). S-0031 added [ADR 0043](adr/0043-hook-architecture.md) (engine; hook architecture for two-layer recording, cadence trigger, STATE.md required-fields verification) and [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md) (engine; skill conversion partition for recipe-shaped vs reference-shaped operations docs); amended [ADR 0022](adr/0022-periodic-project-health-checks.md) Decision + Consequences (cadence-trigger logic corrected from `last_claimed mod` to `next_id mod`); amended [ADR 0043](adr/0043-hook-architecture.md) Decision (post-state-edit.sh ADR-count check dropped per cold-review finding). S-0029 added [ADR 0041](adr/0041-cascade-analysis-discipline.md) (engine; cascade-analysis discipline) and [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) (engine; soft-warn lifecycle, archive-as-canon); amended [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) Consequences (gate-on-the-gate citation + cold-review pass per S-0028 case study); amended [ADR 0022](adr/0022-periodic-project-health-checks.md) Consequences (deliverable-handshake re-pointing post-S-0024 partition for `health_check.py`). The contract layer the [`build_plan/`](../build_plan/) chunks consume by reference. [`engine/tools/validate.py`](tools/validate.py)'s `adr_index_consistency` check audits the ADR-file-vs-index alignment across both subtree READMEs; the `cascade_*` checks (per ADR 0041) audit superseded-citation currency, back-reference orphans, and Consequences-deliverable promises.
