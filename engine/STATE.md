# Project State

> The single file every session reads first. Names what's current and what's next. Updated at every build-session close.

## Current

| Field | Value |
|---|---|
| **Project** | Paideia — knowledge mastery app on a pedagogical dependency graph |
| **GitHub** | https://github.com/StarshipSuperjam/paideia (private) |
| **Current phase** | Phase 0 — closed (S-0003); **Phase 1 — Contract Lock fully closed at S-0012** (§1.1 across S-0004 / S-0005 / S-0006; privacy-posture insertion at S-0007; §1.2 at S-0008; Phase 4.5 input-dataset-survey scaffolding at S-0009; §1.3 at S-0010; privacy ADRs collapsed at S-0011 with ADR 0031; project-direction supersession at S-0012 with ADR 0032 superseding ADR 0002). **Phase 1.5 — Mission Realignment Overhaul fully closed at S-0021.** **S-0022** committed the engine / product wall ([ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md)) and renamed `CHANGELOG.md` → `ENGINE_LOG.md`. **Phase 2 (Build Plan Scaffolding) closed at S-0023** with the [`build_plan/`](../build_plan/) tree. **S-0024** executed the folder migration into `engine/` and `product/` subtrees. **S-0025** closed the partition with the cross-reference sweep. **S-0026** authored the third project expression contract per [ADR 0038](adr/0038-expression-contract-for-ai-authored-code.md). **S-0027 widened the expression contract** per [ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md), instantiated the SQL/migrations row, and **committed the build-readiness gate** per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md). The boundary between *project setup* (Phases 0, 1, 1.5, 2 + the partition migration + code/universal contracts + gate protocol) and *project build* (Phases 3 onward) sat between S-0027 and S-0028. **Phase 3 — SQL Schema Implementation closed at S-0028.** Eight migrations under [`product/seed-graph/migrations/`](../product/seed-graph/migrations/) deployed to `paideia-dev` (PostgreSQL 17.6): `0001_users_mirror.sql` (auth mirror + INSERT/DELETE triggers), `0002_nodes.sql` (graph nodes), `0003_edges.sql` (graph edges), `0004_settings.sql` (graph_version=1), `0005_learner_events.sql` (event-sourced log), `0006_mastery_snapshots.sql` (cached mastery state), `0007_tension_log.sql` (with v1 JSONB vocabulary validation), `0008_security_hardening.sql` (post-apply advisor remediation). All FKs to `public.users(id)` cascade per ADR 0031; RLS enabled on every public table; CHECK constraints enforce ADR 0030 / gate T2-A enums. Three gate-report drifts corrected inline (T1-A DELETE trigger; T2-C/T2-E `nodes.id` UUID→TEXT plus `edge_type` and `provenance`/`evidence` shape) per architecture.md authoritative design. ADR count unchanged at **40 ADRs total — 38 Accepted plus 2 Superseded** (ADR 0002 by ADR 0032; ADR 0032 by ADR 0035), partitioned 7 engine + 33 product. |
| **Last build session** | S-0028 (2026-05-02) — **Phase 3 SQL schema implementation.** First substantive build phase under the universal expression contract ([ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md)) and the build-readiness gate protocol ([ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md)). Eight migrations authored under [`product/seed-graph/migrations/`](../product/seed-graph/migrations/) and applied to `paideia-dev` (PostgreSQL 17.6) via Supabase MCP `apply_migration`: `0001_users_mirror.sql` (public.users mirror + handle_new_auth_user / handle_deleted_auth_user SECURITY DEFINER triggers on auth.users INSERT and DELETE — gate report T1-A originally specified INSERT only; the under-specification was corrected in-flight per the user-directed Option A in plan-mode resolution); `0002_nodes.sql` (Node Schema per architecture.md with `id TEXT PRIMARY KEY` slugified, `confidence_level` and `status` CHECK enums); `0003_edges.sql` (Edge Schema with `source_id`/`target_id TEXT REFERENCES public.nodes(id)`, `edge_type` unconstrained, `weight`/`confidence` CHECK [0,1], `UNIQUE(source_id, target_id, edge_type)`); `0004_settings.sql` (graph_version=1 seeded per gate T2-D; service-role-only RLS); `0005_learner_events.sql` (event-sourced log per ADR 0015 with sub-signals stored raw per ADR 0024, structured context columns per ADR 0026 + ADR 0031, `sub_signals_range_or_null` table-level CHECK per gate T2-B); `0006_mastery_snapshots.sql` (composite PK `(user_id, concept_id)`, `max_historical_score DEFAULT 0` per ADR 0025, two indexes per gate T2-C); `0007_tension_log.sql` (IMMUTABLE `tension_log_exchange_summary_valid_v1(jsonb)` enforces v1 vocabularies from `TENSION_VOCABULARY.md`; `user_id` added per ADR 0031 Consequences though absent from self-correction.md schema); `0008_security_hardening.sql` (post-apply advisor remediation: pin `search_path` on the validation function; REVOKE EXECUTE on the trigger functions from PUBLIC, anon, authenticated). All FKs to `public.users(id)` cascade per ADR 0031; FKs to `public.nodes(id)` carry NO ACTION per ADR 0021 (deprecated/superseded nodes retained because events reference them). RLS on every public table; seven RLS policies (users self-select, nodes/edges authenticated read, settings service-role only, learner_events / mastery_snapshots / tension_log user-isolation). Three gate-report drifts surfaced and corrected in-flight per the architecture.md-authoritative design principle: T1-A (DELETE trigger added inline alongside INSERT trigger), T2-C `mastery_snapshots.concept_id` UUID→TEXT, T2-E `edges.source_id`/`target_id` UUID→TEXT plus `type`→`edge_type` plus `provenance`/`evidence` JSONB→TEXT — all four corrections recorded in ENGINE_LOG `### Changed`. Gate report fragments updated inline. Validator clean at 0/0; SQL gates clean per file (4 gates, 0 hard-fails on each of 8 files); pytest 56/56 passing; pre-commit gate stack clean across all 5 commits. Cold-review pass via Explore subagent: all 8 files match contract; the two known trade-offs (sub-signal NULL permissiveness on composite-applicable interaction types in 0005, `tension_log.user_id` addition in 0007) were both explicitly acknowledged in their respective contract blocks as intentional drift, which the cold reviewer correctly identified as not breaches. Post-apply Supabase security advisor returned only the Supabase-internal `rls_auto_enable` finding (out of project scope). Branch-based rollback verification per gate T2-H was downgraded to "migrations applied cleanly the first time" + live-schema verification because Supabase development branches are cost-bearing and S-0028's auto-mode posture defers cost-bearing operations to explicit user confirmation; full rollback verification is forward-pointed to the next gate exercise / Phase 4 / S-0029. The schema is the substrate every downstream phase reads or writes to. ADR count unchanged at 40 ADRs total — 38 Accepted plus 2 Superseded, partitioned 7 engine + 33 product. |

| **Prior build session** | S-0027 (2026-05-02) — **Build-readiness gate protocol + universal expression contract widening + Phase 3 SQL gate exercise.** Phase A authored [ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md), [`expression-contract-instantiation.md`](operations/expression-contract-instantiation.md), [`migration-discipline.md`](operations/migration-discipline.md), and `validate_sql_gates()` with `--sql-gates` CLI plus pre-commit hook branch (18 new pytest tests; 56 total passing). Phase B authored [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md), [`build-readiness-gate.md`](operations/build-readiness-gate.md), [`engine/build_readiness/`](build_readiness/) directory + README, and the session-build-lifecycle.md step 5 boot-time read of build-readiness reports. Phase C dogfooded the protocol against Phase 3 SQL: produced [`engine/build_readiness/phase_3_sql.md`](build_readiness/phase_3_sql.md), authored [TENSION_VOCABULARY.md](../product/seed-graph/migrations/TENSION_VOCABULARY.md), amended [ADR 0031](../product/adr/0031-erasure-mechanism-and-individual-only-regime.md) Consequences to pin the local users-mirror cascade shape, moved `supabase/migrations/` to `product/seed-graph/migrations/`, cut backup tag `pre-phase-3-v0.0.1`. The universal expression contract and build-readiness gate are in force from S-0027 forward. |

| **Last commit on main** | (see `git log --oneline -1` on main) |
| **Backup tags** | `pre-foundation-v0.0.0` at commit `fa70b8c` — pre-machinery anchor. `pre-phase-3-v0.0.1` at commit `36b0907` (S-0026 close) — pre-Phase-3 anchor cut at S-0027. Both are historical anchors, not operational fallback targets — file-level retrieval via `git show <tag>:<path>` is the legitimate use. |

## Infrastructure

| Component | Value |
|---|---|
| **Supabase project** | `paideia-dev`, project ref `ozooosgnuzxqqypotlke`, PostgreSQL 17.6 |
| **Supabase MCP** | configured in `.mcp.json` (gitignored, contains PAT) |
| **MemPalace** | installed at `/Users/shanekidd/Library/Python/3.9/bin/`; MCP server `mempalace-mcp` configured in `.mcp.json`; wing `paideia` indexed at S-0002 (414 drawers across rooms `general` + `operations`); capture hooks wired in `.claude/settings.json` |
| **Python** | system Python 3.9.6 at `/usr/bin/python3`; user-scope packages at `/Users/shanekidd/Library/Python/3.9/bin/` |
| **Anthropic API** | env var `ANTHROPIC_API_KEY` set in local `.env` (gitignored) |

## Next session work item

**S-0029 — Phase 4 build-readiness gate exercise (gate session) producing `engine/build_readiness/phase_4_graph_validation.md`.** Phase 4 is the next substantive build per [ROADMAP.md](../ROADMAP.md) — fleshing out `engine/tools/validate.py`'s `validate_graph()` extension point against the deployed Phase 3 schema. Per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md), every substantive build session is preceded by a gate session whose deliverable is a build-readiness report. S-0029 runs the gate procedure per [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md): launch up to three Explore agents in parallel against [`build_plan/P_2_graph_validation.md`](../build_plan/P_2_graph_validation.md), [ADR 0016](adr/0016-graph-construction-needs-live-validation.md), and [`product/seed-graph/migrations/PREDICATE_MANIFEST.md`](../product/seed-graph/migrations/PREDICATE_MANIFEST.md); triage findings into Tier 1 / Tier 2 / Tier 3; resolve Tier 1 conversationally with the user; author the report; update STATE.md to point at it.

S-0029 is also the first natural moment to address **two forward-pointers from S-0028's `outcome_summary`** that the next gate session should classify and either resolve or formally defer:
1. **Branch-based rollback verification.** The gate report's T2-H rollback test was downgraded at S-0028 (Supabase development branches are cost-bearing; auto-mode deferred to explicit user confirmation). Decide: ship a local Supabase CLI setup as part of S-0029's deliverable so Phase 4 can verify rollback locally, OR confirm the cost-bearing branch creation as a one-time verification expense, OR accept the reduced-proof posture as standing.
2. **Sub-signal NULL permissiveness in 0005_learner_events.sql.** Gate T2-B's CHECK is permissive on NULL for composite-applicable interaction types per PostgreSQL CHECK semantics (NULL → TRUE). The strict intent ("NULL on composite-type events is a data error" per learner-model.md:60) is application-layer. Decide: tighten the schema-level CHECK in a 0009 migration, OR formalize the application-layer enforcement contract as the standing posture.

Boot procedure: `Start Engine` in a fresh Claude Code session. CLAUDE.md auto-loads. Boot reads `engine/STATE.md` (this file), then notes that S-0029 is a **gate session, not a substantive build session** — per [`session-build-lifecycle.md`](operations/session-build-lifecycle.md) step 5 last paragraph, gate sessions skip the build-readiness-report read. Boot then reads [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md) procedure, [`build_plan/P_2_graph_validation.md`](../build_plan/P_2_graph_validation.md), and [`engine/build_readiness/phase_3_sql.md`](build_readiness/phase_3_sql.md) (as reference for the report-template shape).

### The work (S-0029)

- Run the gate procedure against `build_plan/P_2_graph_validation.md`. Expected Tier 1 candidates: validate_graph's connection model (psycopg vs Supabase MCP), the soft-warn category enumeration (which categories ship in v1 vs deferred), the read-only posture under multi-session Phase 5 parallelism.
- Address the two S-0028 forward-pointers (branch rollback + sub-signal NULL strictness) explicitly in the report or via a Tier 1 conversation.
- Author `engine/build_readiness/phase_4_graph_validation.md` per the [build-readiness-gate.md](operations/build-readiness-gate.md) report template.
- Update [STATE.md](STATE.md) "Next session work item" to point S-0030 at the build session, gated by the new report.

### Source documents (read at S-0029 boot beyond CLAUDE.md auto-load)

- [`engine/STATE.md`](STATE.md) — current state (this file).
- [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md) — gate procedure, report template.
- [`build_plan/P_2_graph_validation.md`](../build_plan/P_2_graph_validation.md) — Phase 4 chunk contract.
- [`engine/adr/0016-graph-construction-needs-live-validation.md`](adr/0016-graph-construction-needs-live-validation.md) — load-bearing ADR for `validate_graph()`.
- [`engine/build_readiness/phase_3_sql.md`](build_readiness/phase_3_sql.md) — reference template (the gate's first exercise).
- [`engine/tools/validate.py`](tools/validate.py) — current `validate_graph()` stub.
- [`product/seed-graph/migrations/PREDICATE_MANIFEST.md`](../product/seed-graph/migrations/PREDICATE_MANIFEST.md), [`ROUTING.md`](../product/seed-graph/migrations/ROUTING.md) — Phase 4 audit consumes the manifest.

### After S-0029

S-0030 — Phase 4 build session per the report S-0029 produces. **The first health-check cadence trigger fires at S-0030** (counter mod 30 == 0; per [`engine/operations/health-check.md`](operations/health-check.md) and [ADR 0022](adr/0022-periodic-project-health-checks.md)); the audit may take precedence over the planned Phase 4 work that session, or the user may defer the audit. The trigger is automatic at boot.

## Open tensions and deferred decisions

See [`product/docs/tensions.md`](../product/docs/tensions.md). As of S-0026 close, the OQ-prefixed active set is 9 items (OQ-PRIVACY-A and OQ-PRIVACY-B closed by ADR 0031; OQ-PHASE8-A settled by ADR 0032; OQ-BYOK-REGIME withdrawn by ADR 0032; OQ-OUTWARD-VOICE added at S-0019):

- OQ-DEC1-A through D — Phase DEC.1 architecture decisions (server-side mastery, two-hop retrieval, embedding strategy, chunk-resolver vs URL pointers); decide-before Phase 6
- **OQ-WALL-BEHAVIOR** (added S-0008, per ADR 0029; simplified S-0012) — soft-wall degradation ladder at cost cap; **single-tier ladder** under cost-priced subscription model (free-vs-paid tier-shaped framing collapsed at S-0012); four candidate steps remain (model downshift, retrieval shrink, concept-engagement length cap, soft refusal); decide-before Phase 8 cost-cap wiring
- **OQ-CONTEXT-COMPRESSION** (added S-0008, per ADR 0029; unaffected by S-0012) — token-amplification mitigation for multi-turn engagements (structured-state replacement + automatic compression + explicit summarization candidates); decide-before Phase 7
- **OQ-PEDAGOGY-INFERENCE-LOCUS** (added S-0008, tagged `watch`) — rule layer vs. distributed inference; revisit when inference registry crosses ~30 entries OR cross-domain edges per domain-pair exceed ~50 OR an operational complaint surfaces
- **OQ-OUTWARD-VOICE** (added S-0019) — third expression-contract gap covering outward product surfaces (UI labels, button text, error messages, learner-facing help, public README, App Store description, learner-visible CHANGELOG entries) that neither ADR 0027 nor ADR 0036 governs; decide-before Phase 7 (the earliest plausible moment such a surface ships)
- OQ-WATCH-FLAG-FILE (tagged `watch`) — separate watch-flag file consideration, surfaced at session-30 health check

## Paperclip integration

**Deferred.** Phase 5 trial / Phase 7 commit per ROADMAP.md. Not installed in Foundation.

## Strong working commitments

12 commitments inherited from pre-Foundation design plus 10 architectural decisions. **All 22 are recorded as ADRs** — the contract layer, now split across [`engine/adr/`](adr/) (5 engine ADRs about the build apparatus) and [`product/adr/`](../product/adr/) (33 product ADRs about Paideia-as-product). The audience-facing summary lives in [`product/docs/MISSION.md`](../product/docs/MISSION.md) (commitments 1–12 with ADR cross-refs; commitment 2 reframed at S-0012 as "operating discipline must not corrupt pedagogy" per ADR 0032); the canonical list with ADR pointers lives in [`ROADMAP.md`](../ROADMAP.md) ("Strong working commitments referenced throughout"). The conversational story behind each decision is recoverable from MemPalace `decision`-tagged drawers.

**ADR count post-S-0027: 40 ADRs total — 38 Accepted plus 2 Superseded** (ADR 0002 by ADR 0032; ADR 0032 by ADR 0035), partitioned into 7 engine + 33 product per [ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md). S-0027 added [ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md) (engine; universal expression contract widening) and [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) (engine; build-readiness gate). The contract layer the [`build_plan/`](../build_plan/) chunks consume by reference. [`engine/tools/validate.py`](tools/validate.py)'s `adr_index_consistency` check audits the ADR-file-vs-index alignment across both subtree READMEs.
