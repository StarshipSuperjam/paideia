# Project State

> The single file every session reads first. Names what's current and what's next. Updated at every build-session close.
>
> **Scope discipline.** This file describes present state only. Session-by-session history lives in `engine/session/archive/S-NNNN.json` (canonical structured archive per ADR 0042) and `engine/changelog/<YYYY>/S-NNNN-*.md` (per-session entries per ADR 0092; the pre-S-0198 monolithic ENGINE_LOG.md is byte-preserved at `engine/changelog/_history/ENGINE_LOG-pre-0.1.0.md`). Cross-reference into those surfaces; do not duplicate their content here. The 37 prior-session prose rows S-0081–S-0119 retired at S-0121 per audit-session inline cleanup — they were duplicating archive + changelog content. Any addition to this file must answer: *would a session reading only STATE.md need this to know what's current?* If the content is "what happened last session," it belongs in the archive + changelog entry.

## Current

| Field | Value |
|---|---|
| **Project** | Paideia — knowledge mastery app on a pedagogical dependency graph |
| **GitHub** | https://github.com/StarshipSuperjam/paideia (OSS, Apache 2.0; public-flip landed at S-0130 with tag [`oss-flip-v1.0.0`](https://github.com/StarshipSuperjam/paideia/releases/tag/oss-flip-v1.0.0)) |
| **Current phase** | **Phase 5 — Philosophy subdomain seeding (closed; S-0045 → S-0076; production-audit closeout at S-0122; production-audit follow-up migrations at S-0123).** 380 nodes + 533 edges (was 536 pre-S-0123; -4 from 0063 prunes, +1 from S-0155 migration 0065). Post-Cluster-2 (S-0208 ADR 0098 + migration 0130): 516 `(pedagogical_dependence, soft_prerequisite)` + 17 `(historical_influence, influenced_by)` per the new compound CHECK constraint on `(edge_layer, edge_type)`. **Post-Cluster-4 (S-0213 ADR 0102 + migration 0140): public.nodes carries 31 columns (17 pre-existing + 14 new) — `node_type TEXT[]` with 10-value element CHECK; `tradition_label TEXT[]` open vocabulary; 5 JSONB columns (`canonical_sources`, `approved_examples`, `misconceptions`, `assessment_items`, `mastery_evidence`); 7 scalar additions (`disciplinary_domain`, `granularity`, `audience_tags`, `accessibility_notes`, `assumed_background`, `jargon_load`, `cultural_specificity`). Schema-only landing per ADR 0102 Adjudication 1; all 380 rows carry column defaults (`node_type=['unclassified']` across the entire corpus); per-domain node_type backfill defers to Cluster 4 sequel sessions claiming migration slots 0141-0149.** Cluster 4 (third Tier-A) landed at S-0213 — ADR 0102 commits 14 columns + 4 CHECK constraints to public.nodes; migration 0140 schema-only (380-row count invariant; all defaults applied); 3 new validate.py checks (`node_type_unclassified` mode-switched soft-warn at landing → hard-fail at LAST per-domain backfill sequel; `node_threshold_concept_lacks_assessment_items`; `node_lacks_cultural_specificity`); 25 new pytests; round-trip verified end-to-end; ADR 0094 T1-A re-verified third time; ADR 0094 T1-B partially closed (D1/D2/D3/D4/D8 within Tier-A; D5 to Cluster 2 amendment; D6/D7 to Cluster 5); ADR 0095 T1-A re-verified third time. Phase 5 build closeout report at [`engine/build_readiness/phase_5_closeout.md`](build_readiness/phase_5_closeout.md); production-audit findings at [`engine/build_readiness/phase_5_production_audit_findings.md`](build_readiness/phase_5_production_audit_findings.md); audit-system-input at [`engine/build_readiness/phase_5_audit_system_input.md`](build_readiness/phase_5_audit_system_input.md); follow-up migrations at [`product/seed-graph/migrations/0061_seed_historical_influence_retyping_part1.sql`](../product/seed-graph/migrations/0061_seed_historical_influence_retyping_part1.sql) / [`0062_seed_direction_flips_part1.sql`](../product/seed-graph/migrations/0062_seed_direction_flips_part1.sql) / [`0062_seed_direction_flips_revert_part1.sql`](../product/seed-graph/migrations/0062_seed_direction_flips_revert_part1.sql) / [`0063_seed_weak_edge_cleanup_part1.sql`](../product/seed-graph/migrations/0063_seed_weak_edge_cleanup_part1.sql) / [`0064_seed_evidence_annotations_part1.sql`](../product/seed-graph/migrations/0064_seed_evidence_annotations_part1.sql). T1-A through T1-D closed for [`engine/tools/fetch_structural_reference.py`](tools/fetch_structural_reference.py) at S-0122 closeout. **Phase 6 self-correction master plan** unblocked at S-0152 via OQ-DEC1-A/B/C/D tension-set settlement (product ADRs 0085–0088); audit-side closeout + follow-up execution complete. **Phase 6 scope expanded at S-0202** via [ADR 0094 product](../product/adr/0094-phase-6-scope.md) to include Tier-A substrate redesign (Clusters 1-5) BEFORE the SEP/embedding self-correction work commits node-table-dependent migrations — dependency order 1 → 2 → 4 → 3 → 5 per synthesis.md; per-cluster product ADRs + migrations land in Session δ₂+ and subsequent build sessions; SEP backfill + embedding work per [ADR 0086](../product/adr/0086-model-agnostic-embedding-storage-architecture.md) sequences AFTER Tier-A migrations so the five semantically-rich fields (`node_type` / `disciplinary_domain` / `granularity` / `canonical_sources` / `misconceptions`) are present at backfill time. **The OSS+BYOK three-session refactor completed: Session A at S-0128 ([ADR 0065](../product/adr/0065-oss-pivot-and-byok-disposition.md) supersedes 0029 + 0035); Session B at S-0129 (downstream doc rewrites); Session C at S-0130 (LICENSE Apache 2.0 + NOTICE/CONTRIBUTING/CODE_OF_CONDUCT authoring + SECURITY.md/README.md OSS rewrites + 141-file PII path sweep + `.claude/settings.json` cleanup; public-visibility flip held at halt-and-confirm gate at session close).** **SWE-hardening rollout — Tier 1 CLOSED, Tier 2 CLOSED entirely, Tier 3 #76 CLOSED.** Tier 1 closed: #65 lockfile (S-0127 / ADR 0064); Pairing B #68 CI mirror + #69 branch protection (S-0131 / ADR 0065 engine + ADR 0066); Pairing A #66 gitleaks + #70 bandit (S-0132 / ADR 0067 + ADR 0068); #67 Dependabot (S-0133 / ADR 0069). Tier 2 closed: Pairing A #70 bandit SAST (S-0132 / ADR 0068, paired with #66); Pairing C #73 `/review` + #74 `/security-review` (S-0134 / ADR 0070 + ADR 0071); #75 frontend-ui-engineering modular split via `/frontend-discipline` + `/paideia-frontend-overlays` (S-0135 / ADR 0072 + ADR 0073); #71 pytest-cov coverage floor (S-0136 / ADR 0074; measured baseline 80%, floor 78%); #72 GitHub issue templates (S-0137 / ADR 0075). Tier 3 first-closure: #76 `/ship` multi-model orchestration (S-0148 / ADR 0081 — composes Tier-2 #73 + #74 + #71). **S-0138 closed the S-0137 in-context harness-allowlist over-reach** via `engine/tools/build_lifecycle_push.py` (sibling to `routine_lifecycle_push.py` per ADR 0054) + new engine ADR 0076. **S-0139 closed analysis-outcome [Issue #81](https://github.com/StarshipSuperjam/paideia/issues/81)** (narrowed at boot — lifecycle-state half already in place) via engine ADR 0077: "Alternatives Considered" template section + Deprecated ADRs join `validate_adr_back_reference_orphan`. ADR collection: **102 (95 Accepted + 7 Superseded; 54 engine + 48 product)** — product ADR 0102 landed at S-0213 (Tier-A Cluster 4 node schema redesign — 14 new columns to public.nodes via migration 0140 with paired rollback; 10-value `node_type TEXT[]` element CHECK including `'unclassified'` transitional default per schema-only Adjudication 1; multi-valued array form per Adjudication 2 — kant_walkthrough §3.2.1.A's phenomenology-fits-3-types multi-typing reality; strict ADR 0008 read on historical_context per Adjudication 3 — movement-shaped entities route via tradition_label without ADR 0008 amendment; bridge_concept retained as synthesis-paper coinage per Adjudication 4 with boundary_concept reserved as D8 open-for-revision target; subfield 9th enum value added per D3 resolve-by-recommendation; tradition_label TEXT[] open vocabulary per D4 resolve-by-recommendation; domain ↔ disciplinary_domain two-level reconciliation; assessment_items as JSONB array not FK; three new validate.py checks landed same-session per `feedback_no_pilot_wait_and_see.md` with 25 new pytests; ROUTING.md + PREDICATE_MANIFEST.md + product/docs/architecture.md amendments; two latent fix-in-context items resolved — mypy typing gap on `**_KEEPALIVE_KWARGS` dict-unpack from S-0208 + bandit B108 false-positive on `/tmp/leaked` sentinel from S-0212, both introduced by SKIP_ENGINE_HOOKS=1 bypasses; orphan-PID-blocker incident empirically observed ADR 0101's named-Tier-3 case → filed [Issue #155](https://github.com/StarshipSuperjam/paideia/issues/155) as the ADR 0101 sequel for server-side wedge cleanup); engine ADR 0101 landed at S-0212 (Subprocess isolation for graph_audit — Issue #151 structural fix; new `engine/tools/graph_audit_worker.py` worker + `_run_graph_audit_subprocess` wrapper in `validate.py`; parent's 90s `subprocess.run(timeout=)` SIGKILL escapes the S-0211-empirically-pinned `conn.cancel()`-wedges-too failure mode; existing inner defenses retained as defense-in-depth; first-exercise wedge fired against PID 56709 during the implementation commit itself, parent SIGKILL verified end-to-end; Issue #151 closed with revised closure criterion: subprocess gate verified in production once); engine ADR 0100 landed at S-0210 (Engine-inspired hook installation + close-friction mitigations — `core.hooksPath` migration cures the S-0209 symlink-staleness lesson at root; `check_settings_sync.py` bidirectional fix closes Issue #154; `SKIP_ENGINE_HOOKS=1` audited bypass replaces opaque `--no-verify`; `recover_partial_close.py` port from Engine sibling project with 7-state diagnosis + 3 recovery flags; suite dispatch + thin pre-commit refactor named in Alternatives Considered as deferred escalations; ADR 0099 observation window confound acknowledged + new acceptance criterion: 3 clean closes against combined ADR 0099 + ADR 0100 apparatus; S-0210 = sample 1; S-0212 = sample 2 — clean close with SKIP_ENGINE_HOOKS=1 used once for the first-exercise wedge bootstrap which is exactly the audited-bypass use case ADR 0100 documents); product ADR 0098 landed at S-0208 (Tier-A Cluster 2 edge-type taxonomy + three-relation layered separation — second Tier-A cluster-implementation ADR per ADR 0094 dependency order; re-verifies ADR 0094 T1-A + ADR 0095 T1-A); product ADR 0097 landed at S-0207 (Tier-A Cluster 1 contestability substrate — first Tier-A cluster-implementation ADR per ADR 0094 dependency order; closes ADR 0094 T1-A + ADR 0095 T1-A); product ADRs 0095 + 0096 landed at S-0203 (PDG papers extraction pre-phase Session δ₂ — [`Phase 6 tool-stack: Postgres+JSONB+recursive CTEs confirmed; OSS Apache-2.0 raises revisit bar`](../product/adr/0095-phase-6-tool-stack-postgres-jsonb-confirmed-with-oss-revisit-bar.md) [amends ADR 0017 in-body] + [`Phase 6 learning-outcome taxonomy: 3-level depth + Decoding-the-Disciplines bottleneck moves`](../product/adr/0096-phase-6-learning-outcome-taxonomy.md); all four Session δ foundational ADRs now landed [0093 + 0094 at S-0202; 0095 + 0096 at S-0203]; remaining Q/D items D3/D4/D5/D7 defer to Cluster 4/5 cluster-implementation ADR authors); product ADRs 0093 + 0094 landed at S-0202 (PDG papers extraction pre-phase Session δ₁ — [`Phase 6 product-trajectory formalization`](../product/adr/0093-phase-6-product-trajectory-formalization.md) + [`Phase 6 scope expansion to include Tier-A substrate redesign`](../product/adr/0094-phase-6-scope.md)); engine ADR 0099 landed at S-0209 (session-close friction mitigations per Issue #153 — Fix A `CLOSE_ALLOWED_GLOBS` widened for ADR READMEs + Fix C ruff format auto-apply in pre-commit + Fix D post-write reminders for ADR-index and changelog-summary + Fix E procedural Step 5b in shutdown sequence; Fix B + Fix F deferred to Alternatives Considered; observation window opened — 3 consecutive clean closes per acceptance criterion 4); engine ADR 0092 landed at S-0198 (per-session changelog directory replaces monolithic ENGINE_LOG.md; supersedes the ENGINE_LOG-naming/structure clauses of ADR 0037; engine/product wall partition + CHANGELOG.md filename reservation remain Accepted; ADR 0037 flips to Superseded in part; `engine-v0.1.0` tag cuts the foundation close); engine ADR 0091 landed at S-0188 (engine-memory substrate SQLite + FTS5; supersedes ADR 0090 because Issue #134 falsified its commitment 2a; ADRs 0090, 0056, 0079 flip to Superseded; ADR 0057 amended in-body for the substrate-coupled cluster-detection paragraph; 5-session implementation S-0189 → S-0193 tracked at [Issue #138](https://github.com/StarshipSuperjam/paideia/issues/138)); engine ADR 0079 amended at S-0187 (new "Amendment (S-0187): in-rebuild threshold override" subsection; superseded the next session by ADR 0091); engine ADR 0090 landed at S-0185 (Phase-6 recall-substrate decision; A1-PROPER comprehensive in-project fix campaign with scheduled maintenance + upstream coordination + empirical investigation; substrate preserved; closed Issue #131 — both superseded at S-0188); engine ADR 0089 landed at S-0163 (Skill ↔ Layer-1 procedure-parity validator check; closes Issue #129); engine ADR 0084 landed at S-0151 (pushback-rule extraction-step extension; closes Issue #77); product ADRs 0085–0088 landed at S-0152 (OQ-DEC1-A/B/C/D tension-set settlement; Phase 6 unblocked). The OQ-DEC1 settlements draw from the 0085–0088 range because engine ADRs 0066–0084 occupy that part of the shared sequence per ADR 0037; product ADRs 0093 + 0094 draw from 0093–0094 because engine ADRs 0089–0092 occupy 0089–0092 in the shared sequence. Full session-by-session history in [`engine/changelog/`](changelog/) (per ADR 0092; historical archive at [`engine/changelog/_history/ENGINE_LOG-pre-0.1.0.md`](changelog/_history/ENGINE_LOG-pre-0.1.0.md)). |
| **Last session** | **S-0213 (2026-05-19, interactive build) — Tier-A Cluster 4 (node schema redesign) landed cleanly per ADR 0102; 14 columns + 4 CHECK constraints on public.nodes via migration 0140 schema-only; 3 new validate.py checks + 25 new pytests; orphan-PID-blocker incident filed as Issue #155 (ADR 0101 sequel).** Five commits: eager-claim ([`61758f0`](https://github.com/StarshipSuperjam/paideia/commit/61758f0)); ADR 0102 + index row + decisions drawer `17caa1fb` ([`ac0c170`](https://github.com/StarshipSuperjam/paideia/commit/ac0c170)); migration 0140 + paired rollback ([`52fa1be`](https://github.com/StarshipSuperjam/paideia/commit/52fa1be)) — applied via mcp__supabase__apply_migration with round-trip verification end-to-end (pre-state captured → apply → 18 Postcondition-Assertions pass → rollback → re-apply stable); validate.py three new checks (`node_type_unclassified` mode-switched, `node_threshold_concept_lacks_assessment_items`, `node_lacks_cultural_specificity`) + 25 pytests + two latent fix-in-context items (mypy typing gap on `**_KEEPALIVE_KWARGS` dict-unpack from S-0208, bandit B108 false-positive on `/tmp/leaked` sentinel from S-0212 — both introduced by SKIP_ENGINE_HOOKS=1 bypasses; both fixed by inline-expansion + nosec annotation) ([`686bbb2`](https://github.com/StarshipSuperjam/paideia/commit/686bbb2)); ROUTING.md + PREDICATE_MANIFEST.md + product/docs/architecture.md amendments ([`ad78d9e`](https://github.com/StarshipSuperjam/paideia/commit/ad78d9e)). Four plan-mode AskUserQuestion adjudications: (1) schema-only backfill posture; (2) node_type TEXT[] array form; (3) strict ADR 0008 read on historical_context; (4) keep bridge_concept with provenance note + boundary_concept future-revision flag. Four resolve-by-recommendation outcomes: D3 subfield 9th enum value; D4 tradition_label TEXT[] open vocabulary; domain ↔ disciplinary_domain two-level reconciliation; assessment_items as JSONB array not FK. **Orphan-PID-blocker incident.** First migration apply timed out with `57014: canceling statement due to statement timeout` — investigation surfaced PID 51686 on the pooler in `idle in transaction` since ~17:08Z (3+ hours before this session's 20:15Z boot), holding AccessShareLock from a graph_audit reader query — orphan residue from a pre-S-0212 wedge that client-side SIGKILL (ADR 0101) didn't clean up server-side. ALTER TABLE needs AccessExclusiveLock; first attempt blocked. ADR 0101's "Out-of-scope alternatives" Tier-3 row had named "apply_migration port — speculative — no migration wedge observed"; this incident is the first empirical observation. User restarted Supabase to clear orphan; second apply attempt succeeded cleanly. Filed [Issue #155](https://github.com/StarshipSuperjam/paideia/issues/155) post-close as ADR 0101 sequel with four concrete options (idle_in_transaction_session_timeout on pooler / apply_migration wrapper pre-flight orphan sweep / standalone orphan-sweep tool / graph_audit_worker termination cleanup). **Classifier-override incident.** AskUserQuestion authorized `pg_terminate_backend(51686)`; classifier denied the subsequent execute_sql call reading the flow as a race rather than as authorization. False-positive — surfaced to user as meta-problem; manual Supabase restart was the recovery path. Pushback drawer `07b19f41` captures the exchange + generalizable pattern (when user authorizes via AskUserQuestion and classifier still blocks, surface as meta-problem rather than silently work around). Lesson drawer `dc53aea9` (SKIP_ENGINE_HOOKS=1 audited bypass per ADR 0100 lets latent gate failures accumulate across sessions; this session was first non-bypassed Python-edit commit since S-0208 and surfaced two latent issues). Engine_memory: 7 calls (search 2, diary_read 1, diary_write 1, add_drawer 3). Diary `2881e3ca`. Cluster 4 dependency-order forward-pointer to Cluster 3 (claims migration sub-range 0150-0159) OR per-domain Cluster 4 backfill sequels (claim slots 0141-0149). Detail in [`engine/session/archive/S-0213.json`](session/archive/S-0213.json). |
| **Pre-S-0213** | **S-0212 (2026-05-19, interactive build) — Issue #151 structural fix landed via subprocess isolation per ADR 0101; new gate verified end-to-end against a live wedge in-session; Issue #151 closed.** Six commits: eager-claim ([`eec3e1b`](https://github.com/StarshipSuperjam/paideia/commit/eec3e1b)); engine ADR 0101 + index row + decisions drawer `9e33ea6d` ([`b176bff`](https://github.com/StarshipSuperjam/paideia/commit/b176bff)); implementation — new `engine/tools/graph_audit_worker.py` (psycopg work happens in subprocess) + `_run_graph_audit_subprocess` wrapper in `validate.py` invoking `subprocess.run(timeout=90, env=scrubbed_env() with SUPABASE_DB_URL, cwd=REPO_ROOT)` + `subprocess.TimeoutExpired` branch in `validate_graph()` recording `graph_audit_subprocess_timeout` hard-fail + 18 new tests (7 worker + 11 wrapper) + 6 existing-test monkey-patch migrations ([`66b346c`](https://github.com/StarshipSuperjam/paideia/commit/66b346c) via SKIP_ENGINE_HOOKS=1 audited bypass per ADR 0100); first-exercise readiness note per ADR 0053 ([`a9aa406`](https://github.com/StarshipSuperjam/paideia/commit/a9aa406)); STATE.md + changelog entry + outcome_summary + scope_delivery (close commit). **First-exercise empirical results landed in-session.** The new subprocess gate fired against a live #151 wedge during the very commit that introduced it: PID 56709 dump at [`.engine_reports/hang-diagnostic-2026-05-19T193154.887479Z-56709.json`](.engine_reports/hang-diagnostic-2026-05-19T193154.887479Z-56709.json) showed: (a) inner watchdog fired at +45s with capture-before-cancel ordering held (capture written before `conn.cancel()` called); (b) `conn.cancel()` did NOT return within the next 45 seconds; (c) parent's 90s outer cap fired and SIGKILLed PID 56709 — premise 2 of ADR 0101 (subprocess.run timeout always delivers SIGKILL regardless of child syscall state) closes verified in production at the very moment of authoring; (d) pre-commit returned non-zero with the new `graph_audit_subprocess_timeout` hard-fail message naming Issue #151, ADR 0101, and the diagnostic file path. **Acceptance criterion #4 retired as obsolete** (measured the bug, not the blocker). Pushback drawer `8fdd0c44` (when user says "fix X" with multiple root-cause hypotheses alive, surface symptom-vs-cause distinction explicitly; the symptom-bound option often dominates because cause-elimination is hypothesis-conditional). Lesson drawer `85fb02e9` (subprocess.run(timeout=N) SIGKILL escapes libpq poll() wedges that no in-process cancel can; generalizable wedge-class pattern). Engine_memory: 6 calls (search 1, diary_read 1, diary_write 1, add_drawer 3). Diary `0c78b351`. Issue #151 updated and closed via [comment](https://github.com/StarshipSuperjam/paideia/issues/151#issuecomment-4491412959). Detail in [`engine/session/archive/S-0212.json`](session/archive/S-0212.json). |
| **Pre-S-0212** | **S-0211 (2026-05-19, interactive build) — Issue #151 hypothesis-agnostic diagnostic capture landed AND first-exercise empirically falsified the cross-process Python contamination hypothesis.** User-directed diversion from the planned Cluster 4 work (third diversion in a row after S-0209 and S-0210) to address the priority:urgent Issue #151 at the data-collection layer. Four commits: eager-claim ([`4230af4`](https://github.com/StarshipSuperjam/paideia/commit/4230af4)); new `engine/tools/hang_diagnostic.py` module + 12 pytests covering best-effort / bounded / secret-scrubbing / lazy-dir invariants ([`a52ca83`](https://github.com/StarshipSuperjam/paideia/commit/a52ca83)); `validate.py:_watchdog()` integration calling `capture_hang_diagnostic("graph_audit_watchdog")` BEFORE `conn.cancel()` + new pytest `test_watchdog_invokes_capture_before_cancel` ([`84fd8e7`](https://github.com/StarshipSuperjam/paideia/commit/84fd8e7) via SKIP_ENGINE_HOOKS=1 audited bypass — user-authorized per ADR 0100); macOS netstat-separator fix discovered via the live-wedge dump ([`69dbaa1`](https://github.com/StarshipSuperjam/paideia/commit/69dbaa1) same bypass mode). **First-exercise empirical results landed in-session.** The diagnostic capture fired against a live #151 wedge during the very commit that introduced it: PID 41424 dump at [`.engine_reports/hang-diagnostic-2026-05-19T183424.199794Z-41424.json`](.engine_reports/hang-diagnostic-2026-05-19T183424.199794Z-41424.json) showed (a) main thread wedged in `psycopg.connection.wait()` on `pgconn.socket` — predicted poll() hang confirmed; (b) ESTABLISHED TCP to ec2-18-214-78-123:5432, keepalive did NOT fire (consistent with pgbouncer L4-traffic hypothesis recorded in lesson drawer `c8f90de5`); (c) `conn.cancel()` ITSELF wedged for 6+ minutes against the same socket — empirically pins the S-0208 falsification shape and validates the capture-before-cancel ordering choice; (d) no suspect non-project Python processes at wedge time (only 4× engine.memory.mcp_server, project-internal, SQLite per ADR 0091, not Supabase) — **the user-hypothesized cross-process Python contamination vector is NOT supported by the captured evidence**. Surviving live hypothesis: structural to the pooler. Pushback drawer `94e4ea6e` (hypothesis-agnostic data collection beats committed hypothesis testing when the bug is intermittent and observability is the predecessor to a fix). Lesson drawer `4e3c8296` (capture-before-cancel ordering is load-bearing because `conn.cancel()` can wedge against the same poisoned socket; supersedes the watchdog's pre-S-0211 assumption that cancel completes synchronously). Engine_memory: 5 calls (search 1, diary_read 1, diary_write 1, add_drawer 2). Diary `90afb5d1`. Approved plan: [`~/.claude/plans/what-is-next-rippling-abelson.md`](~/.claude/plans/what-is-next-rippling-abelson.md). Issue #151 updated via [comment](https://github.com/StarshipSuperjam/paideia/issues/151#issuecomment-4490997861). Detail in [`engine/session/archive/S-0211.json`](session/archive/S-0211.json). |
| **Pre-S-0211** | **S-0210 (2026-05-19, interactive build) — Engine-inspired hook installation + close-friction mitigations landed: ADR 0100 + 4 targeted Engine ports; Issue #154 closed.** (Detail in [`engine/session/archive/S-0210.json`](session/archive/S-0210.json).) |
| **Pre-S-0210** | **S-0209 (2026-05-19, interactive build) — Issue #153 session-close friction mitigations landed: ADR 0099 + Fix A+C+D+E.** (Detail in [`engine/session/archive/S-0209.json`](session/archive/S-0209.json).) |
| **Last build session** | **S-0213 (2026-05-19) — see Last session row.** |


## Next session work item

**S-0213 closed at S-0213.** Cluster 4 (node schema redesign) landed cleanly per ADR 0102 — 14 columns + 4 CHECK constraints on public.nodes via migration 0140 schema-only; round-trip verified end-to-end; 3 new validate.py checks + 25 new pytests; ROUTING/PREDICATE_MANIFEST/architecture.md amendments; two latent fix-in-context items resolved; orphan-PID-blocker incident filed as Issue #155 (ADR 0101 sequel). The five-way diverted-from-Cluster-4 streak (S-0209 → S-0210 → S-0211 → S-0212 → S-0213-arrived) ends with Cluster 4 LANDED at S-0213.

**S-0214 — Next session work item: choose between Cluster 3 OR first Cluster 4 backfill sequel.**

The Phase 6 Tier-A dependency order per [ADR 0094](../product/adr/0094-phase-6-scope.md) is C1 → C2 → **C4** → **C3** → C5. With C4 landed, two natural next sessions are open; **the user picks at next session boot**:

**Option A — Cluster 3 (goal-relative parameterization).** Next in the dependency order. Adds `learning_outcomes` table + `edge_outcome_necessity` field per [ADR 0096](../product/adr/0096-phase-6-learning-outcome-taxonomy.md) (3-level depth + Decoding-the-Disciplines bottleneck moves). Claims migration sub-range 0150-0159. Independent of Cluster 4 backfill; can proceed without per-domain node_type backfill landing first. ADR 0096 already settled the taxonomy; Cluster 3's ADR adopts it for the edge surface.

**Option B — Cluster 4 first per-domain backfill sequel (e.g., epistemology + metaphysics).** Per ADR 0102 backfill commitments table, likely 4-6 sequel sessions (one per subdomain or subdomain-pair) claiming migration slots 0141-0149. The FIRST sequel runs per-node review of ~80 epistemology + metaphysics nodes (or a subdomain-pair chosen by the user), populating `node_type`, `disciplinary_domain`, `granularity`, `canonical_sources`, `misconceptions`, `cultural_specificity`, `tradition_label` from the source seed migrations + paper extraction notes. Validates ADR 0102 T1-A (TEXT[] array correctly models multi-typing at consumer time) AND T2-B (`cultural_specificity` IS-NULL signal is implementable without ambiguity).

**Option C — Issue #155 (ADR 0101 sequel: server-side wedge cleanup).** Newly filed priority:urgent + bug + tech-debt. Likely a 1-2-session sequel adopting one of the four concrete options (`idle_in_transaction_session_timeout` pooler config / `apply_migration` wrapper pre-flight orphan sweep / standalone orphan-sweep tool / graph_audit_worker termination cleanup). High value because every Phase 6+ migration session will hit the orphan-PID-blocker class until this lands. ADR amendment or successor ADR required per ADR 0084 extraction step.

**Recommendation:** **Option C first.** Reasoning: every Cluster 4 backfill sequel + Cluster 3 + Cluster 5 will attempt `mcp__supabase__apply_migration` against the same pooler; without server-side wedge cleanup, each will face the same orphan-blocker risk and require manual Supabase restart. Landing Issue #155 first unblocks the entire downstream Tier-A sequence cleanly. Tier-1 readiness criterion holds: ADR 0101 T1 still satisfied (the client-side gate works); Issue #155 addresses the server-side residue layer. The user adjudicates at next-session boot — Option A and B remain viable if the user prefers to absorb the orphan-restart friction on a per-session basis.

**Cluster 4 ADR 0102 carry-forward items (for the LAST per-domain backfill sequel — whichever session runs):**

- Edit `NODE_TYPE_UNCLASSIFIED_FIRE_MODE` in `engine/tools/validate.py` from `"soft_warn"` to `"hard_fail"`.
- Land a small follow-up migration (claims a slot in the 0141-0149 range, e.g., `0149_nodes_drop_unclassified_enum_value.sql`) that drops `'unclassified'` from the `nodes_node_type_valid` CHECK constraint vocabulary via `ALTER TABLE ... DROP CONSTRAINT ... ADD CONSTRAINT` per ADR 0098 premise 4 pattern. Paired rollback restores `'unclassified'`.
- Verify all 380 nodes have substantive `node_type` values (the count threshold for `node_type_unclassified` check should be 0).
- Update ROUTING.md + PREDICATE_MANIFEST.md to reflect the transition close.
- Issue commentary on this ADR (ADR 0102) marking the T1-A / T1-B / T2-A / T2-B readiness criteria as closed verified or falsified.

**Q1-F2 (boundary_concept rename) carry-forward.** Per ADR 0102 Adjudication 4 + D8 open-for-revision posture, future-revision candidate. A dedicated rename session (NOT a Cluster 4 backfill sequel) does the WebFetch on Star & Griesemer 1989 / Akkerman & Bakker 2011 + adjudicates rename vs keep. Low priority unless a per-domain backfill sequel finds the term genuinely loses semantic content vs the lit-grounded `boundary_concept` alternative.

**T1-A / T1-B / T2-A closure tracking from ADR 0094 + ADR 0095 + ADR 0096 + ADR 0097 + ADR 0098 load-bearing premises:**

- **ADR 0094 T1-A** (premise 2 — Tier-A implementability without re-decomposition) — **closed verified at S-0207; re-verified at S-0208 via ADR 0098 landing; re-verified again at S-0213 via ADR 0102 landing.** Cluster 1 + Cluster 2 + Cluster 4 all fit in one migration each; no scope re-shape required across three Tier-A landings.
- **ADR 0094 T1-B** (premise 3 — D-item adjudication doesn't force Tier-D inclusion) — **closed verified at S-0203 via D6 settlement; partial re-closure at S-0213** (D1, D2, D3, D4, D8 settled within Tier-A scope at the Cluster 4 ADR landing). D5 defers to a future Cluster 2 amendment session; D6 + D7 defer to Cluster 5 — full re-closure at Cluster 5 ADR landing.
- **ADR 0094 T2-A** (premise 6 — five-field embedding semantic-quality claim) closes at Phase 6 self-correction master plan's embedding evaluation step per ADR 0086. All five fields (`node_type` / `disciplinary_domain` / `granularity` / `canonical_sources` / `misconceptions`) now exist on `public.nodes` post-S-0213 migration 0140; per-domain backfill sequels populate the values; the embedding evaluation step runs against the populated schema.
- **ADR 0095 T1-A** (premise 1 — Tier-A schema implementability in Postgres+JSONB) — **closed verified at S-0207; re-verified at S-0208 via ADR 0098 landing; re-verified again at S-0213 via ADR 0102 landing.** Cluster 4 uses TEXT[] + JSONB + nullable TEXT + CHECK constraints — all native Postgres; no Apache AGE escalation across three Tier-A landings.
- **ADR 0095 T1-B** (premise 2 — junction-table cardinality bounded) closes at first Cluster 3 migration landing with measured `edge_outcome_necessity` row count vs Phase-6-projected ≤2000 rows.
- **ADR 0096 T1-A** (premise 1 — 3-level depth granularity empirically adequate) closes at first Cluster 3 cluster-implementation ADR landing.
- **ADR 0097 T1-A** (premise 1 — contestability-as-atomic) — Cluster 2 consumed only `review_status` (the `provisional` default for retyped soft_prerequisites) but the other 8 fields are present in the schema; partial consumption does not falsify per ADR 0097's note that atomicity is authoring-time not query-time. Premise continues to hold.
- **ADR 0097 T1-B** (premise 6 — disagreement_flag computation correctness; threshold value calibration) closes at the first session where learner trace data accumulates sufficient to calibrate `DISAGREEMENT_THRESHOLD`.
- **ADR 0097 T2-A / T2-B / T2-C** — adequacy premises close at first downstream consumer.
- **ADR 0098 T1-A** (premise 2 — `soft_prerequisite` epistemic-conservatism) closes at Cluster 7 SQA predictive-validity pipeline first-exercise. Until then, all 516 stay at soft.
- **ADR 0098 T2-A** (premise 3 — `influenced_by` neutral default for the 17 historical edges) closes at first historical-influence audit (likely Phase 6.5+).
- **ADR 0098 T2-B** (premise 5 — `conceptual_relatedness` reserved-but-unused at landing) closes at Cluster 5 misconception sub-graph landing.
- **ADR 0102 T1-A** (premise 2 — TEXT[] array form correctly models multi-typing for downstream consumers) closes at first downstream consumer's first exercise (likely Cluster 5 misconception sub-graph at Session δ₅+, OR SEP embedding work per ADR 0086 if that lands first). Verified if consumers handle the array form without forcing a primary-typing judgment; falsified if consumers need a derived `node_type_primary TEXT` (ADR 0102 amends to add it).
- **ADR 0102 T1-B** (premise 3 — 10-value enum exhaustive over corpus) closes at the LAST per-domain backfill sequel session (the one that escalates `node_type_unclassified` to hard-fail). Verified if no 11th enum value was needed across all backfill sessions; falsified if specific domains forced an extension per D8 open-for-revision posture.
- **ADR 0102 T2-A** (premise 5 — strict ADR 0008 read on `historical_context` doesn't lose essential semantic content) closes at the LAST per-domain backfill sequel session. Verified if no node forced the generous read; falsified if specific nodes required ADR 0008 amendment.
- **ADR 0102 T2-B** (premise 7 — `node_lacks_cultural_specificity` soft-warn is implementable without ambiguity) closes at first per-domain backfill sequel session. Verified if NULL-vs-not-applicable distinction is clean; falsified if a `cultural_specificity_not_applicable BOOLEAN` flag becomes necessary.

Tier-B trigger-gated issues (#22, #24, #79, #82, #83, #84, #85, #115, #117) remain in the backlog as load-bearing activation reminders per the S-0195 plan's triage; do NOT mass-close.

**T1-C of the per-session changelog first-exercise note** closes when the aggregator runs against ≥2 entries (natural exercise at the next release-prep session OR explicit invocation). S-0198 + S-0200 + S-0202 + S-0208 entries now exist; aggregator will run cleanly against any ≥2-count.

**Carried open items:**

- **Issue #151 closed at S-0212 via subprocess isolation per [ADR 0101](adr/0101-subprocess-isolation-for-graph-audit.md).** The fix moves `_read_graph_from_db` across a process boundary the parent SIGKILLs on a 90s wall-clock cap — escapes the S-0211-empirically-pinned `conn.cancel()`-wedges-too failure mode. New worker module at `engine/tools/graph_audit_worker.py`; wrapper `_run_graph_audit_subprocess` in `engine/tools/validate.py`; existing inner defenses (S-0186 connect_timeout, S-0187 watchdog, S-0208 keepalive, S-0211 capture-before-cancel) retained as defense-in-depth inside the worker. **First-exercise wedge fired against PID 56709** during the implementation commit ([`66b346c`](https://github.com/StarshipSuperjam/paideia/commit/66b346c)); diagnostic file at [`.engine_reports/hang-diagnostic-2026-05-19T193154.887479Z-56709.json`](../.engine_reports/hang-diagnostic-2026-05-19T193154.887479Z-56709.json) shows inner watchdog fired, capture written before cancel, `conn.cancel()` did not return, parent's 90s SIGKILL fired, parent control flow returned cleanly with `graph_audit_subprocess_timeout` hard-fail. Premise 2 of ADR 0101 (subprocess.run timeout always delivers SIGKILL regardless of child syscall state) verified in production at the moment of authoring. Issue closure criterion revised: subprocess gate verified in production once (met by the first-exercise wedge); the bug may persist at the pooler layer but the blocker it produced no longer can. First-exercise readiness note at [`engine/build_readiness/graph_audit_subprocess_first_exercise.md`](build_readiness/graph_audit_subprocess_first_exercise.md) — all Tier 1 findings (T1-A through T1-D) closed in-session. Out-of-scope alternatives (deferred as Tier 3): Supavisor session mode (URL switch only if wedge *frequency* becomes the operational concern); direct DB port 5432 (deferred pending IPv6 verification); apply_migration port (speculative — no migration wedge observed).
- **`edge_contestability_unguarded_high_confidence` firing on all 533 edges** (vs the ~17 the Issue #152 body predicted). Not a defect — the warn correctly identifies edges with high confidence but no recorded counterexamples / rationale; the entire seed corpus is in this state because Cluster 2 retyped only `edge_type`, not `expert_confidence`. Cluster 7 SQA predictive-validity pipeline is the natural successor that will downgrade confidence based on learner-trace evidence.
- **Worktree-local `engine/.memory/engine_memory.sqlite3` stale files** in pre-S-0193 worktrees — the resolver fix means new sessions read the canonical main-repo file, but worktrees that initialized before S-0193 may carry a local file (harmless, gitignored, can be deleted manually).
- **[Issue #155](https://github.com/StarshipSuperjam/paideia/issues/155) — ADR 0101 sequel: server-side wedge cleanup before apply_migration.** Filed at S-0213 close after the orphan-PID-blocker incident empirically observed ADR 0101's "Out-of-scope alternatives" Tier-3 row ("apply_migration port — speculative — no migration wedge observed"). Priority:urgent + bug + tech-debt. Four concrete options enumerated in the Issue body (server-side `idle_in_transaction_session_timeout` config / `apply_migration` wrapper pre-flight orphan sweep / standalone orphan-sweep tool / graph_audit_worker termination cleanup). Every Phase 6+ migration session will hit this class until landed; landing it first unblocks Cluster 4 backfill sequels + Cluster 3 + Cluster 5 cleanly.


## SWE-hardening rollout

> Authored at session-pre-S-0124 (date 2026-05-11) from the audit plan at `~/.claude/plans/not-a-working-session-sequential-twilight.md`. Comparing Paideia's posture against [`affaan-m/everything-claude-code`](https://github.com/affaan-m/everything-claude-code) and [`addyosmani/agent-skills`](https://github.com/addyosmani/agent-skills) produced 22 GitHub Issues (#65–#86) across three tiers + analysis-outcomes. **Each adoption is ADR-tracked per its discipline; sessions default to single-Issue scope BUT may bundle declared pairings (see "Combined-session pairings" below — three pairings declared at S-0125).** Cross-cutting mechanisms additionally require a first-exercise readiness note per [ADR 0053](adr/0053-mechanism-first-exercise-gate.md).

**Pickup rule.** A future session reads this section and picks the next *eligible unit* (single Issue OR a declared combined-session pairing — see "Combined-session pairings" below) with no remaining blockers. The boot surface (per [`issue-discipline.md`](operations/issue-discipline.md)) counts the underlying Issues in the standard backlog totals; this section adds the dependency + trigger + pairing structure the count cannot encode.

### Tier 1 — foundations (adopt before any non-trivial code lands)

| Issue | Title | Blocked by | Pairing |
|---|---|---|---|
| ~~[#65](https://github.com/StarshipSuperjam/paideia/issues/65)~~ | ~~`uv lock` lockfile for reproducible builds~~ — **closed at S-0127 per ADR 0064** | — | — |
| ~~[#66](https://github.com/StarshipSuperjam/paideia/issues/66)~~ | ~~`gitleaks` pre-commit + GitHub-native secret scanning~~ — **closed at S-0132 per ADR 0067** | — | ~~A~~ closed |
| ~~[#68](https://github.com/StarshipSuperjam/paideia/issues/68)~~ | ~~Mirror `validate.py` to GitHub Actions CI~~ — **closed at S-0131 per ADR 0065 engine** | — | — |
| ~~[#69](https://github.com/StarshipSuperjam/paideia/issues/69)~~ | ~~PR template + branch protection on `main`~~ — **closed at S-0131 per ADR 0066** | — | — |
| ~~[#67](https://github.com/StarshipSuperjam/paideia/issues/67)~~ | ~~Dependabot for `pip` + `github-actions` ecosystems~~ — **closed at S-0133 per ADR 0069** | — | — |

### Tier 2 — judgment + observability (adopt during Phase 6 entry)

| Issue | Title | Blocked by | Pairing |
|---|---|---|---|
| ~~[#70](https://github.com/StarshipSuperjam/paideia/issues/70)~~ | ~~`bandit` SAST in pre-commit + CI~~ — **closed at S-0132 per ADR 0068** | — | ~~A~~ closed |
| ~~[#71](https://github.com/StarshipSuperjam/paideia/issues/71)~~ | ~~`pytest-cov` coverage reporting with measured floor~~ — **closed at S-0136 per ADR 0074 (measured baseline 80%, floor 78%)** | — | — |
| ~~[#72](https://github.com/StarshipSuperjam/paideia/issues/72)~~ | ~~GitHub issue templates for 8 type labels~~ — **closed at S-0137 per ADR 0075** | — | — |
| ~~[#73](https://github.com/StarshipSuperjam/paideia/issues/73)~~ | ~~Project-wired `/review` skill (five-axis + anti-rationalization)~~ — **closed at S-0134 per ADR 0070** | — | ~~C~~ closed |
| ~~[#74](https://github.com/StarshipSuperjam/paideia/issues/74)~~ | ~~Project-wired `/security-review` skill (OWASP Top 10)~~ — **closed at S-0134 per ADR 0071** | — | ~~C~~ closed |
| ~~[#75](https://github.com/StarshipSuperjam/paideia/issues/75)~~ | ~~`frontend-ui-engineering` skill~~ — **closed at S-0135 per ADRs 0072 + 0073 (modular split into `/frontend-discipline` + `/paideia-frontend-overlays`)** | — | — closed |

### Tier 3 — composition, release engineering, trigger-gated

| Issue | Title | Status / Trigger |
|---|---|---|
| ~~[#76](https://github.com/StarshipSuperjam/paideia/issues/76)~~ | ~~`/ship` multi-model orchestration~~ — **closed at S-0148 per ADR 0081** | — closed |
| ~~[#77](https://github.com/StarshipSuperjam/paideia/issues/77)~~ | ~~Evaluate `doubt-driven-development` workflow~~ — **closed at S-0151 per ADR 0084 (Option 2 — CLAUDE.md pushback-rule extension with explicit extraction step for high-stakes decision classes)** | — closed |
| [#78](https://github.com/StarshipSuperjam/paideia/issues/78) | `.editorconfig` (cleanup) | — |
| [#80](https://github.com/StarshipSuperjam/paideia/issues/80) | CODEOWNERS for engine/ vs product/ | — (ADR-promotion trigger: ≥2 collaborators) |
| [#79](https://github.com/StarshipSuperjam/paideia/issues/79) | `performance-optimization` skill | **Trigger-gated:** deployable surface exists |
| [#82](https://github.com/StarshipSuperjam/paideia/issues/82) | Release tagging + CHANGELOG automation | **Trigger-gated:** Phase 9 approach OR pre-launch beta |
| [#83](https://github.com/StarshipSuperjam/paideia/issues/83) | SBOM generation | **Trigger-gated:** deployable artifact exists |
| [#84](https://github.com/StarshipSuperjam/paideia/issues/84) | Monthly repo-health metrics workflow | **Trigger-gated:** ≥2 collaborators OR Phase 6 complete |

### Analysis-outcome additions (from agent-skills cross-checks)

| Issue | Title | Status / Trigger |
|---|---|---|
| [#81](https://github.com/StarshipSuperjam/paideia/issues/81) | ADR template enhancement — Alternatives Considered + PROPOSED state + DEPRECATED label | — |
| ~~[#86](https://github.com/StarshipSuperjam/paideia/issues/86)~~ | ~~Revert/rollback discipline ops-doc~~ — **closed at S-0140 per ADR 0078** | — closed |
| [#85](https://github.com/StarshipSuperjam/paideia/issues/85) | API documentation discipline skill | **Trigger-gated:** Phase 6+ backend API surface OR Python public surface > 5 functions |

### Combined-session pairings

Three pairings declared at S-0125 after audit of the S-0124 rollout. Per the feedback memory "Don't split work into multiple sessions to hedge on length", session-per-Issue was the default — not a hard constraint. Each pairing lands two ADRs + two MemPalace `decision` drawers + (where applicable) two first-exercise readiness notes in one session.

| Pairing | Issues | Rationale |
|---|---|---|
| **A — pre-commit security scanners** | [#66](https://github.com/StarshipSuperjam/paideia/issues/66) + [#70](https://github.com/StarshipSuperjam/paideia/issues/70) | Both extend `engine/tools/hooks/pre-commit`, both add tooling config + version pin, both add `validate.py` integration, both add first-exercise readiness notes. #70's Issue body explicitly declares *"Pairs with: #66"*. Ordering: #70 is blocked by #65 + #68, so the combined session lands after #65 and #68 close. |
| **B — GitHub gating layer** | [#68](https://github.com/StarshipSuperjam/paideia/issues/68) + [#69](https://github.com/StarshipSuperjam/paideia/issues/69) | Hard sequential coupling — #69's branch-protection rule requires #68's CI status check to exist before configuration makes sense. Both touch `.github/` exclusively; same scope_lock surface. Splitting forces the second session to rebuild eager-claim/routine-push posture context cold. |
| **C — code-review skills** | [#73](https://github.com/StarshipSuperjam/paideia/issues/73) + [#74](https://github.com/StarshipSuperjam/paideia/issues/74) | Both author Skills in `.claude/skills/` adapted from `addyosmani/agent-skills`; identical authoring pattern (`disable-model-invocation: true`, scope_lock awareness, ADR-citation requirement). #73's anti-rationalization table is explicitly reused by #74. |

Issue bodies remain individually trackable; the pairings are surfaced here as the routing layer.

### Trigger surface — re-check at every session boot

| When this becomes true | Action |
|---|---|
| Phase 6 frontend stack is being chosen | **#75 must already be done.** If not, halt Phase 6 entry until it lands. |
| Phase 6 frontend session opens | Re-evaluate #79 for first-pass perf budgets |
| Phase 6 backend API surface opens | Action #85 |
| Phase 9 approach OR pre-launch beta decided | Action #82 |
| Deployable artifact (container, bundle, package) exists | Action #83; re-evaluate #79 with real measurements |
| Second collaborator added | Promote #80 to ADR-tracked; action #84 |
| Phase 6 complete (audit-style review window) | Action #84 |

### Explicit non-adoptions (no Issue filed; decision recorded here)

| Item | Source | Reason for skip |
|---|---|---|
| ECC agents (`planner`, `chief-of-staff`, `loop-operator`, `e2e-runner`) | ECC | Conflict with [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md), [ADR 0049](adr/0049-scope-lock-at-boot-and-descope-reorder-audit-at-shutdown.md), [ADR 0051](adr/0051-routine-mode-and-engine-loop.md) — existing session/Skill/scope apparatus covers this surface differently and more rigorously |
| ECC skills under `agent-*` / `agentic-engineering` | ECC | Same conflict |
| ECC slash commands wrapping agents/skills | ECC | Not portable; tied to ECC harness |
| ECC `ecc_dashboard.py`, identity / package-manager configs | ECC | Bound to ECC harness model |
| ECC per-language build resolvers | ECC | Python-only on engine; multi-language coverage is a Phase 6 stack-specific decision in-context |
| agent-skills `using-agent-skills` meta-skill | agent-skills | ADR 0044 Skill discipline (recipe-vs-reference partition) is more rigorous |
| agent-skills `git-workflow-and-versioning` (wholesale) | agent-skills | Trunk-based-default conflicts with session ritual; compatible patterns extracted into #86 (revert/rollback discipline) |
| agent-skills `ci-cd-and-automation` (wholesale) | agent-skills | ECC workflows are the pattern source; judgment patterns (no-skip philosophy, 10-min budget, flaky-test discipline, GitHub-Secrets-for-test-creds) baked directly into #68 body |
| Build Cop role from CI skill | agent-skills | Solo-dev not applicable; promote when ≥2 collaborators |

Rejecting any of these later requires explicit reversal (new ADR or amendment to this rollout section).

### Cross-references

- Plan file: `~/.claude/plans/not-a-working-session-sequential-twilight.md`
- Source repos: [`affaan-m/everything-claude-code`](https://github.com/affaan-m/everything-claude-code), [`addyosmani/agent-skills`](https://github.com/addyosmani/agent-skills)
- Adoption discipline: each Issue body names its ADR requirement, MemPalace decision-drawer requirement, first-exercise readiness note (per ADR 0053) where applicable, verification steps, and posture-interaction reasoning.
