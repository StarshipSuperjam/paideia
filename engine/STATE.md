# Project State

> The single file every session reads first. Names what's current and what's next. Updated at every build-session close.

## Current

| Field | Value |
|---|---|
| **Project** | Paideia — knowledge mastery app on a pedagogical dependency graph |
| **GitHub** | https://github.com/StarshipSuperjam/paideia (private) |
| **Current phase** | Phase 0 — closed (S-0003); **Phase 1 — Contract Lock fully closed at S-0012**. **Phase 1.5 — Mission Realignment Overhaul fully closed at S-0021.** **S-0022** committed the engine / product wall ([ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md)) and renamed `CHANGELOG.md` → `ENGINE_LOG.md`. **Phase 2 (Build Plan Scaffolding) closed at S-0023.** **S-0024–S-0025** executed the folder migration into `engine/` and `product/` subtrees and the cross-reference sweep. **S-0026** authored the third project expression contract per [ADR 0038](adr/0038-expression-contract-for-ai-authored-code.md). **S-0027 widened the expression contract** per [ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md) and **committed the build-readiness gate** per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md). **Phase 3 — SQL Schema Implementation closed at S-0028** with eight migrations deployed to `paideia-dev` (PostgreSQL 17.6); RLS enabled on every public table; CHECK constraints enforce gate T2-A enums; three gate-report drifts corrected in-flight per architecture.md-authoritative design. **S-0029 closed the infrastructure-misfire sweep** (Session α foundations) per the approved plan. **S-0030 landed the first project health-check audit** as a manual phase-boundary fire (cadence-trigger off-by-one and two-layer-decision-recording posture gap surfaced as queued work for Session β). **S-0031 closed Session β: harness mechanization** per the approved infrastructure plan — three new non-blocking Claude Code hooks ([ADR 0043](adr/0043-hook-architecture.md)), three new user-defined Skills under `.claude/skills/` ([ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md)), cadence-trigger off-by-one fix. **S-0032 closed the MemPalace audit cleanup + improvements** — diary discipline, `pushback` + `lesson` tags, `scheduled_audits.json`, MemPalace audit category in `health_check.py`, cadence tightened 30 → 10. **S-0033 closed three coupled mechanical fixes** per HANDOFF.md S-0033 entry: pre-commit hook STATUS=$? bug fix; side-discovery audit gate at session shutdown (new `engine/tools/audit_side_discoveries.py` + 22-test suite + new step 6 in shutdown sequence); `engine/scheduled_audits.json` on exploration-mode allowed-paths. **S-0034 closed the Phase 4 build-readiness gate exercise** per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) — gate report at [`engine/build_readiness/phase_4_graph_validation.md`](build_readiness/phase_4_graph_validation.md) triages 3 Tier 1 + 9 Tier 2 + 4 Tier 3 decisions for the S-0035 build session that fleshes out `validate_graph()` against [ADR 0016](adr/0016-graph-construction-needs-live-validation.md)'s contract; cold-review pass per ADR 0040 Step 8 returned 9/9 matches with 0 block-gating findings — the citation-first authoring posture held against the gate-on-the-gate discipline; T2-B explicitly closes the first S-0028 forward-pointer (sub-signal NULL permissiveness out of `validate_graph()` scope), T3-A reaffirms the second (branch-based rollback verification deferred to Phase 5+ pending CLI setup or budget approval). **S-0035 closed the engine-hardening pass** in response to S-0033 (env-leak wrote `core.bare=true` to parent `.git/config`) and S-0034 (chromadb HNSW segment corruption crashed every subsequent session) — [ADR 0045](adr/0045-shared-state-integrity-discipline.md) mechanizes four shared-state integrity rules: subprocess env scrubbing across all hooks + `validate.py`'s 4 code-gate subprocess calls; atomic `cp -a` snapshot + `probe_palace.py` verify + rollback wrapper around `mempalace hook run`; boot-time shared-state probes wired into `validate.py`'s default check set + a new `--health-probe-only` mode invoked from `session-start.sh`; shutdown soft-warn aggregation changes from "final validator run only" to "max-count across all this-session `validate-history.jsonl` invocations." Phase 4 graph validation displaced to S-0036 (the gate report at `engine/build_readiness/phase_4_graph_validation.md` is unchanged structurally; only its slot reference shifts). Validator check count rose 14 → 16; 173 tests passing; ruff + mypy --strict clean. **Phase 4 build session opens at S-0036.** ADR count post-S-0035: **45 ADRs total — 43 Accepted plus 2 Superseded** (S-0035 added [ADR 0045](adr/0045-shared-state-integrity-discipline.md)), partitioned **12 engine + 33 product**. |
| **Last build session** | S-0035 (2026-05-03) — **Engine hardening for restart** per `/Users/shanekidd/.claude/plans/flush-out-a-full-woolly-harp.md`. Output: [ADR 0045](adr/0045-shared-state-integrity-discipline.md) plus four mechanized rules — subprocess env scrubbing (helpers at `engine/tools/scrub_env.{py,sh}` plus `engine/tools/conftest.py` autouse fixture; applied to all 4 hooks + `validate.py`'s 4 code-gate subprocess calls), atomic-write wrapper around `mempalace hook run` in `engine/tools/hooks/mempalace-hook-wrapper.sh` (pre-mine `cp -a` snapshot + KG snapshot + post-mine probe + rollback on broken-2/SIGSEGV + rotate to `palace.last-good`), boot-time shared-state probes (`engine/tools/probe_palace.py` + `engine/tools/probe_repo.py` + `validate_shared_state_health()` in `validate.py` + `--health-probe-only` mode invoked from `session-start.sh`), and shutdown soft-warn aggregation across all this-session `validate-history.jsonl` invocations (per-category max-count). Validator check count rose 14 → 16. 173 tests passing; ruff + mypy --strict clean. CLAUDE.md "Posture vs machinery" narrows further (three new mechanized rules). Phase 4 graph validation displaced to S-0036; the gate report at `engine/build_readiness/phase_4_graph_validation.md` is unchanged structurally — only its slot reference shifts. **Side-discovery audit at close:** 0 markers found. **outcome_summary_soft_warns at close:** all categories at 0; `chromadb_palace_health` 0; `repo_config_health` 0; `diary_skipped` 0. **One new engine ADR:** [ADR 0045](adr/0045-shared-state-integrity-discipline.md). ADR count: 45 total (43 Accepted + 2 Superseded; 12 engine + 33 product). **Bash 3.2 incompatibility surfaced as a side discovery** in `session-start.sh` lines around the persistent-warn loop (`declare -A` and `${!array[@]}` are bash 4+); not introduced by this session, queued for follow-up — see HANDOFF.md S-0035 entry. |

| **Prior build session** | S-0034 (2026-05-02) — **Phase 4 build-readiness gate exercise** per `/Users/shanekidd/.claude/plans/silly-soaring-sphinx.md`. Output: [`engine/build_readiness/phase_4_graph_validation.md`](build_readiness/phase_4_graph_validation.md) authored to triage decisions for the S-0035 build session that fleshes out `engine/tools/validate.py`'s `validate_graph()` stub against [ADR 0016](adr/0016-graph-construction-needs-live-validation.md)'s contract, per the protocol at [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md). **Three Tier 1 commitments:** T1-A bundles all four [`build_plan/P_2_graph_validation.md`](../build_plan/P_2_graph_validation.md) sub-deliverables (4.1 validator extension + 4.2 ROUTING.md + 4.3 PREDICATE_MANIFEST.md + 4.4 seed-chunked-authoring.md) into one S-0035 session with partial-close as the budget escape hatch; T1-B settles PREDICATE_MANIFEST.md authoring to Phase 4 (supersedes the placeholder's "first entries land in Phase 5" framing); T1-C commits psycopg + service_role JWT for defense-in-depth observability across any future RLS policy changes. **Nine Tier 2 decisions:** Kosaraju SCC restricted to the `pedagogical_prerequisite` subgraph (T2-A); hard-fail enumeration (T2-C: duplicate node IDs, dangling edges, prerequisite cycles); soft-warn enumeration with concrete metrics for all seven categories (T2-D: `undeclared_predicate`, `attribute_shape_inconsistency` with v1 70/30 metric, `missing_rigor_score`, `render_readiness_violation`, `synthetic_review_queue`, `orphan_leaf`, `suspicious_cross_domain_ratio` with >40% threshold); soft-warn output format (T2-E: ValidationResult per-category + outcome_summary_soft_warns block extension); `--export-snapshot` mode (T2-F); v1 PREDICATE_MANIFEST table with `pedagogical_prerequisite` + `historical_influence` (T2-G — the only types named in current architecture.md); ROUTING.md range-collision fix (T2-H: `0010-0019` Phase 4 metadata vs `0011-0019` Phase 5 epistemology resolved by reserving `0010` only for Phase 4); stale-path corrections in `engine/operations/tools-validate-interpretation.md:90` and `engine/adr/0016-graph-construction-needs-live-validation.md:38` (T2-I — post-S-0024 partition cleanup). **Four Tier 3 deferrals:** T3-A branch-based rollback verification stays cost-gated to Phase 5+; T3-B architecture.md status-enum prose drift (2 vs 3 values) is documentation-cleanup; T3-C `attribute_shape_inconsistency` v1 metric awaits Phase 5 calibration; T3-D `seed-chunked-authoring.md` stale paths close inside S-0035's 4.4 rewrite. **T2-B closes the first S-0028 forward-pointer** explicitly: sub-signal NULL permissiveness is out of `validate_graph()` scope per ADR 0016's nodes/edges-only contract; the SQL CHECK on `learner_events` enforces at insert. **T3-A reaffirms the second forward-pointer** (branch-based rollback verification) for the next gate session. **Cold-review pass per ADR 0040 Step 8** (Explore sub-agent, fresh context, citation verification): 9 decisions reviewed, 9 matches, 0 block-gating findings — the citation-first authoring posture held; no resolution iteration needed. **Validator clean at close:** 14 checks, 0 hard-fails, 0 soft-warns. **Side-discovery audit at close:** dogfood scan against this session's commits per the gate authored at S-0033. **outcome_summary_soft_warns at close:** all categories at 0; `diary_skipped` 0. **No new ADRs:** S-0034 settled within the existing ADR 0016 contract; ADR 0040's gate procedure was followed without amendment. ADR count unchanged: 44 total (42 Accepted + 2 Superseded; 11 engine + 33 product). |

| **Last commit on main** | (see `git log --oneline -1` on main) |
| **Backup tags** | `pre-foundation-v0.0.0` at commit `fa70b8c` — pre-machinery anchor. `pre-phase-3-v0.0.1` at commit `36b0907` (S-0026 close) — pre-Phase-3 anchor cut at S-0027. Both are historical anchors, not operational fallback targets — file-level retrieval via `git show <tag>:<path>` is the legitimate use. |

## Infrastructure

| Component | Value |
|---|---|
| **Supabase project** | `paideia-dev`, project ref `ozooosgnuzxqqypotlke`, PostgreSQL 17.6 |
| **Supabase MCP** | configured in `.mcp.json` (gitignored, contains PAT) |
| **MemPalace** | installed at `/Users/shanekidd/Library/Python/3.9/bin/`; MCP server `mempalace-mcp` configured in `.mcp.json`; wing `paideia` carries **485 drawers across 5 rooms** as of S-0032 close (general 330, operations 88, decisions 55, foundation-planning-s0001 10, s0003-adr-collection 2); capture hooks wired in `.claude/settings.json` (Stop + PreCompact wrapper PATH-fixed at S-0032 — auto-capture had been silently failing since S-0002 because `mempalace` CLI wasn't on the hook subshell's PATH; verify at next Stop event by checking `.claude/logs/mempalace-hook.log` for `OK exit=0` lines instead of `FAIL exit=127`); project usage scope documented at [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Project usage scope" — uses search, add_drawer, diary (agent_name `claude`), read-only inspection; explicitly excludes KG and tunnels |
| **Python** | system Python 3.9.6 at `/usr/bin/python3`; user-scope packages at `/Users/shanekidd/Library/Python/3.9/bin/` |
| **Anthropic API** | env var `ANTHROPIC_API_KEY` set in local `.env` (gitignored) |

## Next session work item

**S-0036 — Phase 4 graph validation build session.** *(Displaced from S-0035 by the engine-hardening pass per [ADR 0045](adr/0045-shared-state-integrity-discipline.md). The gate report and its Tier 1/2/3 decisions are unchanged structurally — only the slot reference shifts.)* Gated by [`engine/build_readiness/phase_4_graph_validation.md`](build_readiness/phase_4_graph_validation.md). Implements all four [`build_plan/P_2_graph_validation.md`](../build_plan/P_2_graph_validation.md) sub-deliverables in one session per the gate's T1-A:

- **4.1 — Flesh out `engine/tools/validate.py`'s `validate_graph()`** against [ADR 0016](adr/0016-graph-construction-needs-live-validation.md). psycopg + service_role JWT (gate T1-C). Three hard-fails: duplicate node IDs, dangling edge refs, prerequisite cycles via Kosaraju SCC restricted to the `pedagogical_prerequisite` subgraph (gate T2-A/T2-C). Seven soft-warns per gate T2-D enumeration (`undeclared_predicate`, `attribute_shape_inconsistency`, `missing_rigor_score`, `render_readiness_violation`, `synthetic_review_queue`, `orphan_leaf`, `suspicious_cross_domain_ratio`). `--validate-only` (default) and `--export-snapshot path` modes per gate T2-F.
- **4.2 — `product/seed-graph/migrations/ROUTING.md`** range-collision fix (reserve `0010` only for Phase 4 metadata; preserve `0011-0019` for Phase 5 epistemology) per gate T2-H. Add S-0036 per-session narrative entry following the S-0028 pattern.
- **4.3 — `product/seed-graph/migrations/PREDICATE_MANIFEST.md`** v1 registry table per gate T2-G: `pedagogical_prerequisite` + `historical_influence` (the only types named in current architecture.md). Replace the placeholder prose at `PREDICATE_MANIFEST.md:19`.
- **4.4 — `engine/operations/seed-chunked-authoring.md`** per-session migration workflow flesh-out per gate T3-D. Replace the current placeholder; correct post-S-0024 stale `supabase/migrations/` paths in passing.

**Stale-path corrections per gate T2-I:** `engine/operations/tools-validate-interpretation.md:90` and `engine/adr/0016-graph-construction-needs-live-validation.md:38` — both should read `product/seed-graph/migrations/PREDICATE_MANIFEST.md`.

**Seven new `outcome_summary_soft_warns` keys** appear in S-0036's archive per gate T2-E (all-zero counts expected against the schema-only `paideia-dev` state, since no Phase 5 seed content exists yet). The schema example at `engine/operations/session-shutdown-sequence.md` already gained `chromadb_palace_health` and `repo_config_health` at S-0035 per ADR 0045; the seven Phase-4-specific keys add to that block, not replace it.

**Smoke-tests** verify the gate's contract end-to-end: deliberate hard-fail (broken edge ref) blocked at exit 2; deliberate soft-warn (unknown predicate) categorizes as `undeclared_predicate`; pre-commit hook rejects a commit with a deliberate hard-fail. Runtime budget: <3s on the schema-only state per [`build_plan/P_2_graph_validation.md:54`](../build_plan/P_2_graph_validation.md).

**Boot-time check: cadence trigger.** Cadence is 10. At S-0036 boot, `next_id = "0036"`, so `36 % 10 = 6` — no cadence trigger this session. First post-tightening natural fire lands at S-0040.

**Boot-time check: persistent-warn surface.** Per [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md). At S-0036 boot, structured `outcome_summary_soft_warns` archives in window: S-0031, S-0032, S-0033, S-0034, S-0035 (5; S-0030 rolls off). Surface clean expected — S-0035 added zero soft-warns; the four prior archives are also clean.

**Boot-time check: shared-state health probe.** Per [ADR 0045](adr/0045-shared-state-integrity-discipline.md). At S-0036 boot, `session-start.sh` runs `validate.py --health-probe-only`. Healthy expected — palace post-S-0034-recovery is queryable; parent repo's `core.bare` was unset by the prior recovery session per HANDOFF.md.

**Boot-time check: side-discovery audit step 6.** Third firing of the gate authored at S-0033; first was at S-0034 close, second at S-0035 close.

**Boot-time check: scheduled-audit surface.** Per `engine/scheduled_audits.json`. First entry triggers at S-0042 (pushback/lesson tag adoption) — not due at S-0036.

Boot procedure: `Start Engine` in a fresh Claude Code session. CLAUDE.md auto-loads. Boot reads `engine/STATE.md` (this file). Read the gate report first; then re-read the load-bearing source docs below. Halt-on-Tier-1-unresolved per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md): Tier 1 in the gate report is fully resolved at S-0034 close, so S-0036 proceeds.

### Source documents (read at S-0036 boot beyond CLAUDE.md auto-load)

- [`engine/build_readiness/phase_4_graph_validation.md`](build_readiness/phase_4_graph_validation.md) — **the gate report (read first)**.
- [`engine/adr/0016-graph-construction-needs-live-validation.md`](adr/0016-graph-construction-needs-live-validation.md) — the load-bearing contract.
- [`build_plan/P_2_graph_validation.md`](../build_plan/P_2_graph_validation.md) — the chunk being implemented.
- [`engine/tools/validate.py`](tools/validate.py) — current state including the `validate_graph()` stub at lines 1249-1293.
- [`engine/operations/tools-validate-interpretation.md`](operations/tools-validate-interpretation.md) — soft-warn category surface; new categories land here per gate T2-D + T2-I path correction.
- [`engine/operations/session-shutdown-sequence.md`](operations/session-shutdown-sequence.md) — `outcome_summary_soft_warns` schema gains seven new keys per gate T2-E.
- [`engine/operations/migration-discipline.md`](operations/migration-discipline.md) — Layer 1 contract for SQL migrations the validator gates.
- [`product/docs/architecture.md`](../product/docs/architecture.md) — graph schema (nodes, edges); query construction reads from this.
- [`product/seed-graph/migrations/0002_nodes.sql`](../product/seed-graph/migrations/0002_nodes.sql), [`0003_edges.sql`](../product/seed-graph/migrations/0003_edges.sql) — live schema reality.
- [`product/seed-graph/migrations/PREDICATE_MANIFEST.md`](../product/seed-graph/migrations/PREDICATE_MANIFEST.md) — current placeholder; populates per gate T2-G.
- [`product/seed-graph/migrations/ROUTING.md`](../product/seed-graph/migrations/ROUTING.md) — current state; gets range-collision fix + S-0035 narrative entry per gate T2-H.

### After S-0036

S-0037 — likely the build-readiness gate session for the first Phase 5 seed-graph subdomain (epistemology) per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) (substantive build sessions are preceded by gate sessions). If a non-substantive operational session intercedes (health-check, tools refinement, doc cleanup), that takes S-0037.

S-0040 — first cadence-10 health-check trigger fires; the audit window now covers S-0035 (engine hardening) + S-0036 (Phase 4 build's first run + first dogfood of the new validator categories).

S-0042 — scheduled near-term audit of pushback/lesson tag adoption fires at boot via the SessionStart hook reading `engine/scheduled_audits.json` (per the S-0032 audit Improvement G mechanism).

## Open tensions and deferred decisions

See [`product/docs/tensions.md`](../product/docs/tensions.md). As of S-0026 close, the OQ-prefixed active set is 9 items (OQ-PRIVACY-A and OQ-PRIVACY-B closed by ADR 0031; OQ-PHASE8-A settled by ADR 0032; OQ-BYOK-REGIME withdrawn by ADR 0032; OQ-OUTWARD-VOICE added at S-0019):

- OQ-DEC1-A through D — Phase DEC.1 architecture decisions (server-side mastery, two-hop retrieval, embedding strategy, chunk-resolver vs URL pointers); decide-before Phase 6
- **OQ-WALL-BEHAVIOR** (added S-0008, per ADR 0029; simplified S-0012) — soft-wall degradation ladder at cost cap; **single-tier ladder** under cost-priced subscription model (free-vs-paid tier-shaped framing collapsed at S-0012); four candidate steps remain (model downshift, retrieval shrink, concept-engagement length cap, soft refusal); decide-before Phase 8 cost-cap wiring
- **OQ-CONTEXT-COMPRESSION** (added S-0008, per ADR 0029; unaffected by S-0012) — token-amplification mitigation for multi-turn engagements (structured-state replacement + automatic compression + explicit summarization candidates); decide-before Phase 7
- **OQ-PEDAGOGY-INFERENCE-LOCUS** (added S-0008, tagged `watch`) — rule layer vs. distributed inference; revisit when inference registry crosses ~30 entries OR cross-domain edges per domain-pair exceed ~50 OR an operational complaint surfaces
- **OQ-OUTWARD-VOICE** (added S-0019) — third expression-contract gap covering outward product surfaces (UI labels, button text, error messages, learner-facing help, public README, App Store description, learner-visible CHANGELOG entries) that neither ADR 0027 nor ADR 0036 governs; decide-before Phase 7 (the earliest plausible moment such a surface ships)
- OQ-WATCH-FLAG-FILE (tagged `watch`) — separate watch-flag file consideration, surfaced at session-30 health check

## Paperclip integration

**Deferred.** Phase 5 trial / Phase 7 commit per ROADMAP.md. Not installed in Foundation.

## Strong working commitments

12 commitments inherited from pre-Foundation design plus 10 architectural decisions. **All 22 are recorded as ADRs** — the contract layer, now split across [`engine/adr/`](adr/) (5 engine ADRs about the build apparatus) and [`product/adr/`](../product/adr/) (33 product ADRs about Paideia-as-product). The audience-facing summary lives in [`product/docs/MISSION.md`](../product/docs/MISSION.md) (commitments 1–12 with ADR cross-refs; commitment 2 reframed at S-0012 as "operating discipline must not corrupt pedagogy" per ADR 0032); the canonical list with ADR pointers lives in [`ROADMAP.md`](../ROADMAP.md) ("Strong working commitments referenced throughout"). The conversational story behind each decision is recoverable from MemPalace `decision`-tagged drawers.

**ADR count post-S-0035: 45 ADRs total — 43 Accepted plus 2 Superseded** (ADR 0002 by ADR 0032; ADR 0032 by ADR 0035), partitioned into **12 engine + 33 product** per [ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md). S-0035 added [ADR 0045](adr/0045-shared-state-integrity-discipline.md) (engine; shared-state integrity discipline — subprocess env scrubbing, atomic mempalace mine writes, boot-time health probes, shutdown soft-warn aggregation across all session invocations). S-0031 added [ADR 0043](adr/0043-hook-architecture.md) (engine; hook architecture for two-layer recording, cadence trigger, STATE.md required-fields verification) and [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md) (engine; skill conversion partition for recipe-shaped vs reference-shaped operations docs); amended [ADR 0022](adr/0022-periodic-project-health-checks.md) Decision + Consequences (cadence-trigger logic corrected from `last_claimed mod` to `next_id mod`); amended [ADR 0043](adr/0043-hook-architecture.md) Decision (post-state-edit.sh ADR-count check dropped per cold-review finding). S-0029 added [ADR 0041](adr/0041-cascade-analysis-discipline.md) (engine; cascade-analysis discipline) and [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) (engine; soft-warn lifecycle, archive-as-canon); amended [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) Consequences (gate-on-the-gate citation + cold-review pass per S-0028 case study); amended [ADR 0022](adr/0022-periodic-project-health-checks.md) Consequences (deliverable-handshake re-pointing post-S-0024 partition for `health_check.py`). The contract layer the [`build_plan/`](../build_plan/) chunks consume by reference. [`engine/tools/validate.py`](tools/validate.py)'s `adr_index_consistency` check audits the ADR-file-vs-index alignment across both subtree READMEs; the `cascade_*` checks (per ADR 0041) audit superseded-citation currency, back-reference orphans, and Consequences-deliverable promises; `validate_shared_state_health()` (per ADR 0045) audits chromadb palace and git repo sanity.
