# Project State

> The single file every session reads first. Names what's current and what's next. Updated at every build-session close.

## Current

| Field | Value |
|---|---|
| **Project** | Paideia — knowledge mastery app on a pedagogical dependency graph |
| **GitHub** | https://github.com/StarshipSuperjam/paideia (private) |
| **Current phase** | **Phase 5 — Philosophy subdomain seeding (closed; S-0045 → S-0076).** 16/16 tasks complete; T-PHASE-5 target-met. 380 nodes + 536 edges across 15 seed migrations (epistemology / ethics / metaphysics / mind / language / science / logic / political / aesthetics + service nodes + cross-bridges). Phase 5 closeout report at [`engine/build_readiness/phase_5_closeout.md`](build_readiness/phase_5_closeout.md). **Phase 5 production audit master plan landed at S-0082**; routine block paused for the issue close-out campaign (S-0083 → S-0096; **campaign concluded at S-0096 / C7 closing Issue #34**). Phases 0–5 all closed; Phase 6 master plan blocked behind audit closeout + OQ-DEC1 settlement. **59 ADRs — 57 Accepted + 2 Superseded — 24 engine + 35 product** (S-0095 added [ADR 0058](adr/0058-canonical-timestamp-format-and-helper.md) — canonical timestamp format and shared `engine/tools/timestamps.py` helper; **S-0096 amended ADR 0047 in-place with the C7 PDF adapter landing record** — multi-fallback PDFInputAdapter via pypdfium2 + pdfplumber, multi-entry partitioning extension to `extract_entries()`, element-reorder pass, three-source convergence outcome, [Issue #48](https://github.com/StarshipSuperjam/paideia/issues/48) forward-pointer for the page-header-entry-indicator path that closes the Cambridge / Oxford gap). Full session-by-session history in [ENGINE_LOG.md](ENGINE_LOG.md). |
| **Last build session** | S-0096 (2026-05-08) — **Issue close-out campaign C7: PDF input adapter for parse_structural_reference.py.** Interactive `/start-engine` per `~/.claude/plans/i-want-to-delay-sparkling-shell.md` C7 row + the user-approved S-0096 plan at `~/.claude/plans/i-want-to-build-rippling-simon.md`. Closes [Issue #34](https://github.com/StarshipSuperjam/paideia/issues/34) by landing **`PDFInputAdapter`** in [`engine/tools/parse_structural_reference.py`](tools/parse_structural_reference.py) per [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) decision 6 — source-agnostic posture per ADR 0047 decision 1, no per-source code branches, multi-fallback at every detector. Title from `/Title` metadata then first-page-largest-font fallback. Heading from pypdfium2 outline tree (non-recursive — pdfminer's recursive `get_outlines` blows the 1000-deep recursion limit on Routledge's 2041 outline entries) then font-size buckets then `Chapter|Section|Part|Article` text-pattern with prose-connector reject. Anchor from link annotations PLUS textual editorial markers (`See:` / `See also:` / `(see X)` / `→` / `Cf.`) — both paths emit canonical `internal:<slug>` hrefs picked up by one new universal `ENCYCLOPEDIA_CONFIG` pattern. Bulk text via pypdfium2 (~30× faster than pdfplumber's pdfminer backend). Image-only PDFs raise `ValueError` loudly. Paragraph and list-item NOT collected — no downstream consumer. **Multi-entry partitioning in `extract_entries()`** lands the docstring's "future" note: `partition_at_entry_boundaries()` + `extract_entry_from_partition()`; single-h1 HTML still yields one Entry (6 SEP fixture tests load-bearing regression bar pass unchanged); full-volume PDFs yield N Entries. **Element reorder pass** uses `full_text.find` for headings + captured `match.start()` for anchors so cross-references distribute across entries instead of concentrating into the last one. **Three-source convergence** validated against `~/Documents/Claude_Files/temp/` PDFs (never copied into repo): Routledge 2041 entries with 58% carrying cross-refs (mean conf 0.62) — the architecture's strong case; Cambridge 20 + Oxford 15 entries (sparse — outlines enumerate book / letter sections only, entries at body font size; page-header-as-entry-indicator path filed as [Issue #48](https://github.com/StarshipSuperjam/paideia/issues/48) for follow-up). Tests: 19 new (`TestPDFInputAdapter` 11 + `TestMultiEntryPartitioning` 8) using `reportlab`-synthesized fixtures so no third-party PDF blobs land in git per ADR 0011 / ADR 0046 licensing posture. 100 pytests pass; ruff + mypy --strict + ruff format clean. requirements.txt gains `pdfplumber>=0.11.0` + `pypdfium2>=4.0.0` + `reportlab>=4.0.0` (test-only). ADR 0047 Consequences amended in-place with the C7 landing record. **Health-check audit deferral concludes** — C7 was the final campaign session; the cadence-fired audit (formal first-exercise of [ADR 0057](adr/0057-adversarial-stance-for-health-check-audits.md)) runs in the next dedicated session, ~10 sessions overdue (`slots_since=20` vs `cadence=10`). |
| **S-0095 (prior)** | S-0095 (2026-05-08) — **Issue close-out campaign C6: timestamp standardization.** Interactive `/start-engine` per `~/.claude/plans/i-want-to-delay-sparkling-shell.md` C6 row + the user-approved S-0095 plan at `~/.claude/plans/nested-skipping-waffle.md`. Closes [Issue #33](https://github.com/StarshipSuperjam/paideia/issues/33) by landing **[ADR 0058](adr/0058-canonical-timestamp-format-and-helper.md) — Canonical timestamp format and shared timestamps.py helper**: 6-element contract (canonical `%Y-%m-%dT%H:%M:%SZ`, helper-routing for all `engine/tools/**/*.py` emission/parsing, parser tolerance as one-way back-compat per ADR 0042, hooks-stay-shell, 4-file allowlist with rationale, stopwatch math non-goal). New surfaces: [`engine/tools/timestamps.py`](tools/timestamps.py) (60 LOC, 4 functions `emit` / `emit_micros` / `parse` / `today` with full Layer 1 contract docstrings per ADR 0038), [`engine/operations/timestamp-discipline.md`](operations/timestamp-discipline.md) (Layer 1 source-of-truth, adjunct to `code-discipline.md`), `validate_timestamp_helper_bypass()` AST-walk soft-warn in [`engine/tools/validate.py`](tools/validate.py) (validator check count 30 → 31). **13 emit/parse-site migrations** across 9 `engine/tools/` files including the **smoking-gun fix at `health_check.py:960-961`** — `datetime.fromisoformat(s.replace("Z", "+00:00"))` gymnastics replaced with `parse(s)`. **4-file allowlist** with per-file rationale (`apply_migration.py` legacy supabase `schema_migrations.version`; `probe_push_gate.py` branch-name colon-safe compact-time; `audit_mempalace_attribution.py:_parse_palace_dt` palace-storage naive local-time; `scan_mempalace_citations.py:fetch_today_diary` palace-storage naive local-date) — discovered during execution; the original plan's single `apply_migration.py` entry expanded as legitimate non-canonical contracts surfaced. **Tests**: 33 new pytests (`test_timestamps.py` 20 + `test_validate_timestamp_bypass.py` 13) + 1 extension (`test_health_check.py` `test_audit_bloat_duration_trend_handles_legacy_plus_offset` verifying smoking-gun fix parses both Z and `+00:00` shapes). 980 pytests pass across `engine/tools/`; mypy --strict + ruff clean; empirical grep clean (no remaining ad-hoc canonical emissions or `.replace` gymnastics outside helper/allowlisted files). Validator at shutdown: 0 hard-fails; soft-warns hold at expected baseline (374; `timestamp_helper_bypass` count: 0). MemPalace decision drawer at `paideia/decisions` (`drawer_paideia_decisions_9a75ed11c5f1a40981c6fffc`). **Health-check audit deferral continues** — `health_check_overdue` soft-warn fires every commit through C7 (S-0096 boot will be `slots_since=19`, overdue by 9). |
| **S-0094 (prior)** | S-0094 (2026-05-08) — **Issue close-out campaign C5: apply_migration empirical postcondition verification.** Interactive `/start-engine` per `~/.claude/plans/i-want-to-delay-sparkling-shell.md` C5 row + the user-approved S-0094 plan at `~/.claude/plans/hashed-crunching-pnueli.md`. Closes [Issue #23](https://github.com/StarshipSuperjam/paideia/issues/23) by adding a Layer 2.5 step to [`engine/tools/apply_migration.py`](tools/apply_migration.py) — empirical postcondition verification against the live DB after body apply + schema_migrations record. New surfaces: `parse_postcondition_assertions(sql_text) -> list | None` (parses inline `-- Postcondition-Assertions:` block; lines `--   <SELECT returning one integer> :: <expected integer>`; terminates at next contract section header to avoid PostgreSQL `::` casts in following prose), `verify_postconditions(db_url, assertions) -> tuple[bool, list[str]]` (runs all assertions, returns all failures not just first), `verify_shape()` early-detects malformed grammar (exit 2), and `main()` runs Layer 2.5 between record_in_schema_migrations and final success print. **New exit code 8** — distinct in kind from exit 7 (recording fault); body-applied + recorded but assertion mismatch; manual adjudication required (assertion wrong / prose wrong / body silently misbehaved). **schema_migrations recorded on failure** — body has committed; mismatch is contract drift not recording fault; skipping the record means re-fire FK-collides. **Refusal mode for missing block: soft-warn + exit 0** (backward-compat for pre-S-0094 corpus). **All 15 Phase 5 seed migrations annotated** with assertions blocks (`0060_seed_crossbridges_part1.sql` first as the highest silent-skip risk; other 14 via one-shot Python script copying the prose-declared integers into machine-readable form). 23 new pytests in `TestPostconditionVerification` (parser grammar / verifier semantics / main integration / scope_lock unaffected); new `_make_psycopg_stub_with_fetchone_seq` helper for multi-assertion paths; full suite 47 tests green. **Documentation:** [ADR 0055](adr/0055-apply-migration-wrapping-against-production-reads-gate.md) Consequences amendment (exit code 8 + retroactive annotation rationale + trigger-criterion evaluation), [ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md) Consequences amendment (Layer 2.5 added to layer model as side-effect-conditional structural new layer), [`engine/operations/migration-discipline.md`](operations/migration-discipline.md) Layer 2.5 section + Layer 3 cold-review prompt step 3 added, [`engine/operations/expression-contract-instantiation.md`](operations/expression-contract-instantiation.md) Layer model + SQL row updated, [`engine/build_readiness/apply_migration_postcondition_first_exercise.md`](build_readiness/apply_migration_postcondition_first_exercise.md) per ADR 0053 (sibling to apply_migration_first_exercise.md from S-0064), `build_readiness/README.md` class-5 table updated, `CLAUDE.md` routine-mode-machinery bullet rewritten. Validator at shutdown: 0 hard-fails; soft-warns hold at baseline (`health_check_overdue` + `missing_rigor_score` Phase-5 carryover + `issue_collision` for in-scope #23). T1-A first-exercise closes at next routine fire post-S-0094 OR next interactive `--force` re-apply of an annotated seed. Decision drawer in MemPalace `paideia/decisions`. **Health-check audit deferral continues** — S-0095 boot will be `slots_since=18`, overdue by 8. |
| **S-0093 (prior)** | S-0093 (2026-05-07) — **Issue close-out campaign C4: MemPalace effectiveness pair (Issues #38 + #39).** Interactive `/start-engine` per the campaign plan at `~/.claude/plans/i-want-to-delay-sparkling-shell.md` C4 row + the user-approved S-0093 plan at `~/.claude/plans/transient-dazzling-backus.md`. Closes [Issue #38](https://github.com/StarshipSuperjam/paideia/issues/38) (boot-search effectiveness — three formulations + similarity filter + idempotent section into current_plan.md + JSONL telemetry shim) and [Issue #39](https://github.com/StarshipSuperjam/paideia/issues/39) (drawer-citation telemetry — scan outcome_summary + diary + commit messages for drawer IDs / S-NNNN refs / tag-named refs). Both touch the boot-and-close MemPalace adoption-check apparatus introduced at S-0078; landed under one cohesive ADR 0056 amendment block. **New tools:** [`engine/tools/mempalace_boot_search.py`](tools/mempalace_boot_search.py) (449 LOC + 27 pytests) — imports `mempalace.mcp_server.tool_search` directly with `min_similarity=0.6`, runs three formulations (literal / conceptual / adjacent), writes idempotent `## Prior context (MemPalace boot search)` section into `engine/session/current_plan.md`, appends JSONL telemetry shim mimicking PostToolUse hook output so `search_calls` increments correctly; [`engine/tools/scan_mempalace_citations.py`](tools/scan_mempalace_citations.py) (310 LOC + 27 pytests) — sibling shutdown tool runs after diary write + outcome_summary fill, scans three sources for three regex patterns (drawer IDs, S-NNNN archive refs, tag-named refs), writes nested `mempalace_citations` block under existing `mempalace_activity` field. **`validate.py` extension:** new soft-warn `mempalace_zero_citations_after_search` (search_calls > 0 AND citations.total == 0) gated on session id ≥ S-0093; participates in ADR 0042 3-of-5 lifecycle; validator check count 17 → 18. **ADR 0056 amendment + 6 ops doc updates + 3 Skill body updates** per ADR 0044 doc-then-skill flow. 923 engine/tools tests pass; mypy --strict + ruff check + ruff format clean. Cold-context Layer 3 review per ADR 0038 returned zero contract-vs-code drift across the three production deliverables. Decision drawer at `paideia/decisions` (`drawer_paideia_decisions_b583b22e17599be2d621a183`). Live boot-search dry-run against this session's work item returned 15 drawers above the 0.6 threshold — verified end-to-end. **Health-check audit deferral continues** — `health_check_overdue` soft-warn fires every commit through C7 (S-0094 boot will be slots_since=17, overdue by 7). |
| **S-0092 (prior)** | S-0092 (2026-05-07) — **Issue close-out campaign C3: historical full-encoded-path wings deep cleanup.** Interactive `/start-engine` per the campaign plan at `~/.claude/plans/i-want-to-delay-sparkling-shell.md` C3 row (the slot was reached after three S-0089/S-0090/S-0091 MCP-availability refinements pushed C3 from S-0089 → S-0092). Closes [Issue #41](https://github.com/StarshipSuperjam/paideia/issues/41) end-to-end across both Phase 1 (investigation + signal probe) and Phase 3 (bulk-delete execution against the live palace). **Signal probe** at [`engine/docs/audits/historical-wings-signal-probe-S-0092.md`](docs/audits/historical-wings-signal-probe-S-0092.md): two-pass direct-sqlite probe across all 37,634 historical-wing drawers; aggregate query verified zero non-empty `tag`, zero non-empty `type`, all `added_by=mempalace`, all `room=general` — confirming the S-0086 R3 #40C "Recommend retire" hypothesis with a definitive count rather than extrapolation; halt-and-escalate trigger (>20% high-signal density) did not fire. **Tool extension:** `engine/tools/prune_mempalace.py` `--audit-historical-paths --apply` path wired (was previously report-only since S-0088); new helper `fetch_uuids_by_collection` joins `embeddings` -> `segments` -> `collections` so the apply fans deletes out per chromadb collection; `delete_drawers_by_uuid` gained an optional `collection` parameter (default `mempalace_drawers`, preserves backward-compat for orphan-wings + mined-ops-docs callers). **Live execution:** pre-flight backup at `~/.mempalace/palace.S-0092-pre-c3` (550M, full pre-cleanup state); first apply deleted 37,567 drawers in 21s — 67 historical entries surfaced as **closet** rows in `mempalace_closets` (closets carry the same `-Users-...` wing tag as their grouped drawers; `collection.delete` is collection-scoped, silent no-op against the wrong collection). Inline-fix per CLAUDE.md default-to-fix-in-context: extended the apply to fan deletes by collection, added 8 new pytests covering the by-collection split + idempotency + happy paths + helper edge cases, second backup at `~/.mempalace/palace.S-0092-pre-closet-cleanup`, second apply deleted the remaining 67 closets. **Net effect on MemPalace footprint:** 47,298 → 9,819 drawers (-79.2%); wings 13 → 7 (paideia + wing_claude + wing_paideia + sessions + 3 short-form per-worktree wings); zero historical-wing entries remaining across both collections; `mempalace_wing_count_growth` soft-warn comfortably below the post-S-0088 informational threshold of 60. 33 pytests in `test_prune_mempalace.py` pass; mypy --strict + ruff check + ruff format clean. ADR 0056 unchanged (the contract layer wasn't touched; this was a tool extension + execution). Validator at shutdown: 0 hard-fails; soft-warns hold at the post-cleanup baseline. **Health-check audit deferral continues** — `health_check_overdue` soft-warn fires every commit through C7. |
| **S-0091 (prior)** | S-0091 (2026-05-07) — **Routine-protection refinement: close the no-token-no-diary hard-fail in routine mode + LOUD body uniformity + pending-diary index.** Interactive `/start-engine` prompted by user pushback at S-0090 close. Three-front change via in-place ADR 0056 third Decision-history entry: (1) New soft-warn `mempalace_diary_write_skipped_routine` (routine mode only) replaces engine-mode-only `mempalace_diary_write_skipped` hard-fail for routines (engine retains hard-fail; unattended-vs-interactive asymmetry). (2) LOUD body uniformly across modes for the four substrate-availability soft-warns. (3) New pending-diary index `engine/session/diary_pending_index.json` with boot-hook LOUD surface + manual recovery procedure documented in `routine-mode-operations.md` "Deferred diary recovery". 8 new pytests + 5 updated; 37 mempalace tests + 855 engine/tools tests pass; mypy --strict clean. **Third campaign-plan shift in the same campaign:** Issue #41 (C3) S-0091 → S-0092. |
| **S-0090 (prior, condensed)** | S-0090 (2026-05-07) — **Routine-protection refinement of S-0089's MCP-availability tightening.** Converted S-0089's `mempalace_diary_write_skipped_invalid_token` hard-fail to mode-aware soft-warn `mempalace_diary_write_skipped_substrate_intermittent`. Engine retains LOUD ⚠️ surface; routines close cleanly even on intermittent MCP. ADR 0056 amended via in-place Consequences block. (S-0091 superseded the mode-aware body shape with uniform LOUD across both modes.) |
| **S-0089 (prior)** | S-0089 (2026-05-07) — **MCP-availability boot/close warning surfaces + ADR 0056 escape-hatch token tightening.** Interactive `/start-engine` follow-up to S-0088, prompted by user pushback on the S-0087 + S-0088 escape-hatch burial pattern. Three-front tightening landed via in-place ADR 0056 Consequences amendment (no supersession): (1) boot-time MCP probe in `session-start.sh` runs `mempalace status`; LOUD attention block when CLI missing or substrate fails (mode-uniform at boot since hook fires before `/start-engine` or `/start-routine`). (2) Close-time substrate probe in `validate.py --final-check` via new `_check_mempalace_substrate_alive()` helper using `python3 -m mempalace status`; new `mempalace_substrate_at_close` soft-warn fires independently of diary-write state, mode-aware (engine LOUD ⚠️ "DO NOT BURY THIS", routine standard one-line) — *S-0091 superseded the mode-aware body shape with uniform LOUD across both modes*. (3) Token validity tightening: `mempalace_unavailable_acknowledged:` token only downgrades the diary-write hard-fail when `mempalace status` actually fails at close-time; substrate-alive + token = new `mempalace_diary_write_skipped_invalid_token` hard-fail. (4) LOUD body for engine `mempalace_diary_write_acknowledged_skip` even when token is valid — single-session use is investigation-worthy, not "wait for 3-of-5". **Bonus inline fix:** `scan_mempalace_activity.py` write_rollup_to_current was REPLACING the rollup; combined with post-rollup JSONL truncation (Issue #37), running the scan more than once in a session lost earlier counts. New `_merge_rollups()` helper sums counts and spans timestamps. 13 new pytests in `TestSubstrateTokenTightening` + `TestCheckMempalaceSubstrateAlive` (28 total in `test_validate_mempalace_adoption.py`); 3 new pytests in `test_scan_mempalace_activity.py` (50 total). All pass. Validator at shutdown: 0 hard-fails; soft-warns hold at the baseline. MemPalace MCP confirmed live this session. **Campaign plan reorder:** Issue #41 (C3) shifts to S-0090 because the guardrail must land before the next session boot can hit the same burial pattern. |
| **S-0088 (prior)** | S-0088 (2026-05-07) — **Issue close-out campaign session C2. MemPalace cleanup execution + boot-time guardrails.** Interactive `/start-engine` per `~/.claude/plans/proud-whistling-koala.md`. Closed [Issue #40](https://github.com/StarshipSuperjam/paideia/issues/40) (parts A + B: 414 mined ops-doc drawers in `paideia/general` + `paideia/operations` deleted; 79 drawers across 51 orphan `wing_<6-hex>` wings deleted; pre-prune backup at `~/.mempalace/palace.S-0088-pre-prune`; new `engine/tools/prune_mempalace.py` is the standing tool for repeatable maintenance), [Issue #46](https://github.com/StarshipSuperjam/paideia/issues/46) (`mempalace_wing_count_growth` soft-warn at boot via `probe_palace.py` extension that counts distinct `wing` metadata via direct chromadb sqlite query; thresholds in `register_state.json` `wing_count_growth_thresholds` block, bootstrap 60 / 100; fired pre-prune at 64, cleared post-prune at 13), and [Issue #47](https://github.com/StarshipSuperjam/paideia/issues/47) (audit-trail boot surface in `scan_issue_backlog.py` extracting `engine/docs/audits/<file>.md` cross-references from `health-check-finding`-labeled Issues; emits `Active health-check audits: N (M open recommendations)` line; tested live: 1 audit / 1 open recommendation post-C2). **Health-check audit deferral continues** per the campaign plan — `health_check_overdue` soft-warn fires at S-0088 boot (`slots_since=12`, overdue by 2). **Net effect on MemPalace footprint:** 493 drawers removed (47,683 → 47,190); wing count 64 → 13; `mempalace repair-status` post-prune reports status OK (HNSW 47,204 / SQLite 47,072 within flush-lag tolerance). Validator at shutdown: 0 hard-fails; soft-warns hold at the post-prune baseline (366 missing_rigor_score Phase-5 carryover, 1 health_check_overdue per the deferral, 1 pre-existing adr_consequences_deliverable_audit; the 6 pre-prune `issue_collision` warns naturally cleared with no in-scope-Issue commits remaining). 38 new pytests pass across `test_probe_palace.py` (3 new) + `test_validate.py` (13 new) + `test_scan_issue_backlog.py` (13 new) + `test_prune_mempalace.py` (25 new — fresh file). MemPalace MCP server not connected to this conversation; boot-search ran via direct sqlite probe; `mempalace_unavailable_acknowledged` token used in outcome_summary per [ADR 0056](adr/0056-mempalace-mechanical-adoption-checks.md) escape hatch. Next session: C3 (S-0089) per the campaign plan — historical full-encoded-path wings deep cleanup (Issue #41 / #40C, dedicated heavy session). |
| **S-0087 (prior)** | S-0087 (2026-05-07) — **Issue close-out campaign session C1. MemPalace audit contract foundation.** Closed [Issue #43](https://github.com/StarshipSuperjam/paideia/issues/43) (R4 + R5 + R8: ADR 0056 Consequences amendment + mempalace-operations.md Project usage scope tighten + tagging-conventions.md room-targeting clarification), [Issue #42](https://github.com/StarshipSuperjam/paideia/issues/42) (R3 #40D + #40E: KG / tunnels / AAAK-for-project discipline-enforced retirement), [Issue #44](https://github.com/StarshipSuperjam/paideia/issues/44) (R7: dual-temporal-frame "Forward-fit map" subsection), and [Issue #45](https://github.com/StarshipSuperjam/paideia/issues/45) (G1: `mempalace_retired_surface_used` soft-warn defense-in-depth). |
| **S-0086 (prior)** | S-0086 (2026-05-07) — **Issue close-out campaign session S-D. MemPalace adversarial review.** Interactive `/start-engine` per `~/.claude/plans/i-need-a-plan-prancy-ripple.md`. Closed [Issue #35](https://github.com/StarshipSuperjam/paideia/issues/35) by authoring [`engine/docs/audits/mempalace-adversarial-review-S-0086.md`](docs/audits/mempalace-adversarial-review-S-0086.md). **First concrete application of ADR 0057's adversarial-stance posture** (the formal first-exercise of ADR 0057 remains the cadence-fired audit, deferred per user direction to a post-C7 dedicated session). **Verdict: scale back.** Recommendations R4 + R5 + R7 + R8 + #40D + #40E + G1 landed at S-0087 (C1); cleanup execution (R3 #40A + #40B + #40C, plus G2/G3) routes through C2 + C3 of the campaign. |
| **S-0085 (prior, condensed)** | S-0085 (2026-05-07) — **Issue close-out campaign session S-C. Health-check audit posture overhaul.** Interactive `/start-engine` per `~/.claude/plans/i-need-a-plan-prancy-ripple.md`. Closed three Issues with one new ADR + two major doc rewrites: [Issue #30](https://github.com/StarshipSuperjam/paideia/issues/30) (adversarial-stance posture), [Issue #28](https://github.com/StarshipSuperjam/paideia/issues/28) (freshness probes), [Issue #36](https://github.com/StarshipSuperjam/paideia/issues/36) (pushback/lesson drawer querying). **New ADR:** [ADR 0057](adr/0057-adversarial-stance-for-health-check-audits.md) — adversarial stance for project health-check audits. Sibling to [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) at the audit endpoint (gates ask "what could break our session?" prospectively; audits ask "what is broken or stale that we've been carrying as if it were fine?" retrospectively). Five contract elements: (1) **user-buffered execution** — audit findings + guidance suggestions surfaced *before* any retire/convert/replace action; user adjudicates; downstream sessions execute via Issues per ADR 0048 (added per user pushback during plan-mode review); (2) **posture inversion** is load-bearing — Fit/Gaps/Infrastructure-without-function/Bloat each get adversarial prompt-question rewrites; dispositions become recommendation-shape (`recommend retire / recommend convert / recommend preserve-with-affirmative-case`); (3) **stats-as-proxy-for-function** named anti-pattern — every system reference rests on a content probe run during the audit, not a citation of cached stats; (4) **required output ≥1 affirmative retire-candidate-recommendation** (or adversarially-scrutinized explicit "no candidates" subsection); (5) **cold-context probe per audit** — read ≥1 randomly-selected artifact as if no project context. **Rewrote [`engine/operations/health-check.md`](operations/health-check.md):** new "Adversarial stance" section after operative-discipline framing, "Mechanical inputs" renamed to "Mechanical inputs and freshness probes" with per-system probe specs (MemPalace search-recall content quality, validator soft-warn acted-on rate, Supabase migration empirical postcondition, hook-log success-exit verification, register-content capture rate), adversarial prompt-questions in all four posture sections with recommendation-shape dispositions, new "Accumulated pushbacks and lessons" section with concrete `mempalace_search` commands per Issue #36, updated Report-template guidance pointing to new TEMPLATE.md. **Rewrote [`docs/health-checks/TEMPLATE.md`](../docs/health-checks/TEMPLATE.md):** new top-to-bottom shape with Freshness probes run / Operative diagnostic / Non-obvious findings / Fit/Gaps/Infrastructure-without-function/Bloat (adversarial prompts, recommendation-shape dispositions) / Accumulated pushbacks and lessons / Affirmative retire candidates / Cold-context probe / **User adjudication (left blank on arrival)** / Cadence calibration / Summary. **First-exercise readiness note** at [`engine/build_readiness/adversarial_stance_first_exercise.md`](build_readiness/adversarial_stance_first_exercise.md) per [ADR 0053](adr/0053-mechanism-first-exercise-gate.md); T1-A through T1-E pending the next cadence-fired audit (S-0087 by recommended sequencing — projected to coincide with campaign session S-E `apply_migration` postcondition verification). **Handshakes:** [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md) See-also gained ADR 0057 cross-reference; [`engine/adr/README.md`](adr/README.md) index gained the new row and engine-collection prose count fixed (17 → 23) plus total fixed (51 → 58); [`engine/build_readiness/README.md`](build_readiness/README.md) class-5 table gained the new entry; [`CLAUDE.md`](../CLAUDE.md) topical pointer for `health-check.md` cross-references ADR 0057. **No code changes to `engine/tools/health_check.py`** — its existing `audit_mempalace()` `pushback`/`lesson` adoption-count probes are documented as canonical contract per ADR 0057. Validator at shutdown: 0 hard-fails; soft-warns hold steady at expected baseline (366 missing_rigor_score Phase-5 carryover, 14 issue_collision expected for in-scope Issues, 1 adr_consequences_deliverable_audit pre-existing). MemPalace decision drawer captured in `paideia/decisions` room. Next session: S-D (S-0086) per the campaign plan — MemPalace adversarial review (Issue #35), the first concrete application of S-C's new adversarial-stance posture. |
| **S-0084 (prior, condensed)** | S-0084 (2026-05-06) — **Issue close-out campaign session S-B. Vector search restored.** Interactive `/start-engine` per `~/.claude/plans/i-need-a-plan-prancy-ripple.md`. Closed [Issue #31](https://github.com/StarshipSuperjam/paideia/issues/31) with substantially expanded scope: detection wiring + non-destructive restoration. **Detection:** [`engine/tools/probe_palace.py`](tools/probe_palace.py) shells out to upstream's read-only `mempalace repair-status` (discovered during planning to already exist; building a probe from scratch was unnecessary reinvention) and emits `[probe-palace] divergence: HNSW=N1 SQLite=N2 (P%)`; `validate.py` parses and emits new soft-warn category `mempalace_hnsw_divergence` at ≥10% with LOUD-attention body at ≥30%. Validator check count rose from 16 to 17. **Restoration:** [`engine/tools/mempalace_rebuild_hnsw.py`](tools/mempalace_rebuild_hnsw.py) — non-destructive HNSW rebuild bypassing `mempalace repair --mode legacy` (which destroyed 99.7% of SQLite embeddings at S-0078). Reads `(id, document, metadata)` directly from `chroma.sqlite3` (works around chromadb's `get(include=['embeddings'])` "Error finding id" failure on divergent palaces), deletes the collection preserving its metadata, recreates, re-adds via `collection.add(documents=...)` letting chromadb re-compute embeddings via the registered embedding function. CLI flag `--embedding-providers` enables CoreML on Apple Silicon (50× speedup observed: ~50ms/drawer with CoreML vs ~3s/drawer CPU). Run sequence: Phase 0 fresh `palace.S-0084-pre` backup + `palace.test-rebuild` scratch copy → Phase 4 rebuild against scratch (61.5 min for 46,853 drawers + 13s for 118 closets) → Phase 5 atomic-rename swap to live (`mv palace palace.S-0084-stale && mv palace.test-rebuild palace`). Post-swap: `mempalace repair-status` reports **status: OK** (HNSW 46,000 / SQLite 46,853 within upstream's flush-lag tolerance; the 853-drawer gap is the unflushed final sub-1000 batch); `mempalace_search` MCP returns semantic results with populated similarity/distance fields and `matched_via: "drawer"` — vector search restored from BM25 fallback. 66 session-internal captures preserved in `palace.S-0084-stale`. **Upstream tracker filed:** [MemPalace/mempalace#1394](https://github.com/MemPalace/mempalace/issues/1394) for the 99.7%-destruction shape (distinct from #1208's 10K cap); cross-referenced #1238, #1255, #1263, #1308, #1362, #1092 as adjacent. **Doc landings:** [`engine/operations/health-check.md`](operations/health-check.md) gained "Maintenance probes" section; [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Known issues" gained the destructive-repair entry; [`engine/operations/tools-validate-interpretation.md`](operations/tools-validate-interpretation.md) registered the new soft-warn category; [ADR 0045](adr/0045-shared-state-integrity-discipline.md) amended. **Version-pin sweep per user directive:** mempalace version strings stripped from active prose (ADR 0050, mempalace-operations.md, ADR 0048, build_readiness improvements doc, this STATE.md infra entry); pinned only in [`engine/tools/requirements.txt`](tools/requirements.txt) (bumped to `mempalace>=3.3.4` to capture upstream's `repair-status` subcommand). 7 new pytest cases (4 in `test_probe_palace.py`, 3 new file `test_mempalace_rebuild_hnsw.py`) all pass. Validator at shutdown: detection probe surfaces `mempalace_hnsw_divergence` as a soft-warn at the 1.82% flush-lag level — informational, expected to clear once the next mempalace mine flushes the final batch. Next session: S-C (S-0085) per the campaign plan — health-check audit posture overhaul (Issues [#30](https://github.com/StarshipSuperjam/paideia/issues/30), [#28](https://github.com/StarshipSuperjam/paideia/issues/28), [#36](https://github.com/StarshipSuperjam/paideia/issues/36)). |
| **S-0083 (prior)** | S-0083 (2026-05-06) — **Issue close-out campaign session S-A.** S-A landed five Issues: [#37](https://github.com/StarshipSuperjam/paideia/issues/37) (`scan_mempalace_activity.py` truncates `current_mempalace.jsonl` after rollup); [#26](https://github.com/StarshipSuperjam/paideia/issues/26) (`engine/build_readiness/README.md` rewritten to index six file classes); [#29](https://github.com/StarshipSuperjam/paideia/issues/29) (`product/docs/ideation.md` retired with 10-site inbound-ref cleanup); [#32](https://github.com/StarshipSuperjam/paideia/issues/32) + [#21](https://github.com/StarshipSuperjam/paideia/issues/21) (`transcript_token_pct` telemetry retired — deleted `scan_context_telemetry.py` + 14 tests, dropped tiktoken pin, ADR 0049 Decision 3 retired). Validator: 0 hard-fails, 380 soft-warns. |
| **S-0082 (prior)** | S-0082 (2026-05-06) — **Phase 5 production audit master-plan session.** Authored [`engine/build_readiness/phase_5_production_audit.md`](build_readiness/phase_5_production_audit.md) (master-plan gate report) and replaced [`engine/session/auto_target.json`](session/auto_target.json) with T-PHASE-5-AUDIT (12 tasks, max_sessions 14: AUDIT-CB cross-bridge census + 10 per-subdomain + AUDIT-HT hubs+traces). check_target.py parses new target cleanly. Routine block awaits issue close-out campaign completion. |
| **S-0081 (prior)** | S-0081 (2026-05-06) — **Phase 5 production audit warrant gate.** Single-session preflight; PROCEED recommended on threshold-rule outcome (8 substantive findings / 20 sampled edges; 4 of 15 cross-bridges historical-not-pedagogical extrapolating to ~19 of 71 exceeding ≥10 ADR-warranting threshold for `cross_domain_dependency` structural reopen). Output: [`engine/build_readiness/phase_5_production_audit_gate.md`](build_readiness/phase_5_production_audit_gate.md). |
| **Earlier history** | S-0001 → S-0080 prior-session details retired from STATE.md to keep the file scannable; consult [ENGINE_LOG.md](ENGINE_LOG.md) for S-0080 (S-0079 artifact cleanup), S-0079 (mempalace recovery audit), and earlier sessions. |

| **Last commit on main** | (see `git log --oneline -1` on main) |
| **Backup tags** | `pre-foundation-v0.0.0` at commit `fa70b8c` — pre-machinery anchor. `pre-phase-3-v0.0.1` at commit `36b0907` (S-0026 close) — pre-Phase-3 anchor cut at S-0027. Both are historical anchors, not operational fallback targets — file-level retrieval via `git show <tag>:<path>` is the legitimate use. |

## Infrastructure

| Component | Value |
|---|---|
| **Supabase project** | `paideia-dev`, project ref `ozooosgnuzxqqypotlke`, PostgreSQL 17.6 |
| **Supabase MCP** | configured in `.mcp.json` (gitignored, contains PAT) |
| **MemPalace** | installed at `/Users/shanekidd/Library/Python/3.9/bin/`; MCP server `mempalace-mcp` configured in `.mcp.json`; wing `paideia` carries **485 drawers across 5 rooms** as of S-0032 close (general 330, operations 88, decisions 55, foundation-planning-s0001 10, s0003-adr-collection 2); capture hooks wired in `.claude/settings.json` (Stop + PreCompact wrapper PATH-fixed at S-0032 — auto-capture had been silently failing since S-0002 because `mempalace` CLI wasn't on the hook subshell's PATH; verify at next Stop event by checking `.claude/logs/mempalace-hook.log` for `OK exit=0` lines instead of `FAIL exit=127`); project usage scope documented at [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Project usage scope" — uses search, add_drawer, diary (agent_name `claude`), read-only inspection; explicitly excludes KG and tunnels |
| **Python** | **project venv** at `<main_repo>/.venv/` (Python 3.12, uv-managed) per [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md) — engine tools + mempalace run from here; PATH wiring in `engine/tools/scrub_env.sh` resolves bare `python3` / `ruff` / `mypy` / `pytest` / `mempalace` to venv. Version pins live in [`engine/tools/requirements.txt`](tools/requirements.txt). Pre-S-0043 fallback: system Python 3.9.6 at `/usr/bin/python3`; user-scope packages at `/Users/shanekidd/Library/Python/3.9/bin/` (no longer used by engine) |
| **Anthropic API** | env var `ANTHROPIC_API_KEY` set in local `.env` (gitignored) |

## Next session work item

**S-0097 — deferred cadence-fired health-check audit (formal first-exercise of ADR 0057).** Per the issue close-out campaign plan at `~/.claude/plans/i-want-to-delay-sparkling-shell.md` and the first-exercise readiness note at [`engine/build_readiness/adversarial_stance_first_exercise.md`](build_readiness/adversarial_stance_first_exercise.md). The campaign concluded at S-0096 (C7 closed Issue #34); the post-campaign audit was deferred from S-0087 boot. At S-0097 boot the audit will be **~10 sessions overdue** (`slots_since=20` vs `cadence=10`). The audit applies the [ADR 0057](adr/0057-adversarial-stance-for-health-check-audits.md) adversarial-stance posture — five contract elements: user-buffered execution (audit findings + guidance suggestions surfaced before any retire/convert/replace action), posture inversion across Fit / Gaps / Infrastructure-without-function / Bloat, stats-as-proxy-for-function anti-pattern named, ≥1 affirmative retire-candidate-recommendation required, cold-context probe per audit. Findings flow into Phase 6 scoping cleanly.

**Campaign sequence after S-0097 audit:** routine engine loop resumes against `T-PHASE-5-AUDIT` per [ADR 0051](adr/0051-routine-mode-and-engine-loop.md).

**Phase 5 production audit master plan** at [`engine/build_readiness/phase_5_production_audit.md`](build_readiness/phase_5_production_audit.md) is unchanged; T-PHASE-5-AUDIT in [`engine/session/auto_target.json`](session/auto_target.json) is unchanged; routines remain on Manual/paused for the campaign duration.

**After 12 routine evidence sessions complete** (target-met state, post-C7 + post-cadence-audit), the user runs an **interactive `/start-engine` closeout session** to author the production-audit final report + audit-system-input report, file Issues, author structural-reopen ADR memos, and update the build_readiness/README.md per Issue #26 (closeout-indexing already landed at S-0083 ahead of the closeout per the campaign plan).

**OQ-DEC1-A/C/D settlement** (Phase 6 prereq) and **Phase 6 master plan authoring** both land after audit closeout.

**Phase 6 master plan authoring (per [ADR 0051](adr/0051-routine-mode-and-engine-loop.md), shifts to post-audit-closeout):** (a) read [ROADMAP.md](../ROADMAP.md) Phase 6 + [build_plan/P_6_self_correction.md](../build_plan/P_6_self_correction.md) for canonical phase intent; (b) adversarial reconnaissance per [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md); (c) author `engine/build_readiness/phase_6.md` (gate report) + `engine/session/auto_target.json` (new executable contract for T-PHASE-6) + any new ADRs the gate surfaces. Pre-decided Phase 6 deliverables per the closeout's "Forward pointers" section: rigor_score calibration (T3-C; 366 `missing_rigor_score` warns at Phase 5 close form the backlog counter); branch-based rollback verification (T3-A inherited from phase_3_sql.md); `parse_structural_reference.py` first-real-use plus adversarial-triage prompt template (T3-E per [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md)); philosophy of religion readmission criterion (T3-G — class-boundary clause in [ADR 0052 (product)](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md)). On audit PROCEED, audit findings flow into Phase 6 scoping cleanly.

**Routine engine loop paused for issue close-out campaign (S-0083 → S-0096).** T-PHASE-5-AUDIT remains the active target in `auto_target.json` but routines must stay on Manual or paused for the campaign duration — interactive close-out sessions claim slots S-0083+ and would collide with routine fires on `register_state.json`. After the campaign's last session (S-G / S-0089) closes, the user resumes routines on Hourly (or Manual fires); each routine boot reads `auto_target.json`, runs `check_target.py`, finds AUDIT-CB pending (no depends_on; eligible immediately), claims a slot, executes, marks complete, closes. After AUDIT-CB completes, the 10 subdomain tasks become parallel-eligible (subject to max one routine session at a time per the lockfile). After all 10 complete, AUDIT-HT becomes eligible. After AUDIT-HT completes, target-met → routines exit at step 3 without claiming → user runs interactive audit closeout. 14 max_sessions (12 tasks + 2 buffer).

**Cleanup-batch surface — issue close-out campaign concluded at S-0096 (C7 / Issue #34).** Per the revised plan at `~/.claude/plans/i-want-to-delay-sparkling-shell.md` (which absorbed the 10 new MemPalace audit issues from S-0086 and the three S-0089/S-0090/S-0091 MCP-availability refinement sessions). C7 was the final session. S-A (S-0083) closed [#37](https://github.com/StarshipSuperjam/paideia/issues/37), [#26](https://github.com/StarshipSuperjam/paideia/issues/26), [#29](https://github.com/StarshipSuperjam/paideia/issues/29), [#32](https://github.com/StarshipSuperjam/paideia/issues/32), [#21](https://github.com/StarshipSuperjam/paideia/issues/21). S-B (S-0084) closed [#31](https://github.com/StarshipSuperjam/paideia/issues/31) and filed upstream tracker [MemPalace/mempalace#1394](https://github.com/MemPalace/mempalace/issues/1394). S-C (S-0085) closed [#30](https://github.com/StarshipSuperjam/paideia/issues/30), [#28](https://github.com/StarshipSuperjam/paideia/issues/28), [#36](https://github.com/StarshipSuperjam/paideia/issues/36). S-D (S-0086) closed [#35](https://github.com/StarshipSuperjam/paideia/issues/35) and filed Issues #38–#47 from the audit's recommendations + post-audit follow-up. C1 (S-0087) closed [#42](https://github.com/StarshipSuperjam/paideia/issues/42), [#43](https://github.com/StarshipSuperjam/paideia/issues/43), [#44](https://github.com/StarshipSuperjam/paideia/issues/44), [#45](https://github.com/StarshipSuperjam/paideia/issues/45) (MemPalace audit contract foundation). **C2 (S-0088) closed [#40](https://github.com/StarshipSuperjam/paideia/issues/40), [#46](https://github.com/StarshipSuperjam/paideia/issues/46), [#47](https://github.com/StarshipSuperjam/paideia/issues/47)** (MemPalace cleanup execution + wing-count guardrail + audit-trail boot surface). **MCP-availability refinement chain (out-of-campaign, user-driven):** S-0089 added boot/close MCP probes + token-tightening; S-0090 backed off the invalid-token hard-fail to soft-warn for routine protection; S-0091 closed the remaining no-token-no-diary hard-fail in routine mode and made LOUD body uniform across modes plus added the deferred-diary recovery path. **C3 (S-0092) closed [#41](https://github.com/StarshipSuperjam/paideia/issues/41)** (historical full-encoded-path wings deep cleanup; 37,634 drawers + 67 closets removed; palace footprint 47,298 → 9,819). **C4 (S-0093) closed [#38](https://github.com/StarshipSuperjam/paideia/issues/38) + [#39](https://github.com/StarshipSuperjam/paideia/issues/39)** (MemPalace effectiveness pair — boot-search orchestrator + drawer-citation telemetry; ADR 0056 amended). **C5 (S-0094) closed [#23](https://github.com/StarshipSuperjam/paideia/issues/23)** (apply_migration Layer 2.5 empirical postcondition verification; new exit code 8; ADR 0055 + ADR 0039 amended; all 15 Phase 5 seeds annotated). **C6 (S-0095) closed [#33](https://github.com/StarshipSuperjam/paideia/issues/33)** (timestamp standardization; new ADR 0058 + `engine/tools/timestamps.py` helper + `validate.py` AST-walk soft-warn `timestamp_helper_bypass`; 13 emit/parse-site migrations; smoking-gun fix at `health_check.py:960-961`; 4-file allowlist with rationale). **C7 (S-0096) closed [#34](https://github.com/StarshipSuperjam/paideia/issues/34)** (PDF input adapter for parse_structural_reference.py; source-agnostic multi-fallback architecture via pypdfium2 + pdfplumber; multi-entry partitioning extension to `extract_entries()`; element-reorder pass; three-source convergence validated against Routledge / Cambridge / Oxford with the page-header-entry-indicator gap filed as [Issue #48](https://github.com/StarshipSuperjam/paideia/issues/48); ADR 0047 Consequences amended). **Remaining open after campaign:** #1 + #2 (upstream-blocked), #22 (Metabase deferred-with-reason), #24 (graph renderer deferred to post-audit-closeout), **#48 (Cambridge/Oxford page-header path, deferred until consuming Phase 5 sub-session)**, plus the upstream MemPalace tracker (filed-not-closed by design). Issues #20 and #27 closed at S-0078.

**MemPalace wing-filter / wing-naming bugs unfixed** (Issues [#1](https://github.com/StarshipSuperjam/paideia/issues/1), [#2](https://github.com/StarshipSuperjam/paideia/issues/2)) against the venv-managed mempalace. Workaround: unfiltered `mempalace_search`.

**Boot-time check: cadence trigger.** Cleared at S-0077 audit close (`last_audit_session: S-0077`). Fired at S-0087 boot (slots_since=10) and was **explicitly deferred** per user direction — the issue close-out campaign cleared the backlog before the audit runs. `health_check_overdue` validator soft-warn fired every session S-0087 → S-0096 (10 sessions of deferral cost). **The campaign concluded at S-0096 / C7**; the cadence-fired audit (formal first-exercise of ADR 0057 per the readiness note) runs as the next session (S-0097), ~10 sessions overdue at that point (slots_since=20 vs cadence=10).

**Boot-time check: Issues backlog.** S-0093 (C4) closed [Issue #38](https://github.com/StarshipSuperjam/paideia/issues/38) + [Issue #39](https://github.com/StarshipSuperjam/paideia/issues/39). Post-S-0093 backlog: 0 bugs, 3 tech-debt, 1 cleanup, 3 enhancement, 1 health-check, 0 docs (counts overlap by label). 3 in-scope issues remain across C5–C7 (#23, #33, #34); 4 baseline-deferred (#1, #2, #22, #24) plus the upstream MemPalace tracker.

**Boot-time check: audit-trail line (new at S-0088 per Issue #47).** When `health-check-finding`-labeled Issues exist, `scan_issue_backlog.py` emits an `Active health-check audits: N (M open recommendations)` line listing the cited audit reports. Post-S-0092 boot: 1 audit (`mempalace-adversarial-review-S-0086.md`), 0 open recommendations (its R3 #40A/#40B closed at S-0088 via Issue #40, and R3 #40C closed at S-0092 via Issue #41 — the audit's recommendation set is now fully resolved; the audit report stays linked from the audit-trail surface as historical record).

**Boot-time check: wing-count growth (new at S-0088 per Issue #46).** `probe_palace.py` emits `[probe-palace] wings: N (total)` to stderr; `validate.py` emits `mempalace_wing_count_growth` soft-warn at ≥ 60 (informational) or ≥ 100 (LOUD) configurable via `register_state.json` `wing_count_growth_thresholds` block. Post-S-0092 cleanup state: **7 wings** (paideia, wing_paideia, wing_claude, sessions, plus 3 short-form per-worktree wings) — comfortably below the informational threshold of 60. Soft-warn cleared.

**Boot-time check: scope-erosion signal.** Last 5 archives at S-0093 boot will be S-0088, S-0089, S-0090, S-0091, S-0092 — all clean deliveries. Signal silent.

**Boot-time check: MemPalace adoption (new at S-0078 per ADR 0056; extended at S-0089 / S-0090 / S-0091).** From S-0079 forward, the per-session `mempalace_activity` field on archives is mechanically validated. The `validate.py --final-check` audit fires at shutdown step 1 (after `scan_mempalace_activity.py` writes the rollup at step 0). Categories post-S-0091:
- `mempalace_boot_query_skipped` (soft-warn) — no `mempalace_search` call recorded.
- `mempalace_diary_read_skipped` (soft-warn) — no `mempalace_diary_read` call recorded.
- `mempalace_diary_write_skipped` (hard-fail, **engine mode only** per S-0091) — no diary write AND no acknowledgement token in `outcome_summary` AND mode != routine.
- `mempalace_diary_write_skipped_routine` (soft-warn, **routine mode only** per S-0091) — same predicate as the engine hard-fail above but mode == routine. LOUD body. Side effect: appends entry to `engine/session/diary_pending_index.json`. Recovery procedure at [`engine/operations/routine-mode-operations.md`](operations/routine-mode-operations.md) "Deferred diary recovery".
- `mempalace_diary_write_acknowledged_skip` (soft-warn, LOUD body uniformly across modes per S-0091) — token in `outcome_summary` AND substrate unreachable at close.
- `mempalace_diary_write_skipped_substrate_intermittent` (soft-warn, LOUD body uniformly per S-0091) — token in `outcome_summary` AND substrate reachable at close (the contradiction case).
- `mempalace_substrate_at_close` (soft-warn, LOUD body uniformly per S-0091) — `mempalace status` failed at close, independent of diary state.
- `mempalace_retired_surface_used` (soft-warn) — `kg_calls > 0` OR `tunnel_calls > 0`.
- `mempalace_zero_citations_after_search` (soft-warn, added at S-0093 per ADR 0056 amendment + Issue #39) — `search_calls > 0` AND `mempalace_citations.total == 0`. Closed-loop check that retrieved drawers actually inform authored artifacts. Citations block written by `engine/tools/scan_mempalace_citations.py` at shutdown step 8b. Gated on session id ≥ S-0093 (pre-S-0093 archives lack the block by design). Persistent firing per ADR 0042's 3-of-5 surface signals the boot-search formulations need tuning OR retrieved drawers aren't being cited in authored work.

**Boot-time check: pending mempalace diary writes (new at S-0091 per ADR 0056).** `engine/tools/hooks/session-start.sh` reads `engine/session/diary_pending_index.json` and emits a LOUD attention block with the count + session IDs when non-empty. Block stays up at every boot until the recovery procedure removes the entries. Index initialized empty at S-0091.

The first-exercise readiness note at [`engine/build_readiness/mempalace_mechanical_adoption_first_exercise.md`](build_readiness/mempalace_mechanical_adoption_first_exercise.md) tracks T1-B (routine-context first exercise) — closes at the next routine fire post-S-0078 with non-zero `diary_write_calls` in its archived `mempalace_activity`.

**Boot-time check: shared-state health probe.** Per [ADR 0045](adr/0045-shared-state-integrity-discipline.md). Healthy expected.

**Boot-time check: side-discovery audit + HANDOFF disposition audit + archive structured-fields audit.** All three continue running per their respective contracts at every commit + every close.

**Boot-time check: scheduled-audit surface.** Active `audits` array empty. No scheduled audits due.


## Open tensions and deferred decisions

See [`product/docs/tensions.md`](../product/docs/tensions.md). As of S-0026 close, the OQ-prefixed active set is 9 items (OQ-PRIVACY-A and OQ-PRIVACY-B closed by ADR 0031; OQ-PHASE8-A settled by ADR 0032; OQ-BYOK-REGIME withdrawn by ADR 0032; OQ-OUTWARD-VOICE added at S-0019):

- OQ-DEC1-A through D — Phase DEC.1 architecture decisions (server-side mastery, two-hop retrieval, embedding strategy, chunk-resolver vs URL pointers); decide-before Phase 6
- **OQ-WALL-BEHAVIOR** (added S-0008, per ADR 0029; simplified S-0012) — soft-wall degradation ladder at cost cap; **single-tier ladder** under cost-priced subscription model (free-vs-paid tier-shaped framing collapsed at S-0012); four candidate steps remain (model downshift, retrieval shrink, concept-engagement length cap, soft refusal); decide-before Phase 8 cost-cap wiring
- **OQ-CONTEXT-COMPRESSION** (added S-0008, per ADR 0029; unaffected by S-0012) — token-amplification mitigation for multi-turn engagements (structured-state replacement + automatic compression + explicit summarization candidates); decide-before Phase 7
- **OQ-PEDAGOGY-INFERENCE-LOCUS** (added S-0008, tagged `watch`) — rule layer vs. distributed inference; revisit when inference registry crosses ~30 entries OR cross-domain edges per domain-pair exceed ~50 OR an operational complaint surfaces
- **OQ-OUTWARD-VOICE** (added S-0019) — third expression-contract gap covering outward product surfaces (UI labels, button text, error messages, learner-facing help, public README, App Store description, learner-visible CHANGELOG entries) that neither ADR 0027 nor ADR 0036 governs; decide-before Phase 7 (the earliest plausible moment such a surface ships)
- OQ-WATCH-FLAG-FILE (tagged `watch`) — separate watch-flag file consideration, surfaced at session-30 health check

## Paperclip integration

**Rejected at S-0044, superseded by [ADR 0051](adr/0051-routine-mode-and-engine-loop.md).** Investigation surfaced that Paperclip-dispatched Claude work runs under `ANTHROPIC_API_KEY` per Anthropic's April-2026 third-party-orchestration policy (subscription does not cover it; Paperclip GitHub issues #1163, #3270). For a solo project on subscription with no real parallelism need, the recurring per-token API spend doesn't justify Paperclip's value-add. The replacement architecture — Claude Code Routines + `engine/session/auto_target.json` + `engine/tools/check_routine_scope.py` — accomplishes dispatch + termination + anti-rogue at zero recurring spend, on existing apparatus. Paperclip rejection reasoning preserved in ADR 0051 "Alternatives Considered." If the project ever expands beyond Claude (multi-runtime), or hires collaborators (multi-user governance), or accepts API spend on principle (e.g., for unattended Phase 8 evaluation runs), Paperclip becomes a reasonable revisit.

## Strong working commitments

12 commitments inherited from pre-Foundation design plus 10 architectural decisions. **All 22 are recorded as ADRs** — the contract layer, now split across [`engine/adr/`](adr/) (5 engine ADRs about the build apparatus) and [`product/adr/`](../product/adr/) (33 product ADRs about Paideia-as-product). The audience-facing summary lives in [`product/docs/MISSION.md`](../product/docs/MISSION.md) (commitments 1–12 with ADR cross-refs; commitment 2 reframed at S-0012 as "operating discipline must not corrupt pedagogy" per ADR 0032); the canonical list with ADR pointers lives in [`ROADMAP.md`](../ROADMAP.md) ("Strong working commitments referenced throughout"). The conversational story behind each decision is recoverable from MemPalace `decision`-tagged drawers.

**ADR count post-S-0095: 59 ADRs total — 57 Accepted plus 2 Superseded** (ADR 0002 by ADR 0032; ADR 0032 by ADR 0035), partitioned into **24 engine + 35 product** per [ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md). S-0095 added [ADR 0058](adr/0058-canonical-timestamp-format-and-helper.md) (engine; canonical timestamp format and shared `engine/tools/timestamps.py` helper; closes Issue #33). S-0078 added [ADR 0056](adr/0056-mempalace-mechanical-adoption-checks.md) (engine; MemPalace mechanical adoption checks across all session types — closes Issues #27 and #20). S-0064 added [ADR 0055](adr/0055-apply-migration-wrapping-against-production-reads-gate.md) (engine; apply-migration wrapping against the production-reads classifier). S-0061 added [ADR 0054](adr/0054-lifecycle-push-wrapping-against-default-branch-push-gate.md) (engine; lifecycle-push wrapping against the default-branch push gate). S-0058 added [ADR 0053](adr/0053-mechanism-first-exercise-gate.md) (engine; mechanism first-exercise gate before unattended use). S-0046 added [ADR 0052](adr/0052-routine-boot-freshness-and-concurrency-defense.md) (engine; routine boot freshness and concurrency defense). S-0045 added [ADR 0052 (product)](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md) (Phase 5 philosophy-subdomain decomposition — 9 subjects + service nodes + cross-bridges + closeout; class-boundary admit-criterion for future readmissions; philosophy of religion and history-as-subdomain explicitly deferred). S-0044 added [ADR 0051](adr/0051-routine-mode-and-engine-loop.md) (engine; routine-mode session pattern; third session mode; two-layer architecture). S-0043 added [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md) (engine; project-local Python venv). S-0042 added [ADR 0048](adr/0048-handoff-narrowing-and-github-issues-for-cross-session-deferrals.md) and [ADR 0049](adr/0049-scope-lock-at-boot-and-descope-reorder-audit-at-shutdown.md). S-0039 added [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) (engine; structural-reference parser tool and adversarial-triage workflow). S-0038 added [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) (product; structural-reference posture for the philosophy reference class). S-0035 added [ADR 0045](adr/0045-shared-state-integrity-discipline.md) (engine; shared-state integrity discipline). S-0031 added [ADR 0043](adr/0043-hook-architecture.md) (engine; hook architecture) and [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md) (engine; skill conversion partition). S-0029 added [ADR 0041](adr/0041-cascade-analysis-discipline.md) (engine; cascade-analysis discipline) and [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) (engine; soft-warn lifecycle, archive-as-canon). The contract layer the [`build_plan/`](../build_plan/) chunks consume by reference. [`engine/tools/validate.py`](tools/validate.py)'s `adr_index_consistency` check audits the ADR-file-vs-index alignment across both subtree READMEs; the `cascade_*` checks (per ADR 0041) audit superseded-citation currency, back-reference orphans, and Consequences-deliverable promises; `validate_shared_state_health()` (per ADR 0045) audits chromadb palace and git repo sanity. (Note: `adr_index_consistency` audits machine-readable indexes; STATE.md prose claims about ADR counts are NOT mechanically audited — drift in this section requires author-discipline-at-edit-time per the Posture-vs-machinery framing in [CLAUDE.md](../CLAUDE.md). Any edit touching this section or the headline-table "Current phase" row should re-count from `engine/adr/[0-9]*.md` and `product/adr/[0-9]*.md`.)
