# Project State

> The single file every session reads first. Names what's current and what's next. Updated at every build-session close.

## Current

| Field | Value |
|---|---|
| **Project** | Paideia — knowledge mastery app on a pedagogical dependency graph |
| **GitHub** | https://github.com/StarshipSuperjam/paideia (private) |
| **Current phase** | Phase 0 — closed (S-0003); **Phase 1 — Contract Lock fully closed at S-0012**. **Phase 1.5 — Mission Realignment Overhaul fully closed at S-0021.** **S-0022** committed the engine / product wall ([ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md)) and renamed `CHANGELOG.md` → `ENGINE_LOG.md`. **Phase 2 (Build Plan Scaffolding) closed at S-0023.** **S-0024–S-0025** executed the folder migration into `engine/` and `product/` subtrees and the cross-reference sweep. **S-0026** authored the third project expression contract per [ADR 0038](adr/0038-expression-contract-for-ai-authored-code.md). **S-0027 widened the expression contract** per [ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md) and **committed the build-readiness gate** per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md). **Phase 3 — SQL Schema Implementation closed at S-0028** with eight migrations deployed to `paideia-dev` (PostgreSQL 17.6); RLS enabled on every public table; CHECK constraints enforce gate T2-A enums. **S-0029 closed the infrastructure-misfire sweep**; **S-0030 landed the first project health-check audit**; **S-0031 closed Session β: harness mechanization** ([ADR 0043](adr/0043-hook-architecture.md), [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md)); **S-0032 closed the MemPalace audit cleanup + improvements** (cadence tightened 30 → 10); **S-0033 closed three coupled mechanical fixes** (pre-commit STATUS bug, side-discovery audit, scheduled-audits allowed-paths); **S-0034 closed the Phase 4 build-readiness gate exercise**; **S-0035 closed the engine-hardening pass** ([ADR 0045](adr/0045-shared-state-integrity-discipline.md)); **S-0036 closed the deferral-discipline guardrails** plus the bash 3.2 fix. **S-0039 landed the structural-reference parser tool** per new engine [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md). **S-0040 closed the MemPalace wing-filter fix (deferred from S-0038)** concurrent with S-0039. **S-0041 closed the cadence-trigger overdue-catchup logic fix + second project health check audit (catch-up).** **S-0042 closed the engine apparatus improvements pass:** HANDOFF.md narrowed to session-internal handoffs, GitHub Issues absorb cross-session deferrals (ADR 0048); multi-axis dead-weight scanner + rewritten health-check; declared_scope at boot + scope-delivery audit at shutdown + multi-session erosion signal + build-readiness-gate no-descoping clause (ADR 0049); session-end context telemetry. **S-0043 closed the project venv migration** per new engine [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md): uv-managed venv at `<main_repo>/.venv/`, Python 3.12 pinned via tracked `.python-version`; idempotent sourcing-time PATH prepend in `engine/tools/scrub_env.sh` (worktree-local preferred, main fallback); 8 deps explicit in `engine/tools/requirements.txt` (chromadb pin closed silent-dep gap; tiktoken activates ADR 0049's accurate-tokenizer branch; mempalace migrated from user-scope system Python 3.9 → project venv 3.12 at version 3.3.4); upstream Issues #1/#2 re-verified against 3.3.4 (#1 reproduces verbatim; #2 partial-change-not-fix). **Phase 4 — Graph Validation Utility closed at S-0037**. **Phase 4.5 — Input Dataset Survey closed at S-0038** per [ROADMAP §4.5](../ROADMAP.md) with [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md). ADR count post-S-0043: **50 ADRs total — 48 Accepted plus 2 Superseded**, partitioned **16 engine + 34 product** (S-0043 added engine ADR 0050). Validator clean (21 checks, 0 hard-fails; 2 expected `issue_collision` soft-warns from Issues #1/#2 keyword-matching the session's mempalace scope). pytest 434/434 passing in venv. |
| **Last build session** | S-0043 (2026-05-04) — **Project venv migration with hook PATH wiring per ADR 0050.** User-directed displacement of the planned Phase 5 epistemology build-readiness gate (slips to S-0044+) — opened with the user surfacing tiktoken install + old-pip warning and the framing "before we get into heavy dev work." Output: (1) [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md) — engine ADR; uv-managed venv at `<main_repo>/.venv/`; Python 3.12 via tracked `.python-version`; idempotent sourcing-time PATH prepend in `engine/tools/scrub_env.sh` (worktree-local preferred, main fallback) leveraging ADR 0045's PATH-preservation across `scrubbed_env()` for zero-edit propagation through every nested invocation. (2) Three previously implicit deps now explicit in `engine/tools/requirements.txt`: chromadb (closed silent-dep gap); tiktoken (activates ADR 0049's `tiktoken-o200k_base` branch in `scan_context_telemetry.py`); mempalace (migrated from user-scope system Python 3.9 → project venv 3.12 at version 3.3.4). (3) `.mcp.json` mempalace-mcp command path moved to venv (gitignored local edit). (4) Mid-session expansion (user-directed): mempalace 3.3.4 verified against upstream Issues #1 and #2 — Issue #1 (wing-filtered search "Internal error") reproduces verbatim; Issue #2 (per-worktree wing-naming) behavior changed but not fixed (`_wing_from_transcript_path` now returns `wing_<last-token>`; main → `wing_paideia` distinct from documented bare `paideia`; worktrees → `wing_<random-hash>` still per-worktree). Findings captured in ADR 0050 Consequences + `engine/operations/mempalace-operations.md` Known issues + Wing-name reality + Install sections (verification not pushed to GitHub Issues per user direction). (5) End-to-end verification: PATH wiring resolves correctly from worktree to main's `.venv/bin/`; scan_context_telemetry --dry-run flips `tokenizer_used` from `chars-div-4-fallback` to `tiktoken-o200k_base`; validate.py default mode 21 checks 0 hard-fails 2 expected issue_collision soft-warns; pytest 434/434 passing in venv. Decision drawer captured for the two-layer rule. CLAUDE.md mechanization-list gains the venv bullet. Phase 5 epistemology gate slips to S-0044+. |

| **Prior build session** | S-0042 (2026-05-04) — **Engine apparatus improvements: HANDOFF/Issues split + multi-axis health-check rewrite + scope-lock + context telemetry.** User-directed displacement of the planned Phase 5 epistemology build-readiness gate (slips to S-0043+) after a substantive planning thread. Output: (1) [ADR 0048](adr/0048-handoff-narrowing-and-github-issues-for-cross-session-deferrals.md) and [ADR 0049](adr/0049-scope-lock-at-boot-and-descope-reorder-audit-at-shutdown.md) — engine ADRs. (2) [`engine/operations/issue-discipline.md`](operations/issue-discipline.md) Layer 1 ops doc; HANDOFF-vs-Issue rule; six-label taxonomy plus `priority:urgent`. (3) Four new Python tools: `scan_issue_backlog.py`, `scan_issue_collisions.py`, `scan_orphans.py` (5 axes), `scan_context_telemetry.py`. (4) Validator extensions: `validate_issue_collisions()` + `validate_scope_discipline()` (3 new soft-warn categories); `audit_handoff_dispositions.py` fifth pattern. Check count 17 → 21. (5) `session-start.sh` two new boot surfaces. (6) Health-check ops doc rewritten around "is this file doing the work it was created to do?". (7) `session-build-lifecycle.md` + `session-shutdown-sequence.md` + `build-readiness-gate.md` updated; all three Skills mirrored; CLAUDE.md updated. (8) GitHub Issues #1 + #2 filed as seed bug+upstream entries. Validator clean except 2 expected `issue_collision` soft-warns. |

| **Last commit on main** | (see `git log --oneline -1` on main) |
| **Backup tags** | `pre-foundation-v0.0.0` at commit `fa70b8c` — pre-machinery anchor. `pre-phase-3-v0.0.1` at commit `36b0907` (S-0026 close) — pre-Phase-3 anchor cut at S-0027. Both are historical anchors, not operational fallback targets — file-level retrieval via `git show <tag>:<path>` is the legitimate use. |

## Infrastructure

| Component | Value |
|---|---|
| **Supabase project** | `paideia-dev`, project ref `ozooosgnuzxqqypotlke`, PostgreSQL 17.6 |
| **Supabase MCP** | configured in `.mcp.json` (gitignored, contains PAT) |
| **MemPalace** | installed at `/Users/shanekidd/Library/Python/3.9/bin/`; MCP server `mempalace-mcp` configured in `.mcp.json`; wing `paideia` carries **485 drawers across 5 rooms** as of S-0032 close (general 330, operations 88, decisions 55, foundation-planning-s0001 10, s0003-adr-collection 2); capture hooks wired in `.claude/settings.json` (Stop + PreCompact wrapper PATH-fixed at S-0032 — auto-capture had been silently failing since S-0002 because `mempalace` CLI wasn't on the hook subshell's PATH; verify at next Stop event by checking `.claude/logs/mempalace-hook.log` for `OK exit=0` lines instead of `FAIL exit=127`); project usage scope documented at [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Project usage scope" — uses search, add_drawer, diary (agent_name `claude`), read-only inspection; explicitly excludes KG and tunnels |
| **Python** | **project venv** at `<main_repo>/.venv/` (Python 3.12.13, uv-managed) per [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md) — engine tools + mempalace run from here; PATH wiring in `engine/tools/scrub_env.sh` resolves bare `python3` / `ruff` / `mypy` / `pytest` / `mempalace` to venv. Pre-S-0043 fallback: system Python 3.9.6 at `/usr/bin/python3`; user-scope packages at `/Users/shanekidd/Library/Python/3.9/bin/` (no longer used by engine) |
| **Anthropic API** | env var `ANTHROPIC_API_KEY` set in local `.env` (gitignored) |

## Next session work item

**S-0044 — Phase 5 epistemology build-readiness gate** per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md). Slipped two slots — once at S-0042 (engine apparatus improvements per ADRs 0048 + 0049), once at S-0043 (project venv migration per ADR 0050). Both displacements were explicitly user-directed and faithfully captured per the scope-discipline machinery: S-0042's and S-0043's `declared_scope` each named the operational work with `phase: NA-engine-apparatus`; both scope-delivery audits will record `delivered: true`. All Phase 5 preconditions still hold and have been strengthened: S-0039 closed with [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) + the parser tool; S-0040 closed with the wing-filter bugs documented (filed as Issues #1 and #2 at S-0042; re-verified against mempalace 3.3.4 at S-0043 — both still upstream and not in-project remediable); S-0041 fixed the cadence-trigger silent-slide failure mode and ran the catch-up audit; **S-0043 stabilized the Python toolchain** under a project venv with explicit dep pins, eliminating the system-Python-version-drift class of failure during the heavy seed-authoring work to come. The first Phase 5 sub-session (epistemology, per [`build_plan/P_4_seed_graph_build.md`](../build_plan/P_4_seed_graph_build.md) §5.1) requires a gate report at `engine/build_readiness/phase_5_epistemology.md`. The S-0044 session reads the chunk, consults the [Phase 4.5 input dataset survey](../product/docs/content-strategy.md) ("Cross-Domain Reference Inventories — Survey") for per-source disposition, considers running the structural-reference parser against an acquired SEP entry to surface candidates for the gate's Tier 1 inventory, applies adversarial reconnaissance per [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md) (now including the no-descoping-or-reordering clause from ADR 0049), and authors the gate report's Tier 1 / Tier 2 / Tier 3 triage so the S-0045 build session can implement without re-deciding.

If a non-substantive operational session intercedes, that session takes S-0044 and the Phase 5 gate slips to S-0045+.

**Parser tool available for ad-hoc use.** [`engine/tools/parse_structural_reference.py`](tools/parse_structural_reference.py) per [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) is runnable whenever a structural-reference source has been acquired (currently SEP/IEP via the HTML adapter; PDF adapter deferred to first-PDF-source-acquisition). CLI: `python3 engine/tools/parse_structural_reference.py --source <path> --document-type encyclopedia --output -`. The parser produces a JSON focusing brief; the user supplies the brief to an adversarial-triage LLM prompt (template deferred to first-real-use). The brief is discarded after triage; the graph is the only durable artifact.

**MemPalace wing-filter / wing-naming bugs re-verified at S-0043 against mempalace 3.3.4** (now installed in project venv per ADR 0050). Issue [#1](https://github.com/StarshipSuperjam/paideia/issues/1) (wing-filtered search "Internal error: Error finding id"): reproduces verbatim, NOT fixed. Issue [#2](https://github.com/StarshipSuperjam/paideia/issues/2) (per-worktree wing-naming): behavior changed in 3.3.4 (`_wing_from_transcript_path` now returns `wing_<last-token>`) but bug not fixed — main sessions derive `wing_paideia` (distinct from documented bare `paideia`); worktree sessions derive `wing_<random-hash>` still per-worktree. See [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Known issues" for the full 3.3.4 verification + workaround (unfiltered `mempalace_search`).

**Phase 4.5 input dataset survey is closed at S-0038.** The "Cross-Domain Reference Inventories — Survey" section in [`product/docs/content-strategy.md`](../product/docs/content-strategy.md) is populated end-to-end across four tiers; [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) settles the structural-reference posture for the home-discipline reference class; [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) settles the operational mechanism. Phase 5 sub-sessions inherit both at boot per ROADMAP §5.2.

**Phase 4 graph validation remains implementation-closed at S-0037.** The validator code, manifests, routing, and ops-doc are all in place. Two ROADMAP §4 success criteria — `<3s on a 100-node test seed` and `pre-commit hook blocks seed-authoring commits that fail audit` — require real seed content to verify and queue for natural exercise at the first Phase 5 seed session (S-0043+ after the Phase 5 gate). The first Phase 5 session that sets `SUPABASE_DB_URL` and runs `validate.py --validate-only` after a `supabase db push` will exercise the audit end-to-end against real seed content for the first time.

**Boot-time check: cadence trigger (overdue-catchup per ADR 0022 amendment at S-0041).** Cadence is 10. `last_audit_session = "S-0041"`. At S-0044 boot, `slots_since = 44 - 41 = 3` — no cadence trigger this session. Next due fire at S-0051 absent any further audits.

**Boot-time check: Issues backlog (added at S-0042 per ADR 0048).** `session-start.sh` runs `scan_issue_backlog.py` between the probe and persistent-warn surfaces. Default FYI line emitted at every boot; LOUD block fires when any open Issue carries `priority:urgent`. As of S-0043 close, backlog unchanged: 2 bugs (Issues #1, #2 — wing-filter and wing-naming, both `bug + upstream`); 0 tech-debt; 0 cleanup; 0 enhancement; 0 urgent. Both re-verified against mempalace 3.3.4 at S-0043 — both remain upstream-unfixed. Cleanup-batch sessions per [`engine/operations/issue-discipline.md`](operations/issue-discipline.md) when the user judges accumulation worth dedicating a slot to.

**Boot-time check: scope-collision detection (added at S-0042 per ADR 0048).** `validate.py` default mode runs `scan_issue_collisions.py`. Soft-warn per open Issue whose body or title matches a keyword from `declared_scope` (or `working_on` fallback) or a path from staged-files. Non-blocking; informational signal that a session is touching a known-broken surface.

**Boot-time check: persistent-warn surface.** Per [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md). At S-0044 boot, archive window: S-0039, S-0040, S-0041, S-0042, S-0043. Both S-0042 and S-0043 record `issue_collision: 2` (S-0042 from the seed Issues vs. session scope; S-0043 from the same Issues keyword-matching mempalace migration scope). Persistent-warn surface will see `issue_collision` firing in 2 of last 5 (still below 3-of-5 threshold; if S-0044 also fires it, the threshold trips).

**Boot-time check: scope-erosion signal (added at S-0042 per ADR 0049).** `session-start.sh` walks recent archives and counts `scope_delivery.delivered == false`. Surface fires at 3-of-5. As of S-0043 close, S-0042 and S-0043 both record `scope_delivery.delivered == true` — signal remains silent.

**Boot-time check: shared-state health probe.** Per [ADR 0045](adr/0045-shared-state-integrity-discipline.md). Healthy expected.

**Boot-time check: side-discovery audit.** Eleventh firing. **HANDOFF disposition audit:** eighth firing. (HANDOFF.md scope narrowed at S-0042 per ADR 0048 to session-internal handoffs only; cross-session deferrals route to GitHub Issues going forward. The four pre-S-0042 entries remain dispositioned `Resolved`. S-0043 added no HANDOFF entries — fix-in-context throughout.)

**Boot-time check: scheduled-audit surface.** Active `audits` array is empty (consumed at S-0041). No scheduled audits due.

Boot procedure: `Start Engine` in a fresh Claude Code session. CLAUDE.md auto-loads. The S-0044 work is the Phase 5 epistemology build-readiness gate; the gate procedure at [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md) (now including the no-descoping-or-reordering clause from ADR 0049) names the load-bearing reads.

### Source documents (S-0044 boot, Phase 5 epistemology gate)

- [`engine/operations/build-readiness-gate.md`](operations/build-readiness-gate.md) — gate procedure.
- [`engine/operations/seed-chunked-authoring.md`](operations/seed-chunked-authoring.md) — Phase 5 per-session migration workflow.
- [`build_plan/P_4_seed_graph_build.md`](../build_plan/P_4_seed_graph_build.md) — Phase 5 chunk file; epistemology is the first sub-session per §5.1.
- [`engine/build_readiness/phase_4_graph_validation.md`](build_readiness/phase_4_graph_validation.md) — exemplar gate report (S-0037 implementation followed it; the Phase 5 gate inherits structure).
- [`product/docs/content-strategy.md`](../product/docs/content-strategy.md) — the "Cross-Domain Reference Inventories — Survey" section authored at S-0038; Tier 4 (SEP, IEP, Routledge, Oxford, Wikipedia) carries the per-candidate graph-shape disposition for the home discipline.
- [`product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md`](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) — structural-reference posture for the philosophy reference class.
- [`engine/adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md`](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) — parser tool ADR; runnable ad-hoc to surface candidate concept inventory.
- [`engine/tools/parse_structural_reference.py`](tools/parse_structural_reference.py) — the parser tool itself.
- [`product/docs/architecture.md`](../product/docs/architecture.md), [`product/docs/learner-model.md`](../product/docs/learner-model.md) — the contracts the seed graph encodes.
- [`product/seed-graph/migrations/PREDICATE_MANIFEST.md`](../product/seed-graph/migrations/PREDICATE_MANIFEST.md), [`product/seed-graph/migrations/ROUTING.md`](../product/seed-graph/migrations/ROUTING.md) — manifest and routing the first Phase 5 migration consumes.
- [`engine/tools/validate.py`](tools/validate.py) — `validate_graph()` is live; the Phase 5 gate inherits the validator's contract rather than naming the audit categories itself.
- [`docs/health-checks/S-0041.md`](../docs/health-checks/S-0041.md) — the second project health check; the headline silent-slide finding and the universal-scan rewrite story are the cross-session-relevant context.

### After S-0043

S-0044 — natural slot for the Phase 5 epistemology build-readiness gate (twice-displaced — once at S-0042 for engine apparatus, once at S-0043 for the venv migration).

S-0045 — natural slot for the first Phase 5 epistemology seed-authoring session if the gate at S-0044 lands clean.

S-0051 — next natural cadence-aligned health check trigger (`slots_since == cadence` with `last_audit_session = "S-0041"`). The new overdue-catchup logic guarantees the trigger fires at S-0051 even if the slot is consumed by other work — the "overdue" surface fires at S-0052 / S-0053 / etc. until the audit lands. Validator's `health_check_overdue` soft-warn provides defense-in-depth.

## Open tensions and deferred decisions

See [`product/docs/tensions.md`](../product/docs/tensions.md). As of S-0026 close, the OQ-prefixed active set is 9 items (OQ-PRIVACY-A and OQ-PRIVACY-B closed by ADR 0031; OQ-PHASE8-A settled by ADR 0032; OQ-BYOK-REGIME withdrawn by ADR 0032; OQ-OUTWARD-VOICE added at S-0019):

- OQ-DEC1-A through D — Phase DEC.1 architecture decisions (server-side mastery, two-hop retrieval, embedding strategy, chunk-resolver vs URL pointers); decide-before Phase 6
- **OQ-WALL-BEHAVIOR** (added S-0008, per ADR 0029; simplified S-0012) — soft-wall degradation ladder at cost cap; **single-tier ladder** under cost-priced subscription model (free-vs-paid tier-shaped framing collapsed at S-0012); four candidate steps remain (model downshift, retrieval shrink, concept-engagement length cap, soft refusal); decide-before Phase 8 cost-cap wiring
- **OQ-CONTEXT-COMPRESSION** (added S-0008, per ADR 0029; unaffected by S-0012) — token-amplification mitigation for multi-turn engagements (structured-state replacement + automatic compression + explicit summarization candidates); decide-before Phase 7
- **OQ-PEDAGOGY-INFERENCE-LOCUS** (added S-0008, tagged `watch`) — rule layer vs. distributed inference; revisit when inference registry crosses ~30 entries OR cross-domain edges per domain-pair exceed ~50 OR an operational complaint surfaces
- **OQ-OUTWARD-VOICE** (added S-0019) — third expression-contract gap covering outward product surfaces (UI labels, button text, error messages, learner-facing help, public README, App Store description, learner-visible CHANGELOG entries) that neither ADR 0027 nor ADR 0036 governs; decide-before Phase 7 (the earliest plausible moment such a surface ships)
- OQ-WATCH-FLAG-FILE (tagged `watch`) — separate watch-flag file consideration, surfaced at session-30 health check

## Paperclip integration

**Deferred.** Phase 5 trial / Phase 7 commit per ROADMAP.md. Not installed in Foundation.

## Strong working commitments

12 commitments inherited from pre-Foundation design plus 10 architectural decisions. **All 22 are recorded as ADRs** — the contract layer, now split across [`engine/adr/`](adr/) (5 engine ADRs about the build apparatus) and [`product/adr/`](../product/adr/) (33 product ADRs about Paideia-as-product). The audience-facing summary lives in [`product/docs/MISSION.md`](../product/docs/MISSION.md) (commitments 1–12 with ADR cross-refs; commitment 2 reframed at S-0012 as "operating discipline must not corrupt pedagogy" per ADR 0032); the canonical list with ADR pointers lives in [`ROADMAP.md`](../ROADMAP.md) ("Strong working commitments referenced throughout"). The conversational story behind each decision is recoverable from MemPalace `decision`-tagged drawers.

**ADR count post-S-0043: 50 ADRs total — 48 Accepted plus 2 Superseded** (ADR 0002 by ADR 0032; ADR 0032 by ADR 0035), partitioned into **16 engine + 34 product** per [ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md). S-0043 added [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md) (engine; project-local Python venv at main repo root, uv-managed, Python 3.12 pinned; hook PATH wiring via `engine/tools/scrub_env.sh` sourcing-time prepend leveraging ADR 0045's PATH-preservation; previously-implicit chromadb explicit; tiktoken activates ADR 0049 telemetry; mempalace migrated from user-scope to venv with 3.3.4 verification of upstream Issues #1/#2). S-0042 added [ADR 0048](adr/0048-handoff-narrowing-and-github-issues-for-cross-session-deferrals.md) and [ADR 0049](adr/0049-scope-lock-at-boot-and-descope-reorder-audit-at-shutdown.md). S-0039 added [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) (engine; structural-reference parser tool and adversarial-triage workflow). S-0038 added [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) (product; structural-reference posture extends from SEP-only to a class of curated philosophy reference works). S-0035 added [ADR 0045](adr/0045-shared-state-integrity-discipline.md) (engine; shared-state integrity discipline — subprocess env scrubbing, atomic mempalace mine writes, boot-time health probes). S-0031 added [ADR 0043](adr/0043-hook-architecture.md) (engine; hook architecture) and [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md) (engine; skill conversion partition); amended [ADR 0022](adr/0022-periodic-project-health-checks.md) Decision + Consequences (cadence-trigger logic corrected). S-0029 added [ADR 0041](adr/0041-cascade-analysis-discipline.md) (engine; cascade-analysis discipline) and [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) (engine; soft-warn lifecycle, archive-as-canon). The contract layer the [`build_plan/`](../build_plan/) chunks consume by reference. [`engine/tools/validate.py`](tools/validate.py)'s `adr_index_consistency` check audits the ADR-file-vs-index alignment across both subtree READMEs; the `cascade_*` checks (per ADR 0041) audit superseded-citation currency, back-reference orphans, and Consequences-deliverable promises; `validate_shared_state_health()` (per ADR 0045) audits chromadb palace and git repo sanity.
