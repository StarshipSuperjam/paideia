# Project State

> The single file every session reads first. Names what's current and what's next. Updated at every build-session close.

## Current

| Field | Value |
|---|---|
| **Project** | Paideia — knowledge mastery app on a pedagogical dependency graph |
| **GitHub** | https://github.com/StarshipSuperjam/paideia (private) |
| **Current phase** | Phase 0 — closed (S-0003); **Phase 1 — Contract Lock fully closed at S-0012**. **Phase 1.5 — Mission Realignment Overhaul fully closed at S-0021.** **S-0022** committed the engine / product wall ([ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md)) and renamed `CHANGELOG.md` → `ENGINE_LOG.md`. **Phase 2 (Build Plan Scaffolding) closed at S-0023.** **S-0024–S-0025** executed the folder migration into `engine/` and `product/` subtrees and the cross-reference sweep. **S-0026** authored the third project expression contract per [ADR 0038](adr/0038-expression-contract-for-ai-authored-code.md). **S-0027 widened the expression contract** per [ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md) and **committed the build-readiness gate** per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md). **Phase 3 — SQL Schema Implementation closed at S-0028** with eight migrations deployed to `paideia-dev` (PostgreSQL 17.6); RLS enabled on every public table; CHECK constraints enforce gate T2-A enums. **S-0029 closed the infrastructure-misfire sweep**; **S-0030 landed the first project health-check audit**; **S-0031 closed Session β: harness mechanization** ([ADR 0043](adr/0043-hook-architecture.md), [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md)); **S-0032 closed the MemPalace audit cleanup + improvements** (cadence tightened 30 → 10); **S-0033 closed three coupled mechanical fixes**; **S-0034 closed the Phase 4 build-readiness gate exercise**; **S-0035 closed the engine-hardening pass** ([ADR 0045](adr/0045-shared-state-integrity-discipline.md)); **S-0036 closed the deferral-discipline guardrails**. **S-0039 landed the structural-reference parser tool** ([ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md)). **S-0040 closed the MemPalace wing-filter fix.** **S-0041 closed the cadence-trigger overdue-catchup logic fix + second health check audit.** **S-0042 closed the engine apparatus improvements pass** (ADR 0048, ADR 0049). **S-0043 closed the project venv migration** ([ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md)). **S-0044 closed the routine-mode session pattern foundation** ([ADR 0051](adr/0051-routine-mode-and-engine-loop.md)). **S-0045 closed the Phase 5 master plan** ([ADR 0052](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md), `engine/build_readiness/phase_5.md`, `engine/session/auto_target.json` with 16 tasks). **S-0046 closed the routine-mode harness permission allowlist patch.** **S-0047 closed the settings.json desync recovery.** **S-0048 closed the GitHub Issues backlog cleanup** — 5 issues closed in one session: #6 doc rewrite (Auto mode supersedes the allowlist; allowlist now interactive-only), #5 settings.json sync hard-fail guard via `engine/tools/check_settings_sync.py` + pre-commit wire, #3 boot-time concurrent-session collision guard via `engine/tools/check_session_conflict.py` + session-start hook + boot-procedure docs, #4 closed as intractable with policy bullet in CLAUDE.md "Posture vs machinery" (live mid-session context detection infrastructurally blocked), #7 closed via new `engine/tools/setup_env.py` one-time onboarding helper + boot pointer in session-start.sh + README/seed-chunked-authoring updates + live URL paste validated end-to-end (`migration_applied` correctly reads `supabase_migrations.schema_migrations`; `validate.py` graph audit engages with 30 checks). Backlog 7 → 2 (only upstream MemPalace #1, #2 remain). ADR count corrected at S-0052 audit: **52 ADRs total — 50 Accepted plus 2 Superseded**, partitioned **17 engine + 35 product** (drift since S-0045 when ADR 0052 landed; STATE.md had carried the pre-S-0045 numbers through S-0046, S-0047, S-0048, S-0049, S-0050, S-0051; corrected at S-0052 close). **S-0055 added two engine ADRs**: [ADR 0052 Routine-mode boot freshness and concurrency defense](adr/0052-routine-boot-freshness-and-concurrency-defense.md) and [ADR 0053 Mechanism-first-exercise gate](adr/0053-mechanism-first-exercise-gate.md). Total now **54 ADRs — 52 Accepted plus 2 Superseded**, partitioned **19 engine + 35 product**. Validator clean (23 checks, 0 hard-fails, 2 expected `issue_collision` soft-warns from #1/#2). pytest 562/562 passing (was 519; +43 new tests across check_settings_sync, check_session_conflict, setup_env). |
| **Last build session** | S-0056 (2026-05-05) — **Phase 5 metaphysics specialized seed lands; P5-02b complete; metaphysics subdomain fully seeded.** Fourth routine-mode session against T-PHASE-5; b-half of the metaphysics pre-split per [phase_5.md](build_readiness/phase_5.md) T1-B. Authored [`0036_seed_metaphysics_part1.sql`](../product/seed-graph/migrations/0036_seed_metaphysics_part1.sql) — 25 nodes + 36 edges across the four specialized clusters: **(1) Modality** — modality, possible_worlds, modal_realism (Lewis 1986), ersatz_modal_realism (Plantinga/Stalnaker/Adams), essence_metaphysical (Fine 1994), haecceity, counterpart_theory. **(2) Free will** — free_will, determinism, compatibilism (Frankfurt/Wolf/Fischer-Ravizza), incompatibilism (van Inwagen 1983), libertarianism_metaphysical (Kane/O'Connor), moral_responsibility (Strawson 1962), principle_of_alternative_possibilities (Frankfurt 1969). **(3) Properties / Universals** — universals, realism_about_universals (Armstrong 1989), nominalism, tropes (Williams 1953), bundle_theory. **(4) Mereology** — mereology, composition_mereological (van Inwagen 1990 SCQ), simples, gunk, mereological_universalism (Lewis-Sider), mereological_nihilism (Dorr/Sider/van Inwagen). confidence_level 25/25 INTERPRETED (mirrors P5-01a/P5-01b/P5-02a). graph_version 4 → 5. Migration applied via Supabase MCP `apply_migration`; live verification: 106 total nodes, 138 total edges in `paideia-dev`. **Verification**: `check_target.py --task-id P5-02b` passes both criteria (`migration_applied` + `validate_passes`); `validate.py` 30 checks, 0 hard-fails, 103 soft-warns (101 `missing_rigor_score` expected per phase_5.md T2-C — partial-seed shape with 106 total nodes; 2 `issue_collision`: #1, #2 upstream MemPalace). Cold-review pass at shutdown: 7/7 checks PASS — sub-agent verified node/edge counts, single-tag domain[], graph_version=5 everywhere, transaction wrap, all source/target IDs resolve to in-migration or P5-02a nodes, all forward edges (no back-edges from P5-02b → P5-02a), no cycles, no DDL, no new predicates, granularity-principle adherence, 100% INTERPRETED confirmed. **Two-commit pattern continued**: deliverable commit `8dbd982` (0036_*.sql + ROUTING.md narrative) staged scope-check engaged with 4 checks 0 fails — the staged scope-check engages on deliverables under in_progress task; status flip + close bundled in this commit (operational allowlist only, no escape vector). 0 in-band Issues filed; 0 HANDOFF entries. Sub-range slots 0037-0039 left reserved (granularity principle achieved within 0036). Backlog unchanged: 2 upstream MemPalace #1/#2 remain. **Forward state**: P5-02a + P5-02b complete; metaphysics subdomain fully seeded for cross-bridge consumption at P5-11. Phase 5 progress: 4/16 tasks complete; 4 routine sessions consumed of 18 max_sessions per phase_5.md T1-C. Next routine fire's eligibility walk picks P5-03 Logic in walk order (the broad parallel-eligible tier of P5-03/P5-04a/P5-05/P5-06/P5-07a/P5-08/P5-09/P5-10 all have `depends_on: [P5-01a]` already satisfied). |
| **Prior build session** | S-0055 (2026-05-04) — **Routine-mode hardening pass — Issues #12, #13, #14, #15 all closed in one session.** Substantial engine-apparatus work; no Phase 5 deliverable. **(Issue #15, ADR 0052)** Diagnosis corrected mid-session by user: the original "concurrent eager-claim race" framing was wrong — the prior session had already closed before the loser started. Actual failure mode was boot-time staleness (loser routine read pre-update register_state.json after winner's push to origin/main, re-claimed already-taken slot). The S-0051 staleness check at session-start.sh is informational only; the routine boot procedure had no step to act on it. Three-layer fix: (a) [`engine/tools/routine_boot_freshness.py`](tools/routine_boot_freshness.py) mechanical fast-forward at boot Step 0a (primary; addresses actual failure); (b) [`engine/tools/routine_lock.py`](tools/routine_lock.py) O_EXCL atomic lockfile at Step 0b with stale-after-1h auto-eviction (defense-in-depth per user belt-and-suspenders direction); (c) [`engine/tools/routine_eager_claim_recovery.py`](tools/routine_eager_claim_recovery.py) mechanically-verified bounded reset on push-rejection (residual). Skill `routine-mode-lifecycle` updated with steps 0a, 0b, step-8 push-rejection branch, step-11 lock release. Layer 1 ops doc mirrors with new "Concurrency control" subsection. CLAUDE.md posture carve-out for the recovery script. **(Issue #14)** [`engine/tools/_venv_reexec.py`](tools/_venv_reexec.py) `ensure_venv_python()` re-execs under .venv/bin/python3 when psycopg isn't importable. Wired into `check_target.py`, `validate.py`, `setup_env.py` tops. Subtle bug fix discovered during smoke testing: pass the symlink path (not resolved path) as argv[0] so Python uses pyvenv.cfg to bootstrap site-packages — resolving the symlink loads the underlying interpreter without venv site-packages. **(Issue #13)** [`engine/tools/audit_archive_structured_fields.py`](tools/audit_archive_structured_fields.py) hard-fails session close when `outcome_summary_soft_warns` is absent or null in current.json. Pre-commit hook fires on closing commits via `git show :engine/session/archive/S-NNNN.json | python3 ... --from-stdin`. Symmetric in shape to `audit_handoff_dispositions.py`. Defends ADR 0042's persistent-warn surface against silent field-absence lapses (S-0043→S-0047 pattern). Step 6b in shutdown sequence. **(Issue #12, ADR 0053)** Mechanism-first-exercise gate adopted — analogous to ADR 0040's build-readiness gate but mechanism-triggered. Resolves all four open sub-questions: disjunctive trigger criterion (new session mode / new validator soft-warn category / new state file / Consequences spans ≥3 ops docs OR ≥5 tools); same Tier 1/2/3 triage; reports at `engine/build_readiness/<mechanism>_first_exercise.md`; one gate per qualifying mechanism. Cross-cutting ADRs going forward must include trigger-criterion evaluation block. **Pre-work**: local main + worktree branch were stuck on loser commit `2f4b088`; user-confirmed bounded `git reset --hard origin/main` recovered both before eager-claim. **Verification**: 39 new tests across 5 new tools, all passing; validate.py 30 checks 0 hard-fails 78 soft-warns (2 issue_collision = upstream MemPalace #1/#2; 76 missing_rigor_score expected per phase_5.md T2-C). New audit dry-run PASS. **Out of scope (noted)**: orphan `claude/mystifying-easley-3f3789` branch + worktree from Issue #15 still need manual cleanup (`git push origin --delete` + `git worktree remove --force`); mechanizing freshness gate for /start-engine interactive boot; back-port of ADR 0053's trigger-criterion block to existing cross-cutting ADRs (next-touch only). **Forward state**: Phase 5 routine resumes from P5-02b (Metaphysics specialized). Routine cadence can now be tightened safely thanks to ADR 0052 lockfile layer. |
| **Prior-prior build session** | S-0054 (2026-05-04) — **Phase 5 metaphysics core seed lands; P5-02a complete.** Third routine-mode session against T-PHASE-5; first metaphysics migration after the epistemology subdomain fully closed (P5-01a + P5-01b at S-0050/S-0051/S-0053). Authored [`0030_seed_metaphysics_part1.sql`](../product/seed-graph/migrations/0030_seed_metaphysics_part1.sql) — 27 nodes + 30 edges across the four core pillars per [phase_5.md](build_readiness/phase_5.md) T1-B (being/ontology, identity, causation, time). Clusters: **(1) Being/ontology** — metaphysics, ontology, existence, substance, property, relation, event, abstract_object, concrete_object. **(2) Identity** — numerical_identity, leibniz_law, persistence, endurantism, perdurantism, temporal_parts, ship_of_theseus. **(3) Causation** — causation, humean_regularity_theory, counterfactual_theory_of_causation, causal_powers. **(4) Time** — time, a_theory_of_time, b_theory_of_time, presentism, eternalism, growing_block_theory, mctaggarts_paradox. confidence_level 27/27 INTERPRETED (mirrors P5-01a/P5-01b distribution). graph_version 3 → 4. Migration applied via Supabase MCP `apply_migration`; live verification: 81 total nodes, 102 total edges in `paideia-dev`. **Verification**: `check_target.py --task-id P5-02a` passes both criteria (`migration_applied` + `validate_passes`); `validate.py` 30 checks, 0 hard-fails, 81 soft-warns (76 `missing_rigor_score` expected per phase_5.md T2-C — partial-seed shape with 81 total nodes, formula needs more topology context; 5 `issue_collision`: #1, #2 upstream MemPalace + #12, #13 from S-0052 audit + new #14 filed this session). Cold-review pass at shutdown: 17/17 invariants PASS — sub-agent verified node/edge counts, single-tag domain[], graph_version=4 everywhere, transaction wrap, all source/target IDs resolve, no cycles (clean DAG to L7), no DDL, no new predicates, granularity-principle adherence, specialized concepts (modality, free will, universals, tropes, bundle theory, mereology) correctly absent, pedagogical-quality sample (Lewis 1973/2000 counterfactual causation, McTaggart 1908 paradox, endurantism/perdurantism, Humean regularity, Leibniz's Law, Quine's ontology) accurate. **Two-commit pattern adopted (S-0053 finding validated)**: deliverable commit `1920b4a` (0030_*.sql + ROUTING.md) staged scope-check engaged with 4 checks 0 fails — confirming the staged scope-check engages on deliverables under in_progress task when split from status-flip; status flip + close bundled in a single close commit (operational allowlist only, no escape vector). **In-band finding**: routine-fired `check_target.py` / `validate.py` silently FAIL when venv not on PATH (system Python lacks psycopg → false-FAIL on `migration_applied`). Filed as [Issue #14](https://github.com/StarshipSuperjam/paideia/issues/14) (`tech-debt`) per user direction; fix outside P5-02a scope_lock. Three candidate fixes named (auto-reexec under venv Python in tool main, doc-only requirement to source scrub_env.sh, wrapper shell scripts) — adjudication deferred to next interactive session. **Forward state**: P5-02a done unblocks P5-02b (Metaphysics specialized; depends_on [P5-01a, P5-02a] now satisfied); next routine fire's eligibility walk picks P5-02b in walk order. |
| **Prior-prior build session** | S-0053 (2026-05-04) — **Phase 5 epistemology specialized seed lands; P5-01b complete.** Second routine-mode session against T-PHASE-5 (first since S-0050's P5-01a + S-0051's verifier fix). Authored [`0016_seed_epistemology_part1.sql`](../product/seed-graph/migrations/0016_seed_epistemology_part1.sql) — 26 nodes + 38 edges across seven specialized clusters: social epistemology (5 nodes), virtue epistemology (4 nodes), formal/Bayesian epistemology (5 nodes), reliabilism (2 nodes), knowledge-first / understanding / evidentialism (3 nodes), skepticism varieties beyond cartesian (5 nodes), norms (2 nodes — norm_of_assertion + closure_denial). 21 cross-references from P5-01a foundational anchors into specialized concepts; 17 within-cluster edges among the 26 new nodes; all `pedagogical_prerequisite`; all within-domain epistemology. graph_version 2 → 3. confidence_level: 26/26 INTERPRETED (mirrors P5-01a). Migration applied via Supabase MCP `apply_migration`; live verification: 54 total nodes, 72 total edges in `paideia-dev`. **Verification**: `check_target.py --task-id P5-01b` passes both criteria (`migration_applied` + `validate_passes`); `validate.py` 30 checks, 0 hard-fails, 54 soft-warns (50 `missing_rigor_score` expected per phase_5.md T2-C, 4 `issue_collision` carryover from #1, #2, #12, #13). Cold-review pass: matches contract end-to-end (sub-agent confirmed node/edge counts, single-tag domain[], graph_version=3 everywhere, transaction wrap, all source/target IDs resolve, no cycles, no DDL, no new predicates). Per-session ROUTING.md narrative entry appended. P5-01b status flipped pending → in_progress (eager-claim) → complete (post-verify). **Procedural finding (in-band)**: the substantive deliverable commit also flipped P5-01b status to `complete` in auto_target.json, so the pre-commit `check_routine_scope.py --staged` went into "no in_progress task; skipping" mode — bypassing the staged scope-check at the moment scope-check should engage. The actual paths committed (the migration file at `0016_*.sql`, ROUTING.md, status field on auto_target.json) are all individually within scope_lock or operational allowlist, so no real escape; finding is procedural-ideal (separate the deliverable commit from the status-flip commit so the deliverable commit's scope-check engages). Filed as future-session-relevant in outcome_summary; no Issue (the next routine session can adopt the two-commit pattern naturally without a tracked artifact). **Backlog 2 → 2** (no new Issues; #1, #2 upstream MemPalace, #12 tech-debt, #13 health-check-finding remain). **Forward state**: P5-01b done → no new tasks unblocked (P5-02a/b through P5-10 already eligible after S-0051's `depends_on: [P5-01a]` resolution); the next routine fire picks the next eligible by ordering. |
| **Prior-prior build session** | S-0052 (2026-05-04) — **Third project health-check audit (cadence-overdue catch-up; window S-0042 → S-0051).** First cadence audit since the S-0041 overdue-catchup logic landed; the mechanism delivered the behavior its ADR amendment promised — trigger fired correctly at S-0051 (deferred to consume Issue #9/#10 slot), re-fired at S-0052 with overdue-by-1 surface, audit accepted. Three non-obvious findings the scanners couldn't produce: **(A) Routine-mode prerequisites surfaced one-at-a-time across S-0048→S-0051** (Issues #7/#8/#9/#10 each closed in succession). The build-readiness gate at S-0034 was scoped to Phase 4; routine-mode got no equivalent pre-flight. Filed as [Issue #12](https://github.com/StarshipSuperjam/paideia/issues/12) (`tech-debt`) — mechanism-first-exercise gate consideration; not urgent (next mechanism of comparable scope isn't on immediate roadmap), worth deciding before next routine-mode-tier mechanism lands. **(B) `transcript_token_pct` has been quietly producing impossible >1.0 numbers in 30% of audit-window archives** (S-0044=1.11, S-0046=1.62, S-0047=1.70). The field is upper-bound cumulative content; the AI nearly skipped quality gates on a false-pressure misread at S-0051 close (Issue #11). Fixed inline per CLAUDE.md "Default to fix-in-context": reframed [`engine/tools/scan_context_telemetry.py`](tools/scan_context_telemetry.py) output line (`tokens~N pct=X.XXXX` → `session_total_tokens~N (upper-bound; cached+compacted included; not live prompt pressure)`); dropped now-stale `# type: ignore[import-not-found]` on tiktoken (venv has it; mypy --strict flagged it as `unused-ignore`); removed unused `pct` local; added CLAUDE.md "Posture vs machinery" bullet naming the cumulative-vs-live distinction; annotated [`engine/operations/session-shutdown-sequence.md`](operations/session-shutdown-sequence.md) step 7b and [`engine/operations/health-check.md`](operations/health-check.md) Session-load trend section pointing at the >1.0 case and Issue #11. Issue #11 closed. **(C) Persistent-warn surface was structurally unable to fire across half the audit window** — `outcome_summary_soft_warns` absent in 5 consecutive archives (S-0043 → S-0047). The mechanism cannot detect its own missing input. Filed as [Issue #13](https://github.com/StarshipSuperjam/paideia/issues/13) (`health-check-finding`) — structured-soft-warns field-presence audit at session shutdown; symmetric in shape to `audit_handoff_dispositions.py` per ADR 0048; cross-session deferral via Issue is the right pattern (new tool + tests + ops-doc wiring expand audit scope). Plus inline state correction: **STATE.md ADR count drift** corrected from 51 / 16 engine to 52 / 17 engine (had carried pre-S-0045 numbers across 7 STATE.md edits since [ADR 0052](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md) landed). 22 dead-weight scanner candidates triaged with explicit dispositions (22 preserve with reasons; 1 sub-action — OQ-BYOK reference inside ADR 0029 sentence-level edit folded into next cleanup-batch). Cadence calibration: keep at 10; re-evaluate at S-0062. **Verification**: validate.py 23 checks, 0 hard-fails, 3 expected `issue_collision` soft-warns from #1/#2/#11 (the audit's own commit triggered #11 collision; closing the Issue clears it). Report: [`docs/health-checks/S-0052.md`](../docs/health-checks/S-0052.md). Scanner output: [`engine/health_check/dead_weight_candidates_S-0052.md`](health_check/dead_weight_candidates_S-0052.md). Backlog 1 → 2 (closed #11; filed #12 + #13; #1, #2 upstream remain). |
| **Prior-prior build session** | S-0051 (2026-05-04) — **Issue #9 fix unblocks P5-01a; Issue #10 boot-staleness check added in-session per no-cascade direction; label optimization audit (taxonomy + boot-surface fix).** Three coupled work units. **(A) Issue #9 fix**: one-line column swap in [`engine/tools/check_target.py:114`](tools/check_target.py) (`WHERE version = %s` → `WHERE name = %s`). Regression test [`test_migration_applied_queries_name_column`](tools/test_check_target.py) mocks psycopg.connect and asserts the SQL references the `name` column. Live: `check_target.py --task-id P5-01a` reports both criteria PASS. P5-01a flipped to `complete` in [`engine/session/auto_target.json`](session/auto_target.json); `blocked_reason` cleared. HANDOFF S-0050 entry marked Resolved. P5-01b through P5-10 now eligible — routine resumes. **(B) Issue #10 fix (in-band)**: S-0051 boot in worktree `great-wright-9dd19b` saw stale `register_state.json` showing S-0050 in_progress; investigation revealed S-0050's close commit `34b51f2` had pushed cleanly to origin/main but this worktree's local HEAD predated the push. Real bug surfaced: boot-time has no worktree-staleness check. Added new early-boot section to [`engine/tools/hooks/session-start.sh`](tools/hooks/session-start.sh) — runs `git fetch --no-tags --quiet origin main`, compares HEAD via `merge-base --is-ancestor`, emits LOUD attention block with the recovery command (`git fetch && git merge --ff-only origin/main`) when behind. Best-effort; never blocks boot. Filed Issue #10 then fixed in same session per user direction "no more session cascade for every bug — fix it here." **(C) Label optimization**: audit found 5 of 14 labels load-bearing, 2 uncovered scenarios (`documentation` doc-heavy work, `question` in-band uncertainties), 5 dormant stock labels, plus 2 real boot-surface defects (FYI line emitted only 4 of 6 collected categories; bug + upstream issues folded into bug count without disambiguation). Extended [`engine/operations/issue-discipline.md`](operations/issue-discipline.md) taxonomy to 8 type labels (added `documentation`, `question` with authoring procedures); new "Reactive-only labels" section frames `duplicate`/`invalid`/`wontfix` as triage-time-applied; dropped `good first issue`/`help wanted` via `gh label delete` (don't fit solo-developer + AI workflow). Rewrote [`engine/tools/scan_issue_backlog.py`](tools/scan_issue_backlog.py) `format_fyi_line` to emit 7 type categories, subtract upstream-tagged bugs from bug count, surface `upstream` separately as `K upstream-blocked` in the parenthetical. New `count_upstream_bugs` helper plus 2 new tests cover subtraction semantics including floor-at-zero defensive case. Appended ADR 0048 Consequences amendment recording the correction (workflow change, not supersession). [`CLAUDE.md`](CLAUDE.md) fix-in-context section's label routing list extended to match. Live boot surface verified: `0 bugs, 0 tech-debt, 0 cleanup, 0 enhancement, 0 health-check, 0 docs, 0 question (0 urgent; 2 upstream-blocked)`. **Verification**: pytest 580/580 (was 576; +1 regression test for #9, +3 new tests for upstream-bug subtraction). validate.py: 30 checks, 0 hard-fails, 30 soft-warns (1 health_check_overdue: S-0052 will be 11 slots since S-0041, cadence 10, overdue by 1 — surface for next session; 4 issue_collision; 25 missing_rigor_score expected per phase_5.md T2-C). 12 GitHub labels remain: 8 type + 1 priority + 3 reactive-only. Backlog 3 → 0 in-project actionable; 2 upstream remain (#1, #2 MemPalace). |
| **Prior-prior build session** | S-0050 (2026-05-04) — **First Phase 5 routine-mode session: P5-01a Epistemology core seed lands; verifier bug surfaced (Issue #9).** First routine-mode session against `T-PHASE-5`. **Output**: (1) [`product/seed-graph/migrations/0011_seed_epistemology_part1.sql`](../product/seed-graph/migrations/0011_seed_epistemology_part1.sql) — 28 nodes + 34 edges covering the analysis-of-knowledge tradition core (foundational layer: belief, truth, evidence; analysis spine: epistemic_justification, propositional_knowledge, knowledge_how, knowledge, justified_true_belief; Gettier and four standard responses; modal conditions: sensitivity, safety; access and structural dimensions of justification: foundationalism, coherentism, infinitism, internalism, externalism, basic_belief; knowledge-dependent peripheral: epistemic_closure, testimonial_knowledge, fallibilism, certainty, skepticism_epistemic, cartesian_skepticism). All 28 nodes `confidence_level=INTERPRETED` per ADR 0030; all 34 edges `pedagogical_prerequisite`; cross-domain edges deferred to P5-11 per phase_5.md T2-G #1. `graph_version` 1 → 2 in same transaction as the 62 INSERTs. (2) [`product/seed-graph/migrations/ROUTING.md`](../product/seed-graph/migrations/ROUTING.md) — per-session narrative entry. (3) [Issue #9](https://github.com/StarshipSuperjam/paideia/issues/9) (`bug`, `priority:urgent`) — `engine/tools/check_target.py:114` queries `WHERE version = %s` against `supabase_migrations.schema_migrations` but `version` is the timestamp; auto_target.json `id` is the migration name. One-line fix (column swap to `name`) is outside P5-01a's `scope_lock`, so routine-mode posture filed Issue + marked task `blocked: scope-expansion-needed` rather than reaching outside scope. (4) HANDOFF.md entry naming the bug, fix, and next-session action. **Verification**: migration applied via Supabase MCP `apply_migration` (recorded in schema_migrations as `version=20260504192422 name=0011_seed_epistemology_part1`); validate.py with venv Python (graph audit engaged) reports 30 checks, 0 hard-fails, 27 soft-warns (25 `missing_rigor_score` expected for partial seed per phase_5.md T2-C; 2 `issue_collision` carryover from upstream MemPalace #1/#2). **State**: P5-01a `blocked: scope-expansion-needed`; P5-01b through P5-10 stay `pending` because their `depends_on` includes `P5-01a` which must be `complete` before any sibling becomes eligible — routine-mode `_select_task` will return no eligible task on the next fire (HANDOFF-only, no slot churn). Backlog 2 → 3 (#1, #2 upstream MemPalace + new #9). |
| **Prior-prior build session** | S-0049 (2026-05-04) — **Issue #8 fix: walk-up `.env` loader for engine tools.** Surfaced post-S-0048 by the would-be-S-0049 routine fire (commit fb137c4, pre-claim HANDOFF entry): the S-0048 setup-helper closed the dashboard-only-password discoverability gap for *interactive* sessions in the main repo, but two gaps blocked routine-mode entirely — (a) `.env` is gitignored so `git worktree add` does not propagate it to `.claude/worktrees/<branch>/`, and (b) neither `check_target.py:103` nor `validate.py:2325` calls `load_dotenv` (both read `os.environ.get` directly). Net effect: every Phase 5 routine fire failed `migration_applied` for all 16 tasks. **Output**: (1) [`engine/tools/load_env.py`](tools/load_env.py) — `find_dotenv` walks up from cwd up to a 10-ancestor depth cap looking for `.env`; `load_dotenv` parses (reusing `parse_env` from S-0048's `setup_env.py`) and applies to `os.environ` for missing keys (override defaults False, so explicit `KEY=value python3 ...` invocations still win); `load_dotenv_walk_up` combines the two as the standard call site for tool main(). Diagnostic `__main__` mode prints located `.env` + populated keys. (2) [`engine/tools/test_load_env.py`](tools/test_load_env.py) — 14 tests covering walk-up traversal (start hit / nested dirs / nearest-wins / depth cap / cwd default / none-when-absent), load_dotenv (sets missing / preserves pre-set / overrides on flag / skips empty / ignores comments and blanks), load_dotenv_walk_up (no-env empty / idempotent / worktree-shaped tree). (3) [`engine/tools/check_target.py`](tools/check_target.py) `main()` and [`engine/tools/validate.py`](tools/validate.py) `main()` — `load_dotenv_walk_up()` called before argparse so subsequent `os.environ.get` reads see `SUPABASE_DB_URL`. (4) [`engine/tools/validate.py`](tools/validate.py) graph audit — psycopg `ImportError` flipped from hard-fail to soft-skip (records `graph_audit_skipped` + stderr note); the walk-up auto-loads the URL even in pre-commit's system-Python context (no psycopg there), so import failure is now a legitimate "audit not applicable" disposition rather than a user mistake. (5) HANDOFF entry from would-be-S-0049 routine fire updated with `**Resolved:** S-0049 (commit 21285f8)` line. **Live verification (from this worktree, no local `.env`, `SUPABASE_DB_URL` unset in shell)**: `check_target.py --task-id P5-01a` correctly queried the DB and reported `migration_applied` FAIL with "migration 0011_seed_epistemology_part1 not in schema_migrations" (URL loaded via walk-up; DB queried; migration is genuinely absent — Phase 5 hasn't started). `validate.py` ran 30 checks (was 23) — graph audit engaged. Validator clean: 23 checks default-mode, 0 hard-fails, 3 expected `issue_collision` soft-warns at the close commit. pytest 576/576 (was 562; +14 new). ADR count unchanged at 51. **Routine ready to resume**: user can toggle `paideia-engine-loop` back to Hourly. |
| **Prior-prior build session** | S-0048 (2026-05-04) — **GitHub Issues backlog cleanup; 5 issues closed (#3, #4, #5, #6, #7).** Five-phase build session resolving every in-project actionable issue plus closing one as intractable with a documented policy. **Phase B (Issue #6)**: rewrote `engine/operations/routine-mode-operations.md` "Harness permission allowlist" subsection — operational stance is now **Auto** permission mode for routine sessions; project-level safeguards (`check_routine_scope.py --plan/--staged`, master-plan-integrity, `audit_handoff_dispositions.py`, validate.py soft-warns, eager-claim atomic protocol) are the load-bearing anti-rogue layer; allowlist remains useful for interactive sessions only. S-0046 same-commit pairing rule retired. CLAUDE.md mechanization bullet rewritten to match. Skipped infeasible `routine_mode_misconfigured` validate.py soft-warn (no introspection path for permission mode). **Phase C (Issue #5)**: new `engine/tools/check_settings_sync.py` (50 LOC) + `engine/tools/test_check_settings_sync.py` (12 tests) — hard-fails any commit from a worktree where main's `.claude/settings.json` differs from the worktree's tracked copy AND the worktree's copy is not staged. Wired into pre-commit hook. New "Editing settings.json from a worktree" subsubsection in routine-mode-operations.md documents the cp procedure. Synthetic e2e verified (deliberate divergence triggers exit 1 with cp-procedure hint; restore brings exit 0). **Phase D (Issue #3)**: new `engine/tools/check_session_conflict.py` (180 LOC) + `engine/tools/test_check_session_conflict.py` (16 tests) — three dispositions (no-conflict / recent <1h or ambiguous 1h-24h / stale >24h-recovery). Wired into both `engine/tools/hooks/session-start.sh` (informational surface) and `/start-engine` boot procedure step 4b (refusal point). Real e2e against in-flight S-0048 confirmed correct identification. routine-mode-operations.md "Mechanical safeguard" section flipped from "planned" to "landed at S-0048". **Closure (Issue #4)**: new policy bullet in CLAUDE.md "Posture vs machinery" → "postures held by AI discipline" naming the operating policy (user manually monitors context load; sessions archived with `transcript_token_pct >= 0.70` get post-hoc review). Issue closed with policy as comment. **Phase A (Issue #7)**: new `engine/tools/setup_env.py` (180 LOC) + `engine/tools/test_setup_env.py` (15 tests) — one-time interactive `.env` setup helper validating SUPABASE_DB_URL with real `psycopg.connect()`. New boot pointer in session-start.sh fires when `auto_target.json` present AND `.env` missing SUPABASE_DB_URL. README + seed-chunked-authoring docs updated. **Live populate**: user added URL via direct `.env` edit; psycopg connected to PostgreSQL 17.6 successfully; check_target.py P5-01a confirmed `migration_applied` correctly reads schema_migrations (returns not-applied as expected — Phase 5 hasn't started); validate.py graph audit engages (30 checks vs 23 prior). Validator clean. pytest 562/562. ADR count unchanged. |
| **Prior-prior build session** | S-0047 (2026-05-04) — **Settings.json desync patch.** Lands S-0046's missing primary deliverable. Root cause now mechanically guarded by S-0048's `check_settings_sync.py`. |
| **Prior-prior build session** | S-0046 (2026-05-04) — **Routine-mode harness permission allowlist patch.** Approach superseded at S-0048: routine sessions run Auto mode; allowlist remains for interactive sessions. |

| **Prior-prior build session** | S-0044 (2026-05-04) — **Routine-mode session pattern foundation per ADR 0051; Paperclip rejected.** Third session mode (alongside exploration and build); two-layer architecture (master plan session + engine loop session); six-layer defense in depth. Tools: `check_target.py` (5 criterion types + predicate registry), `check_routine_scope.py` (plan/staged scope-check, master-plan integrity, segment-aware glob matching). Schema doc `auto_target.schema.md`. Ops doc `routine-mode-operations.md`. Pre-commit hook gains routine-mode scope-check + master-plan-integrity. Two new validate.py soft-warns. Issue #3 filed: `/start-engine` boot should detect mid-routine `current_status: in_progress` and refuse to claim. Validator clean; pytest 516/516. |

| **Prior-prior build session** | S-0043 (2026-05-04) — **Project venv migration with hook PATH wiring per ADR 0050.** uv-managed venv at `<main_repo>/.venv/`; Python 3.12 pinned via tracked `.python-version`; idempotent sourcing-time PATH prepend in `engine/tools/scrub_env.sh`. 8 deps explicit in `engine/tools/requirements.txt` (chromadb, tiktoken, mempalace 3.3.4 plus pre-existing). Mempalace 3.3.4 verified against upstream Issues #1 and #2 — both still upstream-unfixed. CLAUDE.md mechanization-list gains the venv bullet. |

| **Last commit on main** | (see `git log --oneline -1` on main) |
| **Backup tags** | `pre-foundation-v0.0.0` at commit `fa70b8c` — pre-machinery anchor. `pre-phase-3-v0.0.1` at commit `36b0907` (S-0026 close) — pre-Phase-3 anchor cut at S-0027. Both are historical anchors, not operational fallback targets — file-level retrieval via `git show <tag>:<path>` is the legitimate use. |

## Infrastructure

| Component | Value |
|---|---|
| **Supabase project** | `paideia-dev`, project ref `ozooosgnuzxqqypotlke`, PostgreSQL 17.6 |
| **Supabase MCP** | configured in `.mcp.json` (gitignored, contains PAT) |
| **MemPalace** | installed at `/Users/shanekidd/Library/Python/3.9/bin/`; MCP server `mempalace-mcp` configured in `.mcp.json`; wing `paideia` carries **485 drawers across 5 rooms** as of S-0032 close (general 330, operations 88, decisions 55, foundation-planning-s0001 10, s0003-adr-collection 2); capture hooks wired in `.claude/settings.json` (Stop + PreCompact wrapper PATH-fixed at S-0032 — auto-capture had been silently failing since S-0002 because `mempalace` CLI wasn't on the hook subshell's PATH; verify at next Stop event by checking `.claude/logs/mempalace-hook.log` for `OK exit=0` lines instead of `FAIL exit=127`); project usage scope documented at [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Project usage scope" — uses search, add_drawer, diary (agent_name `claude`), read-only inspection; explicitly excludes KG and tunnels |
| **Python** | **project venv** at `<main_repo>/.venv/` (Python 3.12.13, uv-managed) per [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md) — engine tools + mempalace run from here; PATH wiring in `engine/tools/scrub_env.sh` resolves bare `python3` / `ruff` / `mypy` / `pytest` / `mempalace` to venv. Pre-S-0043 fallback: system Python 3.9.6 at `/usr/bin/python3`; user-scope packages at `/Users/shanekidd/Library/Python/3.9/bin/` (no longer used by engine) |
| **Anthropic API** | env var `ANTHROPIC_API_KEY` set in local `.env` (gitignored) |

## Next session work item

**S-0057 — Next routine fire continues Phase 5 dispatch from P5-03 Logic.** S-0056 closed P5-02b; metaphysics subdomain fully seeded (P5-02a + P5-02b complete). The remaining parallel-eligible tier — all of which have `depends_on: [P5-01a]` already satisfied — is P5-03 Logic, P5-04a Ethics metaethics+normative, P5-04b Ethics applied (depends on P5-04a), P5-05 Political philosophy, P5-06 Aesthetics, P5-07a Philosophy of mind core, P5-07b Philosophy of mind specialized (depends on P5-07a), P5-08 Philosophy of language, P5-09 Philosophy of science, P5-10 Service nodes. Walk-order pick is **P5-03 Logic** — range `0090-0099`, first migration `0090_seed_logic_part1`, scope covers modality (modal logic), conditionals, paradox, vagueness, paraconsistent, deontic per phase_5.md T2-A. Cross-bridges (P5-11) and closeout (P5-12) still wait on the full subject + service tier per phase_5.md T1-D. **Boot infrastructure stable**: ADR 0052 Step 0a/0b boot freshness + lockfile, Issue #14 venv auto-reexec, Issue #13 archive structured-fields audit all wired. **Backlog**: 2 upstream-blocked (#1, #2 MemPalace); 0 in-project actionable. One sub-action folded into next cleanup-batch: OQ-BYOK reference inside [`product/adr/0029-personal-financial-cost-ceiling.md`](../product/adr/0029-personal-financial-cost-ceiling.md) — a sentence-level edit noting OQ-BYOK was resolved at S-0012 per [ADR 0032](../product/adr/0032-personal-project-disposition.md). Pre-existing orphan from Issue #15 (worktree `mystifying-easley-3f3789` + remote branch) needs user-adjudicated cleanup.

**Cadence calibration after S-0052.** Cadence remains 10. Re-evaluate at S-0062 (the next natural-cadence slot). The S-0052 audit produced 2 inline fixes + 2 filed Issues + 1 cleanup-batch sub-action + 22 dead-weight preservations with explicit reasons — meaningful action list, not "consistently no action." Lowering to 7 is plausible if Finding-C-class structural-discipline lapses recur; raising to 15 is plausible if the next audit's action list is small AND Finding-A-class meta-observations have settled.

**Prerequisites — all landed at S-0049 close.**
1. `SUPABASE_DB_URL` is reachable from any worktree via [`engine/tools/load_env.py`](tools/load_env.py) walk-up loader, wired into `check_target.py` and `validate.py` `main()`. Verified live (S-0049): `check_target.py` from this worktree (no local `.env`) correctly queried the DB; `validate.py` graph audit engaged with 30 checks. Pre-S-0049 the Phase 5 routine fired at would-be-S-0049 and pre-claim-bailed because of this gap.
2. Routine permission mode set to **Auto** in the Claude Code Routines UI per the S-0048-revised `engine/operations/routine-mode-operations.md` "Harness permission allowlist" subsection.
3. Concurrent-session collision protection in place per [`engine/tools/check_session_conflict.py`](tools/check_session_conflict.py).
4. `.claude/settings.json` worktree-vs-main sync mechanically guarded per [`engine/tools/check_settings_sync.py`](tools/check_settings_sync.py).

**Verification path before going Hourly.** Recommended: one manual routine fire first, observed end-to-end. Under Auto permission mode the procedure walks through without any prompt firing (target-met check → eligibility → plan authoring → scope-check → claim → task work → criteria verify → shutdown). If anything misfires under Auto, that's a real anomaly worth diagnosing before going unattended.

**Each routine session reads [`engine/build_readiness/phase_5.md`](build_readiness/phase_5.md) at boot** as the gate report covering the full Phase 5 batch. If any Tier 1 finding shows unresolved (cited ADR not Accepted, cited canon-file content missing, etc.), the routine session refuses the task and writes HANDOFF.

**16 tasks total** (per ADR 0052's 9-subject decomposition + service nodes + cross-bridges + closeout, with epistemology/ethics/metaphysics/mind pre-split into core/specialized halves). max_sessions: 18; paused: false. Sequencing: P5-01a anchor → P5-01b + 8 other subjects + service nodes parallel-eligible → P5-11 cross-bridges (after all subjects + services) → P5-12 closeout.

**Interactive sessions remain available.** A user may invoke `/start-engine` for non-routine work (engine apparatus changes, urgent fixes, master-plan revisions per ADR 0051's revision discipline). Issue #3 tracks an open enhancement: `/start-engine` should detect mid-routine `current_status: in_progress` and refuse to claim. Until #3 lands, mixing modes requires manual coordination — pause routine before starting interactive work and unpause after closing.

### Source documents (routine-mode boot, any Phase 5 task)

- [`engine/build_readiness/phase_5.md`](build_readiness/phase_5.md) — gate report; covers Tier 1/2/3 triage for all 16 tasks.
- [`engine/session/auto_target.json`](session/auto_target.json) — executable contract.
- [`engine/operations/routine-mode-operations.md`](operations/routine-mode-operations.md) — routine-mode session procedure (Layer 1 reference).
- [`engine/operations/seed-chunked-authoring.md`](operations/seed-chunked-authoring.md) — per-routine-session migration workflow.
- [`product/docs/content-strategy.md`](../product/docs/content-strategy.md) — Phase 4.5 input dataset survey (Cross-Domain Reference Inventories).
- [`product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md`](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) — structural-reference posture for the philosophy reference class.
- [`product/adr/0052-phase-5-philosophy-subdomain-decomposition.md`](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md) — 9-subdomain decomposition.
- [`engine/adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md`](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) — parser tool (ad-hoc consultation).
- [`engine/tools/parse_structural_reference.py`](tools/parse_structural_reference.py) — the parser tool itself.
- [`product/docs/architecture.md`](../product/docs/architecture.md), [`product/docs/learner-model.md`](../product/docs/learner-model.md) — the contracts the seed graph encodes.
- [`product/seed-graph/migrations/PREDICATE_MANIFEST.md`](../product/seed-graph/migrations/PREDICATE_MANIFEST.md), [`product/seed-graph/migrations/ROUTING.md`](../product/seed-graph/migrations/ROUTING.md) — predicate registry; migration prefix scheme (extended at S-0045).

**Parser tool available for ad-hoc use.** Per [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md). Runnable whenever a structural-reference source has been acquired (currently SEP/IEP via the HTML adapter; PDF adapter deferred to first-PDF-source-acquisition). CLI: `python3 engine/tools/parse_structural_reference.py --source <path> --document-type encyclopedia --output -`. The brief is discarded after triage; the graph is the only durable artifact.

**MemPalace wing-filter / wing-naming bugs unfixed** (Issues [#1](https://github.com/StarshipSuperjam/paideia/issues/1), [#2](https://github.com/StarshipSuperjam/paideia/issues/2)) against mempalace 3.3.4. Workaround: unfiltered `mempalace_search`. See [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Known issues".

**Phase 4.5 input dataset survey is closed at S-0038.** Phase 5 sub-sessions inherit both [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) and [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) at boot per ROADMAP §5.2.

**Phase 4 graph validation remains implementation-closed at S-0037.** The validator code, manifests, routing, and ops-doc are all in place. Two ROADMAP §4 success criteria — `<3s on a 100-node test seed` and `pre-commit hook blocks seed-authoring commits that fail audit` — require real seed content to verify and exercise naturally at the first Phase 5 seed routine session (P5-01a Epistemology core). `SUPABASE_DB_URL` was populated and verified end-to-end at S-0048 (`psycopg.connect()` against PostgreSQL 17.6 successful; `validate.py` graph audit engages with 30 checks vs 23 prior — confirming the 7 graph-audit checks now run). The first routine session that runs `validate.py` after a `supabase db push` will exercise the audit end-to-end against real seed content for the first time.

**Boot-time check: cadence trigger (overdue-catchup per ADR 0022 amendment at S-0041).** Cadence is 10. `last_audit_session = "S-0052"` (bumped at S-0052 close). The trigger next fires at S-0062 boot (`slots_since = 10`). The post-S-0041 mechanism delivered the behavior its ADR amendment promised across the S-0051/S-0052 sequence — fired correctly at S-0051, deferred cleanly to S-0052 once the user consumed the natural slot for Issue #9/#10 work, re-fired at S-0052 with the overdue surface, audit landed, trigger cleared.

**Boot-time check: Issues backlog (added at S-0042 per ADR 0048).** As of S-0055 close, backlog: **2 bugs (Issues #1, #2 — wing-filter and wing-naming; both `bug + upstream` awaiting upstream MemPalace fixes — no in-project actionable bugs)**; 0 tech-debt; 0 cleanup; 0 enhancement; 0 health-check-finding; 0 docs; 0 question; **0 urgent**. S-0055 closed Issues #12, #13, #14, #15 (one session, four closures). Backlog 5 → 0 in-project actionable; 2 upstream-blocked remain.

**Boot-time check: scope-collision detection.** `validate.py` default mode runs `scan_issue_collisions.py`. Routine-mode sessions: expect issue_collision to fire on the seed-authoring scope keywords (Issues #1/#2/#3/#4 may all match). Persistent-warn threshold tripped per [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) at S-0044 close (3-of-5 archives showing `issue_collision`); annotated as expected per scope breadth.

**Boot-time check: scope-erosion signal (added at S-0042 per ADR 0049).** `session-start.sh` walks recent archives and counts `scope_delivery.delivered == false`. Surface fires at 3-of-5. As of S-0056 close, recent archives (S-0052, S-0053, S-0054, S-0055, S-0056) — all five `scope_delivery.delivered` are `true`. Signal silent (0-of-5).

**Boot-time check: structured-archive-fields calibration window (per ADR 0042).** As of S-0056 close, the persistent-warn surface checks the last 5 archives for the `outcome_summary_soft_warns` structured field. S-0052 → S-0056 all structured (5/5) — calibration window satisfied. The `audit_archive_structured_fields.py` audit (S-0055) fires from the pre-commit hook on closing commits and hard-fails any close where the field is absent or null. Future field-absence lapses (S-0043 → S-0047 pattern) are structurally prevented.

**Boot-time check: shared-state health probe.** Per [ADR 0045](adr/0045-shared-state-integrity-discipline.md). Healthy expected.

**Boot-time check: side-discovery audit.** Eighteenth firing. **HANDOFF disposition audit:** fifteenth firing. S-0056 added zero HANDOFF entries (no genuine blockers, scope-expansion-needed, or decision-required signals; no in-band Issue filings either). **Archive structured-fields audit:** second firing — passed; `outcome_summary_soft_warns` populated with 2 categories (issue_collision, missing_rigor_score).

**Boot-time check: scheduled-audit surface.** Active `audits` array is empty (consumed at S-0041). No scheduled audits due.

### After S-0045

S-0048+ — first routine-mode Phase 5 session (P5-01a Epistemology core).

S-0052 (closed) — third project health-check audit, the overdue catch-up after S-0051 consumed the natural cadence-aligned slot. The post-S-0041 overdue-catchup logic exercised end-to-end as designed (fired at S-0051, deferred cleanly, re-fired at S-0052 with overdue surface, audit landed, trigger cleared).

S-0053 — second Phase 5 routine-mode session (P5-01b Epistemology specialized). Epistemology subdomain fully seeded.

S-0054 (closed) — third Phase 5 routine-mode session (P5-02a Metaphysics core). Metaphysics core (being/ontology, identity, causation, time) seeded; 27 nodes + 30 edges. Two-commit pattern (S-0053 procedural finding) validated end-to-end.

S-0056 (now closed) — fourth Phase 5 routine-mode session (P5-02b Metaphysics specialized). Modality, free will, properties/universals, mereology seeded; 25 nodes + 36 edges. Metaphysics subdomain fully seeded. graph_version 4 → 5; live totals 106 nodes, 138 edges.

S-0062 — next natural cadence-aligned health check trigger (`slots_since == cadence` with `last_audit_session = "S-0052"`). The overdue-catchup logic stands behind it.

## Open tensions and deferred decisions

See [`product/docs/tensions.md`](../product/docs/tensions.md). As of S-0026 close, the OQ-prefixed active set is 9 items (OQ-PRIVACY-A and OQ-PRIVACY-B closed by ADR 0031; OQ-PHASE8-A settled by ADR 0032; OQ-BYOK-REGIME withdrawn by ADR 0032; OQ-OUTWARD-VOICE added at S-0019):

- OQ-DEC1-A through D — Phase DEC.1 architecture decisions (server-side mastery, two-hop retrieval, embedding strategy, chunk-resolver vs URL pointers); decide-before Phase 6
- **OQ-WALL-BEHAVIOR** (added S-0008, per ADR 0029; simplified S-0012) — soft-wall degradation ladder at cost cap; **single-tier ladder** under cost-priced subscription model (free-vs-paid tier-shaped framing collapsed at S-0012); four candidate steps remain (model downshift, retrieval shrink, concept-engagement length cap, soft refusal); decide-before Phase 8 cost-cap wiring
- **OQ-CONTEXT-COMPRESSION** (added S-0008, per ADR 0029; unaffected by S-0012) — token-amplification mitigation for multi-turn engagements (structured-state replacement + automatic compression + explicit summarization candidates); decide-before Phase 7
- **OQ-PEDAGOGY-INFERENCE-LOCUS** (added S-0008, tagged `watch`) — rule layer vs. distributed inference; revisit when inference registry crosses ~30 entries OR cross-domain edges per domain-pair exceed ~50 OR an operational complaint surfaces
- **OQ-OUTWARD-VOICE** (added S-0019) — third expression-contract gap covering outward product surfaces (UI labels, button text, error messages, learner-facing help, public README, App Store description, learner-visible CHANGELOG entries) that neither ADR 0027 nor ADR 0036 governs; decide-before Phase 7 (the earliest plausible moment such a surface ships)
- OQ-WATCH-FLAG-FILE (tagged `watch`) — separate watch-flag file consideration, surfaced at session-30 health check

## Paperclip integration

**Rejected at S-0044, superseded by [ADR 0051](adr/0051-routine-mode-and-engine-loop.md).** Investigation surfaced that Paperclip-dispatched Claude work runs under `ANTHROPIC_API_KEY` per Anthropic's April-2026 third-party-orchestration policy (subscription does not cover it; Paperclip GitHub issues #1163, #3270). For a solo project on subscription with no real parallelism need, the recurring per-token API spend doesn't justify Paperclip's value-add. The replacement architecture — Claude Code Routines + `engine/session/auto_target.json` + `engine/tools/check_routine_scope.py` — accomplishes dispatch + termination + anti-rogue at zero recurring spend, on existing apparatus. Paperclip rejection reasoning preserved in ADR 0051 "Alternatives Considered." If the project ever expands beyond Claude (multi-runtime), or hires collaborators (multi-user governance), or accepts API spend on principle (e.g., for unattended Phase 8 evaluation runs), Paperclip becomes a reasonable revisit.

## Strong working commitments

12 commitments inherited from pre-Foundation design plus 10 architectural decisions. **All 22 are recorded as ADRs** — the contract layer, now split across [`engine/adr/`](adr/) (5 engine ADRs about the build apparatus) and [`product/adr/`](../product/adr/) (33 product ADRs about Paideia-as-product). The audience-facing summary lives in [`product/docs/MISSION.md`](../product/docs/MISSION.md) (commitments 1–12 with ADR cross-refs; commitment 2 reframed at S-0012 as "operating discipline must not corrupt pedagogy" per ADR 0032); the canonical list with ADR pointers lives in [`ROADMAP.md`](../ROADMAP.md) ("Strong working commitments referenced throughout"). The conversational story behind each decision is recoverable from MemPalace `decision`-tagged drawers.

**ADR count post-S-0045 (corrected at S-0052 audit): 52 ADRs total — 50 Accepted plus 2 Superseded** (ADR 0002 by ADR 0032; ADR 0032 by ADR 0035), partitioned into **17 engine + 35 product** per [ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md). The pre-S-0052 number (51 / 16 engine) was drift — S-0045's [ADR 0052](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md) addition didn't propagate into the post-S-0045 count claim across S-0046, S-0047, S-0048, S-0049, S-0050, S-0051 STATE.md edits, until the S-0052 audit's mechanical census caught it. S-0045 added [ADR 0052](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md) (product; Phase 5 philosophy-subdomain decomposition — 9 subjects + service nodes + cross-bridges + closeout; class-boundary admit-criterion for future readmissions; philosophy of religion and history-as-subdomain explicitly deferred). S-0044 added [ADR 0051](adr/0051-routine-mode-and-engine-loop.md) (engine; routine-mode session pattern; third session mode; two-layer architecture). S-0043 added [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md) (engine; project-local Python venv). S-0042 added [ADR 0048](adr/0048-handoff-narrowing-and-github-issues-for-cross-session-deferrals.md) and [ADR 0049](adr/0049-scope-lock-at-boot-and-descope-reorder-audit-at-shutdown.md). S-0039 added [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) (engine; structural-reference parser tool and adversarial-triage workflow). S-0038 added [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) (product; structural-reference posture for the philosophy reference class). S-0035 added [ADR 0045](adr/0045-shared-state-integrity-discipline.md) (engine; shared-state integrity discipline). S-0031 added [ADR 0043](adr/0043-hook-architecture.md) (engine; hook architecture) and [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md) (engine; skill conversion partition). S-0029 added [ADR 0041](adr/0041-cascade-analysis-discipline.md) (engine; cascade-analysis discipline) and [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) (engine; soft-warn lifecycle, archive-as-canon). The contract layer the [`build_plan/`](../build_plan/) chunks consume by reference. [`engine/tools/validate.py`](tools/validate.py)'s `adr_index_consistency` check audits the ADR-file-vs-index alignment across both subtree READMEs; the `cascade_*` checks (per ADR 0041) audit superseded-citation currency, back-reference orphans, and Consequences-deliverable promises; `validate_shared_state_health()` (per ADR 0045) audits chromadb palace and git repo sanity.
