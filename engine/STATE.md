# Project State

> The single file every session reads first. Names what's current and what's next. Updated at every build-session close.

## Current

| Field | Value |
|---|---|
| **Project** | Paideia — knowledge mastery app on a pedagogical dependency graph |
| **GitHub** | https://github.com/StarshipSuperjam/paideia (private) |
| **Current phase** | Phase 0 — closed (S-0003); **Phase 1 — Contract Lock fully closed at S-0012**. **Phase 1.5 — Mission Realignment Overhaul fully closed at S-0021.** **S-0022** committed the engine / product wall ([ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md)) and renamed `CHANGELOG.md` → `ENGINE_LOG.md`. **Phase 2 (Build Plan Scaffolding) closed at S-0023.** **S-0024–S-0025** executed the folder migration into `engine/` and `product/` subtrees and the cross-reference sweep. **S-0026** authored the third project expression contract per [ADR 0038](adr/0038-expression-contract-for-ai-authored-code.md). **S-0027 widened the expression contract** per [ADR 0039](adr/0039-universal-expression-contract-across-ai-authoring-patterns.md) and **committed the build-readiness gate** per [ADR 0040](adr/0040-build-readiness-gate-before-substantive-build-sessions.md). **Phase 3 — SQL Schema Implementation closed at S-0028** with eight migrations deployed to `paideia-dev` (PostgreSQL 17.6); RLS enabled on every public table; CHECK constraints enforce gate T2-A enums. **S-0029 closed the infrastructure-misfire sweep**; **S-0030 landed the first project health-check audit**; **S-0031 closed Session β: harness mechanization** ([ADR 0043](adr/0043-hook-architecture.md), [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md)); **S-0032 closed the MemPalace audit cleanup + improvements** (cadence tightened 30 → 10); **S-0033 closed three coupled mechanical fixes** (pre-commit STATUS bug, side-discovery audit, scheduled-audits allowed-paths); **S-0034 closed the Phase 4 build-readiness gate exercise**; **S-0035 closed the engine-hardening pass** ([ADR 0045](adr/0045-shared-state-integrity-discipline.md)); **S-0036 closed the deferral-discipline guardrails** plus the bash 3.2 fix. **S-0039 landed the structural-reference parser tool** per new engine [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md). **S-0040 closed the MemPalace wing-filter fix (deferred from S-0038)** concurrent with S-0039. **S-0041 closed the cadence-trigger overdue-catchup logic fix + second project health check audit (catch-up).** **S-0042 closed the engine apparatus improvements pass:** HANDOFF.md narrowed to session-internal handoffs, GitHub Issues absorb cross-session deferrals (ADR 0048); multi-axis dead-weight scanner + rewritten health-check; declared_scope at boot + scope-delivery audit at shutdown + multi-session erosion signal + build-readiness-gate no-descoping clause (ADR 0049); session-end context telemetry. **S-0043 closed the project venv migration** per new engine [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md). **S-0044 closed the routine-mode session pattern foundation** per new engine [ADR 0051](adr/0051-routine-mode-and-engine-loop.md): third session mode alongside exploration and build; two-layer architecture (master plan + engine loop); six-layer defense in depth; new tools `check_target.py` and `check_routine_scope.py`; new `auto_target.schema.md`; new ops doc `routine-mode-operations.md`; pre-commit hook gains routine-mode scope-check; CLAUDE.md updated. Paperclip rejected (per ADR 0051 Alternatives Considered). **S-0045 closed the Phase 5 master plan** per ADR 0051's two-layer architecture — first phase to use routine-mode dispatch. Output: (1) new product [ADR 0052](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md) ratifying the 9-subdomain decomposition (epistemology, metaphysics, logic, ethics, political philosophy, aesthetics, philosophy of mind, philosophy of language, philosophy of science) plus 3 operational tasks (service nodes, cross-bridges, closeout); deferrals: philosophy of religion, history-as-subdomain. (2) [`engine/build_readiness/phase_5.md`](build_readiness/phase_5.md) — gate report covering Tier 1 (7 entries) + Tier 2 (8 entries) + Tier 3 (7 forward-pointers) for the full Phase 5 batch. (3) [`engine/session/auto_target.json`](session/auto_target.json) — executable contract; 16 explicit tasks (4 subdomains pre-split into core/specialized halves: epistemology, metaphysics, ethics, philosophy of mind); `target_id: T-PHASE-5`; `max_sessions: 18`; `paused: false`. (4) Canon-realignment: ROADMAP.md Phase 5.1 list updated to 9 subjects; build_plan/P_4_seed_graph_build.md sub-sessions list and success criteria updated; ROUTING.md prefix scheme extended (5 new subject ranges; Phase 6 relocated from `0070-0079` to `0120-0129`); product/adr/README.md index entry. (5) Issue #4 filed (per ADR 0048): mid-session context-pressure signal as `enhancement`; routine sessions have no live context-pressure detection (CLAUDE.md budget percentages are AI posture, not machinery); S-0045 engineers around the gap by sizing tasks pre-execution per `auto_target.schema.md:149`. (6) Fix-in-context: `engine/tools/check_target.py _check_validate_passes` was treating validate.py exit 1 as fail, but exit 1 fires on any soft-warn (persistent issue_collision soft-warns would block all routine validate_passes criteria); per the schema doc the criterion passes iff zero hard-fails — fixed to recognize exit 0 OR 1 as pass; 3 regression tests added. ADR count post-S-0045: **51 ADRs total — 49 Accepted plus 2 Superseded**, partitioned **16 engine + 35 product** (S-0045 added product ADR 0052). Validator clean (23 checks, 0 hard-fails, 4 expected `issue_collision` soft-warns from Issues #1/#2/#3/#4 keyword-matching the master-plan scope). pytest 519/519 passing (was 516; +3 new tests for validate_passes exit-code behavior). |
| **Last build session** | S-0047 (2026-05-04) — **Settings.json desync patch; lands S-0046's missing primary deliverable.** S-0046's close commit (2c5da6c) shipped the companion documentation (routine-mode-operations.md subsection, CLAUDE.md mechanization-list bullet, STATE.md and ENGINE_LOG entries) but missed `.claude/settings.json` itself — the actual 60+ pattern allowlist extension. Root cause: harness redirected the in-session Write to main repo's settings.json (where harness reads from) while git tracked the worktree's stale copy; `git status` from the worktree saw no change, so the close commit shipped without the deliverable looking complete. S-0047 lands the missing file: `cp <main>/.claude/settings.json <worktree>/.claude/settings.json && git add && git commit`. Issue #5 filed for the systemic gap (proposes pre-commit check + ops-doc procedure as the durable fix; out of S-0047 scope as a one-task patch). |
| **Prior build session** | S-0046 (2026-05-04) — **Routine-mode harness permission allowlist patch.** Tactical engine-apparatus session that closed the S-0044 gap surfaced when the first manual routine fire (after S-0045 master plan landed) deadlocked at the very first Bash invocation (`python3 engine/tools/check_target.py` for the boot target-met check) waiting for a permission approval that never came. S-0044's foundation shipped a load-bearing assumption ("Ask permissions: Default — Use `.claude/settings.local.json` allowlist for routine's needs") without authoring the allowlist. Output: (1) [`.claude/settings.json`](../.claude/settings.json) `permissions.allow` extended with the routine procedure's complete Bash command surface — engine python tools (every `engine/tools/*.py` script via `python3` with any args), worktree-side git lifecycle (`add/commit/rm/mv/status/diff/log/rev-parse/fetch/show/ls-files/pull --ff-only origin main/config --get`), test/lint stack (`pytest`, `ruff`, `mypy`), GitHub Issues path (`gh issue create/list/view/close/comment` plus `gh label list`), Supabase migration tooling (`supabase db push/migration new/list/repair`), hook scripts (`bash engine/tools/hooks/*.sh`), shell utilities (`date`, `ls`, `cat`, `head/tail/wc/grep/rg/find . -name`), MemPalace CLI (`mempalace search/status/list-drawers/add-drawer/diary read/diary write`), `source engine/tools/scrub_env.sh`. (2) New "Harness permission allowlist" subsection in [`engine/operations/routine-mode-operations.md`](operations/routine-mode-operations.md) documenting the executability vs. correctness layer distinction (harness allowlist vs. project-level scope-lock per ADR 0051), the S-0044 lesson, the same-commit pairing rule (every new Bash invocation in the routine procedure REQUIRES a paired allowlist entry), the verification path, and the recovery path. Routine UI configuration table updated: "Ask permissions: Default" Notes column references `.claude/settings.json` (project-scope, tracked) instead of `.claude/settings.local.json` (gitignored). (3) [`CLAUDE.md`](../CLAUDE.md) mechanization-list bullet added for the harness permission allowlist as the executability layer complementing ADR 0051's scope-lock. (4) Stale untracked `engine/session/current.json` (S-0044 close debris) was implicitly resolved by S-0046's eager-claim writing fresh content. Validator clean (23 checks, 0 hard-fails, 4 expected `issue_collision` soft-warns from Issues #1/#2/#3/#4 keyword-matching the routine-mode-broad scope). pytest 519/519 passing. |
| **Prior-prior build session** | S-0045 (2026-05-04) — **Phase 5 master plan session per ADR 0051.** First phase to use routine-mode's two-layer architecture: this session is interactive (`/start-engine`) and authors the executable contract that subsequent routine-mode sessions execute against. The session is also the build-readiness gate session for Phase 5 per ADR 0040 (the master plan IS the gate; phase_5.md is the report). Subdomain decomposition adjudicated against canonical surfaces in conversation: neither the build_plan/ROADMAP list (6 subjects + services + crossdomain — missing logic, political, aesthetics) nor the post-S-0044 STATE.md text (8 subjects + cross-bridges + closeout — missing philosophy of language and standalone services) was fully accurate. New product [ADR 0052](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md) ratifies the 9-subject decomposition + 3 operational tasks; canon-realignment edits in same session per ADR 0049's no-silent-absorption clause. 16 explicit tasks; epistemology/ethics/metaphysics/mind pre-split into core/specialized halves at master-plan time per `auto_target.schema.md:149` (routine sessions have no live context-pressure signal — the discipline is task-sizing pre-execution). max_sessions=18; paused=false. Issue #4 filed for mid-session context-pressure signal (out of scope; engineering around). Fix-in-context: check_target.py `_check_validate_passes` schema-doc-mismatch (exit 1 was treated as fail; should be pass per "zero hard-fails" semantics). Cold-context review pass surfaced one citation drift in phase_5.md T2-D (scope_lock prose understated coverage of PREDICATE_MANIFEST.md / ROUTING.md across all subdomain tasks); fixed. ADR count rises 50 → 51. Validator clean (23 checks, 0 hard-fails, 4 expected `issue_collision` soft-warns from Issues #1/#2/#3/#4 keyword-matching). pytest 519/519 passing (was 516; +3 new tests for `validate_passes` exit-code behavior). |

| **Prior-prior build session** | S-0044 (2026-05-04) — **Routine-mode session pattern foundation per ADR 0051; Paperclip rejected.** Third session mode (alongside exploration and build); two-layer architecture (master plan session + engine loop session); six-layer defense in depth. Tools: `check_target.py` (5 criterion types + predicate registry), `check_routine_scope.py` (plan/staged scope-check, master-plan integrity, segment-aware glob matching). Schema doc `auto_target.schema.md`. Ops doc `routine-mode-operations.md`. Pre-commit hook gains routine-mode scope-check + master-plan-integrity. Two new validate.py soft-warns. Issue #3 filed: `/start-engine` boot should detect mid-routine `current_status: in_progress` and refuse to claim. Validator clean; pytest 516/516. |

| **Prior-prior build session** | S-0043 (2026-05-04) — **Project venv migration with hook PATH wiring per ADR 0050.** uv-managed venv at `<main_repo>/.venv/`; Python 3.12 pinned via tracked `.python-version`; idempotent sourcing-time PATH prepend in `engine/tools/scrub_env.sh`. 8 deps explicit in `engine/tools/requirements.txt` (chromadb, tiktoken, mempalace 3.3.4 plus pre-existing). Mempalace 3.3.4 verified against upstream Issues #1 and #2 — both still upstream-unfixed. CLAUDE.md mechanization-list gains the venv bullet. |

| **Last commit on main** | (see `git log --oneline -1` on main) |
| **Backup tags** | `pre-foundation-v0.0.0` at commit `fa70b8c` — pre-machinery anchor. `pre-phase-3-v0.0.1` at commit `36b0907` (S-0026 close) — pre-Phase-3 anchor cut at S-0027. Both are historical anchors, not operational fallback targets — file-level retrieval via `git show <tag>:<path>` is the legitimate use. |

## Infrastructure

| Component | Value |
|---|---|
| **Supabase project** | `paideia-dev`, project ref `ozooosgnuzxqqypotlke`, PostgreSQL 17.6 |
| **Supabase MCP** | configured in `.mcp.json` (gitignored, contains PAT) |
| **MemPalace** | installed at `/Users/shanekidd/Library/Python/3.9/bin/`; MCP server `mempalace-mcp` configured in `.mcp.json`; wing `paideia` carries **485 drawers across 5 rooms** as of S-0032 close (general 330, operations 88, decisions 55, foundation-planning-s0001 10, s0003-adr-collection 2); capture hooks wired in `.claude/settings.json` (Stop + PreCompact wrapper PATH-fixed at S-0032 — auto-capture had been silently failing since S-0002 because `mempalace` CLI wasn't on the hook subshell's PATH; verify at next Stop event by checking `.claude/logs/mempalace-hook.log` for `OK exit=0` lines instead of `FAIL exit=127`); project usage scope documented at [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Project usage scope" — uses search, add_drawer, diary (agent_name `claude`), read-only inspection; explicitly excludes KG and tunnels |
| **Python** | **project venv** at `<main_repo>/.venv/` (Python 3.12.13, uv-managed) per [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md) — engine tools + mempalace run from here; PATH wiring in `engine/tools/scrub_env.sh` resolves bare `python3` / `ruff` / `mypy` / `pytest` / `mempalace` to venv. Pre-S-0043 fallback: system Python 3.9.6 at `/usr/bin/python3`; user-scope packages at `/Users/shanekidd/Library/Python/3.9/bin/` (no longer used by engine) |
| **Anthropic API** | env var `ANTHROPIC_API_KEY` set in local `.env` (gitignored) |

## Next session work item

**S-0048+ — First Phase 5 routine-mode session.** Phase 5 transitions to routine-mode dispatch per [ADR 0051](adr/0051-routine-mode-and-engine-loop.md). With S-0046's permission allowlist patch landed, the unattended routine should now actually run — pre-S-0046 the first manual fire deadlocked at the boot procedure's first Bash invocation. The user flips the existing `paideia-engine-loop` Claude Code Routine from Manual to Hourly when ready; the next routine session boots, reads [`engine/session/auto_target.json`](session/auto_target.json), picks the first eligible task (P5-01a Epistemology core; no `depends_on`), runs plan-then-scope-check via [`engine/tools/check_routine_scope.py --plan engine/session/current_plan.md`](tools/check_routine_scope.py), executes the task per [`engine/operations/seed-chunked-authoring.md`](operations/seed-chunked-authoring.md), and closes per [`engine/operations/routine-mode-operations.md`](operations/routine-mode-operations.md).

**Verification path before going Hourly.** Recommended: one manual routine fire first, observed end-to-end. If the boot procedure walks through without any permission prompt firing (target-met check → eligibility → plan authoring → scope-check → claim → task work → criteria verify → shutdown), the allowlist is complete and Hourly is safe. Any prompt that fires names a missing pattern; deny it, extend `.claude/settings.json`, commit, push, retry per [`engine/operations/routine-mode-operations.md`](operations/routine-mode-operations.md) "What to do when a routine deadlocks on a permission prompt".

**Each routine session reads [`engine/build_readiness/phase_5.md`](build_readiness/phase_5.md) at boot** as the gate report covering the full Phase 5 batch. If any Tier 1 finding shows unresolved (cited ADR not Accepted, cited canon-file content missing, etc.), the routine session refuses the task and writes HANDOFF.

**16 tasks total** (per ADR 0052's 9-subject decomposition + service nodes + cross-bridges + closeout, with epistemology/ethics/metaphysics/mind pre-split into core/specialized halves). max_sessions: 18; paused: false. Sequencing: P5-01a anchor → P5-01b + 8 other subjects + service nodes parallel-eligible → P5-11 cross-bridges (after all subjects + services) → P5-12 closeout.

**Interactive sessions remain available.** A user may invoke `/start-engine` for non-routine work (engine apparatus changes, urgent fixes, master-plan revisions per ADR 0051's revision discipline). Issue #3 tracks an open enhancement: `/start-engine` should detect mid-routine `current_status: in_progress` and refuse to claim. Until #3 lands, mixing modes requires manual coordination — pause routine before starting interactive work and unpause after closing.

### Source documents (routine-mode boot, any Phase 5 task)

- [`engine/build_readiness/phase_5.md`](build_readiness/phase_5.md) — gate report; covers Tier 1/2/3 triage for all 16 tasks.
- [`engine/session/auto_target.json`](session/auto_target.json) — executable contract.
- [`engine/operations/routine-mode-operations.md`](operations/routine-mode-operations.md) — routine-mode session procedure (Layer 1 reference).
- [`engine/operations/seed-chunked-authoring.md`](operations/seed-chunked-authoring.md) — per-routine-session migration workflow.
- [`product/docs/content-strategy.md`](../product/docs/content-strategy.md) — Phase 4.5 input dataset survey (Cross-Domain Reference Inventories).
- [`product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md`](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) — structural-reference posture for the philosophy reference class.
- [`product/adr/0052-phase-5-philosophy-subdomain-decomposition.md`](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md) — 9-subdomain decomposition.
- [`engine/adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md`](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) — parser tool (ad-hoc consultation).
- [`engine/tools/parse_structural_reference.py`](tools/parse_structural_reference.py) — the parser tool itself.
- [`product/docs/architecture.md`](../product/docs/architecture.md), [`product/docs/learner-model.md`](../product/docs/learner-model.md) — the contracts the seed graph encodes.
- [`product/seed-graph/migrations/PREDICATE_MANIFEST.md`](../product/seed-graph/migrations/PREDICATE_MANIFEST.md), [`product/seed-graph/migrations/ROUTING.md`](../product/seed-graph/migrations/ROUTING.md) — predicate registry; migration prefix scheme (extended at S-0045).

**Parser tool available for ad-hoc use.** Per [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md). Runnable whenever a structural-reference source has been acquired (currently SEP/IEP via the HTML adapter; PDF adapter deferred to first-PDF-source-acquisition). CLI: `python3 engine/tools/parse_structural_reference.py --source <path> --document-type encyclopedia --output -`. The brief is discarded after triage; the graph is the only durable artifact.

**MemPalace wing-filter / wing-naming bugs unfixed** (Issues [#1](https://github.com/StarshipSuperjam/paideia/issues/1), [#2](https://github.com/StarshipSuperjam/paideia/issues/2)) against mempalace 3.3.4. Workaround: unfiltered `mempalace_search`. See [`engine/operations/mempalace-operations.md`](operations/mempalace-operations.md) "Known issues".

**Phase 4.5 input dataset survey is closed at S-0038.** Phase 5 sub-sessions inherit both [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) and [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) at boot per ROADMAP §5.2.

**Phase 4 graph validation remains implementation-closed at S-0037.** The validator code, manifests, routing, and ops-doc are all in place. Two ROADMAP §4 success criteria — `<3s on a 100-node test seed` and `pre-commit hook blocks seed-authoring commits that fail audit` — require real seed content to verify and exercise naturally at the first Phase 5 seed routine session (P5-01a Epistemology core). The first routine session that sets `SUPABASE_DB_URL` and runs `validate.py` after a `supabase db push` will exercise the audit end-to-end against real seed content for the first time.

**Boot-time check: cadence trigger (overdue-catchup per ADR 0022 amendment at S-0041).** Cadence is 10. `last_audit_session = "S-0041"`. At S-0048 boot (assuming next session claims it), `slots_since = 48 - 41 = 7` — no cadence trigger. Next due fire at S-0051 absent any further audits.

**Boot-time check: Issues backlog (added at S-0042 per ADR 0048).** As of S-0045 close, backlog: 2 bugs (Issues #1, #2 — wing-filter and wing-naming, both `bug + upstream`); 0 tech-debt; 0 cleanup; **2 enhancement** (Issues #3 — interactive-mid-routine guard; #4 — mid-session context-pressure signal); 0 urgent.

**Boot-time check: scope-collision detection.** `validate.py` default mode runs `scan_issue_collisions.py`. Routine-mode sessions: expect issue_collision to fire on the seed-authoring scope keywords (Issues #1/#2/#3/#4 may all match). Persistent-warn threshold tripped per [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) at S-0044 close (3-of-5 archives showing `issue_collision`); annotated as expected per scope breadth.

**Boot-time check: scope-erosion signal (added at S-0042 per ADR 0049).** `session-start.sh` walks recent archives and counts `scope_delivery.delivered == false`. Surface fires at 3-of-5. As of S-0045 close, recent archives (S-0042, S-0043, S-0044, S-0045) all record `scope_delivery.delivered == true` — signal remains silent.

**Boot-time check: shared-state health probe.** Per [ADR 0045](adr/0045-shared-state-integrity-discipline.md). Healthy expected.

**Boot-time check: side-discovery audit.** Twelfth firing. **HANDOFF disposition audit:** ninth firing. S-0045 added no HANDOFF entries — fix-in-context throughout.

**Boot-time check: scheduled-audit surface.** Active `audits` array is empty (consumed at S-0041). No scheduled audits due.

### After S-0045

S-0048+ — first routine-mode Phase 5 session (P5-01a Epistemology core).

S-0051 — next natural cadence-aligned health check trigger (`slots_since == cadence` with `last_audit_session = "S-0041"`). The overdue-catchup logic guarantees the trigger fires at S-0051 even if the slot is consumed by other work; "overdue" surface fires at S-0052 / S-0053 / etc. until the audit lands. Validator's `health_check_overdue` soft-warn provides defense-in-depth. Routine-mode sessions can run health-check audits if they pause routine first; otherwise the user surfaces one via interactive `/start-engine`.

## Open tensions and deferred decisions

See [`product/docs/tensions.md`](../product/docs/tensions.md). As of S-0026 close, the OQ-prefixed active set is 9 items (OQ-PRIVACY-A and OQ-PRIVACY-B closed by ADR 0031; OQ-PHASE8-A settled by ADR 0032; OQ-BYOK-REGIME withdrawn by ADR 0032; OQ-OUTWARD-VOICE added at S-0019):

- OQ-DEC1-A through D — Phase DEC.1 architecture decisions (server-side mastery, two-hop retrieval, embedding strategy, chunk-resolver vs URL pointers); decide-before Phase 6
- **OQ-WALL-BEHAVIOR** (added S-0008, per ADR 0029; simplified S-0012) — soft-wall degradation ladder at cost cap; **single-tier ladder** under cost-priced subscription model (free-vs-paid tier-shaped framing collapsed at S-0012); four candidate steps remain (model downshift, retrieval shrink, concept-engagement length cap, soft refusal); decide-before Phase 8 cost-cap wiring
- **OQ-CONTEXT-COMPRESSION** (added S-0008, per ADR 0029; unaffected by S-0012) — token-amplification mitigation for multi-turn engagements (structured-state replacement + automatic compression + explicit summarization candidates); decide-before Phase 7
- **OQ-PEDAGOGY-INFERENCE-LOCUS** (added S-0008, tagged `watch`) — rule layer vs. distributed inference; revisit when inference registry crosses ~30 entries OR cross-domain edges per domain-pair exceed ~50 OR an operational complaint surfaces
- **OQ-OUTWARD-VOICE** (added S-0019) — third expression-contract gap covering outward product surfaces (UI labels, button text, error messages, learner-facing help, public README, App Store description, learner-visible CHANGELOG entries) that neither ADR 0027 nor ADR 0036 governs; decide-before Phase 7 (the earliest plausible moment such a surface ships)
- OQ-WATCH-FLAG-FILE (tagged `watch`) — separate watch-flag file consideration, surfaced at session-30 health check

## Paperclip integration

**Rejected at S-0044, superseded by [ADR 0051](adr/0051-routine-mode-and-engine-loop.md).** Investigation surfaced that Paperclip-dispatched Claude work runs under `ANTHROPIC_API_KEY` per Anthropic's April-2026 third-party-orchestration policy (subscription does not cover it; Paperclip GitHub issues #1163, #3270). For a solo project on subscription with no real parallelism need, the recurring per-token API spend doesn't justify Paperclip's value-add. The replacement architecture — Claude Code Routines + `engine/session/auto_target.json` + `engine/tools/check_routine_scope.py` — accomplishes dispatch + termination + anti-rogue at zero recurring spend, on existing apparatus. Paperclip rejection reasoning preserved in ADR 0051 "Alternatives Considered." If the project ever expands beyond Claude (multi-runtime), or hires collaborators (multi-user governance), or accepts API spend on principle (e.g., for unattended Phase 8 evaluation runs), Paperclip becomes a reasonable revisit.

## Strong working commitments

12 commitments inherited from pre-Foundation design plus 10 architectural decisions. **All 22 are recorded as ADRs** — the contract layer, now split across [`engine/adr/`](adr/) (5 engine ADRs about the build apparatus) and [`product/adr/`](../product/adr/) (33 product ADRs about Paideia-as-product). The audience-facing summary lives in [`product/docs/MISSION.md`](../product/docs/MISSION.md) (commitments 1–12 with ADR cross-refs; commitment 2 reframed at S-0012 as "operating discipline must not corrupt pedagogy" per ADR 0032); the canonical list with ADR pointers lives in [`ROADMAP.md`](../ROADMAP.md) ("Strong working commitments referenced throughout"). The conversational story behind each decision is recoverable from MemPalace `decision`-tagged drawers.

**ADR count post-S-0045: 51 ADRs total — 49 Accepted plus 2 Superseded** (ADR 0002 by ADR 0032; ADR 0032 by ADR 0035), partitioned into **16 engine + 35 product** per [ADR 0037](adr/0037-engine-product-wall-and-changelog-rename.md). S-0045 added [ADR 0052](../product/adr/0052-phase-5-philosophy-subdomain-decomposition.md) (product; Phase 5 philosophy-subdomain decomposition — 9 subjects + service nodes + cross-bridges + closeout; class-boundary admit-criterion for future readmissions; philosophy of religion and history-as-subdomain explicitly deferred). S-0044 added [ADR 0051](adr/0051-routine-mode-and-engine-loop.md) (engine; routine-mode session pattern; third session mode; two-layer architecture). S-0043 added [ADR 0050](adr/0050-project-venv-and-hook-path-wiring.md) (engine; project-local Python venv). S-0042 added [ADR 0048](adr/0048-handoff-narrowing-and-github-issues-for-cross-session-deferrals.md) and [ADR 0049](adr/0049-scope-lock-at-boot-and-descope-reorder-audit-at-shutdown.md). S-0039 added [ADR 0047](adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) (engine; structural-reference parser tool and adversarial-triage workflow). S-0038 added [ADR 0046](../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) (product; structural-reference posture for the philosophy reference class). S-0035 added [ADR 0045](adr/0045-shared-state-integrity-discipline.md) (engine; shared-state integrity discipline). S-0031 added [ADR 0043](adr/0043-hook-architecture.md) (engine; hook architecture) and [ADR 0044](adr/0044-skill-conversion-recipe-vs-reference.md) (engine; skill conversion partition). S-0029 added [ADR 0041](adr/0041-cascade-analysis-discipline.md) (engine; cascade-analysis discipline) and [ADR 0042](adr/0042-soft-warn-lifecycle-archive-canon.md) (engine; soft-warn lifecycle, archive-as-canon). The contract layer the [`build_plan/`](../build_plan/) chunks consume by reference. [`engine/tools/validate.py`](tools/validate.py)'s `adr_index_consistency` check audits the ADR-file-vs-index alignment across both subtree READMEs; the `cascade_*` checks (per ADR 0041) audit superseded-citation currency, back-reference orphans, and Consequences-deliverable promises; `validate_shared_state_health()` (per ADR 0045) audits chromadb palace and git repo sanity.
