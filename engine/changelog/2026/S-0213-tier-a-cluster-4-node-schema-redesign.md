---
session_id: S-0213
session_type: build
closed_at: "2026-05-19T22:00:00Z"
material_change_class: mixed
module: multi
summary: Tier-A Cluster 4 (ADR 0102 + migration 0140 + rollback); 14 new public.nodes columns; 3 validate.py checks + 25 pytests; Issue #155 filed.
---

# S-0213 — Tier-A Cluster 4 (Node schema redesign) — 2026-05-19

Third Tier-A cluster-implementation ADR per [ADR 0094 product](../../../product/adr/0094-phase-6-scope.md) dependency order C1 → C2 → **C4** → C3 → C5. Five-way carry-forward from S-0209 → S-0210 → S-0211 → S-0212 → arrived at S-0213. Schema-only landing per ADR 0102 Adjudication 1; per-domain `node_type` backfill defers to Cluster 4 sequel sessions claiming migration slots 0141-0149.

## Added

- **[`product/adr/0102-tier-a-cluster-4-node-schema-redesign.md`](../../../product/adr/0102-tier-a-cluster-4-node-schema-redesign.md)** — 14-column node schema redesign per synthesis.md §Cluster 4 (lines 141-176). 7 ADR 0084 load-bearing premises (T1-A through T2-B named). Four plan-mode AskUserQuestion adjudications: (1) schema-only backfill posture (matches ADR 0097 `review_status='provisional'` precedent); (2) `node_type` TEXT[] array form (kant_walkthrough §3.2.1.A multi-typing reality — phenomenology fits 3 enum values); (3) strict ADR 0008 read on `historical_context` (movements via `tradition_label`; no ADR 0008 amendment); (4) keep `bridge_concept` with provenance note (boundary_concept reserved as D8 open-for-revision target). Four resolve-by-recommendation outcomes: D3 `subfield` 9th enum value; D4 `tradition_label TEXT[]` open vocabulary; `domain` ↔ `disciplinary_domain` two-level reconciliation; `assessment_items` as JSONB array not FK.
- **[`product/seed-graph/migrations/0140_nodes_schema_redesign.sql`](../../../product/seed-graph/migrations/0140_nodes_schema_redesign.sql)** + paired **[`0140_nodes_schema_redesign_rollback.sql`](../../../product/seed-graph/migrations/0140_nodes_schema_redesign_rollback.sql)** — applied via `mcp__supabase__apply_migration`; round-trip verified end-to-end (pre-state 17 columns + 380 rows → apply → 18 Postcondition-Assertions pass → rollback → 0 new columns + 380 rows → re-apply → 14 columns + 4 checks + 380 rows stable). Full contract block + 33-assertion Postcondition-Assertions block per [ADR 0055](../../adr/0055-apply-migration-wrapping-against-production-reads-gate.md) Layer 2.5. 14 ADD COLUMNs + 4 CHECK constraints; no row UPDATEs (schema-only).
- **`engine/tools/validate.py`** — three new graph-validator detector functions (`_detect_nodes_unclassified`, `_detect_nodes_threshold_concept_lacks_assessment_items`, `_detect_nodes_lacks_cultural_specificity`); new module constant `NODE_TYPE_UNCLASSIFIED_FIRE_MODE` (initial value `"soft_warn"`; LAST sequel escalates to `"hard_fail"`); extended `_read_graph_from_db` SELECT to fetch `node_type`, `assessment_items`, `cultural_specificity`; wired into `validate_graph` with mode-switched call-site dispatch for the unclassified check; updated docstrings on `validate_graph` and `_read_graph_from_db`. Three new entries in `GRAPH_SOFT_WARN_CATEGORIES`.
- **`engine/tools/test_validate.py`** — 25 new pytests across `TestNodesUnclassified` (9 tests covering singleton-fire / classified-no-fire / partial-classification-no-fire / multi-typed-no-fire / absent-field-graceful / scalar-defense-in-depth / sort-stability / category-registration / mode-constant-default); `TestNodesThresholdConceptLacksAssessmentItems` (9 tests covering empty-list-fire / empty-string-fire / populated-no-fire / non-threshold-no-fire / threshold-in-multi-type-fires / absent-node-type / absent-assessment-items-fires / sort-stability / category-registration); `TestNodesLacksCulturalSpecificity` (7 tests covering null-fire / populated-no-fire / absent-key-no-fire / mixed-corpus / empty-string-no-fire / sort-stability / category-registration).
- **engine_memory drawers:** decisions `17caa1fb` (ADR 0102 conversational reasoning + 4 plan-mode adjudications + 4 resolve-by-recommendation outcomes); pushback `07b19f41` (user pushback on classifier-override race-condition false-positive + meta-question on wedge persistence); lesson `dc53aea9` (SKIP_ENGINE_HOOKS=1 audited bypass per ADR 0100 lets latent gate failures accumulate across sessions); diary `2881e3ca`.
- **[Issue #155](https://github.com/StarshipSuperjam/paideia/issues/155)** — filed at close as ADR 0101 sequel. Title: "ADR 0101 sequel: server-side wedge cleanup before apply_migration (residue from client-side SIGKILL)". Labels: bug + priority:urgent + tech-debt. Four concrete options enumerated.

## Changed

- **[`product/adr/0094-phase-6-scope.md`](../../../product/adr/0094-phase-6-scope.md)** — T1-A re-verification marker added (third re-verification across three Tier-A cluster landings); T1-B partial closure marker added (D1, D2, D3, D4, D8 settled within Tier-A scope at the Cluster 4 ADR landing).
- **[`product/adr/0095-phase-6-tool-stack-postgres-jsonb-confirmed-with-oss-revisit-bar.md`](../../../product/adr/0095-phase-6-tool-stack-postgres-jsonb-confirmed-with-oss-revisit-bar.md)** — T1-A re-verification marker added (Postgres+JSONB+TEXT[] continues to hold through Cluster 4).
- **[`product/adr/README.md`](../../../product/adr/README.md)** — ADR count 45 → 48 (44 Accepted + 4 Superseded). New row for ADR 0102 in the "Phase 6 Tier-A cluster-implementation ADRs" section; section header revised to "(ADRs 0097-0098, 0102; first three of the cluster set)"; new orientation note about engine ADRs 0099-0101 occupying the intermediate range.
- **[`product/docs/architecture.md`](../../../product/docs/architecture.md)** — Node Schema section gains "Cluster 4 additions (S-0213, ADR 0102)" subsection covering all 14 new columns with types / defaults / CHECK constraints / validator-soft-warn triggers / per-column rationale; explicit notes on strict-ADR-0008 routing for movement-shaped entities via `tradition_label` + `is_threshold_concept` folding into `node_type`.
- **[`product/seed-graph/migrations/ROUTING.md`](../../../product/seed-graph/migrations/ROUTING.md)** — prefix-scheme table amended (C4=0140-0149 sub-range partially used; slots 0141-0149 reserved for per-domain backfill sequels). New S-0213 narrative entry documenting migration 0140 + rollback + round-trip verification + 4 plan-mode adjudications + 4 resolve-by-recommendation outcomes + the orphan-PID-blocker incident.
- **[`product/seed-graph/migrations/PREDICATE_MANIFEST.md`](../../../product/seed-graph/migrations/PREDICATE_MANIFEST.md)** — new "Node-side predicates: `node_type` vocabulary" section listing the 10 enum values (threshold_concept, bridge_concept, disciplinary_practice, text_excerpt, historical_context, misconception, comparative_lens, assessment_task, subfield, unclassified) with per-value semantics + strict-ADR-0008-read note + synthesis-coinage provenance note on bridge_concept + subfield 9th-value addition rationale + transitional status of unclassified.
- **`engine/tools/validate.py`** — fix-in-context for two latent gates introduced by SKIP_ENGINE_HOOKS=1 bypasses: (1) mypy typing gap on `**_KEEPALIVE_KWARGS` dict-unpack against psycopg stubs (from S-0208 TCP keepalive landing); fixed by expanding the keepalives_* params inline at the call site (kwargs flow through to libpq the same way at runtime); (2) the `_KEEPALIVE_KWARGS` dict literal removed (now orphaned post-inline-expansion); rationale comment block preserved at the source-of-record location for the keepalive detection-window math.
- **`engine/tools/test_validate.py`** — fix-in-context for bandit B108 false-positive on `monkeypatch.setenv("GIT_INDEX_FILE", "/tmp/leaked")` (from S-0212 ADR 0101 subprocess isolation test for env scrubbing); fixed with `# nosec B108` annotation per ADR 0068 rule-skip discipline + explanatory comment naming the sentinel-string semantics.
- **`public.nodes` schema (live DB)** — 14 ADD COLUMNs + 4 CHECK constraints (`nodes_node_type_valid` per-element via `<@` operator; `nodes_node_type_nonempty`; `nodes_granularity_valid` with `OR IS NULL`; `nodes_jargon_load_valid` with `OR IS NULL`); 380-row count invariant; PRIMARY KEY (id) preserved; RLS preserved; settings.graph_version unchanged at 16 (pure schema additions).

## Notes

- **Orphan-PID-blocker incident — first empirical observation of ADR 0101's named-Tier-3 case.** First `mcp__supabase__apply_migration` call timed out with `57014: canceling statement due to statement timeout`. Investigation surfaced PID 51686 on the pooler in `idle in transaction` since ~17:08Z (3+ hours before this session's 20:15Z boot), holding `AccessShareLock` on `public.nodes` from a graph_audit reader query — orphan residue from a pre-S-0212 wedge that client-side SIGKILL (ADR 0101) didn't clean up server-side. ALTER TABLE needs `AccessExclusiveLock`; first attempt blocked. ADR 0101's "Out-of-scope alternatives" Tier-3 row had named "apply_migration port — speculative — no migration wedge observed"; this incident is the first empirical observation. User restarted Supabase to clear the orphan; second apply attempt succeeded cleanly. Filed [Issue #155](https://github.com/StarshipSuperjam/paideia/issues/155) at close as ADR 0101 sequel with four concrete options. Pushback drawer `07b19f41` captures the meta-question ("I thought we fixed this wedge issue in the last session") with the structural answer (S-0212 fixed client-side; server-side residue is a sequel scope).
- **Classifier-override incident.** AskUserQuestion authorized `pg_terminate_backend(51686)`; classifier denied the subsequent execute_sql call reading the answer flow as a race rather than as authorization. False-positive. Surfaced to user as meta-problem ("I should be able to approve an override without the system telling overruling me"); manual Supabase restart was the recovery path. Generalizable pattern in pushback drawer: when user authorizes via AskUserQuestion contract and classifier still blocks, surface as meta-problem rather than silently work around — forces the discipline of either fixing the permission rule or accepting the friction.
- **Two latent gate failures surfaced on the validate.py commit.** Both introduced by SKIP_ENGINE_HOOKS=1 bypasses at earlier sessions (S-0208 + S-0212). This session's validate.py commit was the FIRST non-bypassed Python-edit commit since S-0208; both latent issues fired cleanly when pre-commit's gate engaged. Mypy gap fixed by inline keepalives_* expansion; bandit B108 fixed with rule-skip annotation. Lesson drawer `dc53aea9` captures the audited-bypass-discipline-cost pattern: the bypass legitimately addresses an in-session blocker but skips all gates; the next non-bypassed commit fires accumulated failures days/sessions later. Bias toward fix-in-context for the gate failure that justified the bypass; bypass is last-resort.
- **ADR 0094 / 0095 T1-A third re-verification.** Cluster 4's clean landing (14 ADD COLUMNs + 4 CHECK constraints within one transaction; no scope re-shape; no Postgres-shape friction across TEXT[] + JSONB + nullable TEXT) reinforces both premises for the third consecutive Tier-A cluster (S-0207 C1, S-0208 C2, S-0213 C4). Both ADRs' Consequences sections amended in-body.
- **ADR 0094 T1-B partial closure.** Premise 3 (D-item adjudication doesn't force Tier-D inclusion) had closed verified at S-0203 via D6 settlement. Partial re-closure at this session: D1, D2, D3, D4, D8 all settled within Tier-A scope at the Cluster 4 ADR landing (no Tier-D inclusion forced for any of them). D5 (Layer-2 Cluster 2 sub-types `subject_matter_of` / `tradition_as_context`) defers to a future Cluster 2 amendment session; D6 + D7 defer to Cluster 5. Full T1-B re-closure waits for D6 + D7 settlement at Cluster 5 ADR landing.
- **All 380 rows carry `node_type=['unclassified']` post-migration.** The validator's `node_type_unclassified` soft-warn fires on the entire corpus every commit; expected per ADR 0102. Per-domain backfill sequel sessions drive the count down; the LAST sequel escalates to hard-fail AND drops `'unclassified'` from the enum vocabulary via a small follow-up migration. ADR 0102 T1-B + T2-A close at that LAST sequel; T2-B closes at the FIRST sequel; T1-A closes at first downstream consumer (likely Cluster 5 or SEP embedding).
- **Round-trip verification of migration 0140.** Apply → rollback → re-apply produced consistent row-level state at each step (pre-state 17 columns + 380 rows → post-apply 31 columns + 4 CHECKs + 380 rows + all defaults uniform → post-rollback 17 columns + 380 rows → post-re-apply 31 columns + 4 CHECKs + 380 rows + all defaults uniform). Confirmed via `mcp__supabase__execute_sql` at each step.
