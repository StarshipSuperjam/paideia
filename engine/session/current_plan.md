## Prior context (MemPalace boot search)

_Generated by `engine/tools/mempalace_boot_search.py` at 2026-05-08T03:01:42Z. Three formulations × similarity ≥0.60 = 13 drawers._

### Literal — `apply_migration postcondition verification empirical db check`
- **sessions/problems** (source: `bwudy987n.txt`; sim: 0.76) — =================== ISSUE #23 =================== apply_migration.py does not empirically verify postconditions against live DB after apply --- ## Gap
- **sessions/problems** (source: `bwudy987n.txt`; sim: 0.64) — **None of these empirically verify postconditions against the live DB after `apply_migration.py` runs.** Cold-review reads the SQL's *claim* to insert N rows; it doesn't run `SE...
- **sessions/architecture** (source: `562804a7-e43a-441b-aa18-30a05ac56d2a.jsonl`; sim: 0.64) — t_check_already_applied_true_when_row_present PASSED [ 45%] → engine/tools/test_apply_migration.py::test_check_already_applied_false_when_no_row PASSED [ 50%] → engine/tools/tes...
- **sessions/technical** (source: `agent-a799f05eed2c21688.jsonl`; sim: 0.63) — **Function `_check_migration_applied`** (line 101-121): - **Contract**: "Verify a migration id is recorded as applied" (docstring, line 102). - **Implementation**: Queries `supa...
- **sessions/problems** (source: `bwudy987n.txt`; sim: 0.63) — After body apply, before recording in `supabase_migrations.schema_migrations`, run each assertion query and compare to expected count. Hard-fail the apply (exit code already-def...

### Conceptual — `apply_migration postcondition verification empirical check`
- **sessions/problems** (source: `bwudy987n.txt`; sim: 0.74) — =================== ISSUE #23 =================== apply_migration.py does not empirically verify postconditions against live DB after apply --- ## Gap
- **sessions/problems** (source: `bwudy987n.txt`; sim: 0.61) — **Option 1 — Structured `Postconditions:` parser.** Extend `engine/tools/apply_migration.py` to parse a structured assertion block from the contract header, e.g.:
- **sessions/problems** (source: `bwudy987n.txt`; sim: 0.60) — After body apply, before recording in `supabase_migrations.schema_migrations`, run each assertion query and compare to expected count. Hard-fail the apply (exit code already-def...

### Adjacent — `apply_migration postcondition verification empirical db check lessons pushback`
- **sessions/problems** (source: `bwudy987n.txt`; sim: 0.62) — Surfaced during exploration session (2026-05-05) when the user pushed back on a "saved Studio queries for spot-checks" recommendation, correctly noting that QA should already be...
- **sessions/problems** (source: `bwudy987n.txt`; sim: 0.73) — =================== ISSUE #23 =================== apply_migration.py does not empirically verify postconditions against live DB after apply --- ## Gap
- **sessions/problems** (source: `bwudy987n.txt`; sim: 0.63) — **None of these empirically verify postconditions against the live DB after `apply_migration.py` runs.** Cold-review reads the SQL's *claim* to insert N rows; it doesn't run `SE...
- **sessions/architecture** (source: `a184275a-6782-41e3-80d2-aa364217b8c7.jsonl`; sim: 0.62) — **What's NOT covered (the gap I noticed):** None of the three layers empirically verify postconditions against the live DB after `apply_migration.py` runs. The cold-review revie...
- **sessions/architecture** (source: `agent-afd59e46771f79d5a.jsonl`; sim: 0.62) — 1. Read the migration's contract comment block (the `--` block at the top spanning Purpose, Loads tables, Preconditions, Postconditions, Invariants, Non-responsibilities, Cross-...
