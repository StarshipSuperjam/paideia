paths_to_modify: ["engine/build_readiness/phase_5_production_audit_evidence/service.md"]
criteria_addressed: [0, 1]

This is the AUDIT-SVC routine evidence session for T-PHASE-5-AUDIT, the eleventh of twelve evidence files (after AUDIT-CB at S-0104, AUDIT-EPI at S-0105, AUDIT-ETH at S-0108, AUDIT-MET at S-0109, AUDIT-MIN at S-0110, AUDIT-LOG at S-0111, AUDIT-LAN at S-0112, AUDIT-SCI at S-0113, AUDIT-POL at S-0114, and AUDIT-AES at S-0115). Per `engine/build_readiness/phase_5_production_audit.md` §"Sample-size policy" the service-nodes subdomain has 28 edges and a sample target of 10 (35.7%) plus ~8 edge-anchored nodes; per §T1-A the per-edge SEP-anchored prompt template is fixed; per §T1-B the evidence file schema is fixed; per §"Empirical-fortification branch" routine sessions remain parametric-only until the build-readiness gate at `engine/build_readiness/fetch_structural_reference_first_exercise.md` records T1-A through T1-D as resolved (not yet — that closure lands at the production-audit closeout interactive session per ADR 0053). Per the AUDIT-SVC task name in `engine/session/auto_target.json` the audit emphasizes the history-terminator sub-class given the S-0081 gate's historical-not-pedagogical pattern (per ADR 0052 product the service subdomain houses formal-method service-nodes — set theory, formal logic, probability — alongside the history-terminator sub-class of bridge nodes that gather pedagogical-prerequisite traffic from the historical-canonical positions of other subdomains). Sample selection runs the deterministic md5(seed='AUDIT-SVC' || source_id || '|' || target_id) ordering against the 28 within-service edges enumerated in the seed migrations under `product/seed-graph/migrations/`; the top 10 by md5 form the edge sample, and the union of source/target nodes ordered by md5(seed='AUDIT-SVC' || node_id) yields the top 8 for the node sample. The deliverable file populates the schema's named sections (Sample metadata, Sampled-edge candidate findings E-1..E-10, Sampled-node candidate findings N-1..N-8, Cross-cutting observations sparingly per §T2-D, SEP citations consulted) and produces candidate findings only — disposition (Issue filing, ADR memos, soft-warn proposals, structural-reopen memos, both final reports) is the closeout interactive session's surface per §T1-F. The cross-cutting observations section attends sparingly to aggregate-scan inputs the closeout will need: any new dialectical-ordering or top-level-discipline-label-as-prereq pattern instances; service-specific structural patterns (e.g., history-terminator nodes sourcing edges to non-service domains per pre-listed audit-system-input proposal #4 `historical_node_as_prereq_source`; formal-method service-nodes whose downstream consumption pattern differs from history-terminators). The evidence file is the only deliverable path modified; service seed migrations remain untouched (corrections, if any, are authored in subsequent build sessions after closeout dispositioning per master-plan §"Anti-scope"). The operational allowlist (current.json, current_plan.md, auto_target.json status fields, register_state.json, ENGINE_LOG.md, archive/S-0116.json) is also touched by the eager-claim and shutdown machinery; pre-commit hook hard-fails out-of-scope deliverable paths.
