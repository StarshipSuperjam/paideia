paths_to_modify: ["engine/build_readiness/seed_qa_evidence/shard_13.md"]
criteria_addressed: [0, 1]

# Plan — SQA-13 (shard 13 QA census)

## Task
Routine task SQA-13 from `T-SEED-QA`. Score the 27 `pedagogical_prerequisite` edges in shard 13 against C1 (prerequisite soundness) and the 20 nodes against C2 (teaching_notes traction) + C3 (summary cold-readability) per the pinned rubric at [`engine/build_readiness/seed_qa_audit.md`](../build_readiness/seed_qa_audit.md). Write the scope-locked evidence file at `engine/build_readiness/seed_qa_evidence/shard_13.md`. Criteria addressed: index 0 (`file_exists`) and index 1 (`validate_passes`).

## Approach
Parametric judgment first per the rubric. Shard content already extracted from the committed `seed_qa_evidence/shards.json`. Audit follow-up migrations 0061–0065 already cross-referenced programmatically — three shard-13 edges touch the audit footprint:

- **E-3 a_theory_of_time → mctaggarts_paradox** — audit-endorsed by 0063 as the proximate path retained when the long-distance shortcut `time → mctaggarts_paradox` was deleted (comment names this edge explicitly). Edge itself unchanged; audit context noted.
- **E-19 bayesian_epistemology → dutch_book_argument** — directly audit-touched by 0063 (retain-with-annotation disposition; evidence backfill from S-0122). Edge `evidence` field carries the S-0122 audit text.
- **E-20 causal_theory_of_reference → causal_theory_of_mental_content** — directly audit-touched by 0062 (direction flip CB-E-65; Kripke-Putnam 1970s reference theory predates Fodor's 1980s mental-content theory). Edge `evidence` field carries the S-0122 audit text.

All three audit-validated directions expected to score Sound on parametric re-inspection (calibration consistent with prior shards 04–12). Evidence file shape follows the pinned schema in `seed_qa_audit.md`; written in one pass at production quality per the end-state-quality first-pass posture.

## Anti-scope
No DB writes (sessions read from `shards.json`). No migration edits. No Issue filing (disposition deferred to closeout SQA-20). No edits outside the single scope-locked path. Single in-session deliverable commit + close commit.

## Prior context (MemPalace boot search)

_Generated by `engine/tools/mempalace_boot_search.py` at 2026-05-15T08:03:52Z. Three formulations × similarity ≥0.60 = 0 drawers._

### Literal — `shard 13 qa census score 27 edges c1 prerequisite soundness 20 nodes c2 teaching_notes traction c3 summary cold-readability per the seed_qa_audit md rubric read the shard from seed_qa_evidence shards json write the evidence file`
- _no drawers above threshold_

### Conceptual — `shard census score edges prerequisite soundness`
- _no drawers above threshold_

### Adjacent — `shard 13 qa census score 27 edges c1 prerequisite soundness 20 nodes c2 teaching_notes traction c3 summary cold-readability per the seed_qa_audit md rubric read the shard from seed_qa_evidence shards json write the evidence file lessons pushback`
- _no drawers above threshold_

Drawer search returned 0 hits at threshold 0.6 (consistent with prior shards in this routine batch). The diary read at boot surfaces the immediately-prior shards' rubric calibration — recurring "discipline → object-of-study" Defensible shape, "umbrella → specific-instance" Sound shape, "parallel-theories with historical succession" Defensible shape (E-13 DN→IBE at S-0174), cycle-deferred-companion Sound calibration (E-14 propositional_attitude→proposition at S-0173), and the eight-shard streak (05–12) of "target more general/foundational" Defensible instances. The rubric is pinned; closeout SQA-20 adjudicates rubric questions, not the evidence session.
