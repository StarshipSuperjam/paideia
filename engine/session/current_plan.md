paths_to_modify: ["product/seed-graph/migrations/0090_seed_logic_part1.sql", "product/seed-graph/migrations/ROUTING.md"]
criteria_addressed: [0, 1]

P5-03 Logic seed lands `0090_seed_logic_part1.sql` covering the six clusters phase_5.md T1-B / engine/build_readiness/phase_5.md names for philosophical logic: modality (alethic — modal logic systems K/T/S4/S5; necessity, possibility, accessibility relations), conditionals (material conditional, indicative vs subjunctive distinction, Stalnaker–Lewis closest-world semantics, conditional logic), paradox (the semantic and set-theoretic paradox family — liar, Curry, Russell, sorites is held back to vagueness), vagueness (sorites paradox, supervaluationism, epistemicism, fuzzy/degree-theoretic accounts), paraconsistent (the LP and adaptive logics; the dialetheist Priest defense of true contradictions), deontic (standard deontic logic and its paradoxes — Chisholm, Ross, gentle-murder).

Migration shape mirrors the four prior Phase 5 seed migrations (0011 / 0016 / 0030 / 0036): full Layer 1 contract block at top per `engine/operations/migration-discipline.md`; single-transaction `BEGIN; ... COMMIT;` wrap; first statement is `UPDATE settings SET value=<n+1>::jsonb WHERE key='graph_version'` reading current value at boot via Supabase MCP `execute_sql` and writing n+1; every subsequent INSERT carries `graph_version_added = <n+1>`. All nodes carry `confidence_level='INTERPRETED'` and `domain=ARRAY['logic']` per phase_5.md T2-B (target INTERPRETED ≥ 70%, mirroring the 100%-INTERPRETED distribution prior seeds chose) and the within-domain rule (cross-domain edges to epistemology / metaphysics / philosophy of language defer to P5-11). All edges are `pedagogical_prerequisite` only — no new predicates registered, so `PREDICATE_MANIFEST.md` is NOT in `paths_to_modify`. Estimated 24-28 nodes + 32-40 edges across the six clusters; final counts settle when authoring closes.

`ROUTING.md` gets a new "S-NNNN — Phase 5 logic seed lands (`0090_seed_logic_part1.sql`)" entry under the per-session-narrative section mirroring S-0050 / S-0053 / S-0054 / S-0056's shape: which clusters, why those concepts, edge structure summary, graph_version increment witness, forward-pointer to remaining tasks.

The plan addresses both criteria: criterion 0 (`migration_applied` for `0090_seed_logic_part1`) is addressed by authoring `0090_seed_logic_part1.sql` AND applying it via Supabase MCP `apply_migration` after the deliverable commit lands; criterion 1 (`validate_passes`) is addressed by running `engine/tools/validate.py` post-apply with `SUPABASE_DB_URL` set, confirming zero hard-fails (soft-warns are acceptable per phase_5.md T2-C philosophy-subdomain recalibrations).

Sub-range slots `0091-0099` left reserved per the granularity-within-single-file convention prior seeds adopted; opening additional slots is only warranted if mid-authoring the inventory grows beyond what 0090 can hold without sacrificing teaching_notes quality. The two-commit pattern from S-0053 / S-0054 / S-0056 holds: deliverable commit (migration + ROUTING.md entry) lands separate from the auto_target.json status-flip commit.
