paths_to_modify: ["engine/build_readiness/phase_5_production_audit_evidence/hubs_6_10_and_traces.md"]
criteria_addressed: [0, 1]

Routine session for T-PHASE-5-AUDIT task AUDIT-HT-2 (second half of decomposed-AUDIT-HT per Issue #58 / S-0117 administrative close). Single deliverable: `engine/build_readiness/phase_5_production_audit_evidence/hubs_6_10_and_traces.md` per master-plan §T1-A "Per-edge prompt template" + §"Per-node prompt template" + §"Three diagnostic syllabus traces" + §"Evidence file schema (fixed)" at `engine/build_readiness/phase_5_production_audit.md`.

Scope:

**Hubs 6–10 (full-incident-edge review)** per the auto_target task definition:
- HUB F: classical_logic (logic, deg 9)
- HUB G: meaning (language, deg 9)
- HUB H: normative_ethics (ethics, deg 9)
- HUB I: vienna_circle_logical_positivism (service, deg 9)
- HUB J: aristotelian_four_causes (service, deg 8)

~48 unique incident edges (raw count subject to inter-hub deduplication once incident edges are enumerated from the 15 Phase 5 seed migrations 0011 / 0016 / 0030 / 0036 / 0040 / 0046 / 0050 / 0060 / 0070 / 0080 / 0090 / 0100 / 0110 / 0020 / 0026). Per-edge SEP-anchored verdict + reasoning + confidence per master-plan §"Per-edge prompt template" verbatim.

**Per-hub-node review** on the 5 hub nodes themselves: SEP-anchored verdict + reasoning + confidence per master-plan §"Per-node prompt template" verbatim. Granularity-mismatch flag is load-bearing — vienna_circle_logical_positivism (Vienna-Circle school name) and aristotelian_four_causes (Aristotelian thinker-framework name) are literal named-examples in the master-plan §"Per-node prompt template" granularity-mismatch list; precedent at S-0119 hubs_1_5.md HUB nodes was 100% sound-high (concept-level appropriate hubs); per-node verdicts here likely include granularity-mismatch verdicts on HUB I + HUB J at minimum.

**Three diagnostic syllabus traces** per master-plan §"Three diagnostic syllabus traces" (T1-D locked):
- TRACE 1: `gettier_problem` (epistemology) — full topo-sort prereq tree per gate's depth-4 / 11-node trace; per-chain-node SEP-anchored review; record syllabus length + max/median depth + cross-domain hops + any questionable prereq + any obvious missing prereq + verdict on overall trace coherence.
- TRACE 2: `modal_realism` (metaphysics) — full topo-sort prereq tree per gate's depth-5 / 8-node trace (plus 2-hop bridge to set theory in service domain).
- TRACE 3: `social_contract_theory` (political philosophy) — substituted for absent `free_speech` per S-0081 gate; gate's depth-3 / 6-node trace; cross-bridges to ethics + service-node priors.

**Routine-mode posture (parametric-only):** per master-plan §"Empirical-fortification branch" routine-mode prohibition (load-bearing until T1-A through T1-D close at the closeout interactive session per ADR 0053). Medium-confidence + mutation-implying verdicts get fortified at the closeout per master-plan §"Forward pointers to closeout" / ADR 0059.

**Anti-scope:** No migration changes; no Issue filings (all findings recorded as evidence per master-plan candidate-findings-only scope); no soft-warn proposal authoring (master-plan pre-listed proposals + routine block's emerging cluster candidates already accumulated; closeout adjudicates). Disposition is interactive at the closeout.

paths_to_modify covers exactly the one deliverable file; operational allowlist (current.json, current_plan.md, auto_target.json status fields, register_state.json, archive/S-0120.json, ENGINE_LOG.md, STATE.md, HANDOFF.md) is permitted by check_routine_scope.py without enumeration. criteria_addressed indexes the two task criteria (file_exists at index 0, validate_passes at index 1).
