paths_to_modify: ["engine/build_readiness/phase_5_production_audit_evidence/hubs_1_5.md"]
criteria_addressed: [0, 1]

This session executes routine task AUDIT-HT-1 of T-PHASE-5-AUDIT — the first half of the
decomposed-AUDIT-HT (decomposed at the S-0117 administrative close per Issue #58 because the
original 96 edges + 10 nodes + 3 traces single-task scope hit context cap mid-authoring).
Scope: 5 top hubs by total degree (epistemic_justification deg 14, existence 11,
propositional_knowledge 11, causation 10, physicalism 10) under full incident-edge review.
The parametric scan of seed migrations (0011 / 0016 epistemology, 0030 / 0036 metaphysics,
0040 / 0046 mind, 0060 cross-bridges) yields 54 unique incident edges (56 raw with 2
inter-hub overlaps deduped: epistemic_justification → propositional_knowledge counted once,
existence → causation counted once). The deliverable is a single evidence file at
engine/build_readiness/phase_5_production_audit_evidence/hubs_1_5.md following the fixed
schema in the master plan §"Evidence file schema (fixed)" — 54 per-edge findings with
SEP-anchored verdict + reasoning + confidence per master-plan §T1-A prompt template, plus 5
per-node findings on the hub nodes themselves per master-plan §"Per-node prompt template",
plus optional cross-cutting observations recorded sparingly per master-plan §T2-D
(closeout is the synthesis surface, not the per-session evidence file). Routine-mode is
parametric-only — empirical-fortification branch prohibited per master-plan §"Empirical-
fortification branch" routine-mode prohibition (load-bearing until T1-A through T1-D close
at the closeout interactive session per ADR 0053). Sample density 100% on incident-edges
(full census per master-plan §"Sample-size policy" hubs row); not sampled — every incident
edge to each hub is reviewed because hubs are calibration anchors for the closeout's
aggregate-scan synthesis. AUDIT-HT-2 is sequential after this task per the auto_target
depends_on chain (sequential rather than parallel so two routine fires do not race to
author both halves of the decomposed AUDIT-HT). The path engine/session/current.json,
register_state.json, ENGINE_LOG.md, archive/S-0119.json, auto_target.json (status fields
only), STATE.md, HANDOFF.md are operational allowlist per ADR 0051; the deliverable path
hubs_1_5.md is the sole task-deliverable path under scope_lock per the auto_target task
spec.
