paths_to_modify: ["engine/build_readiness/seed_qa_evidence/shard_14.md"]
criteria_addressed: [0, 1]

Routine task SQA-14 — fourteenth shard of the T-SEED-QA census. Score 27 pedagogical_prerequisite edges against C1 (prerequisite soundness) and 20 nodes against C2 (teaching_notes traction) + C3 (summary cold-readability) per the pinned engine/build_readiness/seed_qa_audit.md rubric. Read shard content from the committed engine/build_readiness/seed_qa_evidence/shards.json (shard_14 key) — no DB access, sidestepping the Production Reads gate per the census design. Write a single scope-locked evidence file at the lone allowed path.

Criterion 0 (file_exists at engine/build_readiness/seed_qa_evidence/shard_14.md) satisfied by writing the file in the schema fixed by seed_qa_audit.md ("Evidence file schema"): shard metadata, per-edge findings with VERDICT/RATIONALE/AUDIT-TOUCHED, per-node findings with C2/C3 verdicts, shard tally, optional cross-cutting observations. Criterion 1 (validate_passes) satisfied by keeping the change inside the scope_lock allowed_paths so the pre-commit hook does not hard-fail.

Method: parametric judgment first per the rubric; cross-reference S-0122 audit follow-up migrations 0061-0065 against shard-14 edge tuples before flagging any defect (only one edge in shard-14 — E-25 performative_utterance->speech_act — carries inline `evidence` text naming a 0062 direction flip; programmatic tuple cross-check against the other migrations is part of the work). Apply the calibration the prior 13 shards have established (the diary at engine/session/.../wing_claude carries the running pattern catalog): discipline→object-of-study Defensible shape; trope/variant-specific Defensible shape; framework-introduces-its-concept Defensible shape; cycle-deferred-companion Sound-on-deferral-signal pattern. Borderline C3 calls held PASS only when an inline gloss appears in the same summary; ABSENT teaching_notes count as automatic C2 fail.

## Prior context (MemPalace boot search)

_Generated by `engine/tools/mempalace_boot_search.py` at 2026-05-15T09:03:28Z. Three formulations × similarity ≥0.60 = 0 drawers._

### Literal — `shard 14 qa census score 27 edges c1 prerequisite soundness 20 nodes c2 teaching_notes traction c3 summary cold-readability per the seed_qa_audit md rubric read the shard from seed_qa_evidence shards json write the evidence file`
- _no drawers above threshold_

### Conceptual — `shard census score edges prerequisite soundness`
- _no drawers above threshold_

### Adjacent — `shard 14 qa census score 27 edges c1 prerequisite soundness 20 nodes c2 teaching_notes traction c3 summary cold-readability per the seed_qa_audit md rubric read the shard from seed_qa_evidence shards json write the evidence file lessons pushback`
- _no drawers above threshold_

The boot search returned zero drawers above the 0.60 threshold — task-specific institutional memory for this census is concentrated in the daily diary entries (last_n=3 read at boot covers shards 11/12/13), not in standalone drawers. The diary provides the calibration anchor: 4 consecutive 0-defect C1 shards (10/11/12/13), running cumulative rate 6.5% at shard 13 (well under the 13% baseline), Defensible counts in the 1-4 band per shard, recurring sub-shapes catalogued (discipline→object-of-study, trope/variant-specific, framework-introduces-concept). Shard 14 is scored against the same calibration.
