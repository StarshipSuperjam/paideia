paths_to_modify: ["engine/build_readiness/phase_5_production_audit_evidence/language.md"]
criteria_addressed: [0, 1]

S-0112 routine session executes T-PHASE-5-AUDIT task AUDIT-LAN — Philosophy of language subdomain audit, the seventh of the eleven within-subdomain evidence sessions. AUDIT-LAN sits in the depends_on closure of AUDIT-CB (complete @ S-0104) and runs in parallel with the other 9 within-subdomain audits, of which AUDIT-EPI/-ETH/-MET/-MIN/-LOG are complete (S-0105 / S-0106 / S-0107 / S-0108-skipped / S-0109 / S-0110 / S-0111 — see ENGINE_LOG.md). The criteria the deliverable addresses are file_exists (criterion 0) and validate_passes (criterion 1) per auto_target.json AUDIT-LAN.

Scope is the single evidence-file path listed in `task.scope_lock.allowed_paths` plus the operational-allowlist files the lifecycle touches (current_plan.md, current.json, register_state.json, auto_target.json status fields, ENGINE_LOG.md, archive). The deliverable is the candidate-findings evidence file at `engine/build_readiness/phase_5_production_audit_evidence/language.md` per the master-plan §"Evidence file schema (fixed)" at `engine/build_readiness/phase_5_production_audit.md`.

Within-language edge population is 31 (all from `product/seed-graph/migrations/0070_seed_language_part1.sql`; single-file seed per master-plan §T1-B P5-04 — no a/b split). Per the master-plan §"Sample-size policy" table, AUDIT-LAN samples 11 of 31 = 35.5%; node sample is the edge-anchored union (15 unique nodes from the 11 sampled edges) ordered by md5 and the first 8 taken. The session-internal sample-expansion rule (>60% mid-sample defect rate → expand to 50% density) is honored if half-sample defect rate triggers it; otherwise standard 35% density holds.

Sample selection is deterministic md5(seed='AUDIT-LAN' || source_id || '|' || target_id) ordering for edges and md5(seed='AUDIT-LAN' || node_id) ordering for nodes; the 11 edges + 8 nodes are pre-computed and recorded in the evidence file's `## Sample metadata` section. The session applies the master-plan §"Per-edge prompt template" verbatim per edge and §"Per-node prompt template" verbatim per node, with INTERPRETED-only SEP citations per ADR 0011 / ADR 0046 (no quoted prose).

Routine-mode prohibition load-bearing: §"Empirical-fortification branch" (ADR 0059) is interactive-only until `engine/build_readiness/fetch_structural_reference_first_exercise.md` T1-A through T1-D resolve. This routine continues parametric-only review per the per-edge / per-node templates. Medium-confidence + mutation-implying verdicts are recorded as candidate findings; the closeout interactive session will fortify them as a closeout deliverable per the master-plan §"Forward pointers to closeout".

Disposition is closeout-deferred per master-plan §T1-F. The evidence file produces candidate findings only — verdict + reasoning + confidence per the schema, plus optional cross-cutting observations. No Issue filing, no ADR drafts, no soft-warn proposals authored as session deliverables. The four pre-listed audit-system-input proposals at master-plan §"Audit-system-input proposals" are skipped in per-edge verdicts (universal empty `evidence` field is graph-wide and not penalized per master-plan §T2-E); only NEW gate-feasible classes beyond those four are recorded under "other" verdict-NOTES.
