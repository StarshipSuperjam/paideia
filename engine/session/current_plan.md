paths_to_modify: ["engine/build_readiness/seed_qa_evidence/shard_06.md"]
criteria_addressed: [0, 1]

# Plan — S-0167 routine task SQA-06

Routine evidence session for the T-SEED-QA seed-graph QA census. Task SQA-06: score
shard 06 — 27 `pedagogical_prerequisite` edges against C1 (prerequisite soundness) and
20 nodes against C2 (teaching_notes traction) + C3 (summary cold-readability), per the
pinned rubric in `engine/build_readiness/seed_qa_audit.md`.

Shard content is read from the committed `seed_qa_evidence/shards.json` (`shard_06` key)
— routine sessions never touch the DB. Parametric judgment first per the rubric; SEP
fetch reserved for genuinely-uncertain verdicts. Audit follow-up migrations 0061–0065
were cross-referenced before flagging any C1 defect: none of the 27 (source,target)
pairs in this shard match a migration-touched pair, so zero audit-touched edges this
shard.

The single deliverable is the scope-locked evidence file
`engine/build_readiness/seed_qa_evidence/shard_06.md`, written to the fixed schema in
`seed_qa_audit.md`. This satisfies criterion 0 (`file_exists`) and criterion 1
(`validate_passes`). No other tracked paths are touched.

Prior-context note: the diary (last 3 entries) flags a watch item — over-Defensible
drift across the 19 evidence sessions. Shard 06 carries a notable Defensible cluster
(6/27), three of which share a "could-be-reversed: target is the more general concept"
shape; the evidence file addresses this head-on in its cross-cutting observations and
flags the cluster for the SQA-20 closeout's consistency review rather than silently
absorbing the uncertainty.

## Prior context (MemPalace boot search)

_Generated by `engine/tools/mempalace_boot_search.py` at boot. Three formulations ×
similarity ≥0.60 = 0 drawers above threshold._

criteria_addressed maps to auto_target.json SQA-06 criteria: [0] file_exists for
shard_06.md, [1] validate_passes (no hard-fails).
