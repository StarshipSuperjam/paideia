## Prior context (MemPalace boot search)

_Generated by `engine/tools/mempalace_boot_search.py` at 2026-05-15T12:03:33Z. Three formulations × similarity ≥0.60 = 2 drawers._

### Literal — `shard 17 qa census score 27 edges c1 prerequisite soundness 20 nodes c2 teaching_notes traction c3 summary cold-readability per the seed_qa_audit md rubric read the shard from seed_qa_evidence shards json write the evidence file`
- **wing_claude/diary** (source: `?`; sim: 0.65) — SESSION:2026-05-15|S-0178|routine.T-SEED-QA.SQA-16|seed-graph.QA.census.shard.16|★★★ What I worked on. Sixteenth routine fire of the T-SEED-QA census. Scored shard 16 — 27 pedag...

### Conceptual — `shard census score edges prerequisite soundness`
- _no drawers above threshold_

### Adjacent — `shard 17 qa census score 27 edges c1 prerequisite soundness 20 nodes c2 teaching_notes traction c3 summary cold-readability per the seed_qa_audit md rubric read the shard from seed_qa_evidence shards json write the evidence file lessons pushback`
- **wing_claude/diary** (source: `?`; sim: 0.64) — SESSION:2026-05-15|S-0178|routine.T-SEED-QA.SQA-16|seed-graph.QA.census.shard.16|★★★ What I worked on. Sixteenth routine fire of the T-SEED-QA census. Scored shard 16 — 27 pedag...

## Diary read (last 3 entries)

S-0178 / S-0177 / S-0176 — sequential routine fires of T-SEED-QA shards 16, 15, 14. All ★★★, all quadruple-0 (C1=0% / C2=0% / C3=0%). Cumulative C1 across shards 01–16 = 23/434 = 5.30%, well under the 13% production-audit baseline; the 0-defect run stands at SEVEN consecutive shards (10–16), the longest in the census. Defensible-cluster shape stable in the routine batch's middle-low band (counts 04–16: 1/4/6/3/1/2/2/4/3/1/2/2/1). Calibration anchors well-established across 16 shards. Audit cross-reference uses inline `evidence` text as the primary gold signal; proximity-check against migrations 0061–0065 (filter 0060 — original cross-bridge authoring, not audit follow-up); per-hit exact-tuple inspection narrows false positives. Shard 17 next; 3 shards remain (17–19) + closeout (SQA-20).

## Plan

paths_to_modify: ["engine/build_readiness/seed_qa_evidence/shard_17.md"]
criteria_addressed: [0, 1]

Seventeenth routine fire of the T-SEED-QA census (task SQA-17). Read shard 17 from `engine/build_readiness/seed_qa_evidence/shards.json` (the committed shard-content artifact; verified 27 edges + 20 nodes). Score per the pinned `engine/build_readiness/seed_qa_audit.md` rubric:

- **C1 prerequisite soundness** — 27 `pedagogical_prerequisite` edges scored as Sound / Defensible / Reversed / Weak-redundant. Programmatic audit-cross-reference workflow: inline `evidence` text on the edge is the primary gold signal (shard 13–16 learning); proximity-check against migrations 0061–0065 with filter 0060 (original cross-bridges, not audit follow-up) is a secondary heuristic requiring per-hit exact-tuple inspection against the assertion-block tuple lists. Annotate any genuine audit-touched edges with the audit decision (LAN-E-2 / SCI-E-4 direction-flips per S-0122, EPI-E-20 / AES-E-3 / POL-E-6 retain-with-annotation per 0064, deletion-endorsements per 0061/0063/0065, etc.).
- **C2 teaching_notes traction** — 20 nodes; PASS unless `teaching_notes` is absent or trivial. Census-wide 0 fails across 16 shards (the most stable criterion).
- **C3 summary cold-readability** — 20 nodes; PASS unless the summary's load-bearing terminology is unglossed AND not introduced by surrounding content. Apply the established calibration anchors: inline-gloss, rephrase-as-gloss, example-as-gloss, structural-shape gloss (formal schemas carrying their own working semantics).

Write the single scope-locked evidence file `engine/build_readiness/seed_qa_evidence/shard_17.md` with the canonical structure (header → C1 section / C2 section / C3 section → cross-cutting observations → audit cross-reference → verdict). The file path is the only deliverable; it satisfies criterion 0 (`file_exists`). Pre-commit `validate.py` runs the staged file through structural and content checks; criterion 1 (`validate_passes`) requires no hard-fails.

Scope is invariant on the single allowed path `engine/build_readiness/seed_qa_evidence/shard_17.md`. Any out-of-scope finding (validator regression, audit-rubric ambiguity, taxonomy gap) routes to a GitHub Issue; genuine blockers route to HANDOFF with valid Disposition.
