# DEPRECATED — source-of-truth moved to <repo-root>/pyproject.toml at S-0127
# per ADR 0064 (engine/adr/0064-uv-lockfile-and-reproducible-builds.md).
#
# Install with:
#   uv sync
#
# Refresh procedure when deps change is documented at:
#   engine/operations/dependency-discipline.md
#
# This file is retained as a one-line discoverability surface for cold-context
# users (and any unaware automation) landing on the legacy path. The file's
# removal waits until the next health-check audit confirms in-the-wild
# cross-references have caught up.
