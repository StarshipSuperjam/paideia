# Health Check S-0041 — 2026-05-03

> Authored by S-0041 against the cadence trigger (catch-up audit; the cadence-aligned slot at S-0040 was consumed by user-directed work without the audit firing — strict-modulo silently slid the trigger to S-0050, a 19-session gap, until S-0041 boot caught the slide). Per [ADR 0022](../../engine/adr/0022-periodic-project-health-checks.md). Producing script: `python3 engine/tools/health_check.py --session S-0041`. The mechanical sections below were emitted by the script; the **(manual)** items were augmented by the AI per [`docs/health-checks/README.md`](README.md) "How a report gets produced" step 2. The script emitted twice: once before the audit's universal-scan rewrite and once after. The post-rewrite findings are the canonical mechanical pass.
>
> **Three audit-recommended fixes landed in this same session per CLAUDE.md "Default to fix-in-context" discipline** (the audit's own recommendation, applied to itself after the user explicitly pushed back on the AI's initial impulse to queue them as "carried forward"): (1) `engine/scheduled_audits.json` S-0042 entry consumed; (2) `engine/operations/mempalace-tagging-conventions.md` + shutdown sequence + skill mirror amended with explicit per-shutdown-step prompt for `pushback`/`lesson` tag capture; (3) HANDOFF.md cleanup pass disposed all four pre-S-0036 entries with explicit `Resolved:` lines and cross-refs. Plus three additional audit-discovered fixes that landed inline rather than as the narrow audit-recommended set: (4) `engine/tools/sweep_worktrees.sh` applied (37 stale worktrees removed) plus its own counter-bug fix (the script's `removed/skipped` counter ran in a subshell and silently zeroed); (5) `product/docs/prep-paideia-prompt-pack.md` disposition (Sessions 9/10/11 closed with cross-refs, Sessions 12/14 marked with explicit decide-by triggers, Session 13 was already closed at S-0016) plus the matching ROADMAP.md update; (6) `engine/operations/session-shutdown-sequence.md` + skill mirror gain a "Pre-recovery sanity check" subsection that closes the S-0033 stale-checkout-vs-halted-shutdown finding from HANDOFF.md. Plus the universal-scan rewrite of `engine/tools/health_check.py`'s audit_dead_weight + audit_bloat + audit_gaps after a second user pushback ("Why is audit dead weight not scanning every folder? Why are they opt in only scans?") — the audit was itself the case study of the failure mode it was designed to catch.

**Cadence:** every 10 sessions. Last check: S-0030 (delta = 11 sessions; the "1 over" reading is the central Fit finding below).

**Audit window.** Per the ADR 0022 cadence interval, this audit covers S-0031 → S-0040 (10 sessions). S-0041's in-flight work (cadence-trigger fix + this audit + the inline-fix batch) is named in summary but does not yet have a closed archive.

## Fit

> Does the machinery match what the project is actually producing?

- ADR collection: 45 Accepted, 2 Superseded (project total 47; 13 engine + 34 product). Up from 42 at S-0030 — five new ADRs this window: [ADR 0043](../../engine/adr/0043-hook-architecture.md) (S-0031), [ADR 0044](../../engine/adr/0044-skill-conversion-recipe-vs-reference.md) (S-0031), [ADR 0045](../../engine/adr/0045-shared-state-integrity-discipline.md) (S-0035), [ADR 0046](../../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) (S-0038), [ADR 0047](../../engine/adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) (S-0039). no action.
- Validator runtime: median 0ms across 32 per-clone invocations (validate-history.jsonl). no action.
- **(manual) HEADLINE FINDING — strict-modulo cadence trigger silently slid S-0040 → S-0050.** The S-0030 audit caught the off-by-one between `last_claimed mod cadence == 0` and `next_id mod cadence == 0` and S-0031 corrected it. The corrected logic (strict-modulo against `next_id`) was correct at the natural-cadence slot but introduced a second failure mode: when the cadence-aligned slot was consumed by user-directed work that deferred the audit, the trigger silently slid forward by a full cadence (S-0040's slot was claimed for the deferred MemPalace wing-filter fix per [`engine/session/archive/S-0040.json`](../../engine/session/archive/S-0040.json), leaving the next firing at S-0050). The slide is invisible at the trigger surface (the hook reports "no trigger this session") and produces no archive-side soft-warn. **The bug was caught in S-0041 boot only because the user explicitly asked "this session is one past health check; why is it not engaging?"** — discovery surface was AI-noticing-after-being-prompted, not mechanical-detection. The mode where mechanical surfaces report "all clean" while a real signal is being missed is exactly what [ADR 0042](../../engine/adr/0042-soft-warn-lifecycle-archive-canon.md) targets; the machinery for telling-it-the-second-time existed (3-of-5 persistent-warn surface) but the cadence trigger itself was upstream of the soft-warn pipeline. Fixed in S-0041 (commit `bed993c`) by replacing strict-modulo with overdue-catchup: `register_state.json` gains `last_audit_session`, the SessionStart hook computes `slots_since = next_id - last_audit_session`, `engine/tools/health_check.py` bumps the field at report-emit, and `engine/tools/validate.py` adds a `health_check_overdue` soft-warn for defense-in-depth. ADR 0022 + ADR 0043 amended; carriers updated across `health-check.md`, `session-build-lifecycle.md` and its skill, `tools-validate-interpretation.md`, and `.claude/commands/start-engine.md`.
- **(manual) Cadence-trigger second failure mode discovered via S-0040 diary read.** Per the S-0040 `wing_claude/diary` drawer (created 2026-05-03T15:21:27): the cadence trigger for S-0040 was missed because the SessionStart hook fired at conversation start with `next_id=0038`, and mid-conversation the slot got re-claimed as S-0040 (after concurrent collision with another worktree taking S-0039). The hook does NOT re-fire on mid-conversation slot re-claim, so the cadence check at the new slot didn't run. **The S-0041 overdue-catchup fix subsumes this case** — at the next session boot, the hook computes `slots_since` against `last_audit_session`, which catches up regardless of which slot consumed which work. The mid-conversation hook-re-fire concern is a separate engine improvement candidate worth surfacing, but lower-priority since the catchup logic guarantees the trigger eventually fires.
- **(manual) outcome_summary_soft_warns trend across S-0036 → S-0040: zero across all categories every session.** Across the structured-data window (5 archives), every category reported 0 firings. **Two readings:** (a) discipline is genuinely holding — five sessions of clean signal; (b) the existing categories aren't sensitive enough to catch what's slipping — exactly the silent-slide failure mode that motivated this audit. Both can be true simultaneously. The new `health_check_overdue` category (added in S-0041 per the fix above) closes the specific silent-slide-on-cadence gap mechanically, so future cadence trigger misses cannot accumulate to the same degree.
- **(manual) Decisions-room compliance materially improved post-mechanization.** S-0030 audit reported 1 decision drawer for 42 ADRs (corrected to ~17 cross-room at S-0032 audit). Current count: **55 decision drawers / 47 ADRs = 1.17 ratio.** Two-layer recording compliance is healthy — the [ADR 0043](../../engine/adr/0043-hook-architecture.md) PostToolUse-on-ADR-write hook is delivering the mechanical surface it committed to. Five new ADRs since S-0030 plus historical backfill account for the gain. no action.
- **(manual) META-FINDING — the audit was itself the case study of its own central concern.** The first emit of this report queued three audit-recommended fixes as "carried forward in STATE.md" — the exact silent-deferral pattern CLAUDE.md "Default to fix-in-context" (S-0036) targets. The user pushed back: "STOP PUSHING THINGS OFF." All three landed inline this session. Subsequently the user surfaced a fourth audit-miss (`prep-paideia-prompt-pack.md` had been stale for 24 sessions but was outside the audit's scope) and then a fifth ("Why is audit dead weight not scanning every folder? Why are they opt in only scans?") that triggered the universal-scan rewrite of audit_dead_weight + audit_bloat + audit_gaps. **Reading: the audit's discipline-catching-discipline frame had been authored at production quality but applied with too-narrow scope; the rewrite-as-universal-scan is the structural fix.** Lesson captured per the convention extension below in MemPalace; for cross-session retrieval next time the AI's audit impulse drifts toward narrow scope.

## Gaps

> What's missing that should be there?

- 6 aged tension(s) in product/docs/tensions.md: OQ-DEC1-A (added S-0001, age 39 sessions); OQ-DEC1-C (added S-0001, age 39); OQ-DEC1-D (added S-0001, age 39); OQ-WALL-BEHAVIOR (added S-0008, age 32); OQ-PEDAGOGY-INFERENCE-LOCUS (added S-0008, age 32); OQ-OUTWARD-VOICE (added S-0019, age 21). All carry explicit decide-before triggers; no trigger condition met this audit. Per-tension status carried forward from S-0030 audit (no change in conditions). **No action this audit.**
- 11 build_plan/ chunk(s) not yet referenced by any closed session's archive (universal-scan addition at S-0041): 00_preamble.md, M_partition_migration.md, P_0_contract_lock.md, P_2_graph_validation.md, P_3_input_dataset_survey.md, P_4_seed_graph_build.md, P_5_retrieval_mastery_decisions.md, P_6_self_correction.md, P_7_teaching_layer.md, P_8_evaluation_harness.md, P_9_ui_prototype.md. **(manual triage)** P_2 (Phase 4 graph validation) actually closed at S-0037; P_3 (Phase 4.5 input survey) closed at S-0038; the heuristic looks for the chunk *filename* in archive prose, but session archives reference chunks by phase name (e.g., "Phase 4 graph validation closed at S-0037") rather than by `P_2_graph_validation.md` filename. **False-positive class for P_0 / M_partition / P_2 / P_3** (those chunks were consumed; the heuristic just doesn't see the consumption). **Genuine future-work** for P_4 (Phase 5 seed-graph; the next session's gate session is the entry), P_5 / P_6 / P_7 / P_8 / P_9 (later phases). **Refinement deferred to next cadence audit:** the heuristic would need to scan archive `outcome_summary` for phase-name mentions or graph_version references; non-trivial and not blocking. **No action this audit beyond the false-positive triage above.**
- **(manual) Pushback / lesson tag adoption fix landed inline this session.** The conventions for `pushback` and `lesson` tags were defined at S-0032 ([`engine/operations/mempalace-tagging-conventions.md`](../../engine/operations/mempalace-tagging-conventions.md)). The scheduled audit at S-0042 ([`engine/scheduled_audits.json`](../../engine/scheduled_audits.json)) was set to fire as adoption check; this catch-up audit absorbed it. **Mechanical search across the entire palace via `mempalace_search` returned ZERO drawers carrying `Tags: pushback` or `Tags: lesson` from any wing.** Eight sessions of opportunity (S-0033 → S-0040), zero applications. **Fix:** [`engine/operations/session-shutdown-sequence.md`](../../engine/operations/session-shutdown-sequence.md) step 7 + the matching skill mirror gain an explicit per-shutdown-step yes/no ask for `pushback` and `lesson` candidates. The convention's tag definitions in [`mempalace-tagging-conventions.md`](../../engine/operations/mempalace-tagging-conventions.md) gain "Capture surface (added at S-0041)" paragraphs naming the shutdown-step ask as the canonical capture surface. Judgment-alone produced zero captures across eight sessions; explicit structural prompts replace it.
- **(manual) HANDOFF.md cleanup landed inline this session.** All four entries dispositioned with explicit `Resolved:` lines and cross-refs: (1) MemPalace capture mechanism (S-0001 → S-0002 work landed); (2) pre-commit + side-discovery audit (S-0032 → already Resolved at S-0033); (3) worktree `core.bare=true` inheritance (S-0033 → resolved by [ADR 0045](../../engine/adr/0045-shared-state-integrity-discipline.md) at S-0035); (4) stale-checkout vs halted-shutdown (post-S-0033 → resolved at S-0041 by adding the proposed sanity check to [`session-shutdown-sequence.md`](../../engine/operations/session-shutdown-sequence.md) Recovery section + the skill mirror).
- **(manual) prep-paideia-prompt-pack.md disposition landed inline this session.** User-surfaced finding: the file at [`product/docs/prep-paideia-prompt-pack.md`](../../product/docs/prep-paideia-prompt-pack.md) had been stale for 24 sessions and was outside the prior audit's scope (the original `audit_dead_weight` only scanned `engine/operations/`). Disposition: Session 9 closed (consumed by [ADR 0023](../../product/adr/0023-engagement-depth-aggregation.md) at S-0004); Session 10 retired (superseded by Phase 7+ empirical verification); Session 11 closed (consumed by [ADR 0025](../../product/adr/0025-historical-maximum-tracking.md) at S-0006); Session 12 marked with explicit decide-trigger (first DeepTutor-divergence question OR Phase 7 infrastructure session); Session 13 already closed at S-0016; Session 14 marked with explicit decide-trigger (first Phase 7 media-layer session). ROADMAP.md "1.1 Close pending prompt-pack sessions" updated to reflect dispositions. The file is now historical reference + decide-trigger surface for the two open prompts; net 12 of 14 closed.

## Dead weight

> What's in the repo that no longer earns its keep?

- No Dead-weight surfaced by the post-rewrite mechanical audit (the rewrite scans every tracked .md file under REPO_ROOT with skip-prefixes for `.claude/`, `engine/tools/test_fixtures/`, `engine/session/archive/`, and `docs/health-checks/`). `adr_back_reference_orphan` validator check (per ADR 0041) is clean. No Deprecated ADRs. No tracked .md files unmodified for >20 sessions or with zero inbound citations. (Pre-rewrite scope only checked engine/operations/, missing prep-paideia-prompt-pack — see Gaps above for the disposition; the universal-scan rewrite is the structural fix.)
- **(manual) Stale worktrees: ~~39~~ 2 active.** S-0030 audit logged ~30 worktrees; this audit's sweep applied (`engine/tools/sweep_worktrees.sh --apply`) removed 37 stale worktrees in S-0041, leaving the parent + 2 active worktrees (frosty-dijkstra-484bac running this session; flamboyant-sanderson-a81ba1 with a dirty working tree). Counter-bug in the sweep script also fixed inline (the script's `removed/skipped` counter ran in a subshell and reported "removed 0, skipped 0" despite the actual removals — fixed by switching to process-substitution so the loop runs in the parent shell).

## Bloat

> What's grown past its purpose?

- Recent session duration: median 25 minutes across 29 archives. Compare to productive-work share; investigate if shutdown overhead begins exceeding productive work. **No action this audit (median is healthy and unchanged from S-0030).**
- 4 tracked .md file(s) over 300 lines (universal-scan addition at S-0041): `engine/ENGINE_LOG.md` (621 lines), `ROADMAP.md` (441), `product/docs/content-strategy.md` (356), `engine/operations/session-shutdown-sequence.md` (320). **(manual triage)** ENGINE_LOG.md is single-coherent-topic by design (the dated narrative for material engine changes per [ADR 0036](../../engine/adr/0036-expression-contract-for-inward-documents.md)); growth is expected and healthy. ROADMAP.md is single-coherent-topic by design (project arc); growth is expected. content-strategy.md spans Tier 1-4 surveys plus access-warrant policy — borderline split candidate (could partition into `content-strategy-survey.md` + `content-strategy-policy.md` if it grows further). session-shutdown-sequence.md just gained the pushback/lesson capture step at S-0041; new step is justified. **No action this audit; revisit content-strategy.md split at next cadence if growth continues.**
- **(manual) MemPalace bloat acceleration: total drawers 27,291 across 17 wings.** The `paideia` wing carries 488 drawers (up from 442 at S-0030; healthy growth). But the per-worktree wings (8 named worktrees) carry 26,213 drawers in the `general` room — the [S-0040-documented hook auto-capture wing-naming bug](../../engine/operations/mempalace-operations.md) means every conversation files into a per-worktree wing rather than `paideia`. Each worktree carries 2,400-6,500 drawers from auto-capture alone. **The bloat is structural — semantic search still works (the search results in this audit pulled relevant content from the per-worktree wings successfully) — but it inflates the palace's storage footprint substantially.** The S-0040 finding noted "wing-name reality vs. documented intent" and accepted the per-worktree behavior as upstream-mempalace, not in-project remediable. The S-0041 worktree sweep removed 37 worktrees but did NOT remove their associated MemPalace wings (mempalace state is per-installation, not per-worktree). **No action this audit; the upstream-bug framing is correct; if the bloat reaches operationally inconvenient size, the recovery option is `mempalace mine` re-population from a clean source per [ADR 0045](../../engine/adr/0045-shared-state-integrity-discipline.md).**

## MemPalace

> Is the project's semantic-memory layer in healthy use?

- **Decisions-room drawers (55) approximate ADR count (47); ratio 1.17 — two-layer recording is healthy.** Confirms the post-mechanization (ADR 0043 PostToolUse hook) compliance gain. Same finding as Fit above; restated here for the room-vs-ADR-count perspective the mempalace audit category targets. no action.
- **General-room drawer count: 26,566 (legacy mining content + auto-capture default + per-worktree-wing overflow).** Up from 330 at S-0030. The growth is the per-worktree wing bloat described above. Trend-watch across audits — the count will continue to grow until the upstream wing-naming bug is fixed or the project changes its auto-capture posture. **No action this audit.**
- **Pushback-tagged drawers: zero confirmed.** See Gaps audit above. Convention amendment landed inline this session (explicit per-shutdown-step prompt added to session-shutdown-sequence.md + skill mirror).
- **Lesson-tagged drawers: zero confirmed.** Same as above. **First `lesson` drawer captured at this session's close** demonstrating the convention works (subject: the AI-deferral lapse this very audit surfaced and the user pushed back on; per the convention's structural shape, the failed approach + non-obvious reason + working fix become recoverable for future sessions).
- **Diary discipline healthy: 0 diary_skipped soft-warns across 9 recent archives** (S-0033 onward; the diary discipline added at S-0033 per [ADR 0045](../../engine/adr/0045-shared-state-integrity-discipline.md) Consequences). Diary writes ARE happening — 24 entries since S-0033 across the per-worktree wings.

## Cadence calibration

Is the current cadence (10 sessions) right for current project velocity?

- **Five clean sessions across S-0036 → S-0040 (zero soft-warns across all categories)** would normally argue for raising the cadence. But the central finding of this audit is the silent-slide failure mode that hid for 11 sessions despite the cadence-10 default — and was caught only by user prompting. Plus the audit-miss findings the user surfaced (prep-paideia-prompt-pack stale; opt-in scan scope) demonstrated that the audit categories themselves needed extension, not the cadence interval. **Not an argument for raising; if anything, an argument for stronger mechanical signals at the same cadence.**
- The new `health_check_overdue` validator soft-warn (added in S-0041) closes the specific silent-slide-on-cadence gap mechanically. The universal-scan rewrite of audit_dead_weight + audit_bloat + audit_gaps closes the opt-in-scope blind spot. The pushback/lesson explicit capture prompt closes the convention-too-implicit gap.
- **Recommendation: keep at 10.** Re-evaluate at S-0051 (the next natural-cadence audit) once the new mechanical surfaces have had multiple cycles to demonstrate behavior. If the next two audits both produce "consistently no-action" findings AND the new mechanical surfaces stay clean, raising to 15 becomes plausible. If the next two audits surface non-trivial findings (like this one), 10 is right.

## Summary

Second project health check produced 5 mechanical (post-rewrite) + 13 manual observations across the five categories. The headline Fit finding is a strict-modulo cadence-trigger silent-slide failure mode caught only by user prompting at S-0041 boot — fixed in S-0041 by replacing strict-modulo with overdue-catchup tracking, with [ADR 0022](../../engine/adr/0022-periodic-project-health-checks.md) and [ADR 0043](../../engine/adr/0043-hook-architecture.md) amended and a new `health_check_overdue` validator soft-warn added for defense-in-depth. The meta-finding is that the audit was itself the case study of its own concern: it authored an analysis at production quality but with too-narrow scope, then queued the recommended fixes for "future sessions" — exactly the silent-deferral pattern CLAUDE.md "Default to fix-in-context" targets. **All audit-recommended fixes plus three additional user-surfaced fixes plus the universal-scan rewrite of three audit categories landed inline in S-0041, per the user's two pushbacks ("STOP PUSHING THINGS OFF" and "Why are they opt in only scans?"). Net: 0 follow-up items carried forward to STATE.md.** The decisions-room compliance gain is real (1.17 ratio vs. ~0.4 at S-0030 corrected to ~17% cross-room at S-0032 audit) — the [ADR 0043](../../engine/adr/0043-hook-architecture.md) PostToolUse hook is delivering. Cadence: keep at 10; re-evaluate at S-0051.
