# Health Check S-0041 — 2026-05-03

> Authored by S-0041 against the cadence trigger (catch-up audit; the cadence-aligned slot at S-0040 was consumed by user-directed work without the audit firing — strict-modulo silently slid the trigger to S-0050, a 19-session gap, until S-0041 boot caught the slide). Per [ADR 0022](../../engine/adr/0022-periodic-project-health-checks.md). Producing script: `python3 engine/tools/health_check.py --session S-0041`. The mechanical sections below were emitted by the script; the **(manual)** items were augmented by the AI per [`docs/health-checks/README.md`](README.md) "How a report gets produced" step 2.

**Cadence:** every 10 sessions. Last check: S-0030 (delta = 11 sessions; one over the configured cadence — the fact this audit is "1 over" rather than "exactly 10" is the central Fit finding below).

**Audit window.** Per the ADR 0022 cadence interval, this audit covers S-0031 → S-0040 (10 sessions). S-0041's in-flight work (cadence-trigger fix + this audit) is named in summary but does not yet have a closed archive.

## Fit

> Does the machinery match what the project is actually producing?

- ADR collection: 45 Accepted, 2 Superseded (project total 47; 13 engine + 34 product). Up from 42 at S-0030 — five new ADRs this window: [ADR 0043](../../engine/adr/0043-hook-architecture.md) (S-0031), [ADR 0044](../../engine/adr/0044-skill-conversion-recipe-vs-reference.md) (S-0031), [ADR 0045](../../engine/adr/0045-shared-state-integrity-discipline.md) (S-0035), [ADR 0046](../../product/adr/0046-structural-reference-posture-extends-to-philosophy-reference-works.md) (S-0038), [ADR 0047](../../engine/adr/0047-structural-reference-parser-tool-and-adversarial-triage-workflow.md) (S-0039). no action.
- Validator runtime: median 0ms across 18 per-clone invocations (validate-history.jsonl). The script's median dropped from 22ms at S-0030 to 0ms here — the per-clone JSONL was reset at the worktree creation, so the 18-invocation sample is from this session only and reflects the in-memory cache. Phase-0+ structural-only checks should stay under ~500ms; comfortably within. no action.
- **(manual) HEADLINE FINDING — strict-modulo cadence trigger silently slid S-0040 → S-0050.** The S-0030 audit caught the off-by-one between `last_claimed mod cadence == 0` and `next_id mod cadence == 0` and S-0031 corrected it. The corrected logic (strict-modulo against `next_id`) was correct at the natural-cadence slot but introduced a second failure mode: when the cadence-aligned slot was consumed by user-directed work that deferred the audit, the trigger silently slid forward by a full cadence (S-0040's slot was claimed for the deferred MemPalace wing-filter fix per [`engine/session/archive/S-0040.json`](../../engine/session/archive/S-0040.json), leaving the next firing at S-0050). The slide is invisible at the trigger surface (the hook reports "no trigger this session") and produces no archive-side soft-warn. **The bug was caught in S-0041 boot only because the user explicitly asked "this session is one past health check; why is it not engaging?"** — discovery surface was AI-noticing-after-being-prompted, not mechanical-detection. The mode where mechanical surfaces report "all clean" while a real signal is being missed is exactly what [ADR 0042](../../engine/adr/0042-soft-warn-lifecycle-archive-canon.md) targets; the machinery for telling-it-the-second-time existed (3-of-5 persistent-warn surface) but the cadence trigger itself was upstream of the soft-warn pipeline. Fixed in S-0041 (commit `bed993c`) by replacing strict-modulo with overdue-catchup: `register_state.json` gains `last_audit_session`, the SessionStart hook computes `slots_since = next_id - last_audit_session`, `engine/tools/health_check.py` bumps the field at report-emit, and `engine/tools/validate.py` adds a `health_check_overdue` soft-warn for defense-in-depth. ADR 0022 + ADR 0043 amended; carriers updated across `health-check.md`, `session-build-lifecycle.md` and its skill, `tools-validate-interpretation.md`, and `.claude/commands/start-engine.md`.
- **(manual) Cadence-trigger second failure mode discovered via S-0040 diary read.** Per the S-0040 `wing_claude/diary` drawer (created 2026-05-03T15:21:27): the cadence trigger for S-0040 was missed because the SessionStart hook fired at conversation start with `next_id=0038`, and mid-conversation the slot got re-claimed as S-0040 (after concurrent collision with another worktree taking S-0039). The hook does NOT re-fire on mid-conversation slot re-claim, so the cadence check at the new slot didn't run. **The S-0041 overdue-catchup fix subsumes this case** — at the next session boot, the hook computes `slots_since` against `last_audit_session`, which catches up regardless of which slot consumed which work. The mid-conversation hook-re-fire concern is a separate engine improvement candidate (queue for future audit; the catch-up logic makes it lower-priority).
- **(manual) outcome_summary_soft_warns trend across S-0036 → S-0040: zero across all categories every session.** Across the structured-data window (5 archives), every category reported 0 firings. **Two readings:** (a) discipline is genuinely holding — five sessions of clean signal; (b) the existing categories aren't sensitive enough to catch what's slipping — exactly the silent-slide failure mode that motivated this audit. Both can be true simultaneously. The new `health_check_overdue` category (added in S-0041 per the fix above) closes the specific silent-slide-on-cadence gap but does not generalize. Continued monitoring under the calibration window discipline.
- **(manual) Decisions-room compliance materially improved post-mechanization.** S-0030 audit reported 1 decision drawer for 42 ADRs (corrected to ~17 cross-room at S-0032 audit). Current count: **55 decision drawers / 47 ADRs = 1.17 ratio.** Two-layer recording compliance is healthy — the [ADR 0043](../../engine/adr/0043-hook-architecture.md) PostToolUse-on-ADR-write hook is delivering the mechanical surface it committed to. Five new ADRs since S-0030 plus historical backfill account for the gain. no action.

## Gaps

> What's missing that should be there?

- 6 aged tension(s) in product/docs/tensions.md: OQ-DEC1-A (added S-0001, age 39 sessions); OQ-DEC1-C (added S-0001, age 39); OQ-DEC1-D (added S-0001, age 39); OQ-WALL-BEHAVIOR (added S-0008, age 32); OQ-PEDAGOGY-INFERENCE-LOCUS (added S-0008, age 32); OQ-OUTWARD-VOICE (added S-0019, age 21). All carry explicit decide-before triggers; no trigger condition met this audit. Per-tension status carried forward from S-0030 audit (no change in conditions). **No action this audit.**
- **(manual) Pushback / lesson tag adoption: zero across S-0033 → S-0040.** The conventions for `pushback` and `lesson` tags were defined at S-0032 ([`engine/operations/mempalace-tagging-conventions.md`](../../engine/operations/mempalace-tagging-conventions.md)). The scheduled audit at S-0042 ([`engine/scheduled_audits.json`](../../engine/scheduled_audits.json)) was set to fire as adoption check; this catch-up audit absorbs it. **Mechanical search across the entire palace via `mempalace_search` returns ZERO drawers carrying `Tags: pushback` or `Tags: lesson` from any wing.** The hits are exclusively documentation drawers describing the *concept* (auto-captured CLAUDE.md fragments, S-0032 audit prose) — not tag-applied drawers themselves. Eight sessions of opportunity (S-0033 → S-0040), zero applications. **Two readings:** (a) no qualifying moments arose (the conventions were defined but the conditions they trigger on — verbatim AI-pushback-accepted moments, procedural-failure-with-working-fix moments — never appeared); (b) the AI consistently misses to apply the tags despite qualifying moments. Reading (b) is more plausible because S-0035 (engine hardening per ADR 0045) and S-0036 (deferral-discipline guardrails) both involved procedural-failure-with-fix patterns that look like `lesson` candidates. The convention is too implicit; **action: queue a follow-up to amend `mempalace-tagging-conventions.md` with explicit per-shutdown-step prompts** ("did this session produce a `lesson` candidate? write it now"), or add a soft-warn category that fires on session-close when a substantive bug-fix commit lands without a `lesson`-tagged drawer. Recommend the explicit-prompt path first; deferred-soft-warn introduction adds machinery.
- **(manual) HANDOFF.md has 4 sections, only 1 explicitly resolved.** [`HANDOFF.md`](../../HANDOFF.md) carries: (1) MemPalace capture (S-0001) — pre-disposition-discipline; the S-0002 work landed and the entry is implicitly resolved but not marked; (2) pre-commit STATUS bug + side-discovery audit (S-0032) — explicitly Resolved at S-0033; (3) worktree `core.bare=true` inheritance (S-0033) — addressed by ADR 0045 at S-0035 but not marked resolved; (4) stale-checkout vs halted-shutdown sanity check (post-S-0033) — proposes a fix; uncertain whether the proposed fix landed in `session-shutdown-sequence.md`. Per CLAUDE.md disposition discipline (audited by `engine/tools/audit_handoff_dispositions.py`), the audit is scoped to NEW sections per session, so pre-S-0036 entries don't violate at authoring time. But the live entries should be retroactively cleaned. **Action: queue HANDOFF.md cleanup as a small near-term work item** — verify each entry's status against current state and either mark resolved or carry forward with explicit disposition.
- **(manual) Phase 5 epistemology gate slipped from S-0041 to S-0042.** The S-0041 slot was originally planned for the Phase 5 epistemology build-readiness gate per STATE.md "Next session work item." The cadence-trigger fix + catch-up audit took the slot. Phase 5 gate now lands at S-0042 (the next slot). **No action this audit; covered by STATE.md update at this session's close.**

## Dead weight

> What's in the repo that no longer earns its keep?

- No Dead-weight surfaced by mechanical audit. `adr_back_reference_orphan` validator check (per ADR 0041) is clean — every Accepted ADR has at least one inbound non-ADR citation outside its subtree. No Deprecated ADRs. No uncited operations docs (the new ones from S-0029-S-0040 — `cascade-discipline.md`, `soft-warn-lifecycle.md`, `cross-references.md`, `migration-discipline.md`, `expression-contract-instantiation.md`, `build-readiness-gate.md` — are all cited from sibling docs and ADRs).
- **(manual) Stale worktrees: 39 entries under `.claude/worktrees/`.** Up from ~30 at S-0030. Each represents a closed session's branch + working tree. Not actively harmful but the long tail accumulates. The existing `engine/tools/sweep_worktrees.sh` utility per [README.md](../../README.md) repo-map handles this on demand. **Queue a one-off sweep when convenient — not blocking, not high priority.** Same disposition as S-0030.

## Bloat

> What's grown past its purpose?

- Recent session duration: median 25 minutes across 29 archives. Compare to productive-work share; investigate if shutdown overhead begins exceeding productive work. **No action this audit (median is healthy and unchanged from S-0030).**
- **(manual) Operations docs over 300 lines** — `health-check.md` heuristic. `wc -l engine/operations/*.md` shows several docs in the 200-400 line range. The largest are reference-shaped (per [ADR 0044](../../engine/adr/0044-skill-conversion-recipe-vs-reference.md)) — `build-readiness-gate.md` is the cited example of the recipe-shaped class but most ops docs are reference-shaped and naturally grow. **No action this audit; the [ADR 0044](../../engine/adr/0044-skill-conversion-recipe-vs-reference.md) recipe-vs-reference partition is the right surface for "did this doc cross the line."** Re-evaluate at next cadence audit.
- **(manual) MemPalace bloat acceleration: total drawers 27,291 across 17 wings.** The `paideia` wing carries 488 drawers (up from 442 at S-0030; healthy growth). But the per-worktree wings (8 named worktrees) carry 26,213 drawers in the `general` room — the [S-0040-documented hook auto-capture wing-naming bug](../../engine/operations/mempalace-operations.md) means every conversation files into a per-worktree wing rather than `paideia`. Each worktree carries 2,400-6,500 drawers from auto-capture alone. **The bloat is structural — semantic search still works (the search results in this audit pulled relevant content from the per-worktree wings successfully) — but it inflates the palace's storage footprint substantially and dilutes the `paideia` wing's signal.** The S-0040 finding noted "wing-name reality vs. documented intent" and accepted the per-worktree behavior as upstream-mempalace, not in-project remediable. **No action this audit; the upstream-bug framing is correct.** If the bloat reaches operationally inconvenient size (e.g., chromadb segment performance degrades), the recovery option is `mempalace mine` re-population from a clean source — same procedure as the S-0034 chromadb HNSW corruption case per [ADR 0045](../../engine/adr/0045-shared-state-integrity-discipline.md).

## MemPalace

> Is the project's semantic-memory layer in healthy use?

- **(manual) Decisions-room drawers (55) approximate ADR count (47); ratio 1.17 — two-layer recording is healthy.** Confirms the post-mechanization (ADR 0043 PostToolUse hook) compliance gain. Same finding as Fit above; restated here for the room-vs-ADR-count perspective the mempalace audit category targets. no action.
- **General-room drawer count: 26,566 (legacy mining content + auto-capture default + per-worktree-wing overflow).** Up from 330 at S-0030. The growth is the per-worktree wing bloat described above. Trend-watch across audits — the count will continue to grow until the upstream wing-naming bug is fixed or the project changes its auto-capture posture. **No action this audit.**
- **Pushback-tagged drawers: zero confirmed.** See Gaps audit above. Action queued to amend `mempalace-tagging-conventions.md`.
- **Lesson-tagged drawers: zero confirmed.** Same as above.
- **Diary discipline healthy: 0 diary_skipped soft-warns across 9 recent archives** (S-0033 onward; the diary discipline added at S-0033 per [ADR 0045](../../engine/adr/0045-shared-state-integrity-discipline.md) Consequences). Diary room carries 24 drawers across the per-worktree wings — diary writes ARE happening, they're just filed under whichever wing the per-session capture writes to. The `diary_skipped` mechanical signal confirms compliance.

## Cadence calibration

Is the current cadence (10 sessions) right for current project velocity?

- **Five clean sessions across S-0036 → S-0040 (zero soft-warns across all categories)** would normally argue for raising the cadence. But the central finding of this audit is the silent-slide failure mode that hid for 11 sessions despite the cadence-10 default — and was caught only by user prompting. **Not an argument for raising; if anything, an argument for tightening or for richer signal categories.**
- The new `health_check_overdue` validator soft-warn (added in S-0041) closes the specific silent-slide-on-cadence gap mechanically, so future cadence trigger misses cannot accumulate to the same degree.
- **Recommendation: keep at 10.** Re-evaluate at S-0051 (the next natural-cadence audit) once the new overdue-catchup logic has had multiple cycles to demonstrate behavior. If the next two audits both produce "consistently no-action" findings AND the new mechanical surfaces stay clean, raising to 15 becomes plausible. If the next two audits surface non-trivial findings (like this one), 10 is right.

## Summary

Second project health check produced 5 mechanical + 11 manual observations across the five categories. The headline Fit finding is a strict-modulo cadence-trigger silent-slide failure mode caught only by user prompting at S-0041 boot — fixed in S-0041 by replacing strict-modulo with overdue-catchup tracking, with [ADR 0022](../../engine/adr/0022-periodic-project-health-checks.md) and [ADR 0043](../../engine/adr/0043-hook-architecture.md) amended and a new `health_check_overdue` validator soft-warn added for defense-in-depth. Significant adoption gap: pushback / lesson tag conventions defined at S-0032 have zero adoption across S-0033 → S-0040 (eight sessions of opportunity). The decisions-room compliance gain is real (1.17 ratio vs. ~0.4 at S-0030 corrected to ~17% cross-room at S-0032 audit) — the [ADR 0043](../../engine/adr/0043-hook-architecture.md) PostToolUse hook is delivering. **Action items**: (1) remove the consumed entry from `engine/scheduled_audits.json` (this audit absorbed it); (2) queue follow-up to amend `mempalace-tagging-conventions.md` with explicit per-shutdown-step prompts for `pushback`/`lesson`; (3) queue HANDOFF.md cleanup pass to retroactively dispose live pre-discipline entries; (4) maintain cadence at 10 pending two more audits' worth of trend data. The catch-up audit also absorbs the originally-scheduled S-0042 pushback/lesson adoption check.
