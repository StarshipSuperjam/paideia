# Health Check S-0030 — 2026-05-02

> Authored by S-0030 against the cadence trigger (manual fire at the project-setup-to-project-build phase boundary per [`engine/operations/health-check.md`](../../engine/operations/health-check.md) cadence policy "one-off audit at the boundary, independent of the regular cadence"). Per [ADR 0022](../../engine/adr/0022-periodic-project-health-checks.md). Producing script: `python3 engine/tools/health_check.py --session S-0030`. The mechanical sections below were emitted by the script; the **(manual)** items were augmented by the AI per [`docs/health-checks/README.md`](README.md) "How a report gets produced" step 2.

**Cadence:** every 30 sessions. First check.

## Fit

> Does the machinery match what the project is actually producing?

- ADR collection: 40 Accepted, 2 Superseded (project total 42; 9 engine + 33 product). no action.
- Validator runtime: median 22ms across 33 per-clone invocations (validate-history.jsonl). Phase-0+ structural-only checks should stay under ~500ms; comfortably within. no action.
- **(manual) MemPalace coverage gap on the two-layer decision recording rule.** MemPalace `paideia` wing carries 442 drawers across 5 rooms — **341 in `general`, 88 in `operations`, 10 in `foundation-planning-s0001`, 2 in `s0003-adr-collection`, 1 in `decisions`.** Project ADR count is 42 (40 Accepted + 2 Superseded). [CLAUDE.md](../../CLAUDE.md) "Two-layer decision recording" rule mandates every settled decision land in BOTH an ADR AND a MemPalace `decision`-tagged drawer. With 1 decision drawer for 42 ADRs, the discipline has been almost entirely posture. The Session β PostToolUse-on-ADR-write hook (planned per the approved infrastructure plan) will surface this at authoring time; until β lands, the gap accumulates. Queue for β.
- **(manual) Cadence-trigger off-by-one in `engine/operations/health-check.md` and `.claude/commands/start-engine.md`.** The trigger logic reads `last_claimed mod 30 == 0`, which strictly fires at S-NNNN's boot when the *previous* session was the cadence-numbered one (e.g., trigger fires at S-0031 boot when `last_claimed = S-0030 → 30 % 30 = 0`). [ROADMAP.md](../../ROADMAP.md) and ADR 0022 say "first check expected around S-0030" — the project's prose intent is that the cadence-numbered session itself runs the audit. This audit fires manually as a phase-boundary trigger to honor the prose intent; the strict logic would have deferred to S-0031. Queue a small fix in Session β (or earlier) to switch the trigger to `next_id mod 30 == 0` (the slot about to be claimed) for prose-logic alignment.
- **(manual) Mid-audit bug in `engine/tools/health_check.py` discovered + fixed in-session.** First run of the script surfaced 3 false-positive ADR Consequences-deliverable findings (ADRs 0018, 0033, 0035) because `audit_gaps`'s path resolution checked only repo-root-relative paths, while the equivalent validator check `validate_adr_consequences_deliverable_audit` was extended to dual-resolution (repo-root + ADR-directory-relative) at S-0029 Step 7. Symmetry was missed; same bug persisted in two places. Fixed inline; new test `test_audit_gaps_promised_path_relative_to_adr_dir` locks it in (46 pytest now). Records as a Fit finding because it's an example of the discipline-asymmetry mode Cascade Discipline (ADR 0041) targets — the validator ecosystem sprawled, the ops-doc / cross-references map tracks the relationships, and a future restructure should benefit from the symmetry.

## Gaps

> What's missing that should be there?

- 6 aged tension(s) in `product/docs/tensions.md`: OQ-DEC1-A (added S-0001, age 28 sessions), OQ-DEC1-C (added S-0001, age 28 sessions), OQ-DEC1-D (added S-0001, age 28 sessions), OQ-WATCH-FLAG-FILE (added S-0001, age 28 sessions), OQ-WALL-BEHAVIOR (added S-0008, age 21 sessions), OQ-PEDAGOGY-INFERENCE-LOCUS (added S-0008, age 21 sessions). Per-tension triage:
  - **OQ-DEC1-A through D** — Phase DEC.1 architecture decisions; decide-before Phase 6. Actionable only when Phase 5 (seed graph) progresses to the point where DEC.1 architecture choices come up. **No action this audit.**
  - **OQ-WALL-BEHAVIOR** — soft-wall degradation ladder at cost cap; decide-before Phase 8. Phase 8 is several phases out. **No action this audit.**
  - **OQ-PEDAGOGY-INFERENCE-LOCUS** — `watch`-tagged; revisit when inference registry crosses ~30 entries OR cross-domain edges per domain-pair exceed ~50 OR an operational complaint surfaces. None of the trigger conditions met. **No action this audit; remains tagged watch.**
  - **OQ-WATCH-FLAG-FILE** — `watch`-tagged; decide criterion "if `watch`-tagged tensions exceed ~10 entries, separate; if fewer, the unified file is simpler." Current count: **2 watch-tagged tensions out of 13 total OQ entries** (OQ-WATCH-FLAG-FILE itself + OQ-PEDAGOGY-INFERENCE-LOCUS). 2 ≪ 10. **Resolution: keep the unified file; mark OQ-WATCH-FLAG-FILE Resolved in tensions.md per the audit decision.** Action: in-session resolution.

## Dead weight

> What's in the repo that no longer earns its keep?

- No Dead-weight surfaced by mechanical audit. `adr_back_reference_orphan` validator check (per ADR 0041) is clean — every Accepted ADR has at least one inbound non-ADR citation outside its subtree. No Deprecated ADRs. No uncited operations docs.
- **(manual) Stale worktrees per `git worktree list`.** Approximately 30 worktrees under `.claude/worktrees/`. Each worktree branch traces back to a closed session. The accumulation is not actively harmful (each is small) but represents a long tail of "did the work; didn't clean up." The existing `engine/tools/sweep_worktrees.sh` utility per [README.md](../../README.md) repo-map handles this on demand. Queue a one-off sweep when convenient — not blocking, not high priority. **No action this audit.**

## Bloat

> What's grown past its purpose?

- Recent session duration: median 25 minutes across 25 archives. Compare to productive-work share; investigate if shutdown overhead begins exceeding productive work. **No action this audit (median is healthy).**
- **(manual) Operations docs over 300 lines** — `health-check.md` heuristic. Quick check: `wc -l engine/operations/*.md` shows several docs in the 100-300 line range; `session-shutdown-sequence.md` may approach 300 after S-0029 amendments. **No action this audit; revisit at next cadence if any doc consistently exceeds.**
- **(manual) ADR clustering observation.** ADRs 0036, 0038, 0039 (expression contracts for inward docs / code / universal) plus ADR 0041 (cascade discipline) plus ADR 0042 (soft-warn lifecycle) form a consistency-discipline cluster. Each was authored as a separate decision with distinct contracts — appropriate per [`adr-authoring.md`](../../engine/operations/adr-authoring.md) — but the cluster is dense enough that future readers benefit from a meta-pointer. The new [`engine/operations/cross-references.md`](../../engine/operations/cross-references.md) provides this for the engine side. **No action this audit; cross-references.md serves the function.**

## Cadence calibration

Is the current cadence (30 sessions) right for current project velocity?

This is the first audit, so trend data is single-point. Observations:

- 5 mechanical observations across the four categories; 5 manual augmentations. Total 10. Action items: 1 (OQ-WATCH-FLAG-FILE resolved in-session) + several queued for Session β. **Moderate finding density** — neither "consistently no-action" nor "consistently large action lists."
- The off-by-one cadence trigger surfaced in this audit suggests the next strict-trigger fire is S-0031 (where `last_claimed = S-0030 → 30 % 30 = 0`). Per the user-directed re-sequencing, S-0031 is Session β; the trigger fires there but β should defer the audit since it just ran here.
- Recommendation: **keep at 30 sessions for now**; re-evaluate at the second audit. Queue the off-by-one fix in Session β so future cadence triggers fire on the cadence-numbered session boot rather than the session after.

## Summary

First project health check produced 5 mechanical + 5 manual observations across the four categories. Significant Fit findings: the two-layer-decision-recording discipline has been almost entirely posture (1 MemPalace decision drawer for 42 ADRs — the gap is exactly what the planned Session β PostToolUse-on-ADR-write hook will surface mechanically); the cadence trigger has an off-by-one against its prose intent; the audit script discovered + fixed its own bug (path-resolution asymmetry against validate.py — exactly the discipline-symmetry mode ADR 0041 targets). Significant resolution: OQ-WATCH-FLAG-FILE closes (2 watch-tagged tensions ≪ 10 threshold; unified tensions.md remains simpler). All 6 aged tensions reviewed; 4 hold pending later phases; 1 closed; 1 (OQ-PEDAGOGY-INFERENCE-LOCUS) remains tagged watch with explicit trigger conditions. **The audit confirms the soft-warn lifecycle infrastructure landed at S-0029 closes a real loop** — without ADR 0042's structured archive field, this audit would have had no trend canon and would have surfaced as much from manual telemetry inspection as from script. The migration window per soft-warn-lifecycle.md "Migration" section is in effect: structured-data window starts here at S-0030's archive (technically only S-0029's archive carries the field at this audit's read time).
